---
phase: 38-nag-mechanism-ack-semantics
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/scheduler/index.ts
  - src/scheduler/scheduler.test.ts
autonomous: true
requirements: [ACK-03, ACK-06]
must_haves:
  truths:
    - "An ack-required reminder due in tick() re-arms its next nag (nextRun = now + nagInterval) before delivery, keeping enabled=true"
    - "The nag re-arm does not flip enabled=false even for a one-time (cron===null) ack reminder"
    - "Ordinary cron and one-time reminders keep their existing advance behavior unchanged"
    - "tick() never sets ackPending — that is activation's job"
    - "Honors CONTEXT decisions D-04 (scheduler tick owns the nag re-arm) and D-11 (base cron cadence pauses while nagging — only one active nag per reminder)"
  artifacts:
    - path: "src/scheduler/index.ts"
      provides: "requiresAck nag re-arm branch in tick() advance block"
      contains: "requiresAck"
    - path: "src/scheduler/scheduler.test.ts"
      provides: "Nag re-arm unit tests"
      contains: "nagIntervalMinutes"
  key_links:
    - from: "ScheduleEvaluatorImpl.tick"
      to: "ScheduleStore.advanceNextRun"
      via: "nag re-arm with now + nagIntervalMinutes*60000"
      pattern: "advanceNextRun"
---

<objective>
Add the system-native nag re-arm to the scheduler tick advance block (D-04, ACK-03). For a due `requiresAck` reminder, set `nextRun = now + nagIntervalMinutes × 60000` via `advanceNextRun` and keep `enabled = true` — instead of the existing cron-advance or one-time-disable paths. This re-arm lives in the generic ticker, before delivery, so an ack-required reminder keeps nagging with zero head involvement between cycles.

Purpose: ACK-03 ("the scheduler arms the next nag in code before delivering the current one") + ACK-06 (the in-flight nag is always pre-armed, so ack must later cancel it). The tick already enqueues the `schedule_trigger` AND advances `nextRun` in the same iteration — putting the re-arm here is exactly why it lands "before delivery" (RESEARCH "Scheduler arms before activation delivers").

Output: `src/scheduler/index.ts` with the new branch; `src/scheduler/scheduler.test.ts` extended (including `ackPending: false` added to the `makeSchedule` helper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-CONTEXT.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-PATTERNS.md

<interfaces>
<!-- Current shape of the tick() advance block — VERIFIED live at src/scheduler/index.ts:87-99 -->
```typescript
      try {
        if (schedule.cron) {
          const tz = schedule.cronTimezone ?? this.timezone
          const next = nextRunAfter(schedule.cron, now, tz)
          this.scheduleStore.advanceNextRun(schedule.id, next.toISOString())
        } else if (enqueued) {
          // Disable so the tick won't re-fire, but keep the row — activation
          // needs it to read agentContext and cron before deleting it after firing.
          this.scheduleStore.update(schedule.id, { enabled: false, nextRun: null })
        }
      } catch (err) {
        log.error(`[scheduler] Failed to advance schedule ${schedule.id}:`, (err as Error).message)
      }
```

<!-- Store helpers (VERIFIED): advanceNextRun(id, nextRun) only updates nextRun, leaves enabled untouched. -->
From src/db/schedules.ts:
```typescript
advanceNextRun(id: string, nextRun: string): void   // narrow: nextRun only, enabled untouched
```

<!-- nextRunAfter is already imported at src/scheduler/index.ts:5 — no new import needed. -->
From src/scheduler/cron.ts:
```typescript
export function nextRunAfter(expression: string, after: Date, tz: string): Date
```

<!-- makeSchedule test helper — VERIFIED at src/scheduler/scheduler.test.ts:67-89; already has requiresAck:false, needs ackPending:false added -->
From src/scheduler/scheduler.test.ts (line ~83):
```typescript
    requiresAck: false,
    nagIntervalMinutes: null,
    // Phase 38: add ackPending: false here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the requiresAck nag re-arm branch to tick()</name>
  <files>src/scheduler/index.ts</files>
  <read_first>
    - src/scheduler/index.ts — read the full file (103 lines); the advance block is lines 87-99 inside the `for (const schedule of due)` loop
    - .planning/phases/38-nag-mechanism-ack-semantics/38-PATTERNS.md — "src/scheduler/index.ts (service, event-driven)" section: the exact transformed advance block and why `advanceNextRun` (not `update`) is correct
    - .planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md — "Pattern 2: Scheduler Advance Block (D-04)" + the edge-case note on null nagIntervalMinutes
  </read_first>
  <behavior>
    - When a due schedule has `requiresAck === true` and `nagIntervalMinutes !== null`: call `advanceNextRun(schedule.id, (now + nagIntervalMinutes*60000).toISOString())` and do NOT call `update({ enabled: false })`
    - When `requiresAck` is false (or nagIntervalMinutes is null): existing cron-advance / one-time-disable behavior is unchanged
    - tick() does not write `ackPending` anywhere
  </behavior>
  <action>
    Insert a new `if` branch as the FIRST branch of the advance block (before `if (schedule.cron)`), per D-04 and PATTERNS "src/scheduler/index.ts" transformed block. The branch condition is `schedule.requiresAck && schedule.nagIntervalMinutes !== null`. Inside it, compute `const nagNext = new Date(now.getTime() + schedule.nagIntervalMinutes * 60_000)` and call `this.scheduleStore.advanceNextRun(schedule.id, nagNext.toISOString())`. The remaining `else if (schedule.cron) { ... }` and `else if (enqueued) { ... }` branches stay exactly as they are.

    Why `advanceNextRun` not `update`: `advanceNextRun` only touches `nextRun`, leaving `enabled` true throughout the nag loop. The one-time-disable path (`update({ enabled: false, nextRun: null })`) is precisely what must be bypassed so a one-time ack reminder survives to keep nagging (RESEARCH Pitfall 2). The branch must come FIRST so a `requiresAck` one-time reminder (cron===null) never falls into the `else if (enqueued)` disable path.

    Defensive fallback: when `nagIntervalMinutes` is null on a `requiresAck` row (should not occur — Phase 37 create validation requires a nag interval), the `&& nagIntervalMinutes !== null` guard means it falls through to the existing cron/one-time paths. Acceptable per RESEARCH.

    `nextRunAfter` is already imported at line 5 — do not add imports. Do not touch the enqueue block above the advance block.
  </action>
  <acceptance_criteria>
    - `grep -F "schedule.requiresAck && schedule.nagIntervalMinutes !== null" src/scheduler/index.ts` matches
    - `grep -F "nagIntervalMinutes * 60_000" src/scheduler/index.ts` matches (nag timing math)
    - The new branch calls `advanceNextRun` (not `update`) — `grep -A4 "schedule.requiresAck && schedule.nagIntervalMinutes" src/scheduler/index.ts` shows `advanceNextRun`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>tick() has a requiresAck-first branch that re-arms nextRun via advanceNextRun and keeps enabled=true; existing paths untouched; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ackPending to makeSchedule helper + write nag re-arm unit tests</name>
  <files>src/scheduler/scheduler.test.ts</files>
  <read_first>
    - src/scheduler/scheduler.test.ts — read lines 1-219; the `makeSchedule` helper is lines 67-89, the `beforeEach` harness (queueStore/scheduleStore/evaluator) is lines ~92-118, and the advance-block assertion pattern (`vi.mocked(scheduleStore.advanceNextRun).mock.calls[0]`) is lines ~183-219
    - src/scheduler/index.ts — the file under test after Task 1
    - .planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md — "scheduler.test.ts additions" code block (the exact nag re-arm assertion)
  </read_first>
  <behavior>
    - `makeSchedule` helper includes `ackPending: false` so the returned object satisfies the full Schedule type
    - Test A (ACK-03): a due `requiresAck:true, nagIntervalMinutes:60, cron:null` reminder → `advanceNextRun` called once with a nextRun ≈ now + 60min; `update` NOT called with `enabled:false`
    - Test B (ACK-06 setup): a `requiresAck:true` recurring reminder (cron present) still re-arms to the NAG interval, not the cron occurrence (proves the requiresAck branch wins over the cron branch while nagging)
    - Test C (regression): an ordinary cron reminder (`requiresAck:false`) still advances via `nextRunAfter`/cron (existing behavior preserved)
  </behavior>
  <action>
    First add `ackPending: false,` to the `makeSchedule` helper object (after `nagIntervalMinutes: null,`) — required so the helper produces a valid `Schedule` now that Plan 01 added the field (RESEARCH "Wave 0 Gaps").

    Then add unit tests using the existing `beforeEach` harness and `makeSchedule()`:
    - Test A: build a `requiresAck:true, nagIntervalMinutes:60, cron:null, ackPending:false` reminder, mock `getDue` to return it, capture `Date.now()` before `evaluator.tick()`, then assert `advanceNextRun` was called once, the `[id, nextRunIso]` args have `id === <schedule id>`, and `new Date(nextRunIso).getTime()` is within tolerance of `before + 60*60_000` (use a `toBeGreaterThanOrEqual(before + 60*60_000 - 1000)` style bound). Assert `scheduleStore.update` was NOT called with `{ enabled: false }`.
    - Test B: same but `cron:'0 9 * * 1'` (weekly) — assert `advanceNextRun` nextRun ≈ now+60min (nag interval), NOT the next Monday cron occurrence. This pins that the requiresAck branch precedes the cron branch.
    - Test C: an ordinary `requiresAck:false, cron:'*/5 * * * *'` schedule still calls `advanceNextRun` (cron path); no behavior regression.

    Mirror the existing assertion idiom in this file (`vi.mocked(scheduleStore.advanceNextRun).mock.calls[0]!`). Do not modify production code in this task.
  </action>
  <acceptance_criteria>
    - `grep -F "ackPending: false" src/scheduler/scheduler.test.ts` matches (helper updated)
    - `npx vitest run src/scheduler/scheduler.test.ts` exits 0
    - At least one test asserts nag re-arm nextRun ≈ now + nagIntervalMinutes and that the disable path is not taken
    - At least one test asserts a recurring requiresAck reminder re-arms to the nag interval, not the cron occurrence
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/scheduler/scheduler.test.ts</automated>
  </verify>
  <done>makeSchedule carries ackPending; nag re-arm tests cover one-time, recurring-while-nagging, and ordinary-cron-regression; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler tick ↔ schedule store | Tick re-arms nextRun on a row it read from disk; same-process, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-38-03 | Denial of Service | A requiresAck row with nagIntervalMinutes too small spams the user | accept | Nag interval is user-set at creation (Phase 37 validated boundaries); "unmissable" is the intended behavior; out of scope to cap (REQUIREMENTS Out-of-Scope: no max-nag) | low |
| T-38-04 | Tampering | Re-arm flips enabled=false and silences a one-time ack reminder | mitigate | requiresAck branch uses `advanceNextRun` (nextRun-only), never the `update({enabled:false})` path; Test A pins this | medium |

No `high`-severity threats. No package installs.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/scheduler/scheduler.test.ts` exits 0
</verification>

<success_criteria>
- ACK-03: tick() re-arms the next nag in code before delivery, keeping enabled=true, with no head involvement
- A one-time requiresAck reminder is NOT disabled by the tick (survives to keep nagging)
- Recurring requiresAck reminders re-arm to the nag interval (not the cron occurrence) while nagging
- Ordinary reminders/tasks unchanged
</success_criteria>

<output>
Create `.planning/phases/38-nag-mechanism-ack-semantics/38-02-SUMMARY.md` when done
</output>
