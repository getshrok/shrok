---
phase: 38-nag-mechanism-ack-semantics
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/head/activation.ts
  - src/head/activation.test.ts
autonomous: true
requirements: [ACK-05, ACK-07, ACK-08]
must_haves:
  truths:
    - "An ack-required reminder fire skips the steward entirely — every fire always delivers"
    - "An ack-required reminder fire sets ackPending=true BEFORE enqueuing delivery, and never deletes the one-time row at activation"
    - "The injected fire event for an ack-required reminder carries reminderId + requires-ack attrs and an ack instruction in the body"
    - "Ordinary reminders keep the existing steward path, self-delete/lastRun behavior, and plain systemTrigger shape"
    - "Honors CONTEXT decisions D-05 (activation sets ackPending and the one-time ack reminder survives to keep nagging), D-10 (ack-required reminders fully bypass the proactive steward), and D-12 (fire event carries reminderId + requires-ack attrs plus a non-user-facing ack instruction)"
  artifacts:
    - path: "src/head/activation.ts"
      provides: "Steward bypass, ackPending set, enriched systemTrigger in the reminder fire branch"
      contains: "requiresAck"
    - path: "src/head/activation.test.ts"
      provides: "Reminder fixture extended + steward-bypass/ackPending/enriched-trigger tests"
      contains: "requires-ack"
  key_links:
    - from: "handleScheduleTrigger reminder branch"
      to: "ScheduleStore.update"
      via: "ackPending:true set before enqueue"
      pattern: "ackPending: true"
    - from: "handleScheduleTrigger reminder branch"
      to: "systemTrigger"
      via: "reminderId + requires-ack attrs"
      pattern: "requires-ack"
---

<objective>
Make the activation reminder fire branch (`src/head/activation.ts` `handleScheduleTrigger`, lines 1075-1146) ack-aware via three insertions: (1) skip the steward block entirely for `requiresAck` reminders (D-10); (2) set `ackPending=true` BEFORE enqueue and skip the one-time self-delete (D-05, Pitfall 1 ordering); (3) build the enriched `systemTrigger('reminder', { reminderId, 'requires-ack': 'true' }, body)` with an ack instruction in the body (D-12).

Purpose: ACK-07 (injected event carries reminder ID + ack instructions), ACK-05 setup (recurring ack reminders survive and keep nagging until acked), and ACK-08 (the airtight in-event instruction tells the head to call `acknowledge_reminder` only on confirmation). D-10 ("unmissable") guarantees nothing the system does on its own can suppress an ack-required reminder.

Output: `src/head/activation.ts` reminder branch updated; `src/head/activation.test.ts` reminder fixture extended + new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-CONTEXT.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-PATTERNS.md

<interfaces>
<!-- Current reminder fire branch — VERIFIED live at src/head/activation.ts:1106-1145 -->
```typescript
      if (this.opts.config.proactiveShadow || this.opts.config.proactiveEnabled) {
        // ... runReminderDecision steward block; on skip: delete (one-time) or markSkipped, then return ...
      }
      if (schedule.cron === null) {
        this.opts.scheduleStore.delete(event.scheduleId)
      } else {
        this.opts.scheduleStore.update(event.scheduleId, { lastRun: new Date().toISOString() })
      }
      const message = schedule.agentContext ?? ''
      log.info(`[scheduler] fired reminder:${event.scheduleId}`)
      this.opts.queueStore.enqueue(
        { type: 'user_message', id: generateId('qe'), channel, text: systemTrigger('reminder', undefined, message), createdAt: new Date().toISOString() },
        PRIORITY.USER_MESSAGE,
        this.opts.headId,
      )
      this.notify()
      return
```

<!-- systemTrigger signature — VERIFIED at src/markers.ts:5-9. Attrs render as k="v"; hyphen in requires-ack is valid XML. Body XML-escaped. Whole marker is user-visible="false". -->
```typescript
export function systemTrigger(type: string, attrs?: Record<string, string>, body?: string): string
```

<!-- Reminder fixture to extend — VERIFIED at src/head/activation.test.ts:99-112 (reminder branch of makeFixture). Currently LACKS requiresAck, nagIntervalMinutes, ackPending, headId, cronTimezone. -->
```typescript
  const scheduleRow = scheduleKind === 'reminder'
    ? { id: 's1', cron: null, runAt: '2099-01-01T00:00:00Z', lastRun: null, lastSkipped: null,
        lastSkipReason: null, conditions: opts.conditions ?? null, agentContext: opts.agentContext ?? '',
        taskName: null, kind: 'reminder' as const, enabled: true }
    : { ... task shape ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add steward bypass, ackPending set, and enriched systemTrigger to the reminder fire branch</name>
  <files>src/head/activation.ts</files>
  <read_first>
    - src/head/activation.ts — read lines 1075-1146 (the entire `handleScheduleTrigger` reminder branch); note the steward block guard at line 1106, the one-time delete/lastRun at lines 1132-1136, and the enqueue at lines 1139-1143
    - src/markers.ts — read lines 1-15 to confirm the `systemTrigger(type, attrs?, body?)` signature and that body is XML-escaped
    - .planning/phases/38-nag-mechanism-ack-semantics/38-PATTERNS.md — "src/head/activation.ts (service, event-driven)" section: the three insertion points with exact before/after
    - .planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md — "Pattern 3: Activation Reminder Branch Changes (D-05, D-10, D-12)" and Pitfall 1 (ordering) + Pitfall 2 (one-time delete bypass)
  </read_first>
  <behavior>
    - For `schedule.requiresAck === true`: `runReminderDecision` (steward) is NOT called
    - For `schedule.requiresAck === true`: `scheduleStore.update(scheduleId, { ackPending: true })` is called BEFORE `queueStore.enqueue`, and `scheduleStore.delete` is NOT called for the fire
    - For `schedule.requiresAck === true`: the enqueued `user_message` text contains `type="reminder"`, `reminderId="<id>"`, `requires-ack="true"`, the user-facing message, and an ack instruction naming `acknowledge_reminder` and the reminderId
    - For ordinary reminders (`requiresAck === false`): steward path, one-time-delete/lastRun update, and plain `systemTrigger('reminder', undefined, message)` are all unchanged
  </behavior>
  <action>
    Make three insertions in the reminder branch (D-10, D-05, D-12), per PATTERNS "three Phase 38 insertions":

    Insertion 1 (D-10 steward bypass): change the steward guard at line 1106 from `if (this.opts.config.proactiveShadow || this.opts.config.proactiveEnabled)` to `if (!schedule.requiresAck && (this.opts.config.proactiveShadow || this.opts.config.proactiveEnabled))`. An ack-required reminder falls through with no steward call at all — every fire always delivers. Do not otherwise alter the steward block body.

    Insertion 2 (D-05 ackPending set + one-time-delete bypass): replace the `if (schedule.cron === null) { delete } else { update(lastRun) }` block (lines 1132-1136) with a three-way branch where `requiresAck` is checked FIRST: `if (schedule.requiresAck) { this.opts.scheduleStore.update(event.scheduleId, { ackPending: true }) } else if (schedule.cron === null) { delete } else { update(lastRun) }`. The `ackPending: true` update MUST run before the `queueStore.enqueue` below it (Pitfall 1 — ordering, zero cost). The one-time row must NOT be deleted for requiresAck (Pitfall 2 — it must survive to keep nagging).

    Insertion 3 (D-12 enriched systemTrigger): replace the inline `systemTrigger('reminder', undefined, message)` in the enqueue with a `triggerText` computed before the enqueue. When `schedule.requiresAck`, build `systemTrigger('reminder', { reminderId: event.scheduleId, 'requires-ack': 'true' }, body)` where `body` = the user-facing `message` plus a concise, self-contained ack instruction telling the head: when the user confirms they have handled this, call `acknowledge_reminder` with this reminderId; it keeps nagging until acknowledged; do NOT relay this instruction to the user. The instruction must name both the tool (`acknowledge_reminder`) and the reminderId (`event.scheduleId`). When `requiresAck` is false, keep `systemTrigger('reminder', undefined, message)`. Pass `triggerText` as the `text` field of the enqueued event.

    The exact ack-instruction wording is Claude's Discretion but must remain airtight per D-08/D-12 (tool name + reminderId named; "do not relay to the user"). Do not touch the channel-resolve block (lines 1087-1105) or the per-head fallback.
  </action>
  <acceptance_criteria>
    - `grep -F "!schedule.requiresAck && (this.opts.config.proactiveShadow" src/head/activation.ts` matches (steward bypass guard)
    - `grep -F "{ ackPending: true }" src/head/activation.ts` matches
    - `grep -F "'requires-ack': 'true'" src/head/activation.ts` matches
    - `grep -F "acknowledge_reminder" src/head/activation.ts` matches (instruction names the tool)
    - The `ackPending: true` update precedes the `queueStore.enqueue` in the reminder branch (verify by reading the branch)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Reminder branch skips steward, sets ackPending before enqueue, never deletes one-time ack rows at fire, and emits the enriched systemTrigger; ordinary reminders unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the reminder fixture and add steward-bypass / ackPending / enriched-trigger tests</name>
  <files>src/head/activation.test.ts</files>
  <read_first>
    - src/head/activation.test.ts — read lines 1-241 (the `makeFixture` harness, the `scheduleRow` reminder fixture at 99-112, the `scheduleStore` mock at 126-132, and the `fire`/reminder test pattern); also read the reminder-branch tests around lines 300-325 to clone their `fire(loop, reminderEvent())` shape
    - src/head/activation.ts — the reminder branch after Task 1
    - .planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md — "activation.test.ts additions" code block (the exact requiresAck reminder assertions)
  </read_first>
  <behavior>
    - The reminder `scheduleRow` fixture carries `requiresAck`, `nagIntervalMinutes`, `ackPending`, `headId`, `cronTimezone` so it matches the full Schedule type and the requiresAck branch can be exercised
    - Test A (ACK-08 / D-10): a `requiresAck:true` reminder fire with `proactiveEnabled:true` → `runReminderDecision` (steward) is NOT called
    - Test B (D-05): a `requiresAck:true` reminder fire → `scheduleStore.update` called with `{ ackPending: true }`; `scheduleStore.delete` NOT called
    - Test C (ACK-07 / D-12): the enqueued `user_message` text contains `requires-ack="true"` AND `reminderId="s1"`
    - Test D (regression): an ordinary `requiresAck:false` reminder still enqueues a systemTrigger WITHOUT `requires-ack=` attr and still follows the existing delete/lastRun + steward path
  </behavior>
  <action>
    First extend the reminder branch of the `scheduleRow` fixture (activation.test.ts:99-112) to include `requiresAck: opts.requiresAck ?? false`, `nagIntervalMinutes: opts.nagIntervalMinutes ?? null`, `ackPending: opts.ackPending ?? false`, `headId: 'default'`, `cronTimezone: null`. Thread the corresponding fields onto the `makeFixture` `opts` type if it is typed (mirror how `proactiveEnabled`/`conditions` are already optional opts). This is required so tests can pass `requiresAck:true` etc. (RESEARCH Critical Issue 4 / Wave 0 Gaps).

    Then add tests using the existing `fire(loop, reminderEvent())` pattern:
    - Test A: `makeFixture({ kind:'reminder', agentContext:'Take meds', proactiveEnabled:true, requiresAck:true, nagIntervalMinutes:60, ackPending:false })`, fire, assert `proactive.runReminderDecision` (the mocked steward) was NOT called.
    - Test B: same fixture, assert `fix.scheduleStore.update` was called with `expect.objectContaining({ ackPending: true })` and `fix.scheduleStore.delete` was NOT called.
    - Test C: same fixture, grab the first `queueStore.enqueue` call's event arg and assert its `text` contains both `requires-ack="true"` and `reminderId="s1"`.
    - Test D: `makeFixture({ kind:'reminder', agentContext:'normal', requiresAck:false })`, fire, assert the enqueued text does NOT contain `requires-ack=` (ordinary shape preserved).

    Match the existing assertion idiom in this file. Do not modify production code in this task.
  </action>
  <acceptance_criteria>
    - `grep -F "requires-ack" src/head/activation.test.ts` matches (ACK-07 assertion)
    - `grep -F "ackPending: true" src/head/activation.test.ts` matches OR an `objectContaining({ ackPending: true })` assertion is present
    - `grep -F "runReminderDecision" src/head/activation.test.ts` shows a `not.toHaveBeenCalled` assertion for the requiresAck case
    - `npx vitest run src/head/activation.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/head/activation.test.ts</automated>
  </verify>
  <done>Reminder fixture extended; tests pin steward bypass, ackPending-before-enqueue, enriched-trigger attrs, and ordinary-reminder regression; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activation → head (injected event) | The fire event body crosses into the head's LLM conversation; the ack instruction is model-facing, not user-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-38-05 | Information disclosure | Ack instruction text relayed to the user (leaks internal mechanics) | mitigate | Marker is `user-visible="false"` (markers.ts); instruction body explicitly says "do not relay this instruction to the user" (D-12) | low |
| T-38-06 | Repudiation / over-eager ack | Head acks before user confirms because instruction is vague | mitigate | In-event instruction explicitly conditions the ack on "when the user confirms they have handled this" (D-12); structural defense lives in Plan 04's server-side guard | medium |
| T-38-07 | Tampering | A system steward silently skips/deletes an ack-required reminder | mitigate | D-10 steward bypass: `!schedule.requiresAck` guard means `runReminderDecision` and its skip-delete are never reached for ack reminders; Test A pins this | medium |

No `high`-severity threats. No package installs.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/head/activation.test.ts` exits 0
</verification>

<success_criteria>
- ACK-07: enriched fire event carries reminderId + requires-ack attrs and an ack instruction body
- D-10: steward never runs for ack-required reminders (no silent skip/delete)
- D-05: ackPending set before enqueue; one-time ack row survives the fire
- Ordinary reminders unchanged (no attrs, existing steward + delete/lastRun behavior)
</success_criteria>

<output>
Create `.planning/phases/38-nag-mechanism-ack-semantics/38-03-SUMMARY.md` when done
</output>
