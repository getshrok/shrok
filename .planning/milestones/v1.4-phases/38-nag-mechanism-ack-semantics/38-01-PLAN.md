---
phase: 38-nag-mechanism-ack-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schedules.ts
  - src/db/schedules.test.ts
autonomous: true
requirements: [ACK-03, ACK-04, ACK-05, ACK-06]
must_haves:
  truths:
    - "The Schedule type carries a self-documenting ackPending boolean field"
    - "Legacy schedule JSON files (no ackPending) read back with ackPending=false, mtime-stable on second read"
    - "ScheduleStore.update() can set ackPending without a TypeScript error"
    - "ScheduleStore.create() defaults ackPending to false"
    - "Honors CONTEXT decisions D-01 (single ackPending field, no separate nag row), D-02 (one nextRun, no dual-clock storage), and D-03 (lazy migration stamps ackPending)"
  artifacts:
    - path: "src/db/schedules.ts"
      provides: "ackPending on Schedule, CreateScheduleOptions, SchedulePatch; migration + create + update apply-block"
      contains: "ackPending"
    - path: "src/db/schedules.test.ts"
      provides: "Migration + round-trip test coverage for ackPending"
      contains: "ackPending"
  key_links:
    - from: "ScheduleStore.update"
      to: "SchedulePatch"
      via: "patch.ackPending apply-block"
      pattern: "patch\\.ackPending"
    - from: "migrateLegacySchedule"
      to: "ackPending default"
      via: "'field' in obj guard"
      pattern: "'ackPending' in obj"
---

<objective>
Add the `ackPending` boolean to the `Schedule` row schema and every place that touches it: the `Schedule` type, `CreateScheduleOptions`, `SchedulePatch`, `ScheduleStore.create()` defaults, `ScheduleStore.update()` apply-block, and the lazy `migrateLegacySchedule` migration. This is the foundational type change every other Phase 38 plan depends on (RESEARCH "Sequencing / Dependency Order" step 1, Critical Issue 1).

Purpose: `ackPending` is the single new field that represents the armed-nag state (D-01). Without `ackPending` in `SchedulePatch` AND a matching `update()` apply-block, the activation and ack-tool plans fail `npx tsc --noEmit` (RESEARCH Pitfall 4 — a hard tsc blocker). This plan lands the field and its migration so Waves 2/3 compile.

Output: `src/db/schedules.ts` with `ackPending` end-to-end; `src/db/schedules.test.ts` extended with migration + round-trip coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-CONTEXT.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md
@.planning/phases/38-nag-mechanism-ack-semantics/38-PATTERNS.md

<interfaces>
<!-- Current shape of the file being modified — extracted from src/db/schedules.ts (VERIFIED live). -->
<!-- Phase 37 added requiresAck + nagIntervalMinutes the SAME way; copy that pattern exactly. -->

From src/db/schedules.ts (lines 18-23, Schedule type tail):
```typescript
  /** Whether this reminder requires explicit user acknowledgment before it stops nagging. */
  requiresAck: boolean
  /** Total nag cadence in minutes — the interval between repeated nag fires. Null/inert when requiresAck is false (D-07). */
  nagIntervalMinutes: number | null
  createdAt: string
  updatedAt: string
```

From src/db/schedules.ts (line 37-38, CreateScheduleOptions tail):
```typescript
  requiresAck?: boolean
  nagIntervalMinutes?: number | null
```

From src/db/schedules.ts (line 41, SchedulePatch — MISSING ackPending, must extend):
```typescript
export type SchedulePatch = Partial<Pick<Schedule, 'cron' | 'runAt' | 'enabled' | 'nextRun' | 'lastRun' | 'conditions' | 'agentContext' | 'cronTimezone'>>
```

From src/db/schedules.ts (lines 59-61, migrateLegacySchedule guards — append one line):
```typescript
  if (!('headId' in obj)) { obj['headId'] = 'default'; migrated = true }
  if (!('requiresAck' in obj)) { obj['requiresAck'] = false; migrated = true }
  if (!('nagIntervalMinutes' in obj)) { obj['nagIntervalMinutes'] = null; migrated = true }
```

From src/db/schedules.ts (lines 89-90, create() defaults — copy this shape):
```typescript
      requiresAck: options.requiresAck ?? false,
      nagIntervalMinutes: options.nagIntervalMinutes ?? null,
```

From src/db/schedules.ts (lines 129-136, update() apply-block — add one line after cronTimezone):
```typescript
    if (patch.agentContext !== undefined) existing.agentContext = patch.agentContext
    if (patch.cronTimezone !== undefined) existing.cronTimezone = patch.cronTimezone
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ackPending to the Schedule schema and store (type, options, patch, create, update, migration)</name>
  <files>src/db/schedules.ts</files>
  <read_first>
    - src/db/schedules.ts — the file being modified; read lines 1-160 to see the live shape of Schedule, CreateScheduleOptions, SchedulePatch, migrateLegacySchedule, create(), and update()
    - .planning/phases/38-nag-mechanism-ack-semantics/38-PATTERNS.md — "src/db/schedules.ts (model/store, CRUD)" section: the six surgical changes with exact analog lines (this is the self-analog from Phase 37)
    - .planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md — "Critical Issue 1: SchedulePatch does not include ackPending" and "Pattern 1: Lazy Migration Extension (D-03)"
  </read_first>
  <behavior>
    - Schedule type includes `ackPending: boolean` with JSDoc
    - CreateScheduleOptions includes `ackPending?: boolean`
    - SchedulePatch Pick union includes `'ackPending'`
    - ScheduleStore.create() sets `ackPending: options.ackPending ?? false`
    - ScheduleStore.update() applies `if (patch.ackPending !== undefined) existing.ackPending = patch.ackPending`
    - migrateLegacySchedule stamps `ackPending: false` when absent via the idempotent `'ackPending' in obj` guard
  </behavior>
  <action>
    Make six surgical edits to src/db/schedules.ts, mirroring exactly how Phase 37 added `requiresAck`/`nagIntervalMinutes` (D-01, D-03):

    1. In the `Schedule` interface, add `ackPending: boolean` immediately after `nagIntervalMinutes` (before `createdAt`). Give it a self-documenting JSDoc, e.g. "Whether an ack-required reminder currently has an outstanding (un-acknowledged) nag in flight. Inert (false) for non-ack reminders and tasks. Set true at activation delivery (D-05), cleared on ack (D-07)."

    2. In `CreateScheduleOptions`, add `ackPending?: boolean` after `nagIntervalMinutes?`.

    3. In `SchedulePatch`, add `| 'ackPending'` to the `Pick<Schedule, ...>` union. This is the tsc-blocker fix from RESEARCH Critical Issue 1 — without it, `update(id, { ackPending: true })` in Waves 2/3 fails to compile.

    4. In `ScheduleStore.create()`, add `ackPending: options.ackPending ?? false,` after the `nagIntervalMinutes` default line.

    5. In `ScheduleStore.update()`, add `if (patch.ackPending !== undefined) existing.ackPending = patch.ackPending` after the `cronTimezone` apply line. The type change in (3) and this apply-block must change in tandem.

    6. In `migrateLegacySchedule`, append `if (!('ackPending' in obj)) { obj['ackPending'] = false; migrated = true }` after the `nagIntervalMinutes` guard. Use the `'field' in obj` form (NOT `obj['field'] === undefined`) — this is intentional and mtime-stable per D-03 / Phase 35 D-MIGRATION-IDEMPOTENT.

    Do NOT touch any other field, method, or file. `nextRunAfter` is not needed in this file for this plan.
  </action>
  <acceptance_criteria>
    - `grep -c "ackPending" src/db/schedules.ts` returns at least 6 (type, options, patch, create, update, migration)
    - `src/db/schedules.ts` SchedulePatch type includes `'ackPending'` in its Pick union
    - `grep -F "if (!('ackPending' in obj))" src/db/schedules.ts` matches (idempotent guard form)
    - `grep -F "if (patch.ackPending !== undefined) existing.ackPending = patch.ackPending" src/db/schedules.ts` matches
    - `grep -F "ackPending: options.ackPending ?? false" src/db/schedules.ts` matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ackPending exists on Schedule, CreateScheduleOptions, SchedulePatch, in create() defaults, in update() apply-block, and in migrateLegacySchedule; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend schedules.test.ts with ackPending migration + round-trip coverage</name>
  <files>src/db/schedules.test.ts</files>
  <read_first>
    - src/db/schedules.test.ts — read lines 270-360 to see the existing Phase 37 migration test ("first read of legacy reminder JSON ... stamps defaults") and the create/round-trip tests; clone their shape
    - src/db/schedules.ts — the file under test (current state after Task 1)
    - .planning/phases/38-nag-mechanism-ack-semantics/38-RESEARCH.md — "Phase Requirements → Test Map" rows for ACK-09 legacy migration analog and the round-trip pattern
  </read_first>
  <behavior>
    - Test 1 (migration): a legacy reminder JSON with no `ackPending` key reads back with `ackPending === false`, and the on-disk file has `ackPending` stamped after first read
    - Test 2 (idempotent / mtime-stable): a row that already has `ackPending` is NOT rewritten on the second read (assert `'ackPending' in beforeRaw` is true and no migration churn)
    - Test 3 (create default): `store.create({ ... })` without `ackPending` yields a schedule with `ackPending === false`
    - Test 4 (update round-trip): `store.update(id, { ackPending: true })` then `store.get(id)` returns `ackPending === true`; `store.update(id, { ackPending: false })` then `get` returns `false`
  </behavior>
  <action>
    Add tests to src/db/schedules.test.ts following the exact shape of the existing Phase 37 migration test at lines 307-350. Add `ackPending` assertions to the legacy-migration case (a new it() block, or extend the legacy fixture to also assert `'ackPending' in beforeRaw` is false → `ackPending===false` after `get()`, and `afterRaw.ackPending === false`). Add a create-default test asserting a created schedule has `ackPending === false`. Add an update round-trip test: create a reminder, `update(id, { ackPending: true })`, `get` → expect `true`; `update(id, { ackPending: false })`, `get` → expect `false`. Reuse the existing `tmpDir`/`store` `beforeEach` harness in this file — do not introduce a new harness.
  </action>
  <acceptance_criteria>
    - `grep -c "ackPending" src/db/schedules.test.ts` returns at least 4
    - `npx vitest run src/db/schedules.test.ts` exits 0
    - At least one test asserts an on-disk legacy row gains `ackPending: false` after first `get()`
    - At least one test asserts `update(id, { ackPending: true })` round-trips through `get()`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/db/schedules.test.ts</automated>
  </verify>
  <done>schedules.test.ts covers ackPending migration default, mtime-stable idempotency, create default, and update round-trip; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule JSON file ↔ store | Legacy/hand-edited JSON files crossing into the typed `Schedule` shape via migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-38-01 | Tampering | migrateLegacySchedule stamping wrong default | mitigate | `'ackPending' in obj` guard stamps `false` only when absent; round-trip test pins default; idempotent guard prevents re-write of present rows | low |
| T-38-02 | Information disclosure | ackPending leaking across heads | accept | `ackPending` is a per-row boolean on a schedule the user already owns; reads/writes are head-agnostic by id (ids unique) — no cross-head exposure introduced | low |

No `high`-severity threats in this plan. No package installs (no T-38-SC needed).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (the SchedulePatch + apply-block extension is the gating tsc fix for the whole phase)
- `npx vitest run src/db/schedules.test.ts` exits 0
</verification>

<success_criteria>
- `ackPending` present on Schedule, CreateScheduleOptions, SchedulePatch, create() defaults, update() apply-block, migrateLegacySchedule
- Legacy reminder JSON migrates to `ackPending: false` (ACK-09 continuation pattern), mtime-stable on re-read
- `update(id, { ackPending })` round-trips
- tsc clean; schedules.test.ts green
</success_criteria>

<output>
Create `.planning/phases/38-nag-mechanism-ack-semantics/38-01-SUMMARY.md` when done
</output>
