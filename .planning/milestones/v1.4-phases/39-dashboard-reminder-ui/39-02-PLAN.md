---
phase: 39-dashboard-reminder-ui
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - dashboard/src/lib/api.ts
  - dashboard/src/pages/SchedulesPage.tsx
autonomous: false
requirements: [SCHED-01, SCHED-02]
must_haves:
  truths:
    - "Ack-required reminder rows show a static NAGS badge; ordinary reminders show none (D-05 static NAGS badge; D-06 static badge only — no live ackPending 'nagging now' indicator)"
    - "The reminder row sub-label appends the nag cadence (e.g. 'Daily at 09:00 · nags every 1h') when requiresAck is true (D-05 inline nag-cadence sub-label, 'nags' framing)"
    - "The reminder edit modal exposes a Requires-acknowledgment toggle with reveal-when-on minutes/hours/days nag inputs, validated for coupling/floor/ceiling, and saving sends requiresAck + nagIntervalMinutes via PATCH (D-01 multi-slot nag inputs; D-02 reveal-when-on; D-04 client validation; D-11 editable via PATCH)"
    - "api.schedules.create and api.schedules.update payload types carry requiresAck, nagIntervalMinutes, and startAt"
  artifacts:
    - path: "dashboard/src/lib/api.ts"
      provides: "create/update payload types extended with requiresAck/nagIntervalMinutes/startAt"
      contains: "nagIntervalMinutes"
    - path: "dashboard/src/pages/SchedulesPage.tsx"
      provides: "NAGS badge + nag sub-label in ReminderRow; ack fields in reminder edit modal; formatNagInterval helper"
      contains: "NAGS"
  key_links:
    - from: "dashboard/src/pages/SchedulesPage.tsx ReminderRow"
      to: "schedule.requiresAck / schedule.nagIntervalMinutes"
      via: "badge condition + sub-label suffix (fields from Plan 01 type fix)"
      pattern: "schedule\\.requiresAck"
    - from: "dashboard/src/pages/SchedulesPage.tsx edit modal"
      to: "api.schedules.update"
      via: "updateMutation sends requiresAck + nagIntervalMinutes"
      pattern: "api\\.schedules\\.update"
---

<objective>
Deliver the read + edit half of the dashboard reminder UI: the NAGS badge and nag-cadence sub-label on ack-required reminder rows (SCHED-02), and the ack/nag editing surface in the reminder edit modal (SCHED-01 / D-11), wired through the extended api.ts client. This plan depends on Plan 01 having added requiresAck/nagIntervalMinutes/ackPending to the frontend Schedule type — without it these references do not compile.

Purpose: SCHED-02 requires reminders that need acknowledgment to be visually distinguished; D-05 specifies a static "NAGS" badge (amber, fixed color, Head-badge shape) plus an inline nag cadence in the sub-label, framed as "nags" not "ack" (D-05 wording). D-11 makes ack/nag editable via the edit modal using the same reveal-when-on multi-slot UI and the same client-side validation the create form will use (Plan 03).
Output: Extended `dashboard/src/lib/api.ts` payload types and updated `dashboard/src/pages/SchedulesPage.tsx` (ReminderRow badge + sub-label + edit modal ack fields + formatNagInterval helper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-dashboard-reminder-ui/39-CONTEXT.md
@.planning/phases/39-dashboard-reminder-ui/39-RESEARCH.md
@.planning/phases/39-dashboard-reminder-ui/39-PATTERNS.md
@.planning/phases/39-dashboard-reminder-ui/39-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, dashboard Schedule type carries: requiresAck: boolean; nagIntervalMinutes: number | null; ackPending: boolean -->

<!-- dashboard/src/lib/api.ts schedules block (~264-281) — current shapes to extend: -->
<!--   create: (body: { headId; taskName?; kind?; cron?; runAt?; conditions?; agentContext? }) -->
<!--   update: (id, patch: { enabled?; cron?; runAt?; conditions?; agentContext? }) -->
<!--   JSON.stringify drops undefined fields automatically -->

<!-- SchedulesPage.tsx reusable analogs: -->
<!--   Head badge (ReminderRow ~514-521): inline-style span, fixed shape -->
<!--   enable/disable toggle (ReminderRow ~527-538): relative w-9 h-5 rounded-full button -->
<!--   sub-label (~496-509): scheduleLabel via formatCron/formatInTz -->
<!--   edit modal (~555-622): createPortal modal with Save/Cancel, updateMutation -->
<!--   formatNagInterval: NEW local helper (add after existing helpers ~line 82) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend api.ts payloads + formatNagInterval helper</name>
  <files>dashboard/src/lib/api.ts, dashboard/src/pages/SchedulesPage.tsx</files>
  <read_first>
    - dashboard/src/lib/api.ts lines 264-281 (schedules.create + schedules.update inline payload types; usageThresholds.update ~255-260 as the optional-field extension pattern)
    - dashboard/src/pages/SchedulesPage.tsx lines 60-90 (existing local helpers incl. formatRelTime — the pattern to mirror; do NOT refactor the duplicate formatRelTime, F-09)
    - .planning/phases/39-dashboard-reminder-ui/39-PATTERNS.md (api.ts extension diff + formatNagInterval helper source)
  </read_first>
  <action>
    (a) F-02 / D-11 / D-10: In dashboard/src/lib/api.ts, extend the `create` body inline type with `requiresAck?: boolean; nagIntervalMinutes?: number; startAt?: string`, and the `update` patch inline type with `requiresAck?: boolean; nagIntervalMinutes?: number | null`. Do not change the request() calls — JSON.stringify drops undefined keys. Mirror the usageThresholds.update optional-field idiom.
    (b) D-05 helper: In SchedulesPage.tsx, add a local `formatNagInterval(minutes: number | null): string` after the existing local helpers (around line 82). It returns '?' for null/0, else composes "Nd Nh Nm" from `Math.floor(minutes/1440)` days, `Math.floor((minutes%1440)/60)` hours, `minutes%60` minutes, joining non-zero parts with a space. This is used by both the badge tooltip/sub-label here and (independently) by the create/edit forms in Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && cd dashboard && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/lib/api.ts` create body type contains `requiresAck`, `nagIntervalMinutes`, and `startAt`; update patch type contains `requiresAck` and `nagIntervalMinutes`
    - `grep -c "formatNagInterval" dashboard/src/pages/SchedulesPage.tsx` >= 1 (helper defined)
    - `npx tsc --noEmit` (root) and `cd dashboard && npx tsc --noEmit` both clean
  </acceptance_criteria>
  <done>api.ts carries the new create/update fields and SchedulesPage has a formatNagInterval helper; both typecheck.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NAGS badge + nag sub-label + reminder edit-modal ack fields</name>
  <files>dashboard/src/pages/SchedulesPage.tsx</files>
  <read_first>
    - dashboard/src/pages/SchedulesPage.tsx lines 447-624 (ReminderRow: head badge ~514-521, sub-label ~496-509, enable toggle ~527-538, edit modal ~555-622, updateMutation ~454-467, startEdit/state)
    - .planning/phases/39-dashboard-reminder-ui/39-PATTERNS.md (NAGS badge JSX, sub-label suffix, edit-modal state vars, startEdit seeding, updateMutation type, reveal-when-on nag-slot block)
    - .planning/phases/39-dashboard-reminder-ui/39-RESEARCH.md (Pitfall 2: badge condition is requiresAck NOT ackPending; Pitfall 3: omit nagIntervalMinutes / send null when ack off)
  </read_first>
  <action>
    Badge (D-05/D-06): in the ReminderRow flex row near the head badge, render `{schedule.requiresAck && (<span ...>NAGS</span>)}` — fixed amber styling (backgroundColor '#92400e', borderLeft '2px solid #f59e0b'), small bold uppercase, title `Nags every ${formatNagInterval(schedule.nagIntervalMinutes)} until acknowledged`. Condition is `schedule.requiresAck` (NOT ackPending — Pitfall 2). Reminders only (ReminderRow), inert for tasks.
    Sub-label (D-05): append to the existing scheduleLabel line `{schedule.requiresAck && schedule.nagIntervalMinutes ? ` · nags every ${formatNagInterval(schedule.nagIntervalMinutes)}` : null}`.
    Edit modal (D-11/D-12 client side): add local state `editRequiresAck` (boolean), `editNagMinutes`/`editNagHours`/`editNagDays` (numbers). Seed them in startEdit(): `setEditRequiresAck(schedule.requiresAck)`; decompose `schedule.nagIntervalMinutes ?? 0` into days/hours/minutes slots. Add a "Requires acknowledgment" toggle reusing the enable/disable button pattern but flipping local state (`setEditRequiresAck(v => !v)`, no mutation on toggle). When editRequiresAck is on, reveal three number inputs (minutes/hours/days) — compute `editNagSum = editNagMinutes + editNagHours*60 + editNagDays*1440`. Add inline red validation messages + disable Save when: editRequiresAck && editNagSum===0; editNagSum>43200; (floor 1 is satisfied by any positive integer). Extend updateMutation's mutationFn arg type to include `requiresAck?: boolean; nagIntervalMinutes?: number | null`. On Save, send `requiresAck: editRequiresAck` and `nagIntervalMinutes: editRequiresAck && editNagSum>0 ? editNagSum : null` (Pitfall 3 — null when ack off). Saving ack-off relies on the Plan-01 backend to clear ackPending + recompute nextRun (D-12).
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit 2>&1 | tail -15 && npm run build 2>&1 | tail -8 && grep -Ec 'schedule\.requiresAck[[:space:]]*&&' src/pages/SchedulesPage.tsx && [ "$(grep -Ec 'ackPending[[:space:]]*&&' src/pages/SchedulesPage.tsx)" = "0" ] && echo BADGE_GATING_OK</automated>
  </verify>
  <acceptance_criteria>
    - SCHED-02 badge-gating (automated): `grep -Ec 'schedule\.requiresAck[[:space:]]*&&' dashboard/src/pages/SchedulesPage.tsx` returns >= 2 (NAGS badge condition + sub-label suffix both gate on requiresAck)
    - SCHED-02 badge-gating negative (automated): `grep -Ec 'ackPending[[:space:]]*&&' dashboard/src/pages/SchedulesPage.tsx` returns 0 — the badge/sub-label MUST NOT gate on ackPending (Pitfall 2 / D-06; ackPending is never projected to this page)
    - `grep -c "NAGS" dashboard/src/pages/SchedulesPage.tsx` >= 1 (badge label present)
    - `grep -c "nags every" dashboard/src/pages/SchedulesPage.tsx` >= 1 (sub-label + tooltip)
    - Edit modal has editRequiresAck state + reveal-when-on nag slot inputs (`grep -c "editRequiresAck" dashboard/src/pages/SchedulesPage.tsx` >= 1 and `grep -c "editNagMinutes" dashboard/src/pages/SchedulesPage.tsx` >= 1)
    - updateMutation mutationFn type includes `nagIntervalMinutes` (`grep -c "nagIntervalMinutes" dashboard/src/pages/SchedulesPage.tsx` >= 1)
    - `cd dashboard && npx tsc --noEmit` clean and `npm run build` (dashboard) succeeds
  </acceptance_criteria>
  <done>Ack-required reminder rows show the NAGS badge + nag-cadence sub-label gated on requiresAck (not ackPending, asserted by grep); the edit modal lets the user toggle ack and edit nag slots with client validation, sending the fields via PATCH.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify NAGS badge, sub-label, and edit-modal ack editing</name>
  <files>dashboard/src/pages/SchedulesPage.tsx</files>
  <what-built>
    The reminder list now shows a static amber "NAGS" badge on ack-required reminder rows plus a "· nags every Xh" suffix in the sub-label, and the reminder edit modal has a Requires-acknowledgment toggle that reveals minutes/hours/days nag inputs with inline validation. The badge-gating correctness (requiresAck, NOT ackPending) is now asserted by grep in Task 2's automated verify; this checkpoint confirms the *rendered* React behavior (visual + interaction) that the backend-only vitest config (no jsdom) cannot cover — per 39-VALIDATION.md Manual-Only Verifications.
  </what-built>
  <action>
    Pause for human verification. Tasks 1-2 already automated the implementation and the badge-gating grep assertions; this checkpoint confirms the rendered React behavior (visual layout + interaction) that the backend-only vitest config cannot cover. Present the how-to-verify steps to the user and wait for the resume signal.
  </action>
  <how-to-verify>
    1. Open the dashboard Schedules page (jarvis.gigaashley.click or local dev) and confirm an existing ordinary reminder shows NO badge.
    2. Edit a reminder: toggle "Requires acknowledgment" ON — confirm the minutes/hours/days nag inputs appear (hidden when off). Set e.g. 1h, Save.
    3. Confirm that reminder's row now shows the amber "NAGS" badge and the sub-label reads "... · nags every 1h". Hover the badge: tooltip says "Nags every 1h until acknowledged".
    4. Edit it again, set ack ON but clear all nag inputs → confirm Save is disabled with an inline message. Set nag sum > 43200 (e.g. 31 days) → confirm Save disabled with the 30-day message.
    5. Toggle ack OFF and Save → confirm the badge disappears on the row (requiresAck now false).
  </how-to-verify>
  <verify>
    <human-check>NAGS badge renders only on requiresAck reminders, nag sub-label shows the cadence, edit-modal reveal-when-on + validation works, ack-off removes the badge</human-check>
  </verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly</resume-signal>
  <done>User confirms (resume signal "approved") that the NAGS badge, nag sub-label, and edit-modal ack editing render and validate correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard browser → PATCH /api/schedules (via api.schedules.update) | Edit-modal sends requiresAck/nagIntervalMinutes; client validation is convenience only — the Plan-01 backend route is the source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering | edit-modal nag value sent to PATCH | mitigate | Client disables Save on coupling/ceiling violation, but the authoritative validation is the Plan-01 PATCH route (D-04); the UI cannot bypass server checks |
| T-39-07 | Information Disclosure | NAGS badge condition | accept | Badge derives only from requiresAck (already returned by GET behind requireAuth); no new data exposure, no ackPending projection (D-06) |
| T-39-08 | Tampering | api.ts payload typing | mitigate | Optional typed fields only; undefined keys dropped by JSON.stringify so a stale client cannot inject unexpected fields; server ignores unknown body keys |
| T-39-SC | Tampering | npm installs | accept | No package installs in this phase (RESEARCH audit: none new) |
</threat_model>

<verification>
- `cd dashboard && npx tsc --noEmit` clean; `npm run build` (dashboard) succeeds
- `npx tsc --noEmit` (root) clean
- SCHED-02 badge-gating (automated): `grep -Ec 'schedule\.requiresAck[[:space:]]*&&' dashboard/src/pages/SchedulesPage.tsx` >= 2 AND `grep -Ec 'ackPending[[:space:]]*&&' dashboard/src/pages/SchedulesPage.tsx` === 0 — badge/sub-label gate on requiresAck, never ackPending (D-06)
- Manual checkpoint (Task 3) approved: badge renders on requiresAck rows only, sub-label shows nag cadence, edit modal reveal-when-on + validation works, ack-off removes badge
</verification>

<success_criteria>
- SCHED-02: ack-required reminders are visually distinguished (NAGS badge + nag sub-label); ordinary reminders are not (D-05/D-06)
- SCHED-02 badge-gating is automatically verified to use requiresAck and never ackPending (grep assertions in Task 2 + verification block)
- SCHED-01 (edit half / D-11): ack + nag editable from the reminder edit modal with reveal-when-on multi-slot UI and client validation matching the tool's rules (D-04)
- api.ts create/update payloads carry the new fields for Plan 03 to consume
- Dashboard builds and type-checks clean
</success_criteria>

<output>
Create `.planning/phases/39-dashboard-reminder-ui/39-02-SUMMARY.md` when done
</output>
