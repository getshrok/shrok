---
phase: 55-app-serving-subsystem
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/apps/workspace.ts
  - src/apps/db.ts
  - src/apps/discovery.ts
  - src/apps/adapter.ts
autonomous: true
requirements: [APPSRV-02, APPSRV-04, APPSRV-05, APPSRV-06, APPSRV-07]
user_setup: []

must_haves:
  truths:
    - "Apps are discovered by scanning a single global {workspace}/apps directory — no head_id, no DB registry (D-01, D-02)"
    - "A new app folder is picked up on the next request with no restart because discovery loads per-request and a new slug is not in the import cache (D-02 → APPSRV-06)"
    - "An app module that fails to import or lacks get()/action() is registered as { error } and never throws out of discovery (D-04, D-09 → APPSRV-04)"
    - "ensurePackageSymlink(workspacePath) idempotently links {workspace}/node_modules/@ashley-shrok/viewmodel-shell to the repo copy (repoRoot resolved from import.meta.url, NOT cwd; created if absent, repaired if wrong/broken) so a workspace app can resolve its bare-specifier VMS imports (D-11)"
    - "Apps open their OWN co-located node:sqlite via the node:sqlite builtin at new URL('./data.sqlite', import.meta.url); src/apps/db.ts (DatabaseSync+WAL, name-guarded, cached) is a HOST/test helper that is NOT imported by apps (D-03, D-11a → APPSRV-05)"
    - "The Express↔web-Request adapter builds the web Request body from the already-parsed req.body (JSON.stringify), not the raw stream, because express.json consumed it (D-06, D-07 → APPSRV-02)"
    - "Unit tests exercise the REAL loadApp dynamic import against a production-faithful temp workspace ({workspace}/apps/<slug>/ with the package symlink ensured) using .mjs fixtures (which vitest loads without transpile); production apps stay .ts; discovery resolves an entry as app.ts OR app.mjs (D-11b)"
  artifacts:
    - path: "src/apps/workspace.ts"
      provides: "ensurePackageSymlink(workspacePath) — idempotent {workspace}/node_modules/@ashley-shrok/viewmodel-shell symlink to the repo copy (D-11)"
      contains: "symlink"
    - path: "src/apps/db.ts"
      provides: "HOST/test-facing per-app node:sqlite helper (DatabaseSync+WAL, name-guarded, cached) — NOT imported by apps (D-11a)"
      contains: "DatabaseSync"
    - path: "src/apps/discovery.ts"
      provides: "SLUG_RE guard, AppMod/Loaded types, listApps(), loadApp() (resolves app.ts|app.mjs) with per-app load-time error boundary"
      contains: "SLUG_RE"
    - path: "src/apps/adapter.ts"
      provides: "toWebRequest + sendWeb bridge with the D-07 parsed-body fix"
      contains: "toWebRequest"
  key_links:
    - from: "src/apps/workspace.ts ensurePackageSymlink()"
      to: "{repoRoot}/node_modules/@ashley-shrok/viewmodel-shell"
      via: "fs.symlinkSync into {workspace}/node_modules/@ashley-shrok/"
      pattern: "symlinkSync"
    - from: "src/apps/discovery.ts loadApp()"
      to: "agent-authored {workspace}/apps/<slug>/app.ts (or app.mjs)"
      via: "dynamic import() with /* @vite-ignore */"
      pattern: "import\\("
    - from: "src/apps/adapter.ts toWebRequest"
      to: "app module action(Request)"
      via: "JSON.stringify(req.body) body"
      pattern: "JSON.stringify"
---

<objective>
Build the self-contained foundation modules of the app-serving subsystem, ported from the verified PoC
into shrok's `src/` and conventions, plus the workspace package-symlink that makes workspace apps able to
resolve their imports (D-11). None of these modules import the VMS package at module scope (they use Node
globals + node:sqlite + a runtime symlink + runtime dynamic import), so this plan runs in parallel with
the dependency add (Plan 01) — `depends_on: []` is preserved.

- `src/apps/workspace.ts` — `ensurePackageSymlink(workspacePath)`: the idempotent
  `{workspace}/node_modules/@ashley-shrok/viewmodel-shell → {repoRoot}/node_modules/...` symlink so a
  workspace app's bare-specifier `@ashley-shrok/viewmodel-shell/server` import resolves (D-11). Plan 03's
  router calls this at construction; this plan defines + unit-tests it.
- `src/apps/db.ts` — a HOST/test-facing per-app `node:sqlite` helper (DatabaseSync+WAL, name-guarded,
  cached). ⚠️ Per D-11a apps do NOT import it — an app opens its own co-located sqlite directly via the
  `node:sqlite` builtin at `new URL('./data.sqlite', import.meta.url)`. db.ts exists for the host/tests to
  open an app's DB by path (e.g. to verify persistence).
- `src/apps/discovery.ts` — filesystem discovery, the `SLUG_RE` traversal guard, the `AppMod`/`Loaded`
  types, `listApps()` (enumeration, no import) and `loadApp()` (per-request load with the per-app
  load-time error boundary; resolves an entry as `app.ts` OR `app.mjs`) (D-01/D-02/D-04/D-09/D-11b).
- `src/apps/adapter.ts` — the ~15-line Express↔web-Fetch bridge with the D-07 parsed-body fix (D-06/D-07).

Purpose: these are the load-bearing, de-risked core. Plan 03 (router) composes them.
Output: four new modules under `src/apps/` that typecheck and are unit-tested via the real load path (D-11b).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-app-serving-subsystem/55-CONTEXT.md
@.planning/phases/55-app-serving-subsystem/55-PATTERNS.md
@AGENTS.md
@src/db/index.ts
@src/dashboard/routes/sensors.ts

<interfaces>
<!-- The PoC shapes to port (verified). Target these exactly; apply shrok conventions noted below. -->
PoC discovery + error boundary: /tmp/claude-1000/-home-thenasty/441732fc-a717-460b-b63c-e3bc985fa8f6/scratchpad/vms-poc/host.ts (lines 19-42, types + load loop)
PoC adapter: same host.ts lines 53-65 (toWebRequest/sendWeb)
PoC per-app db helper to port: /tmp/.../scratchpad/vms-poc/lib/db.ts (appDb — change DATA_DIR to co-located apps/<slug>/)

Types to define in discovery.ts (from PoC host.ts:19-24):
  AppMod = { meta?: { title?: string; icon?: string; desc?: string }; get: () => { vm: unknown; state: unknown }; action: (req: Request) => Promise<Response> }
  Loaded = { slug: string; meta: Record<string,string>; mod?: AppMod; error?: string }

shrok conventions to mirror:
  SLUG_RE = /^[a-z0-9][a-z0-9-]*$/   (src/dashboard/routes/sensors.ts:12)
  initDb body — new DatabaseSync(path) + PRAGMA journal_mode=WAL + PRAGMA foreign_keys=ON (src/db/index.ts:7-13)
  DatabaseSync is re-exported from src/db/index.ts:5
  mkdir-before-readdir idiom (src/dashboard/routes/sensors.ts:90)
  import.meta.url-relative resolution: path.resolve(path.dirname(fileURLToPath(import.meta.url)), '../..') (server.ts:283/357 idiom)
  noUncheckedIndexedAccess + exactOptionalPropertyTypes are ON (see AGENTS.md TypeScript section)

D-11 resolution note:
  A workspace app lives in {workspace}/apps/<slug>/ — a DIFFERENT tree from the repo node_modules — so
  `import "@ashley-shrok/viewmodel-shell/server"` fails Node's bare-specifier resolution (Node walks up
  from the importing file's OWN dir, not cwd). ensurePackageSymlink puts the package on that walk-up path
  via {workspace}/node_modules/@ashley-shrok/viewmodel-shell → {repoRoot}/node_modules/@ashley-shrok/viewmodel-shell.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace package symlink — src/apps/workspace.ts (D-11)</name>
  <files>src/apps/workspace.ts</files>
  <read_first>
    - src/dashboard/server.ts (lines 283/357 — the fileURLToPath(import.meta.url) resolution idiom; resolve repoRoot the same way, NEVER cwd)
    - .planning/phases/55-app-serving-subsystem/55-CONTEXT.md (D-11, D-11a)
    - .planning/phases/55-app-serving-subsystem/55-PATTERNS.md (the import.meta.url pkgDir note in the shell.ts section)
  </read_first>
  <behavior>
    - ensurePackageSymlink(workspacePath) computes target = {repoRoot}/node_modules/@ashley-shrok/viewmodel-shell where {repoRoot} is resolved from import.meta.url (../.. from src/apps/), and link = {workspace}/node_modules/@ashley-shrok/viewmodel-shell.
    - It recursively mkdirs {workspace}/node_modules/@ashley-shrok/ then ensures the symlink: if an entry exists and is a symlink whose readlink target equals the expected target, it is a no-op; if an entry exists but is wrong or broken, it is removed and re-linked; if absent, it is created.
    - Idempotent: a second call with the same workspace makes no change and does not throw.
    - It must NOT require the package's target to actually exist on disk (symlinkSync creates the link regardless), so it can run in parallel with the Plan 01 install — verification uses readlink, not realpath.
  </behavior>
  <action>
    Create `src/apps/workspace.ts` exporting `ensurePackageSymlink(workspacePath: string): void`. Resolve
    `{repoRoot}` from the compiled module location — `path.resolve(path.dirname(fileURLToPath(import.meta.url)),
    '../..')` (the same import.meta.url idiom server.ts/shell.ts use; NEVER cwd). Compute
    `target = path.join(repoRoot, 'node_modules', '@ashley-shrok', 'viewmodel-shell')`,
    `linkDir = path.join(workspacePath, 'node_modules', '@ashley-shrok')`, and
    `link = path.join(linkDir, 'viewmodel-shell')`. `fs.mkdirSync(linkDir, { recursive: true })`. Then
    idempotently ensure the link: use `fs.lstatSync(link, { throwIfNoEntry: false })`; if it is a symlink and
    `fs.readlinkSync(link) === target`, return (no-op); if any entry exists (wrong target, non-symlink, or
    broken), `fs.rmSync(link, { recursive: true, force: true })` then `fs.symlinkSync(target, link)`; if no
    entry, `fs.symlinkSync(target, link)`. Compare via readlink (NOT realpathSync) so a not-yet-installed
    package (Plan 01 still running in the same wave) does not make this throw. This function MUST run at
    subsystem startup BEFORE any app is loaded — Plan 03's createAppsRouter calls it at factory construction.
    Rationale (D-11): a workspace app does `import { createAction } from "@ashley-shrok/viewmodel-shell/server"`;
    Node resolves bare specifiers from the importing file's own dir upward, so without this link a
    {workspace}/apps/<slug> module cannot resolve the package — keeping workspace apps shaped EXACTLY like the
    vms-apps reference apps (matters for Phase 56 codegen). Honor noUncheckedIndexedAccess and use `.js`
    import extensions.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx vitest run src/apps/workspace.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/apps/workspace.ts` exists exporting `ensurePackageSymlink(workspacePath)`.
    - Source resolves repoRoot via `fileURLToPath(import.meta.url)` (contains NO cwd / `process.cwd()` and NO hardcoded path) and creates the `@ashley-shrok` parent dir before linking; comparison uses `readlinkSync` (not `realpathSync`).
    - `src/apps/workspace.test.ts` proves against a tmp workspace: the link is created and `fs.readlinkSync(link)` ends in `node_modules/@ashley-shrok/viewmodel-shell`; a second call is a no-op (idempotent, no throw); a pre-existing wrong/broken symlink is repaired to the correct target.
    - `npx vitest run src/apps/workspace.test.ts` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The VMS package resolves from any workspace app via an idempotent startup symlink resolved from import.meta.url, runnable before the package is even installed (D-11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-app node:sqlite helper (HOST/test-facing) — src/apps/db.ts</name>
  <files>src/apps/db.ts</files>
  <read_first>
    - src/db/index.ts (initDb body, lines 7-13 — the canonical WAL+FK init to mirror; DatabaseSync re-export)
    - /tmp/claude-1000/-home-thenasty/441732fc-a717-460b-b63c-e3bc985fa8f6/scratchpad/vms-poc/lib/db.ts (the appDb to port — note PoC uses a shared ../data dir; D-03 requires co-location instead)
    - .planning/phases/55-app-serving-subsystem/55-CONTEXT.md (D-11a — db.ts is HOST/tests only, apps do NOT import it)
    - .planning/phases/55-app-serving-subsystem/55-PATTERNS.md ("src/apps/db.ts" section)
  </read_first>
  <behavior>
    - Given a valid app dir and db name, returns a DatabaseSync opened at apps/<slug>/<name>.sqlite with journal_mode=WAL and foreign_keys=ON.
    - Calling twice with the same (appDir, name) returns the SAME cached DatabaseSync instance (no re-open).
    - A db name not matching /^[a-z0-9_-]+$/i throws "Invalid app name" (no path.join with a traversal name).
    - The parent app directory is created (recursive mkdir) before opening the file.
  </behavior>
  <action>
    Create `src/apps/db.ts` exporting a per-app DB helper. Mirror the body of `initDb`
    (src/db/index.ts:7-13) — `new DatabaseSync(path)` + `PRAGMA journal_mode = WAL` + `PRAGMA
    foreign_keys = ON`, importing `DatabaseSync` from `node:sqlite` (or re-using the re-export from
    `../db/index.js`). Per D-03 the sqlite file is co-located in the app's own folder: the helper takes
    the app directory (e.g. `{workspace}/apps/<slug>`) plus a db name and opens `<appDir>/<name>.sqlite`
    — NOT a shared `data/` dir. Keep the PoC's name guard `/^[a-z0-9_-]+$/i` (throw `Invalid app name:
    <name>` on miss) and a per-path Map cache so repeated opens return the same handle. Use
    `fs.mkdirSync(dirname(path), { recursive: true })` before opening. Honor noUncheckedIndexedAccess
    (null-check Map.get results) and use `.js` import extensions.
    ⚠️ Per D-11a this helper is for the HOST + tests ONLY — agent-authored apps do NOT import it. An app
    opens its OWN co-located sqlite directly via the `node:sqlite` builtin at
    `new URL('./data.sqlite', import.meta.url)` (the builtin resolves everywhere, so an app needs no
    cross-tree import). db.ts exists so the host/tests can open an app's DB by path (e.g. to assert a POST
    persisted). Co-location means deleting apps/<slug>/ removes code + data together.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx vitest run src/apps/db.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/apps/db.ts` exists and exports the per-app DB helper.
    - Source contains `DatabaseSync`, `PRAGMA journal_mode = WAL`, the `/^[a-z0-9_-]+$/i` name guard, and a Map cache.
    - `src/apps/db.test.ts` proves: same (appDir,name) returns the same instance; an invalid name throws; the sqlite file lands inside the given app dir (co-located).
    - `npx vitest run src/apps/db.test.ts` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A HOST/test-facing, name-guarded, cached node:sqlite helper co-locates each app's DB in its own folder; apps open their own DB via node:sqlite + import.meta.url and do not import this helper (D-03, D-11a, APPSRV-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Discovery + load-time error boundary — src/apps/discovery.ts</name>
  <files>src/apps/discovery.ts</files>
  <read_first>
    - /tmp/claude-1000/-home-thenasty/441732fc-a717-460b-b63c-e3bc985fa8f6/scratchpad/vms-poc/host.ts (lines 19-42: AppMod/Loaded types + the discovery + per-app error boundary loop)
    - src/dashboard/routes/sensors.ts (SLUG_RE at line 12; mkdir-before-readdir at line 88-95; the filesystem-as-source-of-truth listing shape)
    - src/apps/workspace.ts (ensurePackageSymlink — Task 1; the discovery test calls it to set up the temp workspace symlink, D-11b)
    - .planning/phases/55-app-serving-subsystem/55-CONTEXT.md (D-11b — tests use the real load path with .mjs fixtures)
    - .planning/phases/55-app-serving-subsystem/55-PATTERNS.md ("src/apps/discovery.ts" section, including the hot-discovery + @vite-ignore guidance)
  </read_first>
  <behavior>
    - listApps(appsDir): scans the (mkdir'd) apps dir, returns [{ slug, meta }] for every subdir that has an app entry (app.ts OR app.mjs) and a SLUG_RE-valid name, reading meta.json only (never importing the module — so listing a broken app does not throw). A fresh workspace with no apps dir returns [].
    - loadApp(appsDir, slug): rejects a slug failing SLUG_RE or a reserved `_`-prefixed slug (returns undefined / not-found, never path.join with it); resolves the entry as apps/<slug>/app.ts, else apps/<slug>/app.mjs; returns undefined if neither exists; otherwise dynamic-imports the resolved entry inside try/catch and returns Loaded with { mod } on success or { error } on any import/throw/missing-export failure — never throws.
    - A module missing get() or action() is treated as a load error ("app must export get() and action").
    - Loading a brand-new slug not previously seen works without restart (a new slug is not in the import cache).
  </behavior>
  <action>
    Create `src/apps/discovery.ts`. Define and export the `AppMod` and `Loaded` types verbatim from the
    PoC (host.ts:19-24). Export `SLUG_RE = /^[a-z0-9][a-z0-9-]*$/` (mirror sensors.ts:12) and a
    `RESERVED` set/guard for the `_`-prefixed package routes so a slug can never be `_pkg` or `_skill`
    (reject any slug starting with `_`). Export two functions:
    (1) `listApps(appsDir): { slug: string; meta: Record<string,string> }[]` — `fs.mkdirSync(appsDir,
    {recursive:true})` then `readdirSync(..., {withFileTypes:true})`, keep dirs whose name passes SLUG_RE
    and contain an app entry (`app.ts` OR `app.mjs`), read `meta.json` with a try/catch (optional), and
    return the list WITHOUT importing any module (enumeration must not trigger app code or surface app
    errors). This is the Phase-57 enumeration source.
    (2) `loadApp(appsDir, slug): Promise<Loaded | undefined>` — return undefined if `!SLUG_RE.test(slug)`
    or slug is reserved, or neither `apps/<slug>/app.ts` nor `apps/<slug>/app.mjs` exists (resolve the entry
    preferring `app.ts`, else `app.mjs`, so the route 404s cleanly when absent). Read `meta.json` (optional).
    Then, inside try/catch (the per-app load-time error boundary, D-09), do
    `await import(/* @vite-ignore */ pathToFileURL(appFile).href)` — the `/* @vite-ignore */` and the
    file:// URL are REQUIRED so the dynamic import is resolved by the Node/tsx runtime at request time
    (cross-runtime: production apps are `.ts` and the daemon's tsx transpiles them process-globally; a
    `.mjs` entry is also accepted so vitest can load production-faithful `.mjs` test fixtures with NO
    transpile — D-11b). Validate the module exports `get` and `action` as functions (throw "app must
    export get() and action" otherwise); return `{ slug, meta: {...meta, ...(mod.meta ?? {})}, mod }` on
    success or `{ slug, meta, error: message }` on any failure. Maintain a module-level
    `Map<string, Loaded>` cache keyed by slug so an already-loaded app is not re-imported every request,
    but a cache MISS (new slug) loads fresh — this is exactly how D-02 delivers hot discovery (APPSRV-06):
    the apps dir is a single GLOBAL directory with no head scoping (D-01). Honor exactOptionalPropertyTypes
    (omit `error`/`mod` keys rather than setting undefined) and noUncheckedIndexedAccess.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx vitest run src/apps/discovery.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/apps/discovery.ts` exists exporting `SLUG_RE`, `AppMod`, `Loaded`, `listApps`, `loadApp`.
    - Source contains `SLUG_RE`, a reserved/`_`-prefix guard, `/* @vite-ignore */`, `pathToFileURL`, and resolves an entry as `app.ts` OR `app.mjs`.
    - `src/apps/discovery.test.ts` exercises the REAL loadApp against a production-faithful tmp workspace (D-11b): it calls `ensurePackageSymlink(tmpDir)` (Task 1) so the workspace is shaped like production, then writes `.mjs` fixtures under `tmpDir/apps/<slug>/app.mjs` (each exporting `get`/`action` and opening its own node:sqlite via `new URL('./data.sqlite', import.meta.url)` — NOT importing src/apps/db.ts; fixtures are `.mjs` because vitest will not transpile an external `.ts` the way the daemon's tsx does, and production apps stay `.ts`). It proves: fresh dir → listApps []; a good `.mjs` fixture loads (mod present); a fixture whose app.mjs throws on import → loadApp returns `{ error }` and does NOT throw; a module missing `action` → `{ error }`; a `../`-style or `_pkg` slug → undefined (no traversal); a NEW `.mjs` fixture folder created after first scan is discovered by a subsequent loadApp (hot discovery).
    - `npx vitest run src/apps/discovery.test.ts` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Discovery scans one global apps dir, enumerates without importing, loads per-request (app.ts|app.mjs) with a per-app error boundary, guards slugs against traversal/reserved names, and picks up new apps with no restart — verified via the real load path with .mjs fixtures (D-01/D-02/D-04/D-09/D-11b; APPSRV-04/06/07).</done>
</task>

<task type="auto">
  <name>Task 4: Express↔web-Fetch adapter with the D-07 parsed-body fix — src/apps/adapter.ts</name>
  <files>src/apps/adapter.ts</files>
  <read_first>
    - /tmp/claude-1000/-home-thenasty/441732fc-a717-460b-b63c-e3bc985fa8f6/scratchpad/vms-poc/host.ts (lines 53-65: toWebRequest + sendWeb — the target shape; note the PoC's readRaw at 45-52 is the path we MUST replace per D-07)
    - .planning/phases/55-app-serving-subsystem/55-CONTEXT.md (D-06 adapter, D-07 body-already-parsed gotcha)
    - .planning/phases/55-app-serving-subsystem/55-PATTERNS.md ("src/apps/adapter.ts" section)
  </read_first>
  <action>
    Create `src/apps/adapter.ts` exporting two functions typed against Express's `Request`/`Response`
    (import the Express request/response types like sensors.ts does):
    (1) `toWebRequest(req, body: string): Request` — build a `Headers` from `req.headers`, SKIPPING
    `content-length` and `host` (let fetch recompute length) and any null value; construct a global
    `new Request(\`http://localhost${req.originalUrl}\`, { method: req.method, headers, body })`. Node 22
    provides global `Request`/`Response`/`Headers`.
    (2) `sendWeb(webRes: Response, res): Promise<void>` — `res.status(webRes.status)`, copy every
    `webRes.headers` onto `res.setHeader`, then `res.send(await webRes.text())`.
    Document at the top, in a comment, the D-07 contract: callers MUST pass the body as
    `JSON.stringify(req.body ?? {})` because `src/dashboard/server.ts:170` mounts `express.json` globally
    and has ALREADY consumed the request stream — the PoC's raw-stream read would get nothing here. The
    content-type stays `application/json` (carried in req.headers), which `createAction`
    content-type-detects to parse the VMS `{name,state}` payload. Do NOT read the raw stream. Multipart
    file-upload apps are explicitly deferred (Deferred Ideas) — this adapter handles the JSON action wire
    only.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx vitest run src/apps/adapter.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/apps/adapter.ts` exists exporting `toWebRequest` and `sendWeb`.
    - Source skips `content-length` and `host` headers and contains a comment naming the D-07 `JSON.stringify(req.body)` contract.
    - `src/apps/adapter.test.ts` proves: `toWebRequest` produces a web `Request` whose `.json()` round-trips a `{name,state}` body and whose method/headers (minus content-length/host) are preserved; `sendWeb` copies status + headers + body text onto a mock Express `res`.
    - `npx vitest run src/apps/adapter.test.ts` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A ~15-line Express↔web-Fetch bridge passes the already-parsed JSON body to the app's createAction handler (D-06/D-07; APPSRV-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem slug/name → path.join | A `:slug` directory name or a db `name` could contain traversal sequences; must be validated before any path join. |
| agent-authored app.ts/.mjs → host process | A discovered module may throw at import or be malformed; must be caught at the discovery boundary so it cannot crash the host. |
| repo node_modules → workspace symlink | The startup symlink exposes the repo's installed VMS package on the workspace's module-resolution walk-up path; scoped to the single `@ashley-shrok/viewmodel-shell` package, pointing only at the repo's own copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02-TRAVERSAL | Information Disclosure | discovery.loadApp / db helper name | mitigate | SLUG_RE `/^[a-z0-9][a-z0-9-]*$/` + reserved `_`-prefix guard before any path.join in loadApp; `/^[a-z0-9_-]+$/i` name guard in the db helper (PATTERNS T-49-01-TRAVERSAL precedent). A `../` or `_pkg` slug yields undefined, never a read outside the apps dir. |
| T-55-02-CRASH | Denial of Service | discovery.loadApp | mitigate | Per-app load-time error boundary (try/catch around dynamic import + export validation) registers a broken app as `{ error }` and never throws — one bad app cannot crash discovery or the host (D-09 → APPSRV-04). |
| T-55-02-ISO | Information Disclosure | per-app db helper | mitigate | Each app's sqlite is co-located in apps/<slug>/ and the helper opens only paths under the passed app dir; an app cannot name a path into another app's folder (name guard rejects `/`, `.`). |
| T-55-02-LINK | Tampering | ensurePackageSymlink | mitigate | The symlink target is fixed (the repo's own `@ashley-shrok/viewmodel-shell`, resolved from import.meta.url, not caller-supplied); idempotent create/repair only ever points the link at that one path under {workspace}/node_modules/@ashley-shrok/. |
| T-55-02-EXEC | Elevation | dynamic import of app.ts/.mjs | accept | Apps run in-process by design (REQUIREMENTS.md "same trust posture as skills/sensors/tasks; no new sandbox"). The authoring agent already has file/bash tools. |
</threat_model>

<verification>
- `npx vitest run src/apps/` — workspace, db, discovery, and adapter unit suites pass.
- `npx tsc --noEmit` clean.
- Discovery test demonstrates the real load path (.mjs fixtures + symlink-faithful workspace), the hot-discovery path, and the broken-app error boundary.
</verification>

<success_criteria>
- src/apps/workspace.ts, db.ts, discovery.ts, adapter.ts exist, typecheck, and pass their unit tests.
- ensurePackageSymlink idempotently creates/repairs the workspace VMS symlink (D-11).
- A broken fixture app is contained as `{ error }`, never throwing out of discovery.
- A traversal/reserved slug is rejected before any path.join.
- The adapter passes the parsed JSON body (D-07), not the raw stream.
- Tests use the real loadApp path against a production-faithful temp workspace with .mjs fixtures (D-11b).
</success_criteria>

<output>
Create `.planning/phases/55-app-serving-subsystem/55-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
