---
phase: 41-inbound-synchronous-reply-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/channels/home-assistant/types.ts
  - src/channels/home-assistant/types.test.ts
  - src/config.ts
  - src/config.test.ts
autonomous: true
requirements: [HACV-01, HACV-02, HACV-03, HACV-05]
must_haves:
  truths:
    - "extractLastUserTurn returns the last role:'user' message content and discards earlier turns"
    - "extractLastUserTurn returns null when no user turn is present (caller responds 400)"
    - "buildChatCompletionResponse returns a non-empty content, finish_reason:'stop', zeroed usage, and echoes conversation_id"
    - "ENV_KEY_ALLOWLIST contains HA_INBOUND_API_KEY so the settings API may write it to .env"
  artifacts:
    - path: "src/channels/home-assistant/types.ts"
      provides: "extractLastUserTurn + buildChatCompletionResponse contracts consumed by router.ts (Plan 03)"
      exports: ["extractLastUserTurn", "buildChatCompletionResponse"]
    - path: "src/channels/home-assistant/types.test.ts"
      provides: "Unit coverage for last-user-turn extraction (HACV-03) and response shape (HACV-01/05)"
    - path: "src/config.ts"
      provides: "HA_INBOUND_API_KEY in ENV_KEY_ALLOWLIST"
      contains: "HA_INBOUND_API_KEY"
  key_links:
    - from: "src/channels/home-assistant/router.ts (Plan 03)"
      to: "src/channels/home-assistant/types.ts"
      via: "import { extractLastUserTurn, buildChatCompletionResponse } from './types.js'"
      pattern: "extractLastUserTurn|buildChatCompletionResponse"
---

<objective>
Establish the leaf contracts the inbound `/v1/chat/completions` endpoint consumes: the OpenAI-compat request/response helpers in a new `src/channels/home-assistant/types.ts`, and the `HA_INBOUND_API_KEY` allowlist entry in `src/config.ts`. These are pure functions and a one-line config change with zero dependencies on the rest of Phase 41, so they ship first and downstream plans (router, adapter) import against fixed signatures.

Purpose: Define the data-shape contracts (`extractLastUserTurn`, `buildChatCompletionResponse`) and register the dedicated inbound bearer key (D-02) so Plan 03's router has stable imports and Plan 02's adapter fail-fast has a registered key.
Output: `types.ts` + `types.test.ts` (new), `config.ts` (one allowlist line) + `config.test.ts` (two new tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-inbound-synchronous-reply-endpoint/41-CONTEXT.md
@.planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES (consumed by Plan 03 router). Executor defines these. -->
extractLastUserTurn(messages: Array<{ role: string; content: string | null }>): string | null
buildChatCompletionResponse(content: string, conversationId: string): Record<string, unknown>

<!-- Existing contract this plan reads (the openai SDK ChatCompletion type, for tsc shape checking only): -->
From openai/resources/chat/completions/completions.js:
  type ChatCompletion = { id; object: 'chat.completion'; created; model; choices: [{ index; message: { role: 'assistant'; content }; finish_reason: 'stop'; logprobs: null }]; usage }

<!-- Existing config contract this plan extends: -->
From src/config.ts (~line 492):
  export const ENV_KEY_ALLOWLIST = [ ... , 'HA_ACCESS_TOKEN', 'SEARCH_PROVIDER', ... ] as const
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create types.ts with extractLastUserTurn + buildChatCompletionResponse and their tests</name>
  <files>src/channels/home-assistant/types.ts, src/channels/home-assistant/types.test.ts</files>
  <read_first>
    - src/channels/home-assistant/adapter.test.ts (lines 88-97 — the `noUncheckedIndexedAccess` null-check idiom already used in this directory: `const msg = received[0]; expect(msg).toBeDefined(); if (msg) {...}`)
    - src/types/channel.ts (lines 1-11 — `InboundMessage` shape, the pure-types + co-located-helper file convention to mirror)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md (`types.ts` section, lines 136-187 — the two exports and the loop-from-end extraction idiom)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-RESEARCH.md (Pattern 4 + Pattern 5 + Code Examples — exact response field population and the `content must never be null/empty` rule, PITFALLS P7)
  </read_first>
  <behavior>
    - extractLastUserTurn: given messages `[{role:'system',...},{role:'user',content:'first'},{role:'assistant',...},{role:'user',content:'latest'}]` returns `'latest'` (loops from the END, picks the LAST role:'user').
    - extractLastUserTurn: given messages with a trailing assistant turn after the last user turn still returns the last role:'user' content (HA sends full history; only the latest user turn matters — HACV-03).
    - extractLastUserTurn: returns `null` for `[]`, for an array with no role:'user' entry, and for a last user entry whose `content` is `null` or `''` (empty/whitespace handled by the caller's 400).
    - extractLastUserTurn: tolerates `content === null` on intermediate entries (uses `messages[i]?.role` + `typeof content === 'string'` guard — satisfies noUncheckedIndexedAccess).
    - buildChatCompletionResponse('hello', 'conv-123'): returns object with `object === 'chat.completion'`, `model === 'shrok'`, `choices[0].message.role === 'assistant'`, `choices[0].message.content === 'hello'`, `choices[0].finish_reason === 'stop'`, `choices[0].logprobs === null`, `choices[0].index === 0`.
    - buildChatCompletionResponse: `usage` is `{ prompt_tokens: 0, completion_tokens: 0, total_tokens: 0 }` (all zero — HACV-01).
    - buildChatCompletionResponse: top-level `conversation_id === 'conv-123'` (echoed — HACV-05).
    - buildChatCompletionResponse: `id` starts with `'chatcmpl-'` and `created` is a positive integer (seconds).
  </behavior>
  <action>
    Create `src/channels/home-assistant/types.ts` as a pure-types + pure-function module — no `express` import, no class, runtime deps limited to `randomUUID` from `node:crypto`. Import the openai SDK type for tsc shape checking only: `import type { ChatCompletion } from 'openai/resources/chat/completions/completions.js'` (type-only — zero runtime cost). Use `.js` extensions on any intra-project import (tsconfig `moduleResolution: bundler`).

    Export `extractLastUserTurn(messages: Array<{ role: string; content: string | null }>): string | null` — loop `for (let i = messages.length - 1; i >= 0; i--)`, read `const msg = messages[i]`, and return `msg.content` when `msg?.role === 'user' && typeof msg.content === 'string' && msg.content.length > 0`. Use `msg?.role` (optional chaining) NOT `messages[i].role` to satisfy `noUncheckedIndexedAccess`. Return `null` if the loop completes with no match.

    Export `buildChatCompletionResponse(content: string, conversationId: string): Record<string, unknown>` returning the exact non-streaming Chat Completions shape: `id: 'chatcmpl-' + randomUUID()`, `object: 'chat.completion'`, `created: Math.floor(Date.now() / 1000)`, `model: 'shrok'`, `choices: [{ index: 0, message: { role: 'assistant', content }, finish_reason: 'stop', logprobs: null }]`, `usage: { prompt_tokens: 0, completion_tokens: 0, total_tokens: 0 }`, and the extension field `conversation_id: conversationId` at top level (echo for HA ChatLog stitching — HACV-05). NEVER allow `content` to be null/empty — the caller guarantees a non-empty string before invoking (PITFALLS P7). Do NOT inline an implementation in this plan; write it in the file.

    Create `src/channels/home-assistant/types.test.ts` covering every row in `<behavior>`. Mirror the `noUncheckedIndexedAccess` null-check idiom from `adapter.test.ts:88-97` for any array index assertion. Import via `import { extractLastUserTurn, buildChatCompletionResponse } from './types.js'`.
  </action>
  <verify>
    <automated>npx vitest run src/channels/home-assistant/types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/channels/home-assistant/types.test.ts` exits 0 with all `<behavior>` rows green.
    - `grep -c "export function extractLastUserTurn\|export function buildChatCompletionResponse" src/channels/home-assistant/types.ts` returns 2.
    - `grep -c "from 'express'" src/channels/home-assistant/types.ts` returns 0 (types.ts has no express dependency).
    - `grep -c "conversation_id: conversationId" src/channels/home-assistant/types.ts` returns 1 (HACV-05 echo present).
    - `npx tsc --noEmit` reports no new errors originating in `src/channels/home-assistant/types.ts`.
  </acceptance_criteria>
  <done>extractLastUserTurn and buildChatCompletionResponse exist with the contracts above, fully unit-tested, tsc-clean, and importable by Plan 03's router.</done>
</task>

<task type="auto">
  <name>Task 2: Add HA_INBOUND_API_KEY to ENV_KEY_ALLOWLIST and pin it with config tests</name>
  <files>src/config.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts (lines 489-515 — the `ENV_KEY_ALLOWLIST` const; `HA_ACCESS_TOKEN` is at line 510)
    - src/config.test.ts (lines 505-604 — the existing `Phase 40 home-assistant channel vendor` describe block, including the `ENV_KEY_ALLOWLIST includes HA_ACCESS_TOKEN (D-04)` test at line 600 to mirror)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md (`src/config.ts` section, lines 447-472 — exact placement after HA_ACCESS_TOKEN)
  </read_first>
  <action>
    In `src/config.ts`, add the string literal `'HA_INBOUND_API_KEY'` to the `ENV_KEY_ALLOWLIST` array immediately AFTER the existing `'HA_ACCESS_TOKEN',` entry (line 510), grouping the two HA keys together. This is the dedicated inbound bearer key from D-02 — distinct from the Phase-40 outbound `HA_ACCESS_TOKEN`. The operator invents its value and pastes it into both the Extended OpenAI Conversation HACS component and Shrok's `.env`. Do NOT add any per-channel `haInboundApiKey` field to `ChannelConfigSchema` and do NOT add a flat `ConfigSchema` key — the key lives ONLY in the env allowlist (it is never written to git-tracked `config.json`), mirroring the Phase-40 `HA_ACCESS_TOKEN` handling (Plan 40-01 D-04).

    In `src/config.test.ts`, inside the existing `Phase 40 home-assistant channel vendor` describe block (or a new `Phase 41` describe block in the same file), add a test `ENV_KEY_ALLOWLIST includes HA_INBOUND_API_KEY (D-02)` asserting `expect(ENV_KEY_ALLOWLIST).toContain('HA_INBOUND_API_KEY')`. Mirror the existing `HA_ACCESS_TOKEN` test at line 600. Also add a test asserting `HA_INBOUND_API_KEY` is distinct from `HA_ACCESS_TOKEN` (both present, two separate entries) — `expect(ENV_KEY_ALLOWLIST).toContain('HA_ACCESS_TOKEN')` AND `expect(ENV_KEY_ALLOWLIST).toContain('HA_INBOUND_API_KEY')`.
  </action>
  <verify>
    <automated>npx vitest run src/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'HA_INBOUND_API_KEY'" src/config.ts` returns 1.
    - `npx vitest run src/config.test.ts` exits 0 including the new `HA_INBOUND_API_KEY` assertions.
    - `grep -v '^//' src/config.ts | grep -c "haInboundApiKey"` returns 0 (no per-channel config field — env-only per D-02).
    - `npx tsc --noEmit` reports no new errors in `src/config.ts`.
  </acceptance_criteria>
  <done>HA_INBOUND_API_KEY is in ENV_KEY_ALLOWLIST, distinct from HA_ACCESS_TOKEN, env-only, and pinned by config tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings API → .env file | The dashboard settings router may only write keys present in `ENV_KEY_ALLOWLIST`; adding `HA_INBOUND_API_KEY` here is what authorizes that write path |
| HA payload → Shrok parse | HA's `messages[]` is untrusted input parsed by `extractLastUserTurn` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Information Disclosure | `HA_INBOUND_API_KEY` in allowlist | mitigate | Key registered for `.env` only; never added to `ChannelConfigSchema`/`ConfigSchema` so it cannot land in git-tracked `config.json` (acceptance asserts `haInboundApiKey` field count == 0) |
| T-41-02 | Tampering | `extractLastUserTurn(messages)` | accept | Content is passed downstream (Plan 03) to the head's normal text pipeline — same path as every other channel adapter; no extra sanitization at the type layer. LAN-only + bearer-gated threat model |
| T-41-03 | Denial of Service | malformed/huge `messages[]` | mitigate | `extractLastUserTurn` is O(n) single-pass with no allocation; body-size bounded by the dashboard server's existing `express.json({ limit: '50mb' })` (enforced in Plan 03/04, not here) |
</threat_model>

<verification>
- `npx vitest run src/channels/home-assistant/types.test.ts` exits 0
- `npx vitest run src/config.test.ts` exits 0
- `npx tsc --noEmit` produces no new errors attributable to this plan's files
</verification>

<success_criteria>
- `extractLastUserTurn` and `buildChatCompletionResponse` exported from `types.ts` with the exact contracts in `<interfaces>`, fully unit-tested (HACV-01, HACV-03, HACV-05)
- `HA_INBOUND_API_KEY` present in `ENV_KEY_ALLOWLIST`, distinct from `HA_ACCESS_TOKEN`, env-only (HACV-02 / D-02)
- All new tests green; no new tsc errors
</success_criteria>

<output>
Create `.planning/phases/41-inbound-synchronous-reply-endpoint/41-01-SUMMARY.md` when done
</output>
