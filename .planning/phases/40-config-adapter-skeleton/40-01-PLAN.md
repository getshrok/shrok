---
phase: 40-config-adapter-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/config.test.ts
autonomous: true
requirements: [HACF-01, HACF-02]

must_haves:
  truths:
    - "A { vendor: 'home-assistant', id, haBaseUrl, haVoiceSatelliteEntityId } channel block parses without error (SC1)"
    - "A home-assistant block with a non-assist_satellite entity id prefix throws a clear startup error at loadConfig (SC2)"
    - "A home-assistant block with a malformed haBaseUrl throws a clear startup error at loadConfig (SC2/D-03)"
    - "A home-assistant block missing haBaseUrl or haVoiceSatelliteEntityId throws a clear startup error at loadConfig (SC2/D-01)"
    - "HA_ACCESS_TOKEN is in ENV_KEY_ALLOWLIST so config:set can write it to .env (SC1/D-04)"
    - "A config with NO home-assistant channel still loads unchanged (SC4)"
  artifacts:
    - path: "src/config.ts"
      provides: "home-assistant discriminated-union member + HA_ACCESS_TOKEN allowlist entry + extractSecretValues case"
      contains: "z.literal('home-assistant')"
    - path: "src/config.test.ts"
      provides: "parse-success + 3 negative + allowlist + backward-compat tests for the new vendor"
      contains: "home-assistant"
  key_links:
    - from: "src/config.ts ChannelConfigSchema"
      to: "z.discriminatedUnion('vendor')"
      via: "new union member"
      pattern: "vendor: z.literal\\('home-assistant'\\)"
    - from: "src/config.ts ENV_KEY_ALLOWLIST"
      to: "'HA_ACCESS_TOKEN'"
      via: "array append"
      pattern: "'HA_ACCESS_TOKEN'"
---

<objective>
Add `home-assistant` as a member of the `ChannelConfigSchema` discriminated union in
`src/config.ts` with parse-time validation (URL + strict `assist_satellite.` entity-id
prefix), append `'HA_ACCESS_TOKEN'` to `ENV_KEY_ALLOWLIST`, and add the
`extractSecretValues` switch case. This is the config foundation every later v1.5 phase
builds on — the token-in-`.env` security decision (D-04, PITFALLS P4/P5) must be made here
before any HA key is written anywhere.

Purpose: Wires the new vendor into config so a `home-assistant` channel block parses (SC1)
and malformed config fails fast at boot rather than silently at first use (SC2). Satisfies
HACF-01 (operator configures base URL + token in `.env` + satellite `entity_id`) and HACF-02
(configurable channel vendor in the Zod discriminated union with `entity_id` shape validation).

Output: Modified `src/config.ts`, expanded `src/config.test.ts`.

Note on wave dependency: adding the new union member makes the `_exhaustive: never` guard in
`src/index.ts:325` a tsc error until Plan 40-02 adds the matching `else if` branch. That
cross-file tsc RED is EXPECTED and intentional after this plan alone — Plan 02 (Wave 2) GREENs
it. Do NOT add the index.ts branch here (that is Plan 02's job and a separate file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-config-adapter-skeleton/40-CONTEXT.md
@.planning/phases/40-config-adapter-skeleton/40-PATTERNS.md

<interfaces>
<!-- The existing discriminated union members the new member must mirror. From src/config.ts:18-51. -->
<!-- Executor: copy the zoho-cliq member's object shape; substitute HA fields. No codebase exploration needed. -->

From src/config.ts (existing zoho-cliq member, lines 43-50) — the shape to mirror:
```typescript
z.object({
  id: z.string().min(1),
  vendor: z.literal('zoho-cliq'),
  clientId: z.string().min(1),
  // ...
}),
```

From src/config.ts (extractSecretValues switch, lines 393-399) — exhaustive over vendor; a
no-op case is required when the union grows (TypeScript exhaustiveness on `ch.vendor`).

From src/config.ts (ENV_KEY_ALLOWLIST, lines 482-504) — channel secrets grouped before
SEARCH_PROVIDER. `loadConfig` reads each allowlisted key; `config:set` writes only allowlisted keys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add home-assistant discriminated-union member + extractSecretValues case</name>
  <files>src/config.ts</files>
  <read_first>
    - src/config.ts (the file being modified — read ChannelConfigSchema lines 18-51, extractSecretValues lines 380-407, and ENV_KEY_ALLOWLIST lines 482-504 to see current state and exact insertion points)
    - .planning/phases/40-config-adapter-skeleton/40-PATTERNS.md (the "src/config.ts — discriminated union member addition" and "extractSecretValues switch" sections give the exact code to insert)
    - .planning/research/PITFALLS.md (P4/P10 — token stays in .env, store entity_id with assist_satellite. prefix, validate format at config load)
  </read_first>
  <behavior>
    - Parses a valid home-assistant block: { id: 'home-assistant', vendor: 'home-assistant', haBaseUrl: 'http://homeassistant.local:8123', haVoiceSatelliteEntityId: 'assist_satellite.home_assistant_voice_pe' } → success.
    - Rejects entity id with wrong domain prefix: 'light.kitchen' → Zod error mentioning haVoiceSatelliteEntityId / assist_satellite.
    - Rejects entity id with no domain: 'home_assistant_voice_pe' → Zod error.
    - Rejects malformed haBaseUrl: 'not-a-url' → Zod error on haBaseUrl.
    - Rejects missing haBaseUrl (field absent) → Zod error (no .optional() on required fields).
    - Rejects missing haVoiceSatelliteEntityId (field absent) → Zod error.
    - The home-assistant member has NO haAccessToken / botToken field (token is global per D-04).
  </behavior>
  <action>
    Insert a new `z.object({...})` member into the `ChannelConfigSchema` discriminated union in
    `src/config.ts`, placed immediately after the existing `zoho-cliq` member and before the
    array's closing `])`. The member has exactly these fields:
    `id: z.string().min(1)` (kept for consistency with every other member per D-06 — the adapter's
    registered/routing id is fixed to 'home-assistant' regardless of this value, so keeping the
    field is the consistent choice and the planner-chosen resolution of the D-06 open question);
    `vendor: z.literal('home-assistant')` (the discriminant);
    `haBaseUrl: z.string().url()` (D-03 — Zod rejects malformed URLs at parse time);
    `haVoiceSatelliteEntityId: z.string().regex(/^assist_satellite\.[a-z0-9_]+$/, 'haVoiceSatelliteEntityId must match assist_satellite.<object_id>')` (D-02 — strict assist_satellite. prefix; the exact regex source is `^assist_satellite\.[a-z0-9_]+$`).
    Do NOT add `.optional()` to any HA field (D-01 fail-fast: missing fields fail boot, identical to a
    missing botToken today). Do NOT add a `haAccessToken` field to this member (D-04: token is the
    global HA_ACCESS_TOKEN env key, never per-channel, never in config.json which is git-tracked).
    Then add a `case 'home-assistant': break` to the `switch (ch.vendor)` inside `extractSecretValues`
    (around src/config.ts:393) — a no-op because the HA channel has no per-channel secret fields;
    it exists only so TypeScript's exhaustiveness over the grown union is satisfied and no token value
    is ever pushed into the redaction list from a config.json field.
    Do NOT edit src/index.ts in this task or plan — the resulting `_exhaustive: never` tsc error there
    is the expected cross-file RED that Plan 40-02 resolves.
  </action>
  <verify>
    <automated>npx vitest run src/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "z.literal('home-assistant')" src/config.ts` returns 1 (the discriminated-union member).
    - Source: `grep -qF "assist_satellite" src/config.ts` succeeds (the entity-id regex is present).
    - Source: `grep -qE "haBaseUrl: z\.string\(\)\.url\(\)" src/config.ts` succeeds (D-03 URL validation).
    - Source: `grep -qF "case 'home-assistant': break" src/config.ts` succeeds (extractSecretValues no-op case).
    - Source: `grep -c "haAccessToken" src/config.ts` returns 0 — no per-channel token field added to the schema (D-04).
    - Behavior (Task 2 tests cover): parsing a valid HA block succeeds; wrong-prefix entity id, no-domain entity id, malformed URL, and each missing required field all throw at loadConfig.
    - Type: `npx tsc --noEmit 2>&1 | grep -v 'src/index.ts'` shows NO errors outside src/index.ts (the only expected error is the index.ts exhaustiveness guard, resolved by Plan 02).
  </acceptance_criteria>
  <done>The home-assistant member is in the discriminated union with URL + strict entity-id validation and no per-channel token field; extractSecretValues has a no-op case; the only tsc error in the repo is the expected index.ts exhaustiveness guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add HA_ACCESS_TOKEN to ENV_KEY_ALLOWLIST + config tests</name>
  <files>src/config.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts (ENV_KEY_ALLOWLIST lines 482-504 — the array to append to; note the grouping convention: channel secrets before SEARCH_PROVIDER)
    - src/config.test.ts (read the "Phase 31 ConfigSchema heads[] field" describe block, lines 377-439 — it is the exact analog: it writes a temp config.json with a heads[] block and asserts loadConfig parses or throws /Configuration error/; mirror this structure)
    - .planning/phases/40-config-adapter-skeleton/40-PATTERNS.md ("ENV_KEY_ALLOWLIST addition" section)
  </read_first>
  <behavior>
    - ENV_KEY_ALLOWLIST contains the literal 'HA_ACCESS_TOKEN'.
    - loadConfig with a heads[] containing a valid home-assistant channel parses; cfg.heads[0].channels[0].vendor === 'home-assistant', haBaseUrl and haVoiceSatelliteEntityId round-trip.
    - loadConfig with a home-assistant channel whose haVoiceSatelliteEntityId is 'light.kitchen' throws /Configuration error/.
    - loadConfig with a home-assistant channel whose haBaseUrl is 'not-a-url' throws /Configuration error/.
    - loadConfig with a home-assistant channel missing haBaseUrl throws /Configuration error/.
    - loadConfig with no home-assistant channel (e.g. empty config / flat telegram keys) parses unchanged — SC4 backward-compat.
  </behavior>
  <action>
    In `src/config.ts`, append the literal `'HA_ACCESS_TOKEN'` to the `ENV_KEY_ALLOWLIST` array,
    placed after `'ZOHO_CLIQ_CHAT_ID'` and before `'SEARCH_PROVIDER'` to follow the channel-secrets
    grouping convention (D-04 — this makes `config:set HA_ACCESS_TOKEN <value>` write the token to
    `.env`; the token is never read into config.json). Do NOT add a `haAccessToken` flat key to
    `ConfigSchema` or to the `secrets` object in loadConfig in this phase — per the PATTERNS.md note,
    the adapter reads `process.env['HA_ACCESS_TOKEN']` directly at call-time in Phase 42, so the
    flat-key ConfigSchema addition is deferred to Phase 42 when it is first consumed. For Phase 40
    only the allowlist entry is needed.
    Then add a new `describe('Phase 40 home-assistant channel vendor', …)` block to
    `src/config.test.ts` mirroring the "Phase 31 ConfigSchema heads[] field" block structure
    (temp config.json via USER_CONFIG_PATH, scrub flat env keys in beforeEach, cleanup in afterEach).
    Add `it()` cases covering every bullet in <behavior>: one parse-success assertion on a valid HA
    block, three negative assertions (wrong entity prefix, malformed URL, missing haBaseUrl) each using
    `expect(() => loadConfig()).toThrow(/Configuration error/)`, one assertion that
    `ENV_KEY_ALLOWLIST` includes 'HA_ACCESS_TOKEN' (import the const and assert membership), and one
    SC4 backward-compat assertion that a config with no HA channel still loads. Use `noUncheckedIndexedAccess`-safe
    access (`cfg.heads?.[0]?.channels[0]?.vendor`) as the existing Phase 31 tests do.
  </action>
  <verify>
    <automated>npx vitest run src/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -qF "'HA_ACCESS_TOKEN'" src/config.ts` succeeds (allowlist entry present).
    - Source: `grep -c "haAccessToken" src/config.ts` returns 0 (no flat ConfigSchema key added this phase, per deferral).
    - Test: `npx vitest run src/config.test.ts` exits 0 with the new "Phase 40 home-assistant channel vendor" describe block green.
    - Behavior: the test file asserts ENV_KEY_ALLOWLIST membership of 'HA_ACCESS_TOKEN' explicitly (not just grep).
    - Behavior: the parse-success test asserts vendor === 'home-assistant' and the two HA config fields round-trip.
    - Behavior: each of the three negative tests asserts loadConfig throws /Configuration error/.
    - Behavior: the SC4 test asserts a no-HA-channel config loads without throwing.
    - Security: no test writes or echoes a token value; HA_ACCESS_TOKEN appears only as an allowlist key name, never with a value in config.json fixtures.
  </acceptance_criteria>
  <done>ENV_KEY_ALLOWLIST contains 'HA_ACCESS_TOKEN'; src/config.test.ts has a green Phase 40 block covering parse-success, three fail-fast negatives, allowlist membership, and SC4 backward-compat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config files (config.json, .env) → loadConfig | config.json is git-tracked; .env is gitignored. The HA long-lived access token (full-permission per PITFALLS P4/P5) must cross only the .env boundary. |
| config.json → log output | extractSecretValues feeds the log-redaction registry; any per-channel secret in config.json would be registered for redaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Information Disclosure | HA access token in config.json (git-tracked) | mitigate | No `haAccessToken` field on the home-assistant union member (Task 1) and no flat ConfigSchema key (Task 2); token lives only as `HA_ACCESS_TOKEN` in ENV_KEY_ALLOWLIST → `.env` only (D-04). Acceptance criteria assert `grep -c "haAccessToken" src/config.ts` returns 0. |
| T-40-02 | Tampering | malformed/typo HA config silently accepted, fails at first use | mitigate | D-01/D-02/D-03 Zod validation at parse time: `.url()` on haBaseUrl, strict `^assist_satellite\.[a-z0-9_]+$` regex on entity id, no `.optional()` on required fields → loadConfig throws /Configuration error/ at boot. Pinned by three negative tests (Task 2). |
| T-40-03 | Information Disclosure | token value leaked to test fixtures / git via test config.json | accept | Tests reference HA_ACCESS_TOKEN only as an allowlist key name; no token value is written to any fixture. No secret material introduced by this plan. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — STACK.md confirms zero new npm deps (reuse existing Zod). No Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npx vitest run src/config.test.ts` — new Phase 40 block green; existing config tests still pass (no regression).
- `npx tsc --noEmit 2>&1 | grep -v 'src/index.ts'` — no errors outside src/index.ts (the index.ts exhaustiveness error is the expected, intended cross-file RED resolved by Plan 02).
- `grep -c "z.literal('home-assistant')" src/config.ts` returns 1.
- `grep -c "haAccessToken" src/config.ts` returns 0 (token-in-.env security invariant).
</verification>

<success_criteria>
- SC1 (partial): a valid home-assistant config block parses and HA_ACCESS_TOKEN is allowlisted for `.env`.
- SC2: invalid entity-id prefix, malformed URL, and missing required fields each produce a clear /Configuration error/ at loadConfig.
- SC4 (partial): a config with no home-assistant channel loads unchanged.
- HACF-01 + HACF-02 config-side obligations met (base URL + token-in-.env + entity_id with shape validation; configurable Zod discriminated-union vendor).
</success_criteria>

<output>
Create `.planning/phases/40-config-adapter-skeleton/40-01-SUMMARY.md` when done.
</output>
