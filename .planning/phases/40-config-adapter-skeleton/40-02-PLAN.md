---
phase: 40-config-adapter-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/channels/home-assistant/adapter.ts
  - src/index.ts
  - src/channels/home-assistant/adapter.test.ts
autonomous: true
requirements: [HACF-02]

must_haves:
  truths:
    - "HomeAssistantChannelAdapter instantiates and implements the ChannelAdapter contract (SC3)"
    - "The adapter registers under the fixed id 'home-assistant' in the head's channel map (SC3/D-06)"
    - "start() is a near-noop that logs registration; stop() is a noop; neither throws (skeleton, zero HTTP)"
    - "send() logs a clear warn and returns without throwing — never crashes the activation loop (D-05)"
    - "The index.ts per-head loop has an else-if home-assistant branch satisfying the _exhaustive: never guard (SC3)"
    - "A full inject→queue→activation→reply→router.send() cycle results in lastActiveChannel === 'home-assistant' (SC3)"
    - "Existing deployments with no home-assistant channel boot unchanged (SC4)"
  artifacts:
    - path: "src/channels/home-assistant/adapter.ts"
      provides: "HomeAssistantChannelAdapter stub implementing ChannelAdapter (id/start/stop/send/onMessage) + injectMessage test helper"
      contains: "class HomeAssistantChannelAdapter"
      min_lines: 25
    - path: "src/index.ts"
      provides: "else if (ch.vendor === 'home-assistant') branch + adapter import"
      contains: "ch.vendor === 'home-assistant'"
    - path: "src/channels/home-assistant/adapter.test.ts"
      provides: "adapter contract tests + send() loud-but-safe test + SC3 full-cycle lastActiveChannel test"
      contains: "lastActiveChannel"
  key_links:
    - from: "src/index.ts per-head loop"
      to: "HomeAssistantChannelAdapter"
      via: "else if branch constructs adapter, then onMessage(headRouteMessage) → headRouter.register → start()"
      pattern: "new HomeAssistantChannelAdapter"
    - from: "adapter.injectMessage()"
      to: "ChannelRouterImpl.lastActiveChannel"
      via: "handler (headRouteMessage) → enqueue → activation → router.send('home-assistant') stamps lastActiveChannel on outbound"
      pattern: "getLastActiveChannel"
---

<objective>
Create the `HomeAssistantChannelAdapter` stub (`src/channels/home-assistant/adapter.ts`, NEW),
wire it into the per-head channel build loop in `src/index.ts` with a new
`else if (ch.vendor === 'home-assistant')` branch (resolving the exhaustiveness-guard tsc RED
left by Plan 40-01), and prove the full inbound→outbound routing path stamps
`lastActiveChannel === 'home-assistant'` (SC3) with zero HTTP and zero HA REST calls.

Purpose: Satisfies SC3 (adapter instantiates, registers as a named adapter, and the full routing
path is exercised) and SC4 (zero-config backward compatibility — no-HA deployments boot unchanged),
and completes HACF-02 (the vendor is started at boot like every other adapter). The skeleton leaves
clean seams for Phase 41 (HTTP router + pendingReply slot) and Phase 42 (real send() → assist_satellite
REST) WITHOUT implementing them — the stub send() logs and returns (D-05).

Output: NEW `src/channels/home-assistant/adapter.ts`, modified `src/index.ts`, NEW
`src/channels/home-assistant/adapter.test.ts`.

CRITICAL on SC3 mechanism (corrected from CONTEXT.md by the pattern mapper): `lastActiveChannel`
is OUTBOUND-ONLY — it is stamped by `ChannelRouterImpl.send()`/`sendTyping()` on a successful
outbound send, NOT on inbound receipt. A test that merely injects an inbound message will NOT
stamp it. SC3 must drive the FULL cycle: inject inbound → priority queue → ActivationLoop
processes → head replies → `router.send('home-assistant', …)` → THAT send stamps
`lastActiveChannel`. See Task 3 for the exact mechanism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-config-adapter-skeleton/40-CONTEXT.md
@.planning/phases/40-config-adapter-skeleton/40-PATTERNS.md
@.planning/phases/40-config-adapter-skeleton/40-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — use directly, no exploration. -->

ChannelAdapter contract — from src/types/channel.ts:15-28 (implement the 5 required members; omit optionals):
```typescript
export interface ChannelAdapter {
  id: string
  start(): Promise<void>
  stop(): Promise<void>
  send(text: string, attachments?: Attachment[]): Promise<string | void>
  onMessage(handler: (msg: InboundMessage) => void): void
  // optional sendTyping/sendDebug/onReaction/editMessage — OMIT from the stub
}
```

InboundMessage shape — from src/types/channel.ts:5-11 (injectMessage builds one of these):
```typescript
export interface InboundMessage {
  channel: string
  text: string
  senderName?: string
  attachments?: Attachment[]
  rawPayload?: unknown
}
```

VoiceChannelAdapter — the closest analog, from src/channels/voice/adapter.ts. Copy ONLY the
class shape (readonly id, private headId, private handler, constructor assigns id+headId,
onMessage stores the handler). Drop ALL voice-specific fields (wss, activeSocket, openai, etc.).
Its inbound dispatch pattern `this.handler?.({ channel: this.id, text: transcript })`
(adapter.ts:156) is exactly what injectMessage must replicate.

ChannelRouterImpl.send() — from src/channels/router.ts:12-43. On a successful `adapter.send()`
it sets `this.lastActiveChannel = channelId` (line 21). getLastActiveChannel() returns it
(line 79). This is the OUTBOUND stamp SC3 relies on. The stub's send() returns void without
throwing, so router.send('home-assistant', …) reaches the stamp line.

index.ts adapter imports — from src/index.ts:64-71. Add the HA import alongside these:
```typescript
import { DiscordAdapter } from './channels/discord/adapter.js'
// ...
import { VoiceChannelAdapter } from './channels/voice/adapter.js'
```

index.ts per-head loop + exhaustiveness guard — from src/index.ts:293-338. The new branch goes
immediately before the final `else { const _exhaustive: never = ch; … }` block. The shared
try-block wiring (onMessage(headRouteMessage) → headRouter.register(adapter) → await adapter.start())
is unchanged and applies to the HA adapter identically. headRouteMessage (defined ~src/index.ts:279-289)
enqueues a user_message at priority 100 and calls headLoop.notify().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HomeAssistantChannelAdapter stub</name>
  <files>src/channels/home-assistant/adapter.ts</files>
  <read_first>
    - src/channels/voice/adapter.ts (the closest analog — read lines 1-90 for imports, class fields, constructor, onMessage, start/stop/send shapes; copy ONLY the structure, drop all voice-specific deps)
    - src/types/channel.ts (the ChannelAdapter + InboundMessage interfaces the stub implements — full file, 44 lines)
    - .planning/phases/40-config-adapter-skeleton/40-PATTERNS.md (the "src/channels/home-assistant/adapter.ts (NEW)" section gives the exact imports, class shape, start/stop/send stubs, and injectMessage helper)
    - .planning/phases/40-config-adapter-skeleton/40-CONTEXT.md (D-05 loud-but-safe send, D-06 fixed id)
  </read_first>
  <behavior>
    - new HomeAssistantChannelAdapter() → .id === 'home-assistant' (default).
    - new HomeAssistantChannelAdapter('ha-2', 'work') → .id === 'ha-2' (id arg honored).
    - await start() resolves and does not throw (logs a registration line).
    - await stop() resolves and does not throw (noop).
    - await send('hello') resolves to undefined and does not throw; emits a log.warn whose text mentions Phase 42 and does NOT contain any token value.
    - onMessage(h) then injectMessage('hi', 'Ashley') calls h once with { channel: 'home-assistant', text: 'hi', senderName: 'Ashley' }.
    - injectMessage with no handler registered does not throw (handler is null-guarded).
  </behavior>
  <action>
    Create `src/channels/home-assistant/adapter.ts` exporting
    `class HomeAssistantChannelAdapter implements ChannelAdapter`. Imports use `.js` extensions
    (moduleResolution: bundler): `import type { Attachment } from '../../types/core.js'`,
    `import type { ChannelAdapter, InboundMessage } from '../../types/channel.js'`,
    `import { log } from '../../logger.js'`. Fields: `readonly id: string`,
    `private readonly headId: string`, `private handler: ((msg: InboundMessage) => void) | null = null`.
    Constructor signature `constructor(id: string = 'home-assistant', headId: string = 'default')`
    assigning both (D-06 default id 'home-assistant'; storing headId is correct for Phase 41+ use).
    Implement the 5 required ChannelAdapter members and OMIT all optional members
    (sendTyping/sendDebug/onReaction/editMessage):
    `onMessage(handler)` stores it on this.handler;
    `async start()` logs an info line that the adapter registered (mention id + head + that the
    HTTP listener is wired in Phase 41) and returns — near-noop, NO WebSocket/HTTP setup (skeleton);
    `async stop()` is an empty noop returning void (nothing to tear down until Phase 41);
    `async send(text: string, _attachments?: Attachment[]): Promise<void>` emits a single
    `log.warn` stating send() is not wired until Phase 42 and that the reply is being dropped
    (D-05 loud-but-safe — it MUST NOT throw and MUST NOT log any token value; log only text.length,
    not text content or any secret), then returns. Prefix the unused attachments param with `_`.
    Add a test-only `injectMessage(text: string, senderName?: string): void` method that fires the
    inbound path exactly as a real HA event would: call `this.handler?.(…)` with an InboundMessage
    `{ channel: this.id, text, senderName }`. Because exactOptionalPropertyTypes is ON, do NOT set
    senderName to an explicit undefined — build the object so the key is omitted when senderName is
    undefined (e.g. spread a conditional `...(senderName !== undefined ? { senderName } : {})` or
    equivalent). Document injectMessage as test-only ("Do NOT call from production paths"). Do NOT
    add any HTTP router, pendingReply slot, fetch, or assist_satellite call — those are Phases 41/42.
  </action>
  <verify>
    <automated>npx vitest run src/channels/home-assistant/adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -qF "class HomeAssistantChannelAdapter implements ChannelAdapter" src/channels/home-assistant/adapter.ts` succeeds.
    - Source: `grep -qF "id: string = 'home-assistant'" src/channels/home-assistant/adapter.ts` succeeds (D-06 default id).
    - Source: `grep -qF "injectMessage" src/channels/home-assistant/adapter.ts` succeeds (SC3 test hook present).
    - Source: the file contains NO `import` of `ws`, `node:http`, `fetch`, or any `assist_satellite` string (skeleton has zero HTTP/REST). Verify: `grep -cE "WebSocket|node:http|assist_satellite|http\\.Server" src/channels/home-assistant/adapter.ts` returns 0.
    - Behavior: send() emits exactly one log.warn and resolves to undefined without throwing (Task 3 test asserts a spy on console.warn is called and no throw).
    - Security: send()'s log line does not interpolate any token; `grep -c "HA_ACCESS_TOKEN\|process.env" src/channels/home-assistant/adapter.ts` returns 0 (the stub reads no env this phase).
    - Type: `npx tsc --noEmit 2>&1 | grep -c "src/channels/home-assistant/adapter.ts"` returns 0 (file itself is type-clean; an index.ts error may still exist until Task 2).
  </acceptance_criteria>
  <done>The adapter file exists, implements the 5 required ChannelAdapter members with a default id of 'home-assistant', has a loud-but-safe non-throwing send(), exposes injectMessage for SC3, contains zero HTTP/REST/token code, and is type-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the home-assistant branch into the per-head channel loop</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (lines 64-71 for the adapter import block, and lines 279-338 for headRouteMessage + the per-head loop with the vendor chain and exhaustiveness guard — read current state before editing)
    - src/channels/home-assistant/adapter.ts (the class created in Task 1 — confirm the constructor signature you will call)
    - .planning/phases/40-config-adapter-skeleton/40-PATTERNS.md (the "src/index.ts — per-head channel build loop branch" section gives the exact branch + import to insert)
  </read_first>
  <action>
    In `src/index.ts`, add `import { HomeAssistantChannelAdapter } from './channels/home-assistant/adapter.js'`
    alongside the other adapter imports (near lines 64-71). Then insert a new branch
    `else if (ch.vendor === 'home-assistant') { const ha = new HomeAssistantChannelAdapter(ch.id, head.id); adapter = ha }`
    into the per-head `for (const ch of head.channels)` loop, placed immediately before the final
    `else { const _exhaustive: never = ch; … }` exhaustiveness guard (currently ~src/index.ts:323-327).
    This resolves the tsc RED that Plan 40-01 intentionally introduced. Do NOT add a
    `if (!isMultiHead) currentHAAdapter = ha` line — there is no live dynamic-reconfigure path for HA
    in Phase 40-43 (D-06 single instance). Do NOT touch the shared try-block wiring below the chain
    (`adapter.onMessage(headRouteMessage)` → `headRouter.register(adapter)` → `await adapter.start()` →
    push + log) — it applies to the HA adapter identically and the stub's near-noop start() will not
    trip the catch. Update the stale comment on the exhaustiveness guard if it says "all five vendors"
    to reflect six.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -qF "ch.vendor === 'home-assistant'" src/index.ts` succeeds (the new branch).
    - Source: `grep -qF "new HomeAssistantChannelAdapter(ch.id, head.id)" src/index.ts` succeeds (constructed with config id + head id).
    - Source: `grep -qF "import { HomeAssistantChannelAdapter }" src/index.ts` succeeds (import added).
    - Source: `grep -c "currentHAAdapter" src/index.ts` returns 0 (no singleton tracking added per D-06).
    - Type: `npx tsc --noEmit` exits 0 — the exhaustiveness guard is now satisfied for all six vendors; the cross-file RED from Plan 01 is GREEN.
  </acceptance_criteria>
  <done>The home-assistant branch is wired into the per-head loop, the adapter is imported, no singleton tracking was added, and `npx tsc --noEmit` exits 0 across the whole repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC3 — prove full inject→activation→reply cycle stamps lastActiveChannel + send() loud-but-safe test</name>
  <files>src/channels/home-assistant/adapter.test.ts</files>
  <read_first>
    - src/channels/router.ts (ChannelRouterImpl — confirm send() sets lastActiveChannel on success at line 21 and getLastActiveChannel at line 79; this is the OUTBOUND stamp SC3 relies on)
    - src/channels/channels.test.ts (lines 57-146 — the ChannelRouterImpl test patterns: makeAdapter helper, register, send, getLastActiveChannel assertions, console.warn spy pattern; mirror this style)
    - src/channels/voice/adapter.test.ts (read for the adapter unit-test style used in this repo — describe/it/vi structure)
    - tests/integration/channel-router-isolation.test.ts (the self-contained architectural-regression framing — prefer building the minimal wiring inline in the test rather than importing shared helpers, mirroring this precedent)
    - .planning/phases/40-config-adapter-skeleton/40-PATTERNS.md (the "lastActiveChannel routing — inbound path (SC3)" section: the 5-step cycle, and the OUTBOUND-stamp correction)
  </read_first>
  <behavior>
    - Adapter contract: default id is 'home-assistant'; start()/stop()/send() resolve without throwing.
    - send() loud-but-safe: a console.warn spy is called once when send() runs; send() resolves to undefined; no throw; the warn message contains no token.
    - SC3 full cycle (the load-bearing test): wiring the adapter through a ChannelRouterImpl and driving the inbound→outbound path results in `router.getLastActiveChannel() === 'home-assistant'` — and crucially, calling only injectMessage WITHOUT the outbound send does NOT stamp it (asserts the outbound-only mechanism, guarding against the incorrect inbound-stamp assumption).
  </behavior>
  <action>
    Add tests to `src/channels/home-assistant/adapter.test.ts` (created/extended here) covering the
    <behavior> bullets. Structure as two describe blocks.

    Block A — adapter contract + loud-but-safe send: assert default id, that start/stop/send resolve
    without throwing, and that send() triggers exactly one console.warn (use
    `vi.spyOn(console, 'warn').mockImplementation(() => {})` as channels.test.ts does) and resolves to
    undefined. Assert the captured warn argument string does NOT contain any token-like value (assert it
    does not contain a sentinel token you never pass in — e.g. construct the adapter without any token and
    assert the warn text matches /Phase 42/ and not /HA_ACCESS_TOKEN|Bearer/).

    Block B — SC3 full routing cycle. Build the minimal wiring INLINE (self-contained, per the
    channel-router-isolation precedent — do not depend on shared integration helpers): construct a
    `ChannelRouterImpl`, a `HomeAssistantChannelAdapter`, register the adapter, and wire the inbound
    handler to a tiny stand-in for the activation cycle. Because spinning a real ActivationLoop is heavy,
    model the documented 5-step cycle directly and assert the OUTBOUND stamp is what sets
    lastActiveChannel: (1) call `adapter.onMessage(handler)` where the handler captures the inbound
    InboundMessage; (2) call `adapter.injectMessage('what is the score')` and assert the handler received
    `{ channel: 'home-assistant', text: 'what is the score' }` (inbound path verified); (3) assert that
    at THIS point `router.getLastActiveChannel()` is still null — proving inbound does NOT stamp (this is
    the corrected-assumption guard); (4) simulate the head's reply by calling
    `await router.send('home-assistant', 'on it')` — this drives the stub adapter.send() and the router's
    outbound stamp; (5) assert `router.getLastActiveChannel() === 'home-assistant'`. Add a comment block
    citing that lastActiveChannel is OUTBOUND-ONLY (router.ts:21) so a future refactor that wrongly moves
    the stamp to the inbound path fails step (3).

    Keep the test fast (no network, no real HTTP, no timers). Use noUncheckedIndexedAccess-safe access.
  </action>
  <verify>
    <automated>npx vitest run src/channels/home-assistant/adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: `npx vitest run src/channels/home-assistant/adapter.test.ts` exits 0.
    - Behavior (SC3): the test asserts `router.getLastActiveChannel() === 'home-assistant'` AFTER an outbound `router.send('home-assistant', …)`.
    - Behavior (corrected-assumption guard): the test asserts `router.getLastActiveChannel()` is null immediately after injectMessage and BEFORE the outbound send — pinning the outbound-only mechanism.
    - Behavior (inbound path): the test asserts injectMessage delivers `{ channel: 'home-assistant', text: … }` to the registered handler.
    - Behavior (loud-but-safe): the test asserts send() calls console.warn once and resolves to undefined without throwing.
    - Source: `grep -qF "getLastActiveChannel" src/channels/home-assistant/adapter.test.ts` succeeds.
    - Security: the test does not introduce any real token; the warn-content assertion confirms no token/Bearer string is logged by send().
  </acceptance_criteria>
  <done>The test file proves the full inject→outbound cycle stamps lastActiveChannel === 'home-assistant', explicitly guards that inbound alone does not stamp it (corrected mechanism), verifies the inbound handler delivery, and confirms send() is loud-but-safe — all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound HA message → headRouteMessage (priority queue) | Phase 40 exercises this only via the test-only injectMessage helper; no untrusted network input reaches it yet (HTTP endpoint is Phase 41). |
| activation reply → adapter.send() → logs | The stub send() drops the reply with a log.warn; the boundary here is log output — the reply text length is logged, never a secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Information Disclosure | stub send() log.warn echoing a token or full reply | mitigate | D-05: send() logs only that it is dropping the reply + text.length, never the text content or any token. Acceptance criteria assert the warn text matches /Phase 42/ and contains no token/Bearer string, and `grep -c "process.env" adapter.ts` returns 0 (stub reads no secrets this phase). |
| T-40-05 | Denial of Service | stub send() throwing and crashing the activation loop | mitigate | D-05 loud-but-safe: send() never throws, always returns void. Pinned by the Block-A "resolves without throwing" assertion and the Block-B outbound send reaching the router's stamp line. |
| T-40-06 | Tampering | injectMessage (test hook) reachable from production, allowing message injection | accept | injectMessage is documented test-only and is not invoked anywhere in production paths; in Phase 40 the adapter has no HTTP surface so there is no remote trigger. Phase 41 adds the authenticated HTTP path; until then the only inbound trigger is the test. Low risk, no secret exposure. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — STACK.md confirms zero new npm deps. No Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across the whole repo (exhaustiveness guard satisfied for six vendors — Plan 01's intended cross-file RED is now GREEN).
- `npx vitest run src/channels/home-assistant/adapter.test.ts` exits 0 (contract + loud-but-safe send + SC3 full cycle + corrected-assumption guard).
- `npx vitest run src/channels/channels.test.ts src/config.test.ts` exits 0 (no regression to router/config tests).
- `grep -qF "ch.vendor === 'home-assistant'" src/index.ts` and `grep -qF "class HomeAssistantChannelAdapter" src/channels/home-assistant/adapter.ts` both succeed.
- SC4 backward-compat: existing multi-head-startup integration tests still pass (`npx vitest run tests/integration/multi-head-startup.test.ts`) — no-HA configs boot unchanged.
</verification>

<success_criteria>
- SC3: HomeAssistantChannelAdapter instantiates, registers under id 'home-assistant' in the head's channel map, and the full inject→activation→reply→router.send routing path stamps lastActiveChannel === 'home-assistant' — exercised with zero HTTP.
- SC4: deployments with no home-assistant channel boot unchanged (tsc-clean, existing startup tests pass).
- HACF-02 completed: home-assistant is started at boot like every other adapter via the per-head loop branch.
</success_criteria>

<output>
Create `.planning/phases/40-config-adapter-skeleton/40-02-SUMMARY.md` when done.
</output>
