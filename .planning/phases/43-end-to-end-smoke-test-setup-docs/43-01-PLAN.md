---
phase: 43-end-to-end-smoke-test-setup-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user-guide/home-assistant.md
  - docs/internals/channel-integrations.md
autonomous: true
requirements: [HADOC-01]

must_haves:
  truths:
    - "A self-hoster can read docs/user-guide/home-assistant.md and wire up the HA side from scratch with no guesswork"
    - "The guide covers BOTH topologies: co-located (loopback/LAN, no proxy) and remote/proxied (reverse proxy + the /v1 auth bypass)"
    - "The guide shows copy-paste config.json + .env + Apache bypass blocks with PLACEHOLDER values only (no product constants)"
    - "The developer reference docs/internals/channel-integrations.md cross-links to the new operator guide"
  artifacts:
    - path: "docs/user-guide/home-assistant.md"
      provides: "HADOC-01 operator setup guide"
      min_lines: 80
      contains: "AuthType None"
    - path: "docs/internals/channel-integrations.md"
      provides: "cross-link to the new operator guide in the Related docs section"
      contains: "home-assistant.md"
  key_links:
    - from: "docs/internals/channel-integrations.md"
      to: "docs/user-guide/home-assistant.md"
      via: "Related docs Markdown link"
      pattern: "home-assistant.md"
---

<objective>
Author HADOC-01 — the operator-facing Home Assistant Voice setup guide at
`docs/user-guide/home-assistant.md` — and cross-link it from the developer
reference `docs/internals/channel-integrations.md`.

Purpose: A self-hosting operator must be able to wire up the entire Home Assistant
side from scratch — install Extended OpenAI Conversation (HACS), point its base URL
at Shrok with the API key, select Shrok as the VPE conversation agent, add the
satellite `entity_id`, and (for remote deployments) apply the Apache `/v1` auth-bypass
— using only this guide, with no guesswork. This is the sole code-independent
deliverable of Phase 43 and is fully agent-executable with no hardware.

Output: One new doc file + a one-line cross-link edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase source artifacts (decisions + structure are authoritative)
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-CONTEXT.md
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-RESEARCH.md
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-PATTERNS.md

# Style analog + cross-link target (read before writing)
@docs/user-guide/manual-uninstall.md
@docs/internals/channel-integrations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the HADOC-01 operator setup guide</name>
  <files>docs/user-guide/home-assistant.md</files>
  <read_first>
    - docs/user-guide/manual-uninstall.md (REQUIRED — style/format analog: H1 plain title, prose intro under H1 with no section heading, H2 sections, language-tagged fenced blocks, no YAML front-matter, no screenshots, imperative tone, no emoji decoration)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-RESEARCH.md § "HADOC-01 Operator Guide Structure" (REQUIRED — the full required section list with copy-paste block contents: Overview, Prerequisites, Shrok-Side Configuration with 3.1 config.json + 3.2 .env, HA-Side Setup, Reverse Proxy Setup, Satellite Entity ID, Verify It Works, Troubleshooting)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-CONTEXT.md § D-04/D-05/D-07/D-08/D-09 (REQUIRED — Extended OpenAI Conversation is the documented component; document BOTH topologies; placeholders never product constants; no screenshots)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-PATTERNS.md § "docs/user-guide/home-assistant.md" + "ENV_KEY_ALLOWLIST key names" (REQUIRED — exact env key names HA_ACCESS_TOKEN / HA_INBOUND_API_KEY; docs style conventions)
    - docs/internals/channel-integrations.md § "Home Assistant" (context for what the developer reference already says, so the operator guide does not duplicate internals)
  </read_first>
  <action>
    Create docs/user-guide/home-assistant.md following manual-uninstall.md's style verbatim:
    H1 title (plain noun phrase, e.g. "Home Assistant Voice Setup"), a 1-3 sentence prose
    intro directly under H1 (no heading), then H2 sections. No YAML front-matter, no screenshots,
    no decorative emoji, imperative tone, all fenced blocks language-tagged and self-contained.

    Sections (per RESEARCH § "HADOC-01 Operator Guide Structure"):
    1. Overview — Shrok becomes the VPE's conversation brain via an OpenAI-compatible
       /v1/chat/completions endpoint; brief mention of the two-leg async pattern
       (synchronous in-turn ack + asynchronous assist_satellite.announce for background results).
    2. Prerequisites — HA Core >= 2026.3, HACS installed, Shrok running with a home-assistant
       channel configured, and (remote/proxied topology only) a reverse proxy with the /v1 auth bypass.
    3. Shrok-Side Configuration — 3.1 the config.json `home-assistant` channel block (json fenced
       block with PLACEHOLDER values: haBaseUrl like http://homeassistant.local:8123,
       haVoiceSatelliteEntityId like assist_satellite.home_assistant_voice_YOURDEVICE_assist_satellite);
       3.2 the `.env` keys block using EXACTLY the names HA_ACCESS_TOKEN and HA_INBOUND_API_KEY
       (HA_ACCESS_TOKEN = Shrok's outbound long-lived HA token; HA_INBOUND_API_KEY = the secret HA
       presents to Shrok, invented by the operator and pasted identically into the HACS component).
       Note that both go in .env via ENV_KEY_ALLOWLIST, never in config.json. Recommend creating the
       HA long-lived token under a dedicated minimal-permission HA user, not the owner account.
    4. HA-Side Setup (Extended OpenAI Conversation) — numbered steps: install HACS; download the
       Extended OpenAI Conversation (jekalmin) integration; Add Integration with API Key =
       HA_INBOUND_API_KEY and Base URL = http://<your-shrok-host>:8888/v1 (give the co-located vs
       remote forms as placeholders); select Extended OpenAI Conversation as the assistant's
       Conversation Agent; copy the satellite entity ID for haVoiceSatelliteEntityId.
    5. Reverse Proxy Setup (optional — only when HA is NOT co-located with Shrok) — explain co-located
       needs no proxy config; remote needs the bypass because the proxy's basic-auth would block HA's
       Bearer token. Include the generic Apache `<Location "/v1/">` bypass block (apache fenced block:
       AuthType None / Require all granted, with the trailing-slash form `/v1/`, placed BEFORE the
       catch-all basic-auth <Location /> block — write it GENERICALLY for ANY vhost, do NOT name the
       operator's gigaashley/jarvis vhost files per D-09). Give the apply procedure (edit vhost,
       apache2ctl configtest, systemctl reload apache2) and the verify curl
       (curl -v https://your-domain.com/v1/chat/completions expecting Shrok's JSON 401
       {"error":"Unauthorized"} with NO WWW-Authenticate: Basic header). Note the block is safe because
       Shrok's own bearer check on /v1 enforces auth.
    6. Satellite Entity ID — Finding It — must start with assist_satellite.; copy from HA Settings ->
       Devices & Services -> Devices -> Voice PE; format assist_satellite.home_assistant_voice_<slug>_assist_satellite.
    7. Verify It Works — speak to the VPE; Shrok acks in-turn, async results arrive via announce; check
       the journal with journalctl --user -u shrok | grep home-assistant.
    8. Troubleshooting — the 4-row table from RESEARCH (device silent / "Error talking to OpenAI" /
       announce never fires / satellite stuck speaking) with likely cause + fix.

    Implements HADOC-01 per D-04, D-05, D-07, D-08, D-09. All example values are PLACEHOLDERS — never
    the operator's real entity slug, base URL, or tokens (D-09). Do NOT include screenshots (D-08).
    Do NOT cram developer-internals detail in here — that stays in channel-integrations.md (D-07).
  </action>
  <verify>
    <automated>test -f docs/user-guide/home-assistant.md && [ "$(grep -c '^## ' docs/user-guide/home-assistant.md)" -ge 5 ] && [ "$(grep -c 'AuthType None' docs/user-guide/home-assistant.md)" -ge 1 ] && grep -q 'HA_ACCESS_TOKEN' docs/user-guide/home-assistant.md && grep -q 'HA_INBOUND_API_KEY' docs/user-guide/home-assistant.md && grep -q 'Extended OpenAI Conversation' docs/user-guide/home-assistant.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/user-guide/home-assistant.md` (file exists)
    - `grep -c '^## ' docs/user-guide/home-assistant.md` returns >= 5 (at least 5 H2 sections)
    - `grep -c 'AuthType None' docs/user-guide/home-assistant.md` returns >= 1 (Apache bypass block present)
    - `grep -q 'HA_ACCESS_TOKEN' docs/user-guide/home-assistant.md` AND `grep -q 'HA_INBOUND_API_KEY'` (both exact env key names present, matching ENV_KEY_ALLOWLIST)
    - `grep -q 'Extended OpenAI Conversation' docs/user-guide/home-assistant.md` (the documented HACS component, D-04)
    - Both topologies are covered: a grep for the reverse-proxy section heading AND co-located guidance both find text
    - No screenshots / image markdown (`grep -c '!\[' docs/user-guide/home-assistant.md` returns 0)
    - No file path naming the operator's specific vhost (`gigaashley-le-ssl.conf` / `jarvis-le-ssl.conf` MUST NOT appear — D-09 generality): `grep -c 'gigaashley-le-ssl\|jarvis-le-ssl' docs/user-guide/home-assistant.md` returns 0
  </acceptance_criteria>
  <done>The HADOC-01 operator guide exists, covers both topologies with copy-paste config.json/.env/Apache blocks using placeholder values only, names the Extended OpenAI Conversation component, and passes all machine-verifiable checks from 43-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link the new guide from the developer reference</name>
  <files>docs/internals/channel-integrations.md</files>
  <read_first>
    - docs/internals/channel-integrations.md § "## Related docs" (REQUIRED — the existing list at the bottom of the file ending with the mcp.md entry; this is the ONLY section that changes)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-PATTERNS.md § "docs/internals/channel-integrations.md (EDIT — cross-link only)" (REQUIRED — the exact append line and the constraint that nothing else in the file is touched)
  </read_first>
  <action>
    Append exactly one bullet to the existing `## Related docs` list at the bottom of
    docs/internals/channel-integrations.md, matching the existing entry format
    `- [filename.md](relative-path) — one-sentence description`:

    - [home-assistant.md](../user-guide/home-assistant.md) — operator setup guide for the HA voice integration (HACS, base URL, entity ID, Apache bypass)

    Touch NO other part of the file — the captured Apache snippet subsection and all
    per-channel sections stay byte-identical (D-07: internals stays the developer reference,
    the operator walkthrough lives only in the new guide).
  </action>
  <verify>
    <automated>grep -q 'home-assistant.md' docs/internals/channel-integrations.md && grep -q '\.\./user-guide/home-assistant.md' docs/internals/channel-integrations.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'home-assistant.md' docs/internals/channel-integrations.md` finds the new line
    - The new line resolves to the correct relative path: `grep -q '\.\./user-guide/home-assistant.md' docs/internals/channel-integrations.md`
    - The line is under the `## Related docs` section (appended to the existing list, not elsewhere)
    - No other content in channel-integrations.md changed (the diff is exactly one added line)
  </acceptance_criteria>
  <done>The developer reference cross-links to the new operator guide via a correctly-formatted Related-docs bullet; the rest of the file is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (reader) → their own deployment | The guide instructs operators to handle secrets (HA long-lived token, invented inbound key) and to remove proxy auth on `/v1/`. The doc itself crosses no runtime boundary, but its guidance shapes the operator's security posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | `.env` secret handling in HADOC-01 | mitigate | Guide shows HA_ACCESS_TOKEN / HA_INBOUND_API_KEY as PLACEHOLDERS only; relies on ENV_KEY_ALLOWLIST; recommends a dedicated minimal-permission HA user for the long-lived token, not the owner account. No real token is ever written into committed docs. |
| T-43-02 | Elevation of Privilege | Apache `/v1/` AuthType-None bypass documented for remote self-hosters | mitigate (documented-and-accepted, below `high`) | Guide uses the trailing-slash `<Location "/v1/">` form (scopes the exemption to `/v1/` only, not `/v1anything`), places it before the catch-all, and explicitly states the bypass is safe ONLY because Shrok's own bearer check on `/v1` is the real authentication gate — Apache merely passes the request through. Remote self-hosters are told the bearer check is their authentication, not Apache. |
| T-43-03 | Tampering | npm/pip/cargo installs | accept | This plan installs no packages (docs-only); no supply-chain surface. RESEARCH "Package Legitimacy Audit: N/A". |

All threats are at or below `high` and are documented-and-accepted via the doc content itself. No blocking checkpoint required for this autonomous docs plan.
</threat_model>

<verification>
- `test -f docs/user-guide/home-assistant.md` — file exists
- `grep -c '^## ' docs/user-guide/home-assistant.md` >= 5 — required sections present
- `grep -c 'AuthType None' docs/user-guide/home-assistant.md` >= 1 — Apache bypass block present
- `grep 'home-assistant.md' docs/internals/channel-integrations.md` — cross-link present
- `grep -c 'gigaashley-le-ssl\|jarvis-le-ssl' docs/user-guide/home-assistant.md` == 0 — D-09 generality preserved
- `grep -c '!\[' docs/user-guide/home-assistant.md` == 0 — no screenshots (D-08)
</verification>

<success_criteria>
HADOC-01 is satisfied as a documentation artifact: a self-hoster can wire up the
entire HA side (HACS install, base URL + API key, conversation agent selection,
satellite entity, and the optional Apache `/v1` bypass) from the new guide alone,
for both co-located and remote/proxied topologies, with all example values as
placeholders. The developer reference cross-links to it. (Operator-attested SC3
usability is confirmed separately during the live test in plan 43-02.)
</success_criteria>

<output>
Create `.planning/phases/43-end-to-end-smoke-test-setup-docs/43-01-SUMMARY.md` when done
</output>
