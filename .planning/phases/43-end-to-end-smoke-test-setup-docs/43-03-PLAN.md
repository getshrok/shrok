---
phase: 43-end-to-end-smoke-test-setup-docs
plan: 03
type: execute
wave: 2
depends_on: ["43-02"]
files_modified:
  - src/channels/home-assistant/router.ts
  - src/channels/home-assistant/adapter.ts
  - src/channels/home-assistant/adapter.test.ts
autonomous: false
requirements: [HADOC-01]

must_haves:
  truths:
    - "If (and only if) the live test (plan 43-02) showed a concrete need, exactly the pre-deferred D-01 tunables are applied — nothing larger"
    - "If the live test showed nothing needs tuning, this plan ships ZERO production code (the D-01 'nothing needed' outcome)"
    - "Any code change keeps tsc clean (exit 0) and the HA test suite green (baseline 1625/1625)"
    - "REPLY_DEADLINE_MS remains an `export const` (router.test.ts imports it); the announce timeout and try/catch/finally structure are preserved"
  artifacts:
    - path: "src/channels/home-assistant/router.ts"
      provides: "REPLY_DEADLINE_MS constant (value possibly bumped within the 3000–4000ms envelope)"
      contains: "export const REPLY_DEADLINE_MS"
  key_links:
    - from: "plan 43-02 SUMMARY 'deadline tuning needed: yes/no' verdict"
      to: "this plan's apply-or-no-op decision"
      via: "operator confirmation at the checkpoint"
      pattern: "conditional tuning gate"
---

<objective>
CONDITIONAL bounded tuning (D-01). Apply — only if plan 43-02's live test demonstrated a
specific need — exactly the pre-deferred tunables, and nothing larger. If the live test
showed nothing needs tuning, this plan is a NO-OP that ships zero production code.

Purpose: Phase 43 is a test + docs closeout; RESEARCH establishes no production code change
is guaranteed. The only permitted changes are the three pre-deferred tunables carried forward
from Phases 41/42:
  1. Bump `REPLY_DEADLINE_MS` (router.ts:13) — only if Scenario C showed replies routinely
     miss the 3s window; ceiling ~4000ms, stay under the ~5s device firmware timeout.
  2. P6 busy-satellite skip in `announceOrStartConversation` (adapter.ts) — only if the live
     test showed an actual P6 collision (interleaved audio / stuck state / dropped turn).
  3. Entity-existence check (42 D-04) — only if the live test revealed a wrong entity ID and
     the current silent failure was too confusing to debug.

Anything larger SPINS OUT to backlog/follow-up (D-01) — see the spin-out criteria below; this
plan must NOT plan or implement those.

Output: at most a one-line constant change + an optional ~5-line guard + one test block — or
nothing at all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The live-test verdict that gates this entire plan (read FIRST)
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-02-SUMMARY.md
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-VERIFICATION.md

# Phase source — D-01 scope, tuning candidates, spin-out criteria, exact line anchors
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-CONTEXT.md
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-RESEARCH.md
@.planning/phases/43-end-to-end-smoke-test-setup-docs/43-PATTERNS.md

<interfaces>
<!-- Exact contracts the executor needs. Extracted from the codebase — no exploration required. -->

From src/channels/home-assistant/router.ts (line 13 — the deadline constant):
```typescript
// Conservative internal deadline well below the device's ~5s firmware timeout.
export const REPLY_DEADLINE_MS = 3_000
```
- REPLY_DEADLINE_MS IS an `export const` and IS imported by router.test.ts (line 10) and
  used in router.test.ts (line 377). PRESERVE the `export` keyword on any bump — removing it
  breaks the test import. Used internally at router.ts:93 in the deadline setTimeout.

From src/channels/home-assistant/adapter.ts (the announce path — P6 guard attaches here):
```typescript
const ANNOUNCE_TIMEOUT_MS = 30_000   // line 8 — do NOT change
private async announceOrStartConversation(text: string, wantsReply = false): Promise<void> {
  // token check → service = wantsReply ? 'start_conversation' : 'announce'
  // → AbortController + setTimeout(ANNOUNCE_TIMEOUT_MS) → try { fetch(...) } catch/finally
}
```
- A P6 guard inserts INSIDE the existing try block, BEFORE the fetch call (adapter.ts ~115–128).
  Preserve the try/catch/finally structure. Log via the `[home-assistant]` prefix idiom; NEVER
  log the token value (D-05).

From src/channels/home-assistant/adapter.test.ts (lines 8–21 — reuse, do not redeclare):
```typescript
const TEST_INBOUND_KEY = 'test-inbound-key'
const TEST_HA_TOKEN = 'test-ha-token-12345678'
const TEST_BASE_URL = 'http://ha.test:8123'
const TEST_ENTITY_ID = 'assist_satellite.test_speaker'
function makeAdapter(id = 'home-assistant', headId = 'default') { /* ... */ }
```
- Any new test block goes AFTER Block D (after line 549), reuses these constants + makeAdapter(),
  and follows the Block D fetch-mock envelope (vi.stubGlobal('fetch', mockFetch) + vi.unstubAllGlobals()).
  noUncheckedIndexedAccess is enabled — guard `mockFetch.mock.calls[0]?.[0]` with optional chaining.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Gate on the live-test verdict — decide whether any tuning applies</name>
  <read_first>
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-02-SUMMARY.md (REQUIRED — the "deadline tuning needed: yes/no" verdict + any P6/entity-check finding)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-VERIFICATION.md (REQUIRED — Scenario C lapse-UX outcome, measured latency, any P6 collision or wrong-entity observation)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-RESEARCH.md § "D-01 Bounded-Tuning Candidates" (REQUIRED — the three candidates, their trip-wires, and the spin-out criteria)
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-CONTEXT.md § D-01/D-02/D-03 (REQUIRED — bounded tuning only; no manufactured filler; observe-and-record for uncertain items)
  </read_first>
  <decision>Which (if any) of the three pre-deferred D-01 tunables does the live test justify applying in this phase?</decision>
  <context>
    Phase 43 may ship zero production code. The live test (plan 43-02) produced an explicit
    verdict. This decision turns that verdict into a concrete action so no speculative tuning
    is applied. The deciding agent reads the verdict and recommends one of the options below;
    the operator confirms.

    Spin-out criteria (any of these → it is NOT in-scope here; record as backlog/follow-up, do
    not implement): the change needs a new ChannelAdapter interface field; a new config key beyond
    the existing schema; a router.ts change beyond the REPLY_DEADLINE_MS constant; or it touches
    activation.ts / QueueStore / ChannelRouter.
  </context>
  <options>
    <option id="no-op">
      <name>No tuning needed (most likely)</name>
      <pros>Phase ships zero production code; honors the conservative default; least risk. Matches the expected co-located outcome where the 3s deadline is met consistently.</pros>
      <cons>None — this is the designed success path when the live test is clean.</cons>
    </option>
    <option id="deadline">
      <name>Bump REPLY_DEADLINE_MS only</name>
      <pros>One-line change; addresses Scenario C replies routinely missing the 3s window. Existing tests cover the behavior (the constant is imported by router.test.ts).</pros>
      <cons>Must stay within 3000–4000ms (500ms increments) and well under the ~5s firmware timeout; a near-boundary value risks the device abandoning the socket.</cons>
    </option>
    <option id="p6-skip">
      <name>Add P6 busy-satellite skip (± deadline bump)</name>
      <pros>~5-line guard inside the existing try block; prevents announce racing a non-idle satellite. Only if the live test showed an actual collision.</pros>
      <cons>Adds a test block (Wave-0 requirement); must preserve try/catch/finally and the [home-assistant] log idiom; never log the token.</cons>
    </option>
    <option id="entity-check">
      <name>Add entity-existence check at start() (± others)</name>
      <pros>~5-line GET /api/states startup check; surfaces a wrong entity ID clearly instead of a confusing silent failure.</pros>
      <cons>Only if the live test revealed a wrong entity AND the silent path was too confusing; otherwise the Phase-40 regex validation already covers format errors.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The chosen option is justified by an explicit citation from 43-02-SUMMARY.md / 43-VERIFICATION.md
      (e.g. "Scenario C: replies missed 3s on 3/5 runs → deadline bump") — recorded in this plan's SUMMARY
    - If "no-op" is chosen, Task 2 and Task 3 are skipped and the plan completes with zero production code
    - Any temptation toward a change matching a spin-out criterion is recorded as a backlog/follow-up item, NOT implemented
  </acceptance_criteria>
  <resume-signal>Select: no-op, deadline, p6-skip, or entity-check (and cite the live-test finding that justifies it).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the chosen bounded tuning (SKIP entirely if Task 1 chose no-op)</name>
  <files>src/channels/home-assistant/router.ts, src/channels/home-assistant/adapter.ts, src/channels/home-assistant/adapter.test.ts</files>
  <read_first>
    - .planning/phases/43-end-to-end-smoke-test-setup-docs/43-PATTERNS.md § "router.ts (POSSIBLE EDIT)" + "adapter.ts (POSSIBLE EDIT)" + "adapter.test.ts (test block to add IF P6 guard is implemented)" (REQUIRED — the exact edit shapes, the P6 guard insertion point, the Block-E test envelope)
    - src/channels/home-assistant/router.ts (REQUIRED — the file being modified; REPLY_DEADLINE_MS at line 13, comment at lines 9–12, usage at line 93)
    - src/channels/home-assistant/adapter.ts (REQUIRED — the file being modified; announceOrStartConversation lines 104–141, the try block at ~115–128 where a P6 guard attaches)
    - src/channels/home-assistant/adapter.test.ts (REQUIRED — the file being modified; reuse the constants + makeAdapter() at lines 8–21; new block goes after line 549)
    - The CLAUDE.md / AGENTS.md project rules: moduleResolution bundler (.js import extensions), noUncheckedIndexedAccess, exactOptionalPropertyTypes, node:sqlite — relevant to any code touched here
  </read_first>
  <behavior>
    Only the behavior for the option chosen in Task 1 applies; the others are not implemented.
    - deadline: REPLY_DEADLINE_MS reads the new value (3500 or 4000); it remains an exported const;
      router.test.ts still imports and uses it (existing test at line 377 still passes — it references
      the constant by name, not a literal, so the lapse test holds at the new value).
    - p6-skip: when the satellite state from GET /api/states/<entity> is not 'idle',
      announceOrStartConversation logs and returns WITHOUT calling the announce POST;
      when state is 'idle', it proceeds to the announce POST as today.
      Tests (new Block E in adapter.test.ts): (1) state != 'idle' → only the state-check fetch fires,
      announce POST is NOT called; (2) state == 'idle' → both fetches fire (state check then announce).
    - entity-check: start()/constructor issues GET /api/states/<entity> and throws a clear
      [home-assistant] error if the entity does not exist (404). Add a test covering the throw-on-missing
      and the pass-through-on-present paths.
  </behavior>
  <action>
    SKIP this task entirely if Task 1 selected "no-op" — do not touch any production file.

    Otherwise apply ONLY the chosen change:
    - deadline: change the numeric literal on router.ts line 13 (e.g. 3_000 → 3_500 or 4_000),
      keeping `export const REPLY_DEADLINE_MS`. Update the comment on lines 9–12 to reflect the new
      live-measured rationale. Touch no other line. Do NOT remove the export (router.test.ts imports it).
    - p6-skip: insert the guard inside the existing try block of announceOrStartConversation
      (adapter.ts ~115–128) BEFORE the announce fetch: GET ${haBaseUrl}/api/states/${haVoiceSatelliteEntityId}
      with the Bearer ${HA_ACCESS_TOKEN} header; if the parsed state !== 'idle', log.info('[home-assistant] ...')
      and return. Preserve the AbortController/timeout and the try/catch/finally. Never log the token (D-05).
      Add Block E to adapter.test.ts after line 549 per the PATTERNS § "Block E" envelope, reusing
      TEST_INBOUND_KEY/TEST_HA_TOKEN/TEST_BASE_URL/TEST_ENTITY_ID and makeAdapter().
    - entity-check: add the GET /api/states startup check in start()/constructor per RESEARCH Candidate 3;
      add the matching test.

    Implements D-01 bounded tuning. Do NOT exceed the chosen change; do NOT implement a second option
    "while you're in here". Anything matching a spin-out criterion stays a backlog item.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/channels/home-assistant/ && grep -q 'export const REPLY_DEADLINE_MS' src/channels/home-assistant/router.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (CONTEXT/AGENTS rule: type-check clean)
    - `npx vitest run src/channels/home-assistant/` is green (baseline 1625/1625 preserved; if p6-skip
      or entity-check was added, the new Block covers the added behavior)
    - `grep -q 'export const REPLY_DEADLINE_MS' src/channels/home-assistant/router.ts` (the export keyword is preserved)
    - If deadline was bumped: the new value is within 3000–4000ms — `grep -oE 'REPLY_DEADLINE_MS = [0-9_]+' src/channels/home-assistant/router.ts` shows a value <= 4_000 and < 5_000
    - If p6-skip was added: `grep -c 'api/states' src/channels/home-assistant/adapter.ts` >= 1 AND a new test
      block exists in adapter.test.ts (`grep -c 'not idle\|busy-satellite\|/api/states/' src/channels/home-assistant/adapter.test.ts` >= 1)
    - No change touches activation.ts, QueueStore, ChannelRouter, the ChannelAdapter interface, or adds a
      config key (spin-out criteria): `git diff --name-only` lists only files within src/channels/home-assistant/
    - If Task 1 chose no-op: this task produced zero file changes (no production code shipped)
  </acceptance_criteria>
  <done>Either zero production code was shipped (no-op outcome), or exactly the one chosen bounded tunable was applied with tsc clean and the HA suite green, the REPLY_DEADLINE_MS export preserved, and no spin-out-criterion file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shrok → HA REST API | If the P6 skip or entity-check is added, Shrok makes an additional outbound GET to HA's `/api/states` with the Bearer token — same boundary already crossed by the announce POST. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Information Disclosure | `HA_ACCESS_TOKEN` used in an added `/api/states` GET (p6-skip / entity-check) | mitigate | The token is read from `process.env['HA_ACCESS_TOKEN']` and placed only in the `Authorization` header — NEVER passed to any `log.*` call (D-05; existing redaction idiom in adapter.ts lines 105–108/119 preserved). New log lines use the `[home-assistant]` prefix and describe state/skip only. |
| T-43-09 | Tampering / scope creep | An "innocent" tuning edit growing past the D-01 envelope | mitigate | The acceptance criteria assert `git diff --name-only` lists only `src/channels/home-assistant/` files and that no spin-out-criterion target (activation.ts, QueueStore, ChannelRouter, the interface, config keys) is touched. The decision checkpoint (Task 1) records justification per change. |
| T-43-10 | Tampering | npm/pip/cargo installs | accept | No packages installed — RESEARCH "Package Legitimacy Audit: N/A". No new deps for any of the three candidates. |

All threats are at or below `high` and documented-and-accepted. The token-handling threat (T-43-08) is mitigated by the existing, preserved log-redaction idiom.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/channels/home-assistant/` green (and `npx vitest run` full suite green before verify-work)
- `grep -q 'export const REPLY_DEADLINE_MS' src/channels/home-assistant/router.ts` — export preserved
- `git diff --name-only` lists only files under `src/channels/home-assistant/` (no spin-out files touched)
- If no-op: `git status` shows no production-code changes from this plan
</verification>

<success_criteria>
Phase 43's conditional code track is closed: either zero production code shipped (the
expected clean-test outcome), or exactly one of the three pre-deferred D-01 tunables was
applied within its envelope, with tsc clean, the HA suite green, the REPLY_DEADLINE_MS
export preserved, and no change crossing a spin-out boundary. Anything larger that the
live test surfaced is recorded as a backlog/follow-up item, not built here.
</success_criteria>

<output>
Create `.planning/phases/43-end-to-end-smoke-test-setup-docs/43-03-SUMMARY.md` when done.
State which option was applied (or "no-op — zero production code") and cite the live-test
finding that justified it. List any change deferred to backlog per the spin-out criteria.
</output>
