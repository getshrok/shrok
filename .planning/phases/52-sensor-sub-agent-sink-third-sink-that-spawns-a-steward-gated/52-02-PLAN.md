---
phase: 52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/scheduler/proactive.ts
  - src/scheduler/prompts/sensor-dispatch.md
  - src/head/activation.ts
  - src/scheduler/proactive.test.ts
  - src/head/activation.test.ts
autonomous: true
requirements: [SENSOR-19]
must_haves:
  truths:
    - "A `sensor_sub_agent_trigger` event runs the steward then spawns a sub-agent — without ever waking the conversational head"
    - "The steward decision uses a non-schedule-shaped prompt (no SCHEDULE/LAST_RUN/conditions), fed slug + prompt + ambient + USER.md + history"
    - "On steward LLM failure the decision defaults to RUN (fail-open, RUN_DEFAULT)"
    - "When the steward says skip (and proactive is enabled), no agent is spawned and no schedule row is touched"
    - "The spawned agent carries `trigger:'sensor'` and `skillName:'sensor:<slug>'`"
    - "When proactive is globally disabled, the agent spawns directly with no steward call"
  artifacts:
    - path: "src/scheduler/proactive.ts"
      provides: "runSensorDispatchDecision + SensorDispatchContext (sibling to runProactiveDecision)"
      contains: "runSensorDispatchDecision"
    - path: "src/scheduler/prompts/sensor-dispatch.md"
      provides: "non-schedule steward prompt template with {SLUG}/{SENSOR_PROMPT} tokens"
      contains: "{SENSOR_PROMPT}"
    - path: "src/head/activation.ts"
      provides: "handleSensorSubAgentTrigger handler + early-return dispatcher branch"
      contains: "handleSensorSubAgentTrigger"
  key_links:
    - from: "src/head/activation.ts (handleEvent dispatcher)"
      to: "handleSensorSubAgentTrigger"
      via: "early-return branch on event.type === 'sensor_sub_agent_trigger' (before head activation path)"
      pattern: "sensor_sub_agent_trigger"
    - from: "src/head/activation.ts (handleSensorSubAgentTrigger)"
      to: "src/scheduler/proactive.ts (runSensorDispatchDecision)"
      via: "steward gate reusing config.proactiveEnabled/proactiveShadow"
      pattern: "runSensorDispatchDecision"
    - from: "src/head/activation.ts (handleSensorSubAgentTrigger)"
      to: "agentRunner.spawn"
      via: "trigger:'sensor', skillName:`sensor:${slug}`, task=event.prompt"
      pattern: "trigger: 'sensor'"
---

<objective>
Wire the schedule-less sensor sub-agent dispatch: a new non-schedule steward decision function + prompt template, and a slim activation handler that gates via that steward then spawns silently — never reaching the head activation path. This is the SENSOR-19 core: reuse (not duplicate) the proactive-decision steward and the `agentRunner.spawn` machinery, with a synthetic trigger that carries no schedule row.

Purpose: Turn the `sensor_sub_agent_trigger` event (Plan 01) into an actual gated, head-bypassing sub-agent spawn.
Output: `runSensorDispatchDecision` + `SensorDispatchContext` in proactive.ts, `src/scheduler/prompts/sensor-dispatch.md`, `handleSensorSubAgentTrigger` + dispatcher branch in activation.ts, plus unit tests in proactive.test.ts and activation.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-CONTEXT.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-RESEARCH.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-PATTERNS.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-01-SUMMARY.md

<interfaces>
<!-- Target shapes. Source of truth is the read_first analog code; do NOT invent new machinery. -->

New in src/scheduler/proactive.ts (sibling to runProactiveDecision/ProactiveContext; reuse ProactiveDecision return type, RUN_DEFAULT, stewardComplete, extractJson, interpolate, loadPrompt, formatTimestampedHistory):
```typescript
export interface SensorDispatchContext {
  slug: string
  prompt: string          // the subAgentEvent.prompt value — the instruction to execute
  userMd: string
  recentHistory: Array<{ role: string; content: string; createdAt?: string }>
  ambientContext: string
  currentTime: string
}
export async function runSensorDispatchDecision(
  ctx: SensorDispatchContext, router: LLMRouter, model: string,
  usageStore?: UsageStore, eventId?: string,
): Promise<ProactiveDecision>
```
`runSensorDispatchDecision` calls `loadPrompt('sensor-dispatch')`, interpolates `{CURRENT_TIME,SLUG,SENSOR_PROMPT,USER_MD,AMBIENT,HISTORY}`, uses the IDENTICAL `proactive_decision` tool schema as runProactiveDecision, and returns RUN_DEFAULT in its catch block (D-07 fail-open).

New handler in src/head/activation.ts (slim version of handleScheduleTrigger — NO scheduleStore.get, NO tasksLoader.load, NO markSkipped/lastRun/delete):
```typescript
private async handleSensorSubAgentTrigger(
  event: QueueEvent & { type: 'sensor_sub_agent_trigger' },
): Promise<void>
```
Steward gate reuses `this.opts.config.proactiveShadow || this.opts.config.proactiveEnabled`; on enabled+skip → log + return; else spawn `{ task: event.prompt (+ optional context), trigger:'sensor', headId: this.opts.headId, skillName: `sensor:${event.slug}` }`.

Dispatcher branch in handleEvent (immediately after the `schedule_trigger` early-return ~lines 521–524, BEFORE the head-activation path begins at ~line 526):
```typescript
if (event.type === 'sensor_sub_agent_trigger') {
  await this.handleSensorSubAgentTrigger(event)
  return
}
```
All helpers used by the handler (os, scanAmbient, estimateTokens, generateAgentId, formatIanaTimeLine, TextMessage) are ALREADY imported in activation.ts — no new imports needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runSensorDispatchDecision + SensorDispatchContext + sensor-dispatch.md prompt (with tests)</name>
  <read_first>
    - src/scheduler/proactive.ts (the full file — `ProactiveContext` interface ~73–86, `ProactiveDecision` ~88–93, `RUN_DEFAULT` ~95, `loadPrompt`/`interpolate`/`formatTimestampedHistory` helpers ~19–60, and `runProactiveDecision` body ~155–202 — the exact analog to mirror, dropping schedule-shaped fields)
    - src/scheduler/prompts/tasks.md (the schedule-shaped template to adapt — drop SCHEDULE/LAST_RUN/SCHEDULE_CONDITIONS/SKILL_* tokens; keep the section structure and the run/skip JSON instructions)
    - src/scheduler/proactive.test.ts (existing `runProactiveDecision` tests ~1–90 — `makeContext()`, `makeRouter(content)`, and the "defaults to run on LLM error" pattern are the analogs for the new cases)
    - 52-PATTERNS.md (the "src/scheduler/proactive.ts" and "src/scheduler/prompts/sensor-dispatch.md" pattern sections — exact target shapes; the "Test Patterns → proactive.test.ts" new-case list)
  </read_first>
  <behavior>
    - Test: `runSensorDispatchDecision` returns `{action:'run'}` when the mocked LLM returns a run decision
    - Test: returns `{action:'skip', reason}` when the mocked LLM returns a skip decision with a reason
    - Test: defaults to RUN on LLM error (`decision.action === 'run'` and `decision.reason` contains "defaulting to run") — D-07 fail-open
    - Test: defaults to RUN on malformed/non-JSON LLM content
    - Test: threads the optional `context` field through when the LLM returns it
  </behavior>
  <action>
    In src/scheduler/proactive.ts: add the `SensorDispatchContext` interface (fields `slug`, `prompt`, `userMd`, `recentHistory`, `ambientContext`, `currentTime` — NO `scheduleCron`/`lastRun`/`lastSkipped`/`lastSkipReason`/`conditions`/`skillInstructions`/`skillDescription`) and the exported `runSensorDispatchDecision` function. Mirror `runProactiveDecision` exactly: interpolate `loadPrompt('sensor-dispatch')` with `{CURRENT_TIME, SLUG, SENSOR_PROMPT, USER_MD, AMBIENT, HISTORY}` (USER_MD/AMBIENT fall back to '(empty)'/'(none)', HISTORY via `formatTimestampedHistory`), call `stewardComplete('steward-sensor-dispatch', ...)` with the IDENTICAL `proactive_decision` tool schema, parse via `extractJson`, return `{action:'skip',reason}` on a reasoned skip else `{action:'run', reason, ...(context spread)}`, and return the reused `RUN_DEFAULT` in the catch (log `[proactive:sensor] decision failed, defaulting to run`). Reuse the existing `ProactiveDecision` return type — do NOT add a new return type.
    Create src/scheduler/prompts/sensor-dispatch.md: adapt tasks.md per the PATTERNS target — header reads "A sensor has detected something and is requesting a sub-agent to act on it." with `Sensor: {SLUG}` / `Task: {SENSOR_PROMPT}`, then User profile `{USER_MD}`, Ambient context `{AMBIENT}`, Recent conversation `{HISTORY}`, then the "default to running (the sensor already decided something happened)" run/skip guidance and the same JSON-only response format. (The workspace-override mechanism in `loadPrompt` applies automatically — no code change needed.)
    Add the five behavior tests to src/scheduler/proactive.test.ts mirroring the `runProactiveDecision` cases.
  </action>
  <verify>
    <automated>npx vitest run src/scheduler/proactive.test.ts && test -f src/scheduler/prompts/sensor-dispatch.md && grep -q "runSensorDispatchDecision" src/scheduler/proactive.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/scheduler/proactive.ts exports `runSensorDispatchDecision` and `SensorDispatchContext`
    - `SensorDispatchContext` has NO schedule-shaped fields (no `scheduleCron`/`lastRun`/`lastSkipped`/`conditions`)
    - src/scheduler/prompts/sensor-dispatch.md exists, contains `{SLUG}` and `{SENSOR_PROMPT}`, and does NOT contain `{SCHEDULE}` or `{LAST_RUN}`
    - The function returns RUN_DEFAULT-shaped output on error (reason contains "defaulting to run")
    - `npx vitest run src/scheduler/proactive.test.ts` exits 0 with the five new cases present
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The non-schedule steward decision function + its prompt exist, fail open on error, and are pinned by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handleSensorSubAgentTrigger handler + early-return dispatcher branch (with tests)</name>
  <read_first>
    - src/head/activation.ts (the `handleEvent` dispatcher ~496 and the `schedule_trigger` early-return ~521–524; the full `handleScheduleTrigger` ~1096–1317 — the proactive-gate block ~1200–1238 and spawn block ~1270–1316 are the analog to slim down; confirm `os`/`scanAmbient`/`estimateTokens`/`generateAgentId`/`formatIanaTimeLine`/`TextMessage` are already imported at the top)
    - src/scheduler/proactive.ts (the new `runSensorDispatchDecision` from Task 1 — its signature and SensorDispatchContext shape)
    - src/head/activation.test.ts (existing `handleScheduleTrigger` tests ~248–314 — `fire()` private-method invoker ~256–261, `jobEvent()` factory ~248–254, spawn-assertion pattern ~271–284, and the `vi.mock('../scheduler/proactive.js', ...)` block ~26–33 / fixture wiring ~172–173 are the analogs; you must add `runSensorDispatchDecision: vi.fn()` to that mock)
    - 52-PATTERNS.md (the "src/head/activation.ts — early-return dispatcher branch" and "handleSensorSubAgentTrigger new method" sections — the full slim-handler target; the "Test Patterns → activation.test.ts" new-case list)
  </read_first>
  <behavior>
    - Test: firing a `sensor_sub_agent_trigger` event calls `agentRunner.spawn` exactly once with `trigger:'sensor'`, `skillName:'sensor:<slug>'`, and `task` equal to the event's prompt
    - Test: with `proactiveEnabled:true` and a mocked SKIP decision, `agentRunner.spawn` is NOT called and no schedule-store mutation occurs (no markSkipped)
    - Test: with `proactiveEnabled:false` (and shadow off), `agentRunner.spawn` is called directly and `runSensorDispatchDecision` is NOT called
    - Test (head-not-woken): firing the event does NOT reach the head activation path — the assembler/head LLM run is never invoked (assert the head-activation entrypoint used by other tests, e.g. `assembler.assemble`, is not called) — the handler early-returns
  </behavior>
  <action>
    In src/head/activation.ts `handleEvent`: add an early-return branch `if (event.type === 'sensor_sub_agent_trigger') { await this.handleSensorSubAgentTrigger(event); return }` immediately after the existing `schedule_trigger` branch and BEFORE the `let typingInterval`/`coalescedEvents`/`activationStart` assignments (~line 526) — falling through would wake the head (Pitfall 1).
    Add a new private method `handleSensorSubAgentTrigger(event: QueueEvent & { type: 'sensor_sub_agent_trigger' })`: it is a slimmed `handleScheduleTrigger`. Steward gate reuses `this.opts.config.proactiveShadow || this.opts.config.proactiveEnabled` (D-06 — same gate the task path uses, do NOT invent a parallel gate); build the `SensorDispatchContext` from `event.slug`/`event.prompt`, `identityLoader.readFile('USER.md') ?? ''`, the reversed `messages.getRecentTextByTokens(...)` mapped to `{role,content,createdAt}`, `scanAmbient(workspacePath, headId)` (guard on `config.workspacePath`, `.replace(/^~/, os.homedir())`), and `formatIanaTimeLine(new Date(), config.timezone)`; call `runSensorDispatchDecision(ctx, this.opts.llmRouter, this.opts.config.stewardModel, this.opts.usageStore, event.id)`. On shadow, log `[proactive:sensor:shadow]`. On `proactiveEnabled && action==='skip'`, log `[proactive:sensor] Skipped` and return (NO schedule-store call — no row exists). Otherwise capture `decision.context`. Then `generateAgentId('sensor:'+slug)`, assemble the prompt (`event.prompt`, plus `Context from recent conversation: <context>` when present), and `await this.opts.toolExecutorOpts.agentRunner.spawn({ agentId, task: prompt, trigger:'sensor', headId: this.opts.headId, skillName: \`sensor:${event.slug}\` })`. Do NOT call `scheduleStore.get`, `tasksLoader.load`, `markSkipped`, `advanceNextRun`, or any schedule-row lifecycle.
    Add the four behavior tests to src/head/activation.test.ts, extending the existing `vi.mock('../scheduler/proactive.js', ...)` to also mock `runSensorDispatchDecision`.
  </action>
  <verify>
    <automated>npx vitest run src/head/activation.test.ts && grep -q "handleSensorSubAgentTrigger" src/head/activation.ts && grep -q "trigger: 'sensor'" src/head/activation.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/head/activation.ts `handleEvent` has an early-return branch for `sensor_sub_agent_trigger` BEFORE the head-activation path begins
    - `handleSensorSubAgentTrigger` calls `agentRunner.spawn` with `trigger: 'sensor'` and `skillName` of the form `sensor:<slug>`, and never calls `scheduleStore.get`/`tasksLoader.load`/`markSkipped`
    - On steward skip (proactiveEnabled), no spawn occurs; on proactive disabled, spawn occurs without a steward call
    - The handler early-returns so the head activation/LLM path is never reached (head-not-woken test green)
    - `npx vitest run src/head/activation.test.ts` exits 0 with the four new cases
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A `sensor_sub_agent_trigger` event is steward-gated and spawns a `trigger:'sensor'` agent without ever waking the head, pinned by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue event → activation handler | A `sensor_sub_agent_trigger` carrying an operator-authored prompt crosses into the spawn path |
| handler → steward LLM | Prompt + ambient/history context sent to the steward model for the run/skip decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | Denial of Service | runaway sub-agent dispatch cost (sensor emitting `subAgentEvent` every tick) | mitigate | The steward gate (`runSensorDispatchDecision`) ALWAYS runs when proactive is enabled (D-05 — no per-sensor bypass) and honors global `proactiveEnabled`/`proactiveShadow` (D-06 — same gate as tasks). A skip suppresses the spawn entirely. This is the explicit cost-control mitigation called out in the phase security domain. |
| T-52-04 | Elevation of Privilege | head woken / message sent by a silent dispatch | mitigate | `handleEvent` early-returns on `sensor_sub_agent_trigger` BEFORE the head-activation path (Pitfall 1); the head-not-woken test pins that the assembler/LLM run is never invoked. `trigger:'sensor'` also bypasses the relay steward gate (activation.ts:662 `!== 'scheduled'`) — D-09 reuses the existing `agent_completed` path, no new suppression machinery. |
| T-52-05 | Availability | steward LLM failure blocks dispatch | accept | D-07 fail-open: on steward error the decision is RUN_DEFAULT (the sensor already deterministically decided something happened). Accepted: a failing steward errs toward dispatching rather than dropping the trigger. |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs (RESEARCH "Environment Availability" — code/config only). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/scheduler/proactive.test.ts src/head/activation.test.ts` exits 0
- `grep "runSensorDispatchDecision" src/scheduler/proactive.ts` and `grep "handleSensorSubAgentTrigger" src/head/activation.ts` both match
- `src/scheduler/prompts/sensor-dispatch.md` exists and contains no `{SCHEDULE}`/`{LAST_RUN}` tokens
</verification>

<success_criteria>
- Non-schedule steward decision function + prompt exist and fail open
- Activation handler steward-gates then spawns `trigger:'sensor'` silently
- Head is provably never woken by the trigger (early-return test green)
- All new proactive + activation unit tests pass
</success_criteria>

<output>
Create `.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-02-SUMMARY.md` when done
</output>
