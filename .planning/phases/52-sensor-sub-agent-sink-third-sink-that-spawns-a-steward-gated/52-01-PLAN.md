---
phase: 52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/core.ts
  - src/types/agent.ts
  - src/sensors/runner.ts
  - src/sensors/runner.test.ts
autonomous: true
requirements: [SENSOR-17, SENSOR-18]
must_haves:
  truths:
    - "A sensor payload's `subAgentEvent: { prompt }` enqueues a `sensor_sub_agent_trigger` queue event (D-01 payload contract; D-03 object `{ prompt }` shape, not a bare string; D-12 dedicated queue type, not an overloaded schedule_trigger)"
    - "A sensor payload's `headEvent: { text }` enqueues a `sensor_event` — the Phase-51 behavior under the new key name (D-02 rename `event`→`headEvent`)"
    - "The old `event` key no longer enqueues anything — no back-compat (D-02)"
    - "Any combination of the three sinks, or none, in one payload is valid; a malformed/absent `subAgentEvent` is silently skipped, not an error (D-04)"
  artifacts:
    - path: "src/types/core.ts"
      provides: "sensor_sub_agent_trigger QueueEvent member + PRIORITY.SENSOR_SUB_AGENT_TRIGGER"
      contains: "sensor_sub_agent_trigger"
    - path: "src/types/agent.ts"
      provides: "'sensor' added to the AgentState.trigger union"
      contains: "'sensor'"
    - path: "src/sensors/runner.ts"
      provides: "headEvent + subAgentEvent sink parse/enqueue (no bare 'event' key)"
    - path: "src/sensors/runner.test.ts"
      provides: "Phase-52 sink unit tests (rename, new sink, malformed-skip)"
  key_links:
    - from: "src/sensors/runner.ts"
      to: "src/types/core.ts"
      via: "import { PRIORITY } and enqueue { type: 'sensor_sub_agent_trigger' }"
      pattern: "sensor_sub_agent_trigger"
---

<objective>
Lay the contract + types + runner foundation for the sensor third sink. Add the new `sensor_sub_agent_trigger` queue event type and its priority, add the `'sensor'` trigger enum value, and in the sensor runner rename the Phase-51 `event` sink → `headEvent` and add the new `subAgentEvent` sink that enqueues the new event type. No activation/steward wiring here — this plan is the self-contained foundation that Plan 02 builds on.

Purpose: A clean, typed, schedule-less dispatch event the rest of the phase consumes. Renaming `event` → `headEvent` and adding `subAgentEvent` is the user-facing sensor contract change (SENSOR-17/18).
Output: Updated `src/types/core.ts`, `src/types/agent.ts`, `src/sensors/runner.ts`, plus Wave-0 unit tests in `src/sensors/runner.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-CONTEXT.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-RESEARCH.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-PATTERNS.md

<interfaces>
<!-- Extracted contracts the executor needs. Source of truth is the read_first files; these are the target shapes. -->

New QueueEvent member to add in src/types/core.ts (mirror the existing `sensor_event` member, swap `text` → `prompt`):
```typescript
| {
    type: 'sensor_sub_agent_trigger'
    id: string
    slug: string
    prompt: string
    createdAt: string
  }
```

New PRIORITY entry in src/types/core.ts (immediately after `SENSOR_EVENT: 15`):
```typescript
SENSOR_SUB_AGENT_TRIGGER: 10,
```

Trigger union change in src/types/agent.ts (line ~48):
```typescript
trigger: 'manual' | 'scheduled' | 'ad_hoc' | 'sensor'
```
(`SpawnOptions.trigger` is `AgentState['trigger']` — picks the new value up automatically. The DB column is typed `string` and cast at src/db/agents.ts:56 — NO migration needed.)

runner.ts destructure line (replaces `const { ambient, event } = payload`):
```typescript
const { ambient, headEvent, subAgentEvent } = payload
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sensor_sub_agent_trigger queue type + priority + 'sensor' trigger enum</name>
  <read_first>
    - src/types/core.ts (the QueueEvent discriminated union lines ~83–173 — read the existing `sensor_event` member ~158–172 and `schedule_trigger` member ~116–131 as the analog/anti-analog; the PRIORITY map lines ~177–194)
    - src/types/agent.ts (the AgentState `trigger` union at line ~48 and `SpawnOptions.trigger` at line ~84)
    - 52-PATTERNS.md (the "src/types/core.ts" and "src/types/agent.ts" pattern sections — exact target shapes with JSDoc)
  </read_first>
  <action>
    In src/types/core.ts: add a new `sensor_sub_agent_trigger` member to the `QueueEvent` union with fields `type`, `id: string`, `slug: string`, `prompt: string`, `createdAt: string` — mirror the existing `sensor_event` member's JSDoc style, noting it is the silent sub-agent dispatch sink that never reaches the head activation path (SENSOR-17/19). Add `SENSOR_SUB_AGENT_TRIGGER: 10` to the `PRIORITY` map immediately after `SENSOR_EVENT: 15` (matching `SCHEDULE_TRIGGER`=10 — background work, no live user waiting). Do NOT overload `schedule_trigger` (its non-nullable `scheduleId` is consumed by `scheduleStore.get`, per D-12 / RESEARCH).
    In src/types/agent.ts: add `'sensor'` to the `AgentState.trigger` string-literal union (the only edit — `SpawnOptions.trigger` references `AgentState['trigger']` so it inherits the value; no DB migration per RESEARCH §5).
  </action>
  <verify>
    <automated>grep -q "sensor_sub_agent_trigger" src/types/core.ts && grep -q "SENSOR_SUB_AGENT_TRIGGER" src/types/core.ts && grep -q "'sensor'" src/types/agent.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/types/core.ts contains a `sensor_sub_agent_trigger` QueueEvent member with `slug: string` and `prompt: string` fields
    - src/types/core.ts PRIORITY map contains `SENSOR_SUB_AGENT_TRIGGER` set to 10
    - src/types/agent.ts `trigger` union contains the `'sensor'` literal
    - `schedule_trigger` member is unchanged (still has `scheduleId: string`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The new queue type, priority, and trigger enum value exist and the repo type-checks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename `event`→`headEvent` + add `subAgentEvent` sink in runner.ts (with tests)</name>
  <read_first>
    - src/sensors/runner.ts (the dual-sink dispatch block lines ~130–166 — the destructure `const { ambient, event } = payload` and the `event` guard/enqueue; the function JSDoc lines ~41–52 that says `{ ambient?, event? }`)
    - src/sensors/runner.test.ts (existing `describe('runSensor — dual-sink, head-scoped', ...)` block; the `makeSink()`/`writeScript()` factories ~12–21 and the both-fields test ~45–68 are the analog for new cases)
    - 52-PATTERNS.md (the "src/sensors/runner.ts" pattern section — exact rename + additive branch + JSDoc target; the "Test Patterns → runner.test.ts" section lists the five new cases)
  </read_first>
  <behavior>
    - Test: a script emitting `{ headEvent: { text: "x" } }` enqueues exactly one event with `type:'sensor_event'` at `PRIORITY.SENSOR_EVENT` for the run's headId (rename preserves Phase-51 behavior)
    - Test: a script emitting the OLD `{ event: { text: "x" } }` enqueues NOTHING (sink.enqueue called 0 times — no back-compat)
    - Test: a script emitting `{ subAgentEvent: { prompt: "do X" } }` enqueues exactly one event with `type:'sensor_sub_agent_trigger'`, `slug`, `prompt:"do X"`, at `PRIORITY.SENSOR_SUB_AGENT_TRIGGER`, for the run's headId
    - Test: a malformed `subAgentEvent` (missing `prompt`, non-object, or null) is silently skipped — sink.enqueue NOT called, run still resolves (no failure marker thrown)
    - Test: all three sinks in one payload (`ambient` + `headEvent` + `subAgentEvent`) → ambient file written AND sink.enqueue called twice (one sensor_event + one sensor_sub_agent_trigger)
  </behavior>
  <action>
    In src/sensors/runner.ts: change the destructure to `const { ambient, headEvent, subAgentEvent } = payload`. Rename every `event` reference in the existing head-event guard/enqueue block to `headEvent` (inner `['text']` check and the `type:'sensor_event'` enqueue are UNCHANGED — only the source key renames). Immediately after that block, add a parallel `subAgentEvent` guard following the same "present + well-typed → act, else skip (not an error)" pattern: non-null, `typeof === 'object'`, not an array, with `typeof (...)['prompt'] === 'string'`; on match, `enqueue.enqueue({ type:'sensor_sub_agent_trigger', id: generateId('qe'), slug, prompt, createdAt: new Date().toISOString() }, PRIORITY.SENSOR_SUB_AGENT_TRIGGER, headId)` wrapped in the same try/catch → `writeFailure('failed to enqueue sensor sub-agent trigger: ...')`. Update the function JSDoc (~41–52) to document all three sinks `{ ambient?, headEvent?, subAgentEvent? }` per the PATTERNS target text. Add the five behavior tests above to src/sensors/runner.test.ts using the existing `makeSink()`/`writeScript()` factories.
  </action>
  <verify>
    <automated>npx vitest run src/sensors/runner.test.ts && grep -q "headEvent" src/sensors/runner.ts && ! grep -E "const \{ ambient, event \}" src/sensors/runner.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/sensors/runner.ts no longer destructures a bare `event` key (`grep -E "const \{ ambient, event \}" src/sensors/runner.ts` returns nothing)
    - src/sensors/runner.ts references `headEvent` and `subAgentEvent` and enqueues `sensor_sub_agent_trigger` at `PRIORITY.SENSOR_SUB_AGENT_TRIGGER`
    - The function JSDoc documents all three sinks
    - `npx vitest run src/sensors/runner.test.ts` exits 0 with the five new cases present and passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The runner emits the new sink contract, the old `event` key is dead, and all sink behaviors are pinned by passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor script stdout → runner | Operator-authored sensor script emits JSON; runner parses untrusted-shape JSON (but operator-trust content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `subAgentEvent` parse in runner.ts | mitigate | "present + well-typed → act, else skip" guard: non-null object with `prompt:string` required before enqueue; malformed shape is silently skipped (pinned by the malformed-skip test), never throws into the queue |
| T-52-02 | Denial of Service | uncontrolled sub-agent dispatch (a sensor emitting `subAgentEvent` every tick) | accept (mitigated downstream) | The runner only enqueues; the cost gate is the steward (Plan 02) + global `proactiveEnabled` (D-05/D-06). No mitigation lands in this plan; the enqueue itself is bounded by the sensor's own schedule cadence (operator-set). |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this phase (RESEARCH "Environment Availability" — code/config only; `write-file-atomic`/`generateId` already in deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/sensors/runner.test.ts` exits 0
- `grep "sensor_sub_agent_trigger" src/types/core.ts` and `grep "'sensor'" src/types/agent.ts` both match
- The bare `event` destructure is gone from runner.ts
</verification>

<success_criteria>
- New queue type + priority + trigger enum exist and type-check
- Runner renamed `event`→`headEvent` and emits `subAgentEvent`→`sensor_sub_agent_trigger`
- Five new runner unit tests pass (rename, dead old key, new sink, malformed-skip, all-three-active)
</success_criteria>

<output>
Create `.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-01-SUMMARY.md` when done
</output>
