---
phase: 52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated
plan: 03
type: execute
wave: 3
depends_on: ["52-02"]
files_modified:
  - skills/sensors/SKILL.md
  - CHANGELOG.md
  - package.json
  - dashboard/package.json
autonomous: true
requirements: [SENSOR-18]
user_setup: []
must_haves:
  truths:
    - "The bundled sensor authoring doc documents all three sinks (ambient, headEvent, subAgentEvent) with which-sink guidance"
    - "The three workspace sensors are migrated off the old `event` key (no broken sensor)"
    - "The CHANGELOG records the new sink + rename as user-facing Added/Changed, and version 0.5.0 is cut"
  artifacts:
    - path: "skills/sensors/SKILL.md"
      provides: "three-sink contract + Which-sink guidance table"
      contains: "subAgentEvent"
    - path: "CHANGELOG.md"
      provides: "dated [0.5.0] section with the sub-agent sink entry + fresh [next] section"
      contains: "subAgentEvent"
    - path: "package.json"
      provides: "version bumped to 0.5.0"
      contains: "0.5.0"
    - path: "dashboard/package.json"
      provides: "version bumped to 0.5.0 in lockstep"
      contains: "0.5.0"
  key_links:
    - from: "skills/sensors/SKILL.md"
      to: "the runner contract"
      via: "documented stdout shape { ambient?, headEvent?, subAgentEvent? }"
      pattern: "subAgentEvent"
---

<objective>
Land the author-facing + release work now that the contract is wired (Plans 01–02): update the bundled sensor SKILL.md to document the three-sink contract with which-sink guidance, migrate the workspace sensors off the dead `event` key (verifying the non-listed ones too), record the change in the CHANGELOG, and cut the `0.5.0` release (both `package.json` files in lockstep + tag).

Purpose: A sensor author must see the new contract, and no shipped/workspace sensor may break on the rename. The release makes the running version reflect what's on `main`.
Output: Updated `skills/sensors/SKILL.md`, migrated workspace sensors, dated `CHANGELOG.md` `[0.5.0]` + fresh `[next]`, bumped `package.json` + `dashboard/package.json`, and tag `v0.5.0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-CONTEXT.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-RESEARCH.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-PATTERNS.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-01-SUMMARY.md
@.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-02-SUMMARY.md
@AGENTS.md

<notes>
- Workspace sensors live at `~/.shrok/workspace/sensors/` — OUTSIDE the repo (not tracked by git, not in `files_modified`). Edit them in place on disk; they are migration targets, not committed artifacts.
- Per AGENTS.md "Versioning & releases": bump BOTH `package.json` and `dashboard/package.json` in lockstep, commit source only (NEVER stage `dashboard/dist/` — CI is its sole writer), date the in-flight CHANGELOG header, open a fresh `## [next]` above it, annotated tag, push `--follow-tags`. The CHANGELOG already has an undated `## [0.5.0]` with prior shipped work — this phase's entry joins that section, which then gets dated and tagged `v0.5.0`.
- CHANGELOG entries are user-language only — NO internal planning artifacts (no phase numbers, no SENSOR-xx requirement IDs, no `.planning/` paths).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skills/sensors/SKILL.md to the three-sink contract + migrate workspace sensors</name>
  <read_first>
    - skills/sensors/SKILL.md (the full file — the stdout contract line ~28 `{ "ambient"?: string, "event"?: { "text": string } }` and the two-sink section ~41–end with `### Event` heading and `"event"` references)
    - 52-PATTERNS.md (the "skills/sensors/SKILL.md — three-sink contract update" section — exact contract-line replacement, the renamed heading, the new `### Sub-agent event (dispatch, silent)` subsection, and the "Which sink?" table)
    - 52-RESEARCH.md (§6 Workspace Sensor Migration table + §7 SKILL.md changes + Open Question 2 on disk-space/weather)
    - ~/.shrok/workspace/sensors/relay/sensor.mjs (the `event: { text }` emit ~line 55)
    - ~/.shrok/workspace/sensors/example-sensor/sensor.mjs (the `event: { text }` emit ~line 67)
  </read_first>
  <action>
    In skills/sensors/SKILL.md: replace the stdout contract line with `{ "ambient"?: string, "headEvent"?: { "text": string }, "subAgentEvent"?: { "prompt": string } }`. Rename the `### Event` section heading to `### Head event (push, active)` and update all `"event"` references in that section to `"headEvent"`. Add a new `### Sub-agent event (dispatch, silent)` subsection documenting `subAgentEvent: { prompt }` — that it dispatches a sub-agent silently (no head wake, no user-facing message), the prompt-as-interface design (no task-name field; name a task in the prompt if needed), and the proactive steward gate that defaults to running. Add the "Which sink?" guidance table: `ambient` = always-present snapshot; `headEvent` = wake the head to talk to the user / make a judgment; `subAgentEvent` = get quiet work done without a message. Update any worked examples that used `event:` to `headEvent:`, and add one `subAgentEvent` example.
    Migrate workspace sensors (on disk, ~/.shrok/workspace/sensors/): in relay/sensor.mjs rename the emitted `event:` key to `headEvent:` (inner `{ text }` unchanged). In example-sensor/sensor.mjs rename `event:` → `headEvent:` AND add a `subAgentEvent` demo on a distinct watermark (e.g. every 10th run, `payload.subAgentEvent = { prompt: 'Note in the journal that the example sensor has reached <runs> runs.' }`). VERIFY calendar/sensor.mjs is ambient-only (untouched), and grep disk-space/sensor.mjs + weather/sensor.mjs for any `event` key (RESEARCH Open Question 2) — if either emits `event`, migrate it to `headEvent` too; if confirmed ambient-only, leave untouched and note it in the SUMMARY. If the repo ships a bundled example-sensor seed (e.g. under `skills/` or a seed dir) that emits `event:`, migrate that copy in lockstep.
  </action>
  <verify>
    <automated>grep -q "subAgentEvent" skills/sensors/SKILL.md && grep -q "headEvent" skills/sensors/SKILL.md && ! grep -qE '"event"\s*:\s*\{\s*"text"' skills/sensors/SKILL.md && ! grep -RElq "\bevent\s*:\s*\{\s*text" ~/.shrok/workspace/sensors/ 2>/dev/null; echo "skill+sensors checked"</automated>
  </verify>
  <acceptance_criteria>
    - skills/sensors/SKILL.md stdout contract line is `{ "ambient"?: string, "headEvent"?: { "text": string }, "subAgentEvent"?: { "prompt": string } }`
    - skills/sensors/SKILL.md contains a `### Sub-agent event` subsection and a "Which sink?" table, and no longer documents a bare `"event"` sink
    - `~/.shrok/workspace/sensors/relay/sensor.mjs` emits `headEvent` (no bare `event:` key)
    - `~/.shrok/workspace/sensors/example-sensor/sensor.mjs` emits `headEvent` AND a `subAgentEvent` demo
    - `~/.shrok/workspace/sensors/calendar/sensor.mjs` unchanged (ambient-only); disk-space + weather verified to carry no `event` key (or migrated if they do) — recorded in SUMMARY
  </acceptance_criteria>
  <done>The sensor authoring doc teaches all three sinks and no workspace/bundled sensor still emits the dead `event` key.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entry + cut version 0.5.0 (lockstep bump + tag)</name>
  <read_first>
    - CHANGELOG.md (the top `## [0.5.0]` undated in-flight section with its existing `### Added`/`### Changed`/`### Fixed` subsections)
    - AGENTS.md (the "Versioning & releases" + "Changelog" sections — lockstep bump rule, no-dist-staging rule, date+tag+`--follow-tags` cut sequence, user-language-only entry rule)
    - package.json (the `"version": "0.4.0"` field)
    - dashboard/package.json (the `"version": "0.4.0"` field — must match root)
    - 52-RESEARCH.md (§"Version and Changelog" — confirms target is 0.5.0)
  </read_first>
  <action>
    In CHANGELOG.md `## [0.5.0]`: add to `### Added` a user-language bullet for the new silent sub-agent sensor sink (a sensor can now quietly hand work to a sub-agent — e.g. create a reminder — without your assistant sending a message, gated by the steward so it only fires when there's something to do). Add to `### Changed` a bullet noting sensor authors now choose between a "head event" (wake the assistant to talk/judge) and a "sub-agent event" (do quiet work), and that the old single event sink was renamed accordingly. Use NO requirement IDs / phase numbers / internal paths.
    Bump `package.json` `version` to `0.5.0` and `dashboard/package.json` `version` to `0.5.0` (lockstep). Date the CHANGELOG header `## [0.5.0] — YYYY-MM-DD` (today's date) and insert a fresh empty `## [next]` section above it. Commit source only — do NOT stage `dashboard/dist/`. Then per AGENTS.md cut the release: annotated tag `git tag -a v0.5.0 -m "v0.5.0"` and push the commit + tag with `--follow-tags` (push happens at the normal GSD commit step; the tag must ship — do not leave it local-only).
  </action>
  <verify>
    <automated>grep -q '"version": "0.5.0"' package.json && grep -q '"version": "0.5.0"' dashboard/package.json && grep -q "subAgentEvent" CHANGELOG.md && grep -Eq "## \[0.5.0\] — [0-9]{4}-[0-9]{2}-[0-9]{2}" CHANGELOG.md && grep -q "## \[next\]" CHANGELOG.md && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` and `dashboard/package.json` both read `"version": "0.5.0"`
    - CHANGELOG.md `## [0.5.0]` header is dated `— YYYY-MM-DD` and a fresh `## [next]` section sits above it
    - The CHANGELOG sub-agent-sink entry uses user language (no `SENSOR-1x`, no phase numbers, no `.planning/` paths)
    - An annotated tag `v0.5.0` is created and pushed with the commit (`git tag -l v0.5.0` non-empty; not local-only after the push step)
    - `npx tsc --noEmit` exits 0 (no source regression from the bump)
    - `dashboard/dist/` is NOT staged in any commit this task makes
  </acceptance_criteria>
  <done>0.5.0 is cut: both package.json files bumped in lockstep, CHANGELOG dated with the sink entry + fresh next section, and tag v0.5.0 pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs/release ↔ operator | Documentation + version metadata; no runtime trust boundary crossed |
| workspace sensor files ↔ runner | Operator edits on-disk sensor scripts the runner executes (operator-trust, unchanged from Phase 51) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | Tampering | accidental `dashboard/dist/` commit | mitigate | Acceptance criterion forbids staging `dashboard/dist/`; AGENTS.md rule (CI is sole writer) is read_first. Pure docs/version task — no runtime surface. |
| T-52-07 | Repudiation | release/tag drift (version ≠ tag ≠ changelog) | mitigate | The cut sequence pins tag `v0.5.0` to the commit that set package.json to 0.5.0 and dated `## [0.5.0]` — per AGENTS.md "tag ⇄ package.json ⇄ changelog stay equal". `--follow-tags` ensures the tag isn't left local-only. |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — version-string edits only. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep '"version": "0.5.0"' package.json dashboard/package.json` matches both
- CHANGELOG `## [0.5.0] — <date>` dated + `## [next]` present + `subAgentEvent` entry present
- `git tag -l v0.5.0` non-empty
- `skills/sensors/SKILL.md` documents three sinks; no workspace sensor emits the bare `event` key
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Sensor SKILL.md documents all three sinks with which-sink guidance
- Workspace (and any bundled) sensors migrated off `event` → `headEvent` + the example demos `subAgentEvent`
- CHANGELOG records the change in user language; `0.5.0` cut (lockstep bump + dated header + fresh next + tag pushed)
</success_criteria>

<output>
Create `.planning/phases/52-sensor-sub-agent-sink-third-sink-that-spawns-a-steward-gated/52-03-SUMMARY.md` when done
</output>
