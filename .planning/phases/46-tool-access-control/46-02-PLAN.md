---
phase: 46-tool-access-control
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - src/system.ts
  - src/index.ts
  - src/sub-agents/tool-surface.ts
  - src/head/enforcement.test.ts
autonomous: true
requirements: [TOOLCFG-05, TOOLCFG-06, TOOLCFG-07]
must_haves:
  truths:
    - "a head whose resolved head-tool allowlist is a subset is offered ONLY those HEAD_TOOLS at activation"
    - "a head with no head-tool override and no global default retains every HEAD_TOOL (everything-on default)"
    - "a sub-agent spawned by a head receives that head's resolved agent-tool allowlist via assembleTools"
    - "core orchestration tools (spawn_agent/message_agent/cancel_agent) are filterable like any other tool — no guardrail re-adds them"
    - "the spawning head's per-head agent override resolves against worker_defaults.allowedTools and reaches LocalAgentRunner.agentDefaults"
  artifacts:
    - path: "src/system.ts"
      provides: "per-head override threading from buildSystem deps into runner agentDefaults + the filtered HEAD_TOOLS passed to ActivationLoop"
      contains: "headToolsOverride"
    - path: "src/head/enforcement.test.ts"
      provides: "tests proving head filtering and agent threading honor the tri-state resolution"
      contains: "resolveAllowlist"
  key_links:
    - from: "src/index.ts head loop"
      to: "buildSystem deps"
      via: "head.headToolsOverride / head.agentToolsOverride spread"
      pattern: "headToolsOverride|agentToolsOverride"
    - from: "src/system.ts"
      to: "LocalAgentRunner.agentDefaults.allowedTools"
      via: "resolveAllowlist(agentToolsOverride, config.workerDefaults.allowedTools)"
      pattern: "agentDefaults"
    - from: "src/system.ts buildSystem"
      to: "ActivationLoop headTools (the offered HEAD_TOOLS surface)"
      via: "resolveAllowlist(headToolsOverride, headToolDefaults.allowedTools) then HEAD_TOOLS.filter"
      pattern: "HEAD_TOOLS\\.filter|resolvedHeadTools"
---

<objective>
Wire the two runtime enforcement points to the resolution helper from 46-01, threading each head's per-head overrides from config all the way to where tools are assigned. (1) Head enforcement: filter HEAD_TOOLS to the head's resolved head-tool allowlist and pass it as the ActivationLoop's `headTools` (the existing param the activation flow already consumes at activation.ts:844 via `this.opts.headTools ?? HEAD_TOOLS`). (2) Agent enforcement: the spawning head's resolved agent-tool allowlist becomes the LocalAgentRunner's agentDefaults.allowedTools, which assembleTools already honors. Everything-on defaults and no core-tool guardrails (TOOLCFG-07).

Purpose: This is where config becomes behavior. The existing agentDefaults path (assembleTools) already filters correctly, and the head surface is already gated by the existing `headTools` activation param — the work is computing the per-head value in buildSystem and threading it into both seams. No edits to activation.ts or local.ts are needed: activation.ts:844 stays `this.opts.headTools ?? HEAD_TOOLS` (we now always populate that param), and local.ts agentDefaults is set by system.ts.
Output: Threading through index.ts → system.ts → runner agentDefaults + the filtered HEAD_TOOLS passed to ActivationLoop; enforcement tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-tool-access-control/46-CONTEXT.md
@.planning/phases/46-tool-access-control/46-01-SUMMARY.md

<interfaces>
<!-- From 46-01: the resolution helper this plan consumes. -->
src/sub-agents/tool-access.ts:
```typescript
export function resolveAllowlist(
  perHeadOverride: string[] | null | undefined,
  globalDefault: string[] | null | undefined,
): string[] | null  // null = all tools, string[] = subset
```

<!-- READ-CONTEXT ANCHOR (NOT modified by this plan). The head enforcement point already exists.
     src/head/activation.ts:841-845 (inside the activation flow):
const toolLoopOpts = {
  ...
  tools: this.opts.headTools ?? HEAD_TOOLS,   // <-- already consumes the headTools param; STAYS AS-IS
  ...
}
     ActivationLoopOptions already has `headTools?: ToolDefinition[]` (activation.ts:107) and `headId: string` (activation.ts:80).
     HEAD_TOOLS is imported from src/head/index.js; each entry is a ToolDefinition with `.name`.
     This plan does NOT touch activation.ts — it populates the existing `headTools` opt from system.ts. -->

<!-- READ-CONTEXT ANCHOR (NOT modified by this plan). The agent enforcement point already works —
     assembleTools (src/sub-agents/tool-surface.ts:196,248-278) filters optional/MCP/schedule/reminder/note
     tools by deps.agentDefaults.allowedTools (null=all, []=baseline, [array]=subset). The job is to make
     agentDefaults.allowedTools carry the per-head-resolved value, not raw config.workerDefaults. That
     agentDefaults object is set by system.ts (the LocalAgentRunner construction); src/sub-agents/local.ts
     consumes it unchanged — local.ts is NOT modified by this plan. -->

<!-- The wiring seam (MODIFIED HERE). src/system.ts buildSystem(deps): deps.headId + deps.customPrompt are per-head;
     `config` is global. LocalAgentRunner is constructed at system.ts:258-296 with `agentDefaults: config.workerDefaults` (line 275).
     ActivationLoop is constructed at system.ts:374 and receives headTools only when deps.headTools is set (line 397). -->

<!-- The host (MODIFIED HERE). src/index.ts:266-278 loops resolvedHeads and calls buildSystem({ ..., headId: head.id,
     ...(head.customPrompt !== undefined ? { customPrompt: head.customPrompt } : {}) }).
     resolvedHeads now carry headToolsOverride / agentToolsOverride (from 46-01). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread per-head overrides through buildSystem into runner agentDefaults and the ActivationLoop headTools param</name>
  <files>src/system.ts, src/index.ts</files>
  <action>Pass each head's overrides into buildSystem and consume them there.
  (1) In src/system.ts, extend SystemDeps with two optional fields mirroring the customPrompt comment style: `headToolsOverride?: string[] | null` and `agentToolsOverride?: string[] | null` (per-head, key-absent = inherit global). Import `resolveAllowlist` from './sub-agents/tool-access.js'.
  (2) AGENT enforcement (TOOLCFG-06): at the LocalAgentRunner construction (system.ts:275), replace `agentDefaults: config.workerDefaults` with an agentDefaults whose allowedTools is the resolved per-head value while env stays as config.workerDefaults.env. Compute `const resolvedAgentTools = resolveAllowlist(deps.agentToolsOverride, config.workerDefaults.allowedTools)` once near the top of buildSystem, then pass `agentDefaults: { env: config.workerDefaults.env, allowedTools: resolvedAgentTools }`. (AgentDefaults type is `{ env: string[] | null; allowedTools: string[] | null }` from tool-surface.ts:20-23 — resolveAllowlist returns exactly `string[] | null`, so this type-checks.)
  (3) HEAD enforcement (TOOLCFG-05): compute `const resolvedHeadTools = resolveAllowlist(deps.headToolsOverride, config.headToolDefaults.allowedTools)`. Build the filtered head tool list: when `resolvedHeadTools === null`, pass the full HEAD_TOOLS (already imported at system.ts:33); otherwise `HEAD_TOOLS.filter(t => resolvedHeadTools.includes(t.name))`. Pass this as the activation loop's `headTools` (system.ts:397) — BUT preserve the test-override precedence: if `deps.headTools` is explicitly provided (the existing testing override), it wins. So compute `const effectiveHeadTools = deps.headTools !== undefined ? deps.headTools : (resolvedHeadTools === null ? HEAD_TOOLS : HEAD_TOOLS.filter(t => resolvedHeadTools.includes(t.name)))` and always pass `headTools: effectiveHeadTools` to ActivationLoop (drop the conditional spread at line 397). Note: `new Set(HEAD_TOOLS.map(...))` at system.ts:303 feeds InjectorImpl's known-head-tool-names set — leave it on the FULL HEAD_TOOLS (it is a recognizer, not the offered surface; filtering it would mislabel a disabled tool as unknown). Do NOT edit src/head/activation.ts — it already reads `this.opts.headTools ?? HEAD_TOOLS`; we are simply always populating that opt now.
  (4) In src/index.ts (the resolvedHeads loop ~266-278), add the two override fields to the buildSystem call using the same key-present spread as customPrompt: `...(head.headToolsOverride !== undefined ? { headToolsOverride: head.headToolsOverride } : {})` and likewise for agentToolsOverride.
  Do NOT change assembleTools — it already consumes agentDefaults.allowedTools correctly. Do NOT change src/sub-agents/local.ts — it consumes agentDefaults unchanged.</action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx tsc --noEmit && npx vitest run src/system.test.ts src/head/enforcement.test.ts</automated>
  </verify>
  <done>buildSystem computes resolvedAgentTools + resolvedHeadTools via resolveAllowlist; runner agentDefaults.allowedTools is the resolved agent value; the ActivationLoop receives the filtered HEAD_TOOLS via its existing headTools param; deps.headTools test override still wins; activation.ts + local.ts untouched; root tsc passes; system tests AND the new enforcement tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Enforcement tests — head filtering at activation + agent threading through assembleTools</name>
  <files>src/head/enforcement.test.ts, src/sub-agents/tool-surface.ts</files>
  <action>Create src/head/enforcement.test.ts with two enforcement layers proven against the resolution helper. tool-surface.ts is listed as modifiable ONLY in case a tiny exported test seam is needed (e.g. it already exports assembleTools + AgentDefaults — prefer using those as-is and make NO change unless a test genuinely cannot reach the path; if unchanged, that is the expected outcome).
  HEAD filtering tests (TOOLCFG-05/07): assert the filter expression used in system.ts Task 1 — `resolvedHeadTools === null ? HEAD_TOOLS : HEAD_TOOLS.filter(t => resolvedHeadTools.includes(t.name))` — for: (a) override absent + global default absent → resolveAllowlist returns null → full HEAD_TOOLS offered (every name present, including spawn_agent/message_agent/cancel_agent — the no-guardrail assertion); (b) override = ['write_identity','get_usage'] → only those two HEAD_TOOLS offered, and spawn_agent is absent (core tool genuinely removable); (c) override absent + global default = ['spawn_agent'] → only spawn_agent offered (global head default applies). Import HEAD_TOOLS from '../head/index.js' and resolveAllowlist from '../sub-agents/tool-access.js'.
  AGENT threading tests (TOOLCFG-06/07): drive assembleTools (mirror the harness in src/sub-agents/tool-surface.test.ts — temp dirs, real stores, AgentToolRegistryImpl, stub McpRegistry) with deps.agentDefaults set to the OUTPUT of resolveAllowlist for: (a) per-head override = ['bash'] against global workerDefaults.allowedTools = null → assembled toolEntries include bash but exclude an unrelated optional tool (e.g. some other tool name from OPTIONAL_TOOL_NAMES); (b) per-head override absent against global = ['get_usage'] → get_usage present, others filtered; (c) both absent (null result) → unrestricted (a representative optional tool present). Assert on toolEntries.map(e => e.definition.name).</action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx vitest run src/head/enforcement.test.ts</automated>
  </verify>
  <done>Head-filtering tests prove subset filtering AND the everything-on/no-guardrail default (core tools removable). Agent-threading tests prove the resolved per-head agent allowlist reaches assembleTools and gates the assembled surface. All green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config (per-head override) → head LLM tool surface | a misconfigured allowlist could over- or under-expose tools to the model |
| config (per-head override) → spawned sub-agent tool surface | the spawning head's allowlist constrains the agent's capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-E | Elevation of privilege | head/agent tool surface | mitigate | Filtering is fail-closed against the resolved allowlist; a subset can only REMOVE tools. Default (null) = everything-on by explicit design decision (TOOLCFG-07), not an accident — covered by the everything-on default test. |
| T-46-02-T | Tampering | spawn_agent removal | accept | Disabling spawn_agent/message_agent/cancel_agent breaks delegation — explicitly the operator's choice, no guardrail (locked decision). Tested as removable. |
| T-46-02-I | Information disclosure | InjectorImpl known-tool set | mitigate | Recognizer set stays on full HEAD_TOOLS so a disabled tool is not misclassified as an unknown/injected tool. |
| T-46-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. |
</threat_model>

<verification>
- `cd /home/thenasty/shrok && npx tsc --noEmit` passes.
- `npx vitest run src/head/enforcement.test.ts src/system.test.ts` green.
- Spot-check: with a head configured to a 2-tool head override, the effectiveHeadTools passed to ActivationLoop has length 2.
</verification>

<success_criteria>
- Head tool surface filtered to the resolved head allowlist via the ActivationLoop headTools param (TOOLCFG-05).
- Spawned agents receive the spawning head's resolved agent allowlist (TOOLCFG-06).
- Everything-on default and core-tool removability proven (TOOLCFG-07).
- assembleTools unchanged in behavior; activation.ts + local.ts untouched; deps.headTools test override precedence preserved.
- Root tsc clean; no dashboard/dist staged; src/icw untouched; no git tag.
</success_criteria>

<output>
Create `.planning/phases/46-tool-access-control/46-02-SUMMARY.md` when done. Record: the exact filter expression computed in buildSystem and passed as the ActivationLoop headTools param, where resolvedAgentTools/resolvedHeadTools are computed, and confirmation core tools are removable + everything-on default holds + activation.ts/local.ts were not modified.
</output>
