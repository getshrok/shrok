---
phase: 46-tool-access-control
plan: 04
type: execute
wave: 3
depends_on: ["46-03"]
files_modified:
  - dashboard/src/components/TagSelect.tsx
  - dashboard/src/components/kind/KindEditorPage.tsx
  - dashboard/src/pages/settings/draft.tsx
  - dashboard/src/pages/settings/BehaviorTab.tsx
  - dashboard/src/pages/settings/HeadCard.tsx
  - dashboard/src/pages/settings/ToolOverrideControl.tsx
  - dashboard/src/pages/settings/ToolOverrideControl.test.tsx
  - CHANGELOG.md
autonomous: false
requirements: [TOOLCFG-08, TOOLCFG-09]
must_haves:
  truths:
    - "the Settings page exposes editable global head-tool and agent-tool allowlists populated from /api/tools"
    - "the global head-tool + agent-tool defaults flow through the Settings draft layer (DraftState/initDraft/isDirty/buildBody) so a change is detected dirty and reaches the PUT body — TOOLCFG-08 persists, not a silent no-op"
    - "each head card exposes a per-head head-tool override and agent-tool override with three explicit, visually-distinct modes: Inherit global / All tools / Choose subset"
    - "the subset mode shows a TagSelect picker fed by the live tool registry; non-subset modes hide it"
    - "saving a per-head override sends the correct tri-state value (sentinel=inherit, null=all, array=subset) to the API"
    - "CHANGELOG [0.3.0] Added documents config-driven tool access control (closes #7)"
  artifacts:
    - path: "dashboard/src/components/TagSelect.tsx"
      provides: "shared TagSelect extracted for reuse across KindEditorPage, Settings, and head cards"
      contains: "export"
    - path: "dashboard/src/pages/settings/draft.tsx"
      provides: "DraftState/initDraft/isDirty/buildBody carry headToolDefault + agentToolDefault so the global allowlists persist via the Settings PUT"
      contains: "headToolDefault"
    - path: "dashboard/src/pages/settings/ToolOverrideControl.tsx"
      provides: "reusable tri-state (Inherit/All/Subset) control + global two-state (All/Subset) control"
      contains: "Inherit"
    - path: "dashboard/src/pages/settings/HeadCard.tsx"
      provides: "per-head head-tool + agent-tool override UI wired to api.heads.setToolOverrides"
      contains: "setToolOverrides"
    - path: "CHANGELOG.md"
      provides: "user-facing entry"
      contains: "closes #7"
  key_links:
    - from: "dashboard/src/pages/settings/HeadCard.tsx"
      to: "api.heads.setToolOverrides"
      via: "tri-state mode → API body"
      pattern: "setToolOverrides"
    - from: "dashboard/src/pages/settings/BehaviorTab.tsx"
      to: "draft set('headToolDefault'/'agentToolDefault')"
      via: "global allowlist editors mutate the draft"
      pattern: "headToolDefault|agentToolDefault"
    - from: "dashboard/src/pages/settings/draft.tsx buildBody"
      to: "settings.update PUT body"
      via: "emit headToolDefault/agentToolDefault when JSON.stringify differs from saved"
      pattern: "headToolDefault|agentToolDefault"
    - from: "ToolOverrideControl subset mode"
      to: "api.tools.list (tools + headTools)"
      via: "TagSelect options"
      pattern: "TagSelect"
---

<objective>
The dashboard presentation layer for tool access control: a shared TagSelect, a reusable tri-state override control, the global allowlist editors on the Settings page, and the per-head override controls on each head card — all wired to the 46-03 API. Plus the CHANGELOG entry. This is pure React over the contract 46-03 established; the tri-state must be visually explicit (Inherit global vs All tools vs Choose subset are three different things, not three values in one dropdown of tool names).

Purpose: Operators need to see and set both layers without touching config.json, and the inherit/all/subset distinction must be unmistakable in the UI (TOOLCFG-09's explicit requirement).
Output: Extracted TagSelect, ToolOverrideControl + its test, the Settings draft-layer wiring for the two global defaults, Settings global editors, per-head card controls, CHANGELOG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-tool-access-control/46-CONTEXT.md
@.planning/phases/46-tool-access-control/46-03-SUMMARY.md

<interfaces>
<!-- TagSelect currently lives INLINE in dashboard/src/components/kind/KindEditorPage.tsx:216-310 (not exported).
     Signature: TagSelect({ values, onChange, options, placeholder?, readOnly? }). Extract verbatim into a shared file. -->

<!-- api.tools.list() now returns { tools: string[]; headTools: string[] } (from 46-03). -->
<!-- api.heads.setToolOverrides(id, { headToolsOverride?, agentToolsOverride? }) where each value is
     string[] | null | '__inherit__' (from 46-03). -->
<!-- HeadDTO carries headToolsOverride?: string[] | null and agentToolsOverride?: string[] | null. -->
<!-- SettingsData carries agentToolDefault: string[] | null and headToolDefault: string[] | null (from 46-03 GET + DTO). -->

<!-- The Settings DRAFT layer — dashboard/src/pages/settings/draft.tsx — is the ONLY save path for Settings.
     buildBody (draft.tsx:249-331) is the SOLE builder of the Settings PUT body. There is no other place where
     a global setting reaches the PUT. So a new SettingsData field that BehaviorTab edits MUST also be threaded
     through DraftState + initDraft + isDirty + buildBody, or the edit never persists (silent no-op).
     - DraftState (draft.tsx:3-88): the in-flight edit shape.
     - initDraft(s) (draft.tsx:90-165): seeds the draft from the settings GET.
     - isDirty(draft, s) (draft.tsx:167-247): array fields are compared with JSON.stringify (see llmProviderPriority
       at draft.tsx:181) — NOT `!==`, which is reference equality and always-true/always-false for arrays.
     - buildBody(draft, s) (draft.tsx:249-331): emits a key only when it differs from saved; array fields use
       JSON.stringify diffing (see llmProviderPriority at draft.tsx:251). null is a meaningful value here
       (null = "all tools"), so the diff must compare null-vs-array faithfully — JSON.stringify handles both. -->

<!-- HeadCard (dashboard/src/pages/settings/HeadCard.tsx) — the customPrompt block at lines 215-236 is the
     exact pattern for a per-head editor with its own useMutation + Save button. Add the override controls near it. -->

<!-- Settings tabs: BehaviorTab receives s (SettingsData) + d (draft) + set(key,value). It is the home for
     global behavioral toggles — add the two global allowlist editors there. SettingsModal wires it at
     dashboard/src/pages/settings/SettingsModal.tsx:188-194 with the draft/set/save flow. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared TagSelect + build the ToolOverrideControl tri-state component with tests</name>
  <files>dashboard/src/components/TagSelect.tsx, dashboard/src/components/kind/KindEditorPage.tsx, dashboard/src/pages/settings/ToolOverrideControl.tsx, dashboard/src/pages/settings/ToolOverrideControl.test.tsx</files>
  <action>(A) Move the inline TagSelect (KindEditorPage.tsx:216-310) verbatim into a new dashboard/src/components/TagSelect.tsx as a named export with the same props ({ values, onChange, options, placeholder?, readOnly? }). In KindEditorPage.tsx, delete the inline definition and import it from the new file — behavior must be byte-identical (the existing triggerTools picker still works).
  (B) Create dashboard/src/pages/settings/ToolOverrideControl.tsx. Export two small pure helpers AND two components:
  - helpers `modeForValue(value: string[] | null | '__inherit__' | undefined): 'inherit' | 'all' | 'subset'` and `valueForMode(mode, subset: string[]): string[] | null | '__inherit__'` so the mode↔value mapping is unit-testable without rendering.
  - `GlobalToolControl({ value, onChange, options })` — TWO modes for a GLOBAL default: "All tools" (value === null) and "Choose subset" (value is string[]). A segmented/radio mode selector switches between them; subset mode renders TagSelect with options. Switching to All sets value=null; switching to Subset sets value=[] (empty, editable). value type: `string[] | null`.
  - `HeadToolOverrideControl({ value, onChange, options })` — THREE explicit modes for a per-head override: "Inherit global" / "All tools" / "Choose subset". Uses modeForValue to derive the initial mode (undefined or '__inherit__' → inherit; null → all; array → subset) and valueForMode on mode change (inherit → '__inherit__'; all → null; subset → current subset array). TagSelect shown ONLY in subset mode. Render the three modes visually distinct (a 3-button segmented control, active one accent-colored), and label the inherit option clearly as "use the global default" per TOOLCFG-09. value type: `string[] | null | '__inherit__'`.
  (C) Create dashboard/src/pages/settings/ToolOverrideControl.test.tsx (vitest). Test the exported helpers directly: modeForValue(undefined)==='inherit', modeForValue('__inherit__')==='inherit', modeForValue(null)==='all', modeForValue(['a'])==='subset'; valueForMode('inherit', x)==='__inherit__', valueForMode('all', x)===null, valueForMode('subset', ['a'])===['a']. (If the dashboard already has @testing-library/react wired, optionally add a render test that switching to subset reveals the TagSelect; the helper tests are the required floor.)</action>
  <verify>
    <automated>cd /home/thenasty/shrok/dashboard && npx tsc --noEmit && npx vitest run src/pages/settings/ToolOverrideControl.test.tsx</automated>
  </verify>
  <done>TagSelect is shared (KindEditorPage imports it, triggerTools picker unaffected); GlobalToolControl (2-mode) and HeadToolOverrideControl (3-mode) exist; mode↔value mapping tested including the inherit sentinel; dashboard tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Thread global defaults through the Settings draft layer + render the global allowlist editors (TOOLCFG-08)</name>
  <files>dashboard/src/pages/settings/draft.tsx, dashboard/src/pages/settings/BehaviorTab.tsx</files>
  <action>(A) Draft-layer wiring — dashboard/src/pages/settings/draft.tsx (THE only Settings save path; without this the global editors are a silent no-op). Make FOUR coordinated edits, mirroring how `llmProviderPriority` (an existing array field) is handled:
  1. DraftState (draft.tsx:3-88): add `headToolDefault: string[] | null` and `agentToolDefault: string[] | null`.
  2. initDraft(s) (draft.tsx:90-165): initialize both from the settings GET — `headToolDefault: s.headToolDefault` and `agentToolDefault: s.agentToolDefault` (SettingsData carries these from 46-03).
  3. isDirty(draft, s) (draft.tsx:167-247): compare BOTH with JSON.stringify, NOT `!==` — `if (JSON.stringify(draft.headToolDefault) !== JSON.stringify(s.headToolDefault)) return true` and the same for agentToolDefault. (Reference `!==` on arrays/null is wrong — see the existing llmProviderPriority line at draft.tsx:181 for the correct pattern; JSON.stringify also distinguishes null from [] from ['a'], the meaningful tri-state-for-display distinction.)
  4. buildBody(draft, s) (draft.tsx:249-331): emit each key only when it differs from saved, using JSON.stringify diffing — `if (JSON.stringify(draft.headToolDefault) !== JSON.stringify(s.headToolDefault)) body.headToolDefault = draft.headToolDefault` and the same for agentToolDefault (mirror the llmProviderPriority emit at draft.tsx:251). Emit the raw value including null (null = "all tools" is a meaningful state the PUT must carry — do NOT drop null).
  (B) Global editors — dashboard/src/pages/settings/BehaviorTab.tsx: add a "Tool access" section with two GlobalToolControl editors: one for the global HEAD tool default (bound to `d.headToolDefault`, options = headTools from api.tools.list), one for the global AGENT tool default (bound to `d.agentToolDefault`, options = tools from api.tools.list). Fetch the tool registry with `useQuery({ queryKey: ['tools'], queryFn: api.tools.list, staleTime: Infinity })` (same pattern as KindEditorPage.tsx:469). Wire each control's onChange through `set('headToolDefault', value)` / `set('agentToolDefault', value)` so they participate in the modal's existing draft + Save (buildBody now carries them — see part A). Add concise helper copy explaining "All tools" = unrestricted and that per-head overrides can further restrict. The new keys exist on both DraftState (part A) and SettingsData (46-03), so `set(...)` type-checks.</action>
  <verify>
    <automated>cd /home/thenasty/shrok/dashboard && npx tsc --noEmit</automated>
  </verify>
  <done>draft.tsx DraftState/initDraft/isDirty/buildBody all carry headToolDefault + agentToolDefault with JSON.stringify diffing; a global allowlist change is detected dirty and rides the existing settings.update PUT (no silent no-op); the Settings Behavior tab shows editable global head-tool + agent-tool allowlists populated from /api/tools; dashboard tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Per-head override controls on HeadCard (TOOLCFG-09) + CHANGELOG</name>
  <files>dashboard/src/pages/settings/HeadCard.tsx, CHANGELOG.md</files>
  <action>(A) In HeadCard.tsx, add a "Tool access" block modeled on the customPrompt block (HeadCard.tsx:215-236). Fetch tools via useQuery(['tools'], api.tools.list, staleTime Infinity). Render two HeadToolOverrideControl instances: HEAD tools (initial value = head.headToolsOverride, options = toolsQuery.data.headTools) and AGENT tools (initial value = head.agentToolsOverride, options = toolsQuery.data.tools). Hold their current values in local state, initialized from the head DTO and defaulting to '__inherit__' when the key is absent (use modeForValue's contract: absent → inherit). Add a single Save button driving `useMutation({ mutationFn: () => api.heads.setToolOverrides(head.id, { headToolsOverride, agentToolsOverride }), onSuccess: onSaved })`. Show the same mandatory-restart note style HeadsTab uses for head changes. Ensure the three modes render visually distinct per the Task 1 component.
  (B) Update CHANGELOG.md: add one bullet under `## [0.3.0]` → `### Added`: a user-facing line describing config-driven tool access control — operators can restrict which tools the head and its sub-agents may use, globally and per-head, from the dashboard Settings and head-management UI, with per-head overrides able to inherit the global default, allow all, or pick a subset. End the bullet with `(closes #7)`. Do NOT mention phases, requirement IDs, or .planning paths (per AGENTS.md changelog rule).</action>
  <verify>
    <automated>cd /home/thenasty/shrok/dashboard && npx tsc --noEmit && cd /home/thenasty/shrok && grep -q "closes #7" CHANGELOG.md</automated>
  </verify>
  <done>Each head card exposes per-head head-tool + agent-tool overrides with the three explicit modes wired to api.heads.setToolOverrides; CHANGELOG [0.3.0] Added documents the feature with (closes #7); dashboard tsc passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify tool-access UI end-to-end</name>
  <action>This is a human-verification checkpoint — no autonomous implementation. Present the build/run/verify steps below to the operator and pause for the resume signal.</action>
  <what-built>Settings global tool-access editors and per-head Inherit/All/Subset override controls, wired end-to-end through the 46-03 API to config.json and the enforcement points from 46-02.</what-built>
  <how-to-verify>
    1. Build the dashboard locally to verify (do NOT stage dist): `cd /home/thenasty/shrok/dashboard && npm run build` — confirm it completes with no errors. Leave dashboard/dist/ unstaged (CI is the sole writer).
    2. Run shrok against the working tree and open the dashboard. In Settings → Behavior, confirm the "Tool access" section shows both global allowlists with All/Subset modes and that the subset picker lists real tool names. Change one to a subset, Save, reload, and confirm it round-trips (this exercises the draft-layer persistence fix).
    3. In Settings → Heads, open a head card. Confirm the per-head head-tool and agent-tool controls show three distinct modes (Inherit global / All tools / Choose subset), that picking "Choose subset" reveals the tag picker, and that Save persists. Reload and confirm the chosen mode/values round-trip.
    4. Set a head's agent override to a 1-tool subset, restart, spawn an agent from that head, and confirm the agent only has that tool (cross-checks 46-02 enforcement).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any UI/wiring issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard UI → settings/heads API | operator input shaped by the UI; the API (46-03) is the validating boundary, the UI only constrains shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-T | Tampering | UI-submitted override values | mitigate | The UI emits only the three valid shapes (sentinel/null/array); the 46-03 API re-validates server-side, so a crafted request cannot bypass the UI constraints. |
| T-46-04-I | Information disclosure | tool name display | accept | Tool names only, behind the authenticated dashboard. No secrets. |
| T-46-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses existing react-query, @testing-library, vitest. |
</threat_model>

<verification>
- `cd /home/thenasty/shrok/dashboard && npx tsc --noEmit` passes.
- `npx vitest run src/pages/settings/ToolOverrideControl.test.tsx` green.
- `cd /home/thenasty/shrok/dashboard && npm run build` completes; dashboard/dist/ left UNSTAGED.
- `grep -q "closes #7" CHANGELOG.md`.
</verification>

<success_criteria>
- Global head + agent allowlists editable on the Settings page from /api/tools (TOOLCFG-08), persisting through the draft layer to the PUT (not a silent no-op).
- Per-head overrides editable with explicit Inherit/All/Subset modes, visually distinct (TOOLCFG-09).
- TagSelect shared (no regression to the kind editor's triggerTools picker).
- CHANGELOG documents the feature, closes #7.
- Dashboard tsc clean; `npm run build` succeeds; dashboard/dist NOT staged; src/icw untouched; no git tag.
</success_criteria>

<output>
Create `.planning/phases/46-tool-access-control/46-04-SUMMARY.md` when done. Record: the shared TagSelect path, the ToolOverrideControl component/helper names, the draft.tsx fields/diff approach used for the global defaults, where the global editors and per-head controls live, and confirmation the CHANGELOG entry closes #7 with no internal-planning references.
</output>
