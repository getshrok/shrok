---
phase: 54-single-source-of-truth-for-sub-agent-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sub-agents/agents.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "T1–T7 test cases exist in agents.test.ts and FAIL against current code (RED)"
    - "Existing Phase 53 tests A–D still pass unmodified (regression baseline)"
  artifacts:
    - path: "src/sub-agents/agents.test.ts"
      provides: "7 new test cases T1–T7 in a 'Phase 54: DB-sourced history' describe block"
      contains: "describe('Phase 54"
  key_links:
    - from: "src/sub-agents/agents.test.ts"
      to: "agentStore.get(id)?.history"
      via: "DB-surface assertion (not captured LLM messages)"
      pattern: "agentStore\\.get\\(.*\\)\\?\\.history"
---

<objective>
Author the 7 behavioral tests (T1–T7) from VALIDATION.md as a new `describe('Phase 54: DB-sourced history', ...)` block in `src/sub-agents/agents.test.ts`. These tests encode the phase's correctness contract and MUST be authored FIRST (RED) — they fail against the current long-lived-in-memory-array code and turn GREEN only after the Wave 2/3 refactor.

Purpose: Lock the anti-double-injection invariant, the DB-sourced resume paths, mid-loop delivery, compaction, suspend→answer→continue, and the restart-reaping regression as executable assertions BEFORE the loop is restructured. This is the safety net for the highest-risk task in the phase.
Output: 7 new test cases. RED is the expected and correct outcome of this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-CONTEXT.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-RESEARCH.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-VALIDATION.md

<interfaces>
<!-- Test helpers the executor must reuse (do NOT reinvent). Verified live src/sub-agents/agents.test.ts:328–450 -->

freshDb(): in-memory SQLite with all migrations applied (line 328)
makeLLMRouter(responses: LLMResponse[]): LLMRouter — returns responses in sequence (line 335)
makeToolCallResponse(toolName: string, input: Record<string,unknown>): LLMResponse (line 346)
makeEndTurnResponse(): LLMResponse (line 357)
makeStewardDoneResponse(): LLMResponse (line 369)
makeStewardQuestionResponse(question: string): LLMResponse (line 381)
makeRunner(llmRouter, db, overrides?): { runner, agentStore, inboxStore, queueStore, skillLoader } (line 392)
  - overrides may set { skillLoader, agentModel, stewardModel, spawnAgentStewardEnabled, unifiedLoader }
  - runner.spawn({ task, name, trigger, headId, skillName? }) → agentId
  - runner.update(agentId, message) — running/completed/suspended dispatch
  - runner.signal(agentId, answer) — resume a suspended agent
  - runner.awaitAll(timeoutMs) — drain all in-flight loops

Assertion surface (the DB, not in-memory): `agentStore.get(agentId)?.history` returns Message[]
deserialized from agent_messages. This is what proves persistence + no-duplication.

Phase 53 reference tests (the template for shape): agents.test.ts:1957–2141 (Tests A–D).
Test C (lines 2030–2075) is the closest analog for T2/T7 (suspend→signal→assert single task row).
Test B (lines 1986–2022) is the closest analog for T3 (mid-loop update, exactly-once).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author T1–T5 + T7 (DB-sourced history + anti-double-injection)</name>
  <files>src/sub-agents/agents.test.ts</files>
  <read_first>
    - src/sub-agents/agents.test.ts (lines 328–450 for helpers; 1950–2141 for the Phase 53 describe block — append the new block AFTER it)
    - .planning/phases/54-single-source-of-truth-for-sub-agent-history/54-VALIDATION.md (the T1–T7 table — match the -t test-name substrings exactly)
    - .planning/phases/54-single-source-of-truth-for-sub-agent-history/54-RESEARCH.md (Pitfalls 1, 2, 3, 5, 6 — these are the failure modes the tests must catch)
    - src/sub-agents/local.ts:239–272 (update() two paths) and :644–992 (loopIteration) — so test names/expectations match the real control flow being refactored
  </read_first>
  <action>
    Append a new `describe('Phase 54: DB-sourced history', () => { ... })` block at the END of agents.test.ts (after the Phase 53 block closes at line 2141). Author six `it(...)` cases whose names contain the EXACT substrings VALIDATION.md greps for:

    - T1 name MUST match `/resume.*emitter/` — "resume-after-idle via the live-emitter path". Spawn a manual agent that calls a tool then end_turns (so it parks waiting on inbox / completes). While parked/idle, `runner.update(agentId, 'T1-followup')`. After `awaitAll`, assert `agentStore.get(id)?.history` contains the followup exactly once AND the original task exactly once (history is DB-sourced on wake; no in-memory divergence). Drive timing with the same `setTimeout`-in-the-router pattern as Phase 53 Test B (line 1991–2000) to keep the agent in-loop when the update lands.

    - T2 name MUST match `/resume.*suspended/` — completed-agent-gets-new-work via the resumeSuspended path. Spawn, let it complete, then `runner.update(id, 'T2-new-work')` (PATH A in update(): status==='completed' → resume + resumeSuspended). Assert the original task appears exactly once in `history` (NOT re-injected by resumeSuspended) and the new-work signal is present.

    - T3 name MUST match `/mid-loop/` — mirror Phase 53 Test B structure (router: round 1 bash tool_call, round 2 end_turn; update sent ~90ms in). Assert the `[Message received: T3-payload]` row appears EXACTLY ONCE in `history` (onRoundComplete injected + persisted; the next loopIteration DB reload must not duplicate it). This is the Pitfall 2 guard.

    - T4 name MUST match `/compact/` — drive enough rounds (or a low `archivalThreshold` override if exposable, else many tool rounds) to trigger `maybeArchiveHistory`; assert that after compaction the agent does NOT see the full pre-summary history doubled on the next pass — i.e. the count of original-task rows in `history` stays 1 and a summary message is present. If a deterministic compaction trigger is impractical in-unit, instead assert the weaker-but-real invariant that the post-run `history` has no duplicated task row after a multi-round run that crossed `archivalThreshold` (document the chosen approach in the SUMMARY). Use `makeStewardDoneResponse()` for any steward call the archival path makes.

    - T5 name MUST match `/suspend.*answer/` — adapt Phase 53 Test C (lines 2030–2075): bash call → question (suspends) → steward question → resume via `runner.signal(id, 'T5-answer')` → end_turn. Assert the answer row is present with NO `injected` flag (genuine user input) and the task appears exactly once.

    - T7 name MUST match `/double.*inject/` — the anti-double-injection invariant. After a suspend→resumeSuspended cycle (reuse the Test C setup), assert `history.filter(task-row predicate)` has length EXACTLY 1. This is the canonical guard for Pitfall 1.

    For every assertion, assert against `agentStore.get(agentId)?.history` (the DB surface) using the same `m.kind === 'text' && tm.role === 'user' && tm.content.includes(...)` predicate shape used in Phase 53 tests — NOT against captured LLM router messages. Use unique sentinel strings per test (`T1-followup`, `T2-new-work`, etc.) so filters are unambiguous. Keep per-test `freshDb()` isolation. Do NOT modify any Phase 53 test or any production file.
  </action>
  <verify>
    <automated>npx vitest run src/sub-agents/agents.test.ts -t "Phase 54" 2>&1 | grep -E "resume.*emitter|resume.*suspended|mid-loop|compact|suspend.*answer|double.*inject"</automated>
  </verify>
  <acceptance_criteria>
    - All six new `it()` names exist and match the VALIDATION.md `-t` substrings: `npx vitest run src/sub-agents/agents.test.ts -t "resume.*emitter"`, `-t "resume.*suspended"`, `-t "mid-loop"`, `-t "compact"`, `-t "suspend.*answer"`, `-t "double.*inject"` each SELECT exactly one test (non-zero matched).
    - These six tests are RED now (they fail against current code) — this is expected and correct. At minimum T7/T2/T3 must demonstrably fail (current code double-injects or diverges); document which are RED vs already-passing in the SUMMARY.
    - `npx tsc --noEmit` is clean (no type errors introduced; strict config: noUncheckedIndexedAccess, exactOptionalPropertyTypes).
    - Phase 53 tests A–D still pass: `npx vitest run src/sub-agents/agents.test.ts -t "Phase 53"` exits 0.
    - No production file (`src/sub-agents/local.ts`, `src/db/agents.ts`, `src/index.ts`) is modified: `git diff --name-only` lists only `src/sub-agents/agents.test.ts`.
  </acceptance_criteria>
  <done>Six tests (T1–T5, T7) authored with VALIDATION-matching names, asserting the DB history surface, RED against current code, Phase 53 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author T6 (restart-reaping regression)</name>
  <files>src/sub-agents/agents.test.ts</files>
  <read_first>
    - src/index.ts:348–358 (the per-head orphaned-agent reaping loop — getByStatus('running') → fail + enqueue agent_failed; this MUST stay unchanged through the phase, T6 pins it)
    - src/db/agents.ts:293–300 (getByStatus) and :261–264 (fail) — the methods reaping calls
    - src/sub-agents/agents.test.ts:328–450 (helpers, esp. AgentStore/QueueStore construction in makeRunner)
  </read_first>
  <action>
    Add a seventh `it(...)` to the same `describe('Phase 54: DB-sourced history', ...)` block whose name matches `/restart.*reap/`. The test exercises the reaping CONTRACT without booting `src/index.ts`: construct an `AgentStore` + `QueueStore` over a `freshDb()`, create a `running` agent (spawn then force-set status to running via `agentStore.updateStatus`, or directly via `create` + `resume`), then replicate the index.ts:349–358 reaping sequence inline against that store — `getByStatus('running')` → for each, `fail(id, 'process restarted mid-execution', headId)` + `queueStore.enqueue` an `agent_failed` event. Assert: (1) the agent's status is now `failed` with error `'process restarted mid-execution'`; (2) an `agent_failed` queue event exists for it; (3) reaping read NO message history — i.e. it never called `getHistoryWithinBudget` / did not attempt a DB history rebuild for resume (assert structurally: reaping uses only `getByStatus` + `fail` + `enqueue`; the test documents that no history-load path is invoked on the reap branch). This pins that Phase 54's DB-sourced reload is a LIVE-session refactor only and the boot path stays terminate-on-restart.
  </action>
  <verify>
    <automated>npx vitest run src/sub-agents/agents.test.ts -t "restart.*reap"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/sub-agents/agents.test.ts -t "restart.*reap"` selects exactly one test.
    - The test PASSES now (it asserts existing reaping behavior that Phase 54 must preserve — it is GREEN from the start, serving as a frozen regression guard).
    - `npx tsc --noEmit` clean.
    - Only `src/sub-agents/agents.test.ts` modified.
  </acceptance_criteria>
  <done>T6 authored, green now, pins the unchanged restart-reaping contract (index.ts:349–358).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds test code only; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | (n/a) | test authoring | accept | Per RESEARCH Security Domain: this phase makes no changes to authentication, authorization, session handling, input validation, or cryptography. No ASVS L1 categories apply. This plan adds only Vitest test cases — zero runtime surface. No high-severity threats introduced. |

No package-manager installs occur in this plan (no `T-54-SC` applicable).
</threat_model>

<verification>
- `npx vitest run src/sub-agents/agents.test.ts -t "Phase 54"` runs all 7 new tests; T1–T5+T7 RED (expected), T6 green.
- `npx vitest run src/sub-agents/agents.test.ts -t "Phase 53"` exits 0 (regression baseline intact).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- 7 test cases (T1–T7) exist with VALIDATION-matching names.
- T6 green; T1–T5/T7 RED against current code (the RED that Waves 2–3 turn green).
- Phase 53 tests A–D unchanged and green.
- No production code touched.
</success_criteria>

<output>
Create `.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-01-SUMMARY.md` when done. Record which of T1–T5/T7 are RED vs incidentally passing, and the compaction-trigger approach chosen for T4.
</output>
