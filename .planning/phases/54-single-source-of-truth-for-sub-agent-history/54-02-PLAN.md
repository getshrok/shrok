---
phase: 54-single-source-of-truth-for-sub-agent-history
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - src/db/agents.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "AgentStore.getHistoryWithinBudget(agentId, tokenBudget) returns chronological Message[] windowed newest-first by token cost"
    - "An agent's full persisted history is retrievable in chronological order when budget is generous"
  artifacts:
    - path: "src/db/agents.ts"
      provides: "getHistoryWithinBudget method + stmtGetMessagesDesc prepared statement"
      contains: "getHistoryWithinBudget"
  key_links:
    - from: "src/db/agents.ts getHistoryWithinBudget"
      to: "estimateTokens (src/db/token.ts)"
      via: "per-message budget accounting in the reverse-iterate loop"
      pattern: "estimateTokens"
    - from: "src/db/agents.ts getHistoryWithinBudget"
      to: "agent_messages ORDER BY rowid DESC"
      via: "new stmtGetMessagesDesc prepared statement"
      pattern: "ORDER BY rowid DESC"
---

<objective>
Add a single new read method to `AgentStore`: `getHistoryWithinBudget(agentId, tokenBudget): Message[]`, mirroring `MessageStore.getRecent` (the head's per-turn assembly primitive). It fetches `agent_messages` newest-first, accumulates by token cost, and returns the selected window in chronological order. This is the DB-read primitive the Wave 3 loop refactor calls on each tool-loop pass.

Purpose: Give sub-agents the same budget-windowed DB-sourced history assembly the head already uses, so the long-lived in-memory array can be retired in Wave 3.
Output: One new prepared statement + one new method on `AgentStore`. No schema change, no behavior change to existing methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-RESEARCH.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-PATTERNS.md

<interfaces>
<!-- The exact analog to mirror. Verified live. -->

CANONICAL ANALOG — MessageStore.getRecent (src/db/messages.ts:187–199):
  getRecent(headId, tokenBudget): Message[] — newest-first SQL, reverse-iterate with
  `selected.unshift(msg)` to return chronological, break when used+cost > tokenBudget,
  cost via estimateTokens([msg]).

EXISTING in src/db/agents.ts (verified live):
  - Imports (lines 1–5): DatabaseSync/StatementSync/transaction from './index.js';
    Message, TextMessage from '../types/core.js'. estimateTokens is NOT yet imported here.
  - stmtGetMessages (lines 148–150): 'SELECT data FROM agent_messages WHERE agent_id = ? ORDER BY rowid ASC'
  - Deserialization idiom (line 224, 296–297, 310–311):
      const msgRows = this.stmtGetMessages.all(id) as unknown as { data: string }[]
      const history: Message[] = msgRows.map(r => JSON.parse(r.data) as Message)
  - rowid is the chronological-order invariant for agent_messages (no created_at column).
  - estimateTokens lives in src/db/token.ts (already imported by src/db/messages.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stmtGetMessagesDesc + getHistoryWithinBudget to AgentStore</name>
  <files>src/db/agents.ts</files>
  <read_first>
    - src/db/agents.ts (lines 1–5 imports; 144–150 prepared-statement block; 220–226 get(); 293–314 getByStatus/getRecent deserialization idiom — current state before edit)
    - src/db/messages.ts:187–199 (MessageStore.getRecent — the EXACT pattern to copy)
    - src/db/token.ts (estimateTokens signature — takes Message[] , returns number)
    - .planning/phases/54-single-source-of-truth-for-sub-agent-history/54-PATTERNS.md (the "New method: getHistoryWithinBudget" section — the verified target shape)
    - .planning/phases/54-single-source-of-truth-for-sub-agent-history/54-RESEARCH.md (Anti-Pattern: "Reusing stmtGetMessages (rowid ASC) for budget windowing" — must add the DESC variant, not reverse the ASC results)
  </read_first>
  <behavior>
    - Generous budget (e.g. 1_000_000): returns ALL of an agent's persisted messages in chronological (insertion/rowid ASC) order — identical sequence to `get(id).history`.
    - Tight budget: returns only the most-recent suffix that fits, still in chronological order (oldest-of-the-window first).
    - Zero / empty history: returns [].
    - Output order invariant: for any budget, the returned array is chronological (the newest message is LAST), achieved via newest-first query + `unshift`.
  </behavior>
  <action>
    Two edits in `src/db/agents.ts`, no signature changes elsewhere:

    1. Import `estimateTokens` from `./token.js` at the top of the file (mirror how `src/db/messages.ts` imports it). Respect `moduleResolution: bundler` — use the `.js` extension.

    2. In the constructor's prepared-statement block (immediately after `stmtGetMessages` at lines 148–150), add a sibling DESC statement assigned to a new private field `stmtGetMessagesDesc`:
       `'SELECT data FROM agent_messages WHERE agent_id = ? ORDER BY rowid DESC'`.
       Declare the field on the class alongside the other `stmt*: StatementSync` declarations.

    3. Add the method `getHistoryWithinBudget(agentId: string, tokenBudget: number): Message[]` near the other read methods (after `getRecent` ~line 314 is a natural home). Copy `MessageStore.getRecent` semantics exactly: run `stmtGetMessagesDesc.all(agentId) as unknown as { data: string }[]`; iterate newest-first; for each row `JSON.parse(row.data) as Message`; `const cost = estimateTokens([msg])`; if `used + cost > tokenBudget` `break`; else `selected.unshift(msg)` and `used += cost`; return `selected`. Use the `JSON.parse(row.data) as Message` deserialization idiom already used at line 224 (NOT a column-per-field mapping — agent_messages stores a JSON blob). Add a JSDoc one-liner mirroring getRecent's.

    Do NOT touch `updateWorkStart`, `stmtGetMessages`, `compactHistory`, or any other method — those changes (retirement) happen in Wave 3 against `local.ts`, and the `work_start` column / `updateWorkStart` method deliberately stay as inert schema-compat surface.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/sub-agents/agents.test.ts -t "Phase 53"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "getHistoryWithinBudget" src/db/agents.ts` returns the method; `grep -n "ORDER BY rowid DESC" src/db/agents.ts` returns the new statement; `grep -n "estimateTokens" src/db/agents.ts` returns the import + the per-message call.
    - Behavior assertion (round-trip): with a generous budget, `getHistoryWithinBudget(id, 1_000_000)` returns the same ordered sequence as `get(id).history` for an agent with persisted messages (verify by adding a focused micro-assertion in an existing test scratch run, or confirm via the Wave 3 tests turning green — at minimum confirm chronological order: last element is the most-recently-appended message).
    - `npx tsc --noEmit` clean under strict config (noUncheckedIndexedAccess: index results are null-checked; exactOptionalPropertyTypes respected).
    - Phase 53 tests A–D still green: `npx vitest run src/sub-agents/agents.test.ts -t "Phase 53"` exits 0.
    - Only `src/db/agents.ts` modified.
  </acceptance_criteria>
  <done>getHistoryWithinBudget + stmtGetMessagesDesc added, mirroring MessageStore.getRecent; tsc clean; Phase 53 green; no other method changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | A new read-only SQLite query over the existing `agent_messages` table within the same process; no new trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02 | (n/a) | getHistoryWithinBudget read method | accept | Per RESEARCH Security Domain: no auth/authz/session/validation/crypto change. The new method is a parameterized read (`agent_id = ?` bound, never string-concatenated) over data this process already owns and already reads via `get()`/`getRecent()`. No ASVS L1 category applies; no high-severity threat introduced. |

No package-manager installs occur (no `T-54-SC` applicable).
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/sub-agents/agents.test.ts -t "Phase 53"` exits 0 (existing DB-history assertions unaffected).
- New method present and parameterized (DESC statement, estimateTokens accounting).
</verification>

<success_criteria>
- `AgentStore.getHistoryWithinBudget` exists, mirrors `MessageStore.getRecent`, returns chronological budget-windowed history.
- Companion `stmtGetMessagesDesc` (rowid DESC) added; ASC statement untouched.
- No schema change; `updateWorkStart`/`work_start` column untouched.
- tsc clean; Phase 53 regression green.
</success_criteria>

<output>
Create `.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-02-SUMMARY.md` when done.
</output>
