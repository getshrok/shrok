---
phase: 54-single-source-of-truth-for-sub-agent-history
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sub-agents/local.ts
  - src/system.ts
  - src/sub-agents/agents.test.ts
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Sub-agent windowing reads on a context-window-class historyBudget that is STRICTLY GREATER than archivalThreshold, so windowed history can exceed archivalThreshold."
    - "maybeArchiveHistory can fire: when DB history exceeds archivalThreshold, compaction runs, a kind:'summary' message is persisted via compactHistory, and the next DB reload includes that summary."
    - "T4 proves compaction fired by asserting a kind:'summary' message exists in the rebuilt DB history (NOT content.includes against the agent's response text)."
    - "T1-T3, T5-T7 and Phase 53 regression tests remain green; no test is weakened."
  artifacts:
    - path: "src/sub-agents/local.ts"
      provides: "loopIteration windows on this.historyBudget (context-window-class), not this.archivalThreshold; maybeArchiveHistory still fires at archivalThreshold"
      contains: "this.historyBudget"
    - path: "src/system.ts"
      provides: "Threads a historyBudget derived from contextWindowTokens into LocalAgentRunner"
      contains: "historyBudget"
    - path: "src/sub-agents/agents.test.ts"
      provides: "T4 asserts kind:'summary' produced by compaction; budgets set so historyBudget > archivalThreshold and seeded history exceeds archivalThreshold"
      contains: "kind === 'summary'"
  key_links:
    - from: "src/sub-agents/local.ts:766 windowing call"
      to: "this.historyBudget"
      via: "getHistoryWithinBudget(agentId, this.historyBudget)"
      pattern: "getHistoryWithinBudget\\(agentId, this\\.historyBudget\\)"
    - from: "src/sub-agents/local.ts maybeArchiveHistory call"
      to: "this.archivalThreshold"
      via: "archivalThreshold: this.archivalThreshold"
      pattern: "archivalThreshold: this\\.archivalThreshold"
---

<objective>
Close the single blocking gap from 54-VERIFICATION.md (BL-01): sub-agent steward compaction is permanently dead code because the loop windows history with `getHistoryWithinBudget(agentId, this.archivalThreshold)` at `local.ts:766` while `maybeArchiveHistory` (line ~820) early-returns whenever `estimateTokens(history) <= archivalThreshold`. Passing the same constant at both sites makes `maybeArchiveHistory` always early-return, so history beyond `archivalThreshold` (120k default) is hard-dropped with no summary — a behavior regression vs pre-Phase-54.

Implement the LOCKED operator decision (Option A1 — fix the budget, KEEP compaction): window on a separate context-window-class `historyBudget` that is STRICTLY GREATER than `archivalThreshold`, exactly mirroring the head (`assembler.ts:163-166`). Keep `maybeArchiveHistory` firing at `archivalThreshold`. The invariant `historyBudget > archivalThreshold` makes windowed history able to exceed `archivalThreshold`, which re-enables compaction → summary persisted via `compactHistory` → subsequent DB reads include the summary.

Then fix T4 so it actually verifies compaction fired by asserting a real `kind:'summary'` message exists in the rebuilt DB history, instead of `content.includes()` against the agent's own mock response.

Purpose: restore the steward-summarization path for long-running sub-agents and make T4 a genuine regression guard for compaction.
Output: a windowed-on-historyBudget loop with live compaction, and a non-false-passing T4.

Scope guard: this gap plan addresses BL-01 (compaction) + T4 ONLY. The reviewer WARNINGS WR-01 (skill-memory in-memory loss), WR-02 (cross-provider orphan tool_result), WR-03 (resume race) are explicitly OUT OF SCOPE — they are tracked as follow-ups in 54-REVIEW.md. Do NOT touch them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-VERIFICATION.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-CONTEXT.md
@.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-VALIDATION.md
@./AGENTS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

From src/sub-agents/local.ts (LocalAgentRunnerOptions, lines ~77-90 + constructor ~156-170):
```
pollIntervalMs?: number
checkStatusTimeoutMs?: number
archivalThreshold?: number          // default 120_000 (constructor line 158)
toolOutputMaxChars?: number
// historyBudget?: number          // <-- ADD THIS new optional opt
```
Constructor currently sets: `this.archivalThreshold = opts.archivalThreshold ?? 120_000`

From src/sub-agents/local.ts:766 (the windowing call to fix):
```
const history = this.agentStore.getHistoryWithinBudget(agentId, this.archivalThreshold)
```

From src/sub-agents/local.ts:820 (the compaction call — LEAVE archivalThreshold here):
```
await maybeArchiveHistory(agentId, history, {
  llmRouter: this.llmRouter,
  usageStore: this.usageStore,
  stewardModel: this.stewardModel,
  archivalThreshold: this.archivalThreshold,
  agentStore: this.agentStore,
  agentId,
})
```

From src/db/agents.ts:323 — windowing returns history with estimateTokens(result) <= tokenBudget:
```
getHistoryWithinBudget(agentId: string, tokenBudget: number): Message[]
```

From src/db/agents.ts:356 — compaction persists the summary into the DB (so agentStore.get(id).history will contain it):
```
compactHistory(agentId: string, deleteBeforeId: string, summaryMsg: Message | null): void
```

From src/sub-agents/archival.ts:24-104 — maybeArchiveHistory:
  - early-returns if `estimateTokens(history) <= deps.archivalThreshold`
  - `cutoff = Math.floor(history.length * 0.3)`; returns if `cutoff < 1` (so history needs >= 4 messages)
  - on success: calls `deps.llmRouter.complete(deps.stewardModel, [...], [], {...})` for the summary,
    then `agentStore.compactHistory(agentId, lastCompactedId, summaryMsg)` where summaryMsg.kind === 'summary'
  - mutates `history` in place via splice

From src/head/assembler.ts:163-166 — THE PATTERN TO MIRROR:
```
const remaining = Math.max(0, this.config.contextWindowTokens - baseSystemTokens - outputReserve)
const memoryFraction = (this.config.memoryBudgetPercent ?? 30) / 100
const memoryBudget = Math.floor(remaining * memoryFraction)
const historyBudget = remaining - memoryBudget
```

From src/system.ts:377 — how the runner already derives archivalThreshold from contextWindowTokens:
```
archivalThreshold: Math.floor(config.contextWindowTokens * config.archivalThresholdFraction),
```
(config.archivalThresholdFraction default 0.80 — src/config.ts:206; config.contextWindowTokens default 40_000 — src/config.ts:204)

From src/sub-agents/agents.test.ts:392-451 — makeRunner signature/overrides (extend overrides to accept historyBudget):
```
overrides: { spawnAgentStewardEnabled?: ...; archivalThreshold?: number } = {}
...
...(overrides.archivalThreshold !== undefined ? { archivalThreshold: overrides.archivalThreshold } : {}),
```

From src/sub-agents/agents.test.ts:369-378 — makeStewardDoneResponse() returns `{"type":"done"}`; the steward summary call in archival.ts returns plain summary text (mock its `content` to a sentinel).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decouple windowing budget from compaction trigger (mirror the head's historyBudget)</name>
  <files>src/sub-agents/local.ts, src/system.ts</files>
  <read_first>
    - src/sub-agents/local.ts (LocalAgentRunnerOptions lines ~77-90; constructor ~156-170; windowing call line 766; maybeArchiveHistory call ~820)
    - src/head/assembler.ts (lines 163-166 — the historyBudget derivation to mirror)
    - src/system.ts (lines ~340-384 — where LocalAgentRunner is constructed with archivalThreshold)
    - src/config.ts (lines 204, 206 — contextWindowTokens default 40_000; archivalThresholdFraction default 0.80)
  </read_first>
  <behavior>
    - After the change, `getHistoryWithinBudget` at the loop's DB-reload site is called with a context-window-class budget (`this.historyBudget`), NOT `this.archivalThreshold`.
    - `this.historyBudget > this.archivalThreshold` always holds: the runner derives historyBudget from contextWindowTokens (fraction 1.0 of the window for sub-agents) while archivalThreshold is contextWindowTokens * 0.80, so the window strictly exceeds the trigger.
    - `maybeArchiveHistory` still receives `archivalThreshold: this.archivalThreshold` (unchanged), so it fires when windowed history exceeds archivalThreshold.
    - `npx tsc --noEmit` is clean (respect noUncheckedIndexedAccess + exactOptionalPropertyTypes: use the `...(opts.historyBudget !== undefined ? { ... } : {})` spread idiom or a plain `?? default`, never assign `undefined` to an optional).
  </behavior>
  <action>
Add a new optional `historyBudget?: number` field to the `LocalAgentRunnerOptions` interface in src/sub-agents/local.ts (next to `archivalThreshold?: number`, ~line 79), with a JSDoc comment: "Context-window-class budget for DB history windowing at loop entry (mirrors the head's assembler historyBudget). MUST be strictly greater than archivalThreshold so windowed history can exceed the compaction trigger and maybeArchiveHistory stays live." Add a corresponding `private historyBudget: number` field to the class (next to `private archivalThreshold: number`, ~line 116).

In the constructor (~line 158, right after the `this.archivalThreshold = opts.archivalThreshold ?? 120_000` line), set:
`this.historyBudget = opts.historyBudget ?? Math.max(this.archivalThreshold * 2, 200_000)`
This default guarantees the INVARIANT `historyBudget > archivalThreshold` even when a caller passes only `archivalThreshold` (the test path) — `archivalThreshold * 2` is strictly greater than `archivalThreshold` for any positive threshold, and the 200_000 floor keeps real runs windowing on a generous window. Add an inline assertion immediately after the assignment that fails loud on misconfiguration:
`if (this.historyBudget <= this.archivalThreshold) throw new Error(`historyBudget (${this.historyBudget}) must exceed archivalThreshold (${this.archivalThreshold}) — otherwise compaction is dead code`)`. Place the assertion AFTER both `this.archivalThreshold` and `this.historyBudget` are set.

Change the windowing call at line 766 from `getHistoryWithinBudget(agentId, this.archivalThreshold)` to `getHistoryWithinBudget(agentId, this.historyBudget)`. Update the adjacent comment to note the budget is the context-window-class historyBudget (mirroring the head), distinct from archivalThreshold which is the compaction trigger.

Leave the `maybeArchiveHistory` call (~line 820) UNCHANGED — it must keep passing `archivalThreshold: this.archivalThreshold`. Confirm ordering is preserved: window-load (line 766) happens THEN maybeArchiveHistory (~820) each pass, so windowed history (up to historyBudget) is what gets handed to the compaction check.

In src/system.ts, where the LocalAgentRunner is constructed (~line 377, the `archivalThreshold: Math.floor(...)` line), add a sibling `historyBudget:` field derived from contextWindowTokens, mirroring assembler.ts but allocating the full window to history for sub-agents (sub-agents have no separate memory budget at the loop boundary): `historyBudget: config.contextWindowTokens`. This is strictly greater than `archivalThreshold` (= contextWindowTokens * 0.80) for the default 0.80 fraction. (If archivalThresholdFraction were ever configured to >= 1.0, the constructor assertion above is the backstop.)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -n "getHistoryWithinBudget(agentId, this.historyBudget)" src/sub-agents/local.ts` returns the windowing call (line ~766) — windowing now uses historyBudget.
    - `grep -c "getHistoryWithinBudget(agentId, this.archivalThreshold)" src/sub-agents/local.ts` returns 0 — the old archivalThreshold windowing is gone.
    - `grep -n "archivalThreshold: this.archivalThreshold" src/sub-agents/local.ts` still returns the maybeArchiveHistory call — compaction trigger unchanged.
    - `grep -n "private historyBudget" src/sub-agents/local.ts` returns the new class field; `grep -n "historyBudget?: number" src/sub-agents/local.ts` returns the new option.
    - `grep -n "historyBudget:" src/system.ts` returns the new construction field.
    - The constructor contains a throw guarding `this.historyBudget <= this.archivalThreshold` (verify by reading the constructor region).
  </acceptance_criteria>
  <done>Sub-agent loop windows on a context-window-class historyBudget strictly greater than archivalThreshold; maybeArchiveHistory still fires at archivalThreshold; tsc clean; the invariant is enforced by a constructor assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix T4 to assert real kind:'summary' compaction (non-false-passing)</name>
  <files>src/sub-agents/agents.test.ts</files>
  <read_first>
    - src/sub-agents/agents.test.ts (T4 at line ~2312; makeRunner at 392-451; makeStewardDoneResponse 369-378; makeToolCallResponse / makeEndTurnResponse / makeLLMRouter helpers near the top of the file)
    - src/sub-agents/archival.ts (full file — confirms compaction needs estimateTokens(history) > archivalThreshold AND history.length >= 4 for cutoff >= 1; the steward summary call is `llmRouter.complete(stewardModel, ...)`; success persists a kind:'summary' via compactHistory)
    - src/db/agents.ts (getHistoryWithinBudget 323-335; appendMessages 343-347; compactHistory 356-376; get(id) returns history from agent_messages)
    - src/db/token.ts (estimateTokens — to size seeded messages above archivalThreshold)
  </read_first>
  <behavior>
    - T4 sets budgets so `historyBudget > archivalThreshold` AND drives DB history above `archivalThreshold` while staying within `historyBudget` (so the windowed history that reaches maybeArchiveHistory exceeds the trigger).
    - The seeded/accumulated DB history has >= 4 messages (so archival's `cutoff = floor(length*0.3) >= 1`).
    - The steward summary LLM call is mocked to return a sentinel summary string; archival persists it as a `kind:'summary'` message via compactHistory.
    - T4's PASS assertion is `expect(history.some(m => m.kind === 'summary')).toBe(true)` — it asserts a genuine summary message exists, NOT `content.includes(summaryContent)` against an assistant text turn.
    - T4 still also asserts no duplication regression (task message appears at most once) — keep that anti-regression check.
    - `npx vitest run src/sub-agents/agents.test.ts` exits 0 with all Phase 54 (T1-T7) and Phase 53 tests green.
  </behavior>
  <action>
Rewrite the T4 test body (the `it('compact: ...')` block at ~line 2312) so it genuinely exercises compaction:

1. Set budgets via makeRunner overrides so the invariant holds and compaction can fire: pass `{ archivalThreshold: 30, historyBudget: 200_000 }` (extend `makeRunner`'s `overrides` type and its spread to thread `historyBudget?: number` the same way `archivalThreshold` is threaded at line 447 — add `historyBudget?: number` to the overrides object type at line 395 and add `...(overrides.historyBudget !== undefined ? { historyBudget: overrides.historyBudget } : {})` after the archivalThreshold spread). `historyBudget: 200_000 > archivalThreshold: 30` satisfies the constructor assertion.

2. Seed the agent's DB history ABOVE the archivalThreshold and with >= 4 messages BEFORE the loop runs the compaction pass. Two acceptable mechanisms — pick the one that fits the existing scaffolding cleanly:
   (a) Spawn the agent, then immediately `agentStore.appendMessages(agentId, [...])` several `kind:'text'` user/assistant messages whose combined `estimateTokens` clearly exceeds 30 (e.g. 5-6 messages of a sentence each ~> 40 tokens total), so when the loop reloads on its first pass the windowed history (budget 200_000 includes all of them) exceeds archivalThreshold and maybeArchiveHistory fires. OR
   (b) Drive enough real tool-call rounds (makeToolCallResponse → tool_call + tool_result messages persisted by runToolLoop) before an end_turn so the accumulated DB history exceeds 30 tokens with >= 4 rows.
   Prefer (a) for determinism — seed via appendMessages right after spawn, before awaitAll. If seeding after spawn races the loop, seed the messages with the SAME task content via the spawn task plus appendMessages such that the first loop reload sees them; document the chosen approach in a comment.

3. Mock the LLM so the STEWARD SUMMARY call returns the sentinel. The steward summary call is `llmRouter.complete(stewardModel, ...)` issued INSIDE maybeArchiveHistory before the agent's first turn. Use a call-counter or argument-based router (inspect the `_tier`/first arg) so the steward-model call returns `{ content: 'Phase54-T4-compact-summary-content', model:'test-model', inputTokens:5, outputTokens:5, stopReason:'end_turn', toolCalls:[] }`, and subsequent agent calls return makeEndTurnResponse() (and makeStewardDoneResponse() for the completion-steward classification). Distinguish the steward call by tier name when possible (stewardModel default is 'dumb'); fall back to call-order only if tier inspection is not available in the mock.

4. Replace the weak assertion. DELETE the `summaryOrNoticeRow` find that ORs `content.includes(summaryContent)`. Assert instead:
   `const history = agentStore.get(agentId)?.history ?? []`
   `expect(history.some(m => m.kind === 'summary')).toBe(true)`  // compaction actually produced a summary
   Optionally also `expect(history.find(m => m.kind === 'summary')?.content).toContain('Phase54-T4-compact-summary-content')` to bind the summary to the steward output.

5. KEEP the anti-duplication regression: `const taskRows = history.filter(...Phase54-TaskT4...)`; `expect(taskRows.length).toBeLessThanOrEqual(1)`.

Do NOT modify T1-T3, T5-T7, or any Phase 53 test. Do NOT lower any other test's strictness.
  </action>
  <verify>
    <automated>npx vitest run src/sub-agents/agents.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/sub-agents/agents.test.ts` exits 0; the Phase 54 describe block reports 7/7 and Phase 53 tests pass.
    - In src/sub-agents/agents.test.ts the T4 block contains `m.kind === 'summary'` in its primary PASS assertion.
    - `grep -n "content.includes(summaryContent)" src/sub-agents/agents.test.ts` returns 0 matches (the false-passing branch is removed).
    - T4 uses makeRunner overrides with both `archivalThreshold` (small, e.g. 30) and `historyBudget` (large, e.g. 200_000), and `makeRunner`'s overrides type + spread now thread `historyBudget`.
    - Sanity that T4 is non-false-passing: temporarily reverting Task 1 (windowing back on archivalThreshold) makes T4 FAIL — confirm by reasoning/optional local check; the final committed code has Task 1 in place so the test passes.
  </acceptance_criteria>
  <done>T4 drives DB history above archivalThreshold within the larger historyBudget window, the steward summary call is mocked, and the test asserts a genuine kind:'summary' message exists — it now fails if compaction does not fire. Full sub-agents test file is green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (type-clean under noUncheckedIndexedAccess + exactOptionalPropertyTypes).
- `npx vitest run src/sub-agents/agents.test.ts` exits 0 — Phase 54 T1-T7 and Phase 53 A-D all green.
- `grep -c "getHistoryWithinBudget(agentId, this.archivalThreshold)" src/sub-agents/local.ts` → 0 (windowing no longer uses archivalThreshold).
- `grep -n "getHistoryWithinBudget(agentId, this.historyBudget)" src/sub-agents/local.ts` → present.
- `grep -n "archivalThreshold: this.archivalThreshold" src/sub-agents/local.ts` → present (maybeArchiveHistory trigger unchanged).
- `grep -c "content.includes(summaryContent)" src/sub-agents/agents.test.ts` → 0.
- `grep -n "m.kind === 'summary'" src/sub-agents/agents.test.ts` → present in the T4 block.
- Full suite sanity (optional, before verify-work): `npx vitest run` green.
</verification>

<success_criteria>
- BL-01 closed: sub-agent compaction/steward summarization fires when DB history exceeds archivalThreshold, because the loop now windows on a strictly-larger context-window-class historyBudget (mirroring the head). The invariant `historyBudget > archivalThreshold` is enforced by a constructor assertion and held by the system.ts wiring.
- maybeArchiveHistory and the archival machinery are KEPT (Option A1 honored — not removed).
- T4 is a genuine, non-false-passing regression guard: it asserts a real `kind:'summary'` message produced by `compactHistory`, and would fail if compaction were dead code.
- No other T1-T7 or Phase 53 test is weakened; tsc clean; sub-agents test file green.
- WR-01/WR-02/WR-03 untouched (out of scope; tracked in 54-REVIEW.md).
</success_criteria>

<output>
Create `.planning/phases/54-single-source-of-truth-for-sub-agent-history/54-04-SUMMARY.md` when done.
</output>
