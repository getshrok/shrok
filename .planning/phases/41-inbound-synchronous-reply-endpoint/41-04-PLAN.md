---
phase: 41-inbound-synchronous-reply-endpoint
plan: 04
type: execute
wave: 3
depends_on: ["41-03"]
files_modified:
  - src/dashboard/server.ts
  - src/index.ts
  - docs/internals/channel-integrations.md
autonomous: true
requirements: [HACV-01, HACV-06]
must_haves:
  truths:
    - "The /v1/* path is excluded from the dashboard CSRF/same-origin middleware so HA's cross-origin POST is not rejected (HACV-06)"
    - "The /v1 router is mounted on the existing dashboard Express server (port 8888) before the SPA catch-all, driven by the configured home-assistant adapter(s)"
    - "index.ts collects the home-assistant adapter(s) and passes them to DashboardServer via homeAssistantAdapters"
    - "The Apache <Location \"/v1/\"> AuthType None / Require all granted snippet is captured in-repo for HADOC-01 (NOT applied to the live vhost)"
  artifacts:
    - path: "src/dashboard/server.ts"
      provides: "CSRF /v1/* exclusion, homeAssistantAdapters option, /v1 router mount before SPA fallback"
      contains: "homeAssistantAdapters"
    - path: "src/index.ts"
      provides: "haAdapters collection + DashboardServer wiring"
      contains: "homeAssistantAdapters"
    - path: "docs/internals/channel-integrations.md"
      provides: "Captured Apache /v1 auth-bypass snippet for HADOC-01"
      contains: "AuthType None"
  key_links:
    - from: "src/index.ts"
      to: "src/dashboard/server.ts"
      via: "homeAssistantAdapters: haAdapters in the DashboardServer constructor options"
      pattern: "homeAssistantAdapters"
    - from: "src/dashboard/server.ts"
      to: "src/channels/home-assistant/router.ts"
      via: "app.use('/v1', createHomeAssistantRouter(haAdapter, haInboundApiKey))"
      pattern: "createHomeAssistantRouter"
---

<objective>
Wire the inbound endpoint into the running process: exclude `/v1/*` from the dashboard CSRF/same-origin middleware (HACV-06), add a `homeAssistantAdapters?` option to `DashboardServerOptions`, mount the `/v1` router (from Plan 03) before the SPA catch-all, and collect the HA adapter(s) in `index.ts` to pass them through. Also capture the Apache `/v1` auth-bypass snippet in-repo for HADOC-01 — recorded only, NOT applied to the live vhost (D-03; the live edit + `curl -v` verification are Phase 43).

Purpose: Connects Plan 03's router to the live dashboard Express server so a configured `home-assistant` channel exposes a working `/v1/chat/completions`, and records (without applying) the Apache bypass so the Phase-43 setup guide has the exact block.
Output: `server.ts` (3 edits), `index.ts` (2 edits), `channel-integrations.md` (new captured-snippet section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-inbound-synchronous-reply-endpoint/41-CONTEXT.md
@.planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md

<interfaces>
<!-- Contract this plan CONSUMES (from Plan 03): -->
From src/channels/home-assistant/router.ts:
  createHomeAssistantRouter(adapter: HomeAssistantChannelAdapter, inboundApiKey: string): Router

<!-- Existing contracts this plan extends (verified line anchors): -->
From src/dashboard/server.ts:
  (52-101)  interface DashboardServerOptions { ... dashboardAdapters: Map<...>; schedules?; ... resolveCurrentHeads?: () => ResolvedHead[] }
  (145-149) CSRF block: app.use((req,res,next)=>{ if(['GET','HEAD','OPTIONS'].includes(req.method)) return next(); requireSameOrigin(req,res,next) })
  (271-281) SPA fallback: app.use(express.static(distPath)); app.get('/api/*', 404 json); app.get('*', sendFile index.html)
From src/index.ts:
  (72)      import { HomeAssistantChannelAdapter } from './channels/home-assistant/adapter.js'  (already present)
  (234)     const dashboardAdapters = new Map<string, DashboardChannelAdapter>()  (collection-before-loop analogue)
  (324-326) } else if (ch.vendor === 'home-assistant') { const ha = new HomeAssistantChannelAdapter(ch.id, head.id); adapter = ha }
  (417-448) new DashboardServer({ ..., dashboardAdapters, schedules, ... })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: server.ts — CSRF /v1/* exclusion + homeAssistantAdapters option + /v1 router mount</name>
  <files>src/dashboard/server.ts</files>
  <read_first>
    - src/dashboard/server.ts (lines 1-50 imports; 52-101 DashboardServerOptions; 142 global express.json; 145-149 CSRF block; 195-269 router mounts; 271-281 SPA static + catch-all)
    - src/channels/home-assistant/router.ts (Plan 03 — `createHomeAssistantRouter` factory signature to import)
    - src/channels/home-assistant/adapter.ts (Plan 02 — the `HomeAssistantChannelAdapter` type for the option field)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md (`src/dashboard/server.ts` section, lines 377-443 — exact CSRF guard edit, options-field placement, mount-before-SPA placement; SPA catch-all must stay after the mount)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-RESEARCH.md (Pattern 2 CSRF exclusion; Pattern 6 options + mount; Pitfall 3 guard-order; the "mount before SPA fallback" anti-pattern note)
  </read_first>
  <action>
    Edit 1 — CSRF exclusion (HACV-06): in the middleware at lines 145-149, add `if (req.path.startsWith('/v1/')) return next()` as the SECOND early-return, placed BEFORE the `requireSameOrigin(req, res, next)` call (Pitfall 3 — order matters). Comment it: HA presents a bearer token; the `/v1` router validates auth itself. The method/GET-HEAD-OPTIONS early-return stays first; the `/v1/` path guard is second; `requireSameOrigin` last.

    Edit 2 — options field: add an optional field to `DashboardServerOptions` (after the existing optional fields, e.g. near `resolveCurrentHeads?`): `homeAssistantAdapters?: HomeAssistantChannelAdapter[]`. Add the import `import type { HomeAssistantChannelAdapter } from '../channels/home-assistant/adapter.js'` and the value import `import { createHomeAssistantRouter } from '../channels/home-assistant/router.js'` to the import block (note `../channels/...` path from `src/dashboard/`).

    Edit 3 — router mount: inside `DashboardServer.start()`, mount `/v1` AFTER the last `/api/...` route mount and BEFORE the `distPath` static block (line ~271, before `app.use(express.static(distPath))` and the `app.get('*', ...)` SPA fallback). Guard it: `if (this.opts.homeAssistantAdapters?.length) { const haInboundApiKey = process.env['HA_INBOUND_API_KEY'] ?? ''; for (const haAdapter of this.opts.homeAssistantAdapters) { app.use('/v1', createHomeAssistantRouter(haAdapter, haInboundApiKey)) } }`. Read the key at `start()` call time, not module-eval time (Pitfall 4). Do NOT mount it after the SPA `app.get('*', ...)` — that catch-all would intercept `/v1/chat/completions` (Express mount order). The `express.json({limit:'50mb'})` at line 142 already parses the body — the router relies on it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "src/dashboard/server.ts" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "req.path.startsWith('/v1/')" src/dashboard/server.ts` returns 1, and reading lines 145-150 confirms the `/v1/` guard is BEFORE the `requireSameOrigin` call (Pitfall 3).
    - `grep -c "homeAssistantAdapters" src/dashboard/server.ts` returns at least 2 (the option field + the mount-loop reference).
    - `grep -c "createHomeAssistantRouter" src/dashboard/server.ts` returns at least 2 (import + call).
    - The `app.use('/v1', ...)` mount appears BEFORE `app.use(express.static(distPath))` / `app.get('*', ...)` in source order (read the relevant span to confirm).
    - `npx tsc --noEmit` reports no errors in `src/dashboard/server.ts`.
  </acceptance_criteria>
  <done>/v1/* is CSRF-excluded before requireSameOrigin; DashboardServerOptions carries homeAssistantAdapters?; the /v1 router mounts before the SPA fallback; tsc-clean.</done>
</task>

<task type="auto">
  <name>Task 2: index.ts wiring + capture the Apache /v1 auth-bypass snippet for HADOC-01</name>
  <files>src/index.ts, docs/internals/channel-integrations.md</files>
  <read_first>
    - src/index.ts (line 72 existing import; 234 dashboardAdapters Map-before-loop; 324-326 the home-assistant build branch; 417-448 the DashboardServer constructor options object)
    - src/dashboard/server.ts (the version produced by Task 1 — the `homeAssistantAdapters?` option it now accepts)
    - docs/internals/channel-integrations.md (the existing per-channel structure — six adapters; add a new top-level `## Home Assistant` section consistent with the others)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md (`src/index.ts` section, lines 476-517 — haAdapters array-before-loop, push in the HA branch, constructor wiring)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-CONTEXT.md (D-03 — the captured Apache snippet is RECORDED only, NOT applied; placed before the catch-all basic-auth block in gigaashley-le-ssl.conf in Phase 43)
  </read_first>
  <action>
    index.ts Edit 1 — collection array: before the per-head channel build loop (near line 234 where `dashboardAdapters` is declared), add `const haAdapters: HomeAssistantChannelAdapter[] = []`. Single-instance per Phase-40 D-04, but typed as an array to match the `homeAssistantAdapters?: HomeAssistantChannelAdapter[]` option shape.

    index.ts Edit 2 — populate + wire: in the `else if (ch.vendor === 'home-assistant')` branch (lines 324-326), after `const ha = new HomeAssistantChannelAdapter(ch.id, head.id)` and `adapter = ha`, add `haAdapters.push(ha)`. Then in the `new DashboardServer({ ... })` options object (lines 417-448), add `homeAssistantAdapters: haAdapters,` next to the existing `dashboardAdapters,` field. The import at line 72 already exists — reuse it. Note: the HA adapter constructor now fails fast on a missing `HA_INBOUND_API_KEY` (Plan 02 D-02), so a configured `home-assistant` channel without the env key throws here at boot, which is the intended fail-fast.

    docs Edit — capture the Apache snippet (D-03, for HADOC-01): add a `## Home Assistant` section to `docs/internals/channel-integrations.md` mirroring the existing per-channel structure. Document: the `home-assistant` vendor exposes an inbound `POST /v1/chat/completions` on the dashboard Express server (port 8888); auth is the dedicated `HA_INBOUND_API_KEY` bearer (distinct from `HA_ACCESS_TOKEN`); HA's full `messages[]` is reduced to the last user turn; replies are held up to ~3s and lapse to the Phase-42 announce path otherwise. Then include a fenced Apache config block captured for the Phase-43 vhost edit (RECORDED, NOT APPLIED): a `<Location "/v1/">` block with `AuthType None` and `Require all granted`, with a note that it MUST be placed BEFORE the catch-all basic-auth block in `gigaashley-le-ssl.conf` so HA's Bearer token reaches Shrok instead of hitting Apache's Basic 401. Add an explicit note: this snippet is documentation for Phase 43; Phase 41 does not edit any live vhost.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "src/index.ts" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "haAdapters" src/index.ts` returns at least 3 (declaration, push, constructor wiring).
    - `grep -c "homeAssistantAdapters: haAdapters" src/index.ts` returns 1.
    - `grep -c "AuthType None" docs/internals/channel-integrations.md` returns 1, and `grep -c "Require all granted" docs/internals/channel-integrations.md` returns 1.
    - `grep -ci "not applied\|do not edit\|Phase 43" docs/internals/channel-integrations.md` returns at least 1 (the captured-only note).
    - `grep -c "HA_INBOUND_API_KEY" docs/internals/channel-integrations.md` returns at least 1.
    - `npx tsc --noEmit` reports no errors in `src/index.ts`.
    - `npx vitest run` (full suite) exits 0 — no regressions from the wiring.
  </acceptance_criteria>
  <done>index.ts collects and passes home-assistant adapters to DashboardServer; the Apache /v1 auth-bypass snippet is captured in docs for HADOC-01 (recorded, not applied); full suite green; tsc-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HA (cross-origin) → dashboard Express middleware chain | The CSRF/same-origin middleware sits in front of all routes; excluding `/v1/*` shifts authn responsibility to the router's bearer check |
| Apache front door → Shrok :8888 | Apache proxies :8888; the captured (unapplied) snippet governs whether HA's Bearer reaches Shrok vs. hitting Apache Basic auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-13 | Elevation of Privilege | CSRF exclusion scope | mitigate | The exclusion is scoped to the exact `/v1/` prefix only; all other state-changing routes still run `requireSameOrigin`; the `/v1` router enforces its own bearer authn (Plan 03), so excluding CSRF does not create an unauthenticated mutation path |
| T-41-14 | Spoofing | Apache Basic-401 confusion (HADOC) | mitigate | Captured snippet documents `AuthType None / Require all granted` placed before the catch-all basic-auth block so HA's Bearer reaches Shrok's JSON 401 rather than Apache's Basic 401; applied + verified in Phase 43 (D-03) |
| T-41-15 | Information Disclosure | inbound key at mount | mitigate | `HA_INBOUND_API_KEY` read from `process.env` at `start()` and passed to the router factory; never logged; never written to docs as a value (only the var name appears) |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; no slopcheck checkpoint required |
</threat_model>

<verification>
- `npx tsc --noEmit` reports no errors in server.ts or index.ts
- `npx vitest run` (full suite) exits 0 — wiring introduces no regressions
- The captured Apache snippet exists in docs with the not-applied note
</verification>

<success_criteria>
- `/v1/*` excluded from CSRF before requireSameOrigin; the /v1 router mounted before the SPA fallback (HACV-06)
- DashboardServerOptions.homeAssistantAdapters? carries the configured adapter(s); index.ts collects and passes them (HACV-01 end-to-end wiring)
- Apache `/v1` auth-bypass snippet captured in docs for HADOC-01, recorded not applied (D-03)
- Full test suite green; tsc-clean
</success_criteria>

<output>
Create `.planning/phases/41-inbound-synchronous-reply-endpoint/41-04-SUMMARY.md` when done
</output>
