---
phase: 41-inbound-synchronous-reply-endpoint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/channels/home-assistant/adapter.ts
  - src/channels/home-assistant/adapter.test.ts
autonomous: true
requirements: [HACV-02, HACV-03, HACV-04]
must_haves:
  truths:
    - "The adapter holds at most one pendingReply slot ({resolve, reject, timer}); setPendingReply replaces an existing slot (latest turn wins, prior timer cleared, prior promise rejected — no leak)"
    - "clearPendingReply clears the timer and nulls the slot with no dangling reference (req.on('close') / deadline path)"
    - "send() with an open slot clears the timer, resolves the slot with the reply text, and nulls the slot (exactly-once in-turn delivery)"
    - "send() with no open slot logs a warn that mentions Phase 42 and logs no token value (Phase 42 announce seam)"
    - "The constructor fails fast with a clear error when HA_INBOUND_API_KEY is absent from env (D-02 boot fail-fast)"
    - "dispatchInbound routes the extracted user turn through the registered handler with channel='home-assistant' and the conversation id in rawPayload"
  artifacts:
    - path: "src/channels/home-assistant/adapter.ts"
      provides: "pendingReply slot lifecycle (setPendingReply/clearPendingReply), upgraded send(), dispatchInbound, fail-fast constructor"
      contains: "pendingReply"
    - path: "src/channels/home-assistant/adapter.test.ts"
      provides: "Block C tests for slot lifecycle + upgraded send() + fail-fast; Block A/B preserved"
  key_links:
    - from: "src/channels/home-assistant/router.ts (Plan 03)"
      to: "src/channels/home-assistant/adapter.ts"
      via: "adapter.setPendingReply / adapter.clearPendingReply / adapter.dispatchInbound"
      pattern: "setPendingReply|clearPendingReply|dispatchInbound"
    - from: "channelRouter.send('home-assistant', text)"
      to: "adapter.send()"
      via: "resolves the held pendingReply slot if open"
      pattern: "pendingReply.*resolve"
---

<objective>
Upgrade the Phase-40 `HomeAssistantChannelAdapter` stub into the slot-owning adapter the inbound endpoint drives. Add the `pendingReply` promise slot ({resolve, reject, timer}) mirroring `VoiceChannelAdapter.activeSocket`, `setPendingReply`/`clearPendingReply` lifecycle methods, the upgraded `send()` decision branch (resolve the held HTTP slot if open, else log-and-return as the Phase-42 announce seam), a `dispatchInbound` production helper, and the D-02 boot-time fail-fast for a missing `HA_INBOUND_API_KEY`.

Purpose: The adapter owns the single-active-slot state (D-04 replace-on-concurrent) that Plan 03's router drives; send() completes the inbound contract (D-01 one-reply exactly-once); the fail-fast constructor enforces the dedicated inbound key at boot (D-02).
Output: `adapter.ts` upgraded; `adapter.test.ts` gains a Block C (slot lifecycle + send upgrade + fail-fast) while Blocks A and B are preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-inbound-synchronous-reply-endpoint/41-CONTEXT.md
@.planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES on HomeAssistantChannelAdapter (consumed by Plan 03 router): -->
interface PendingReply { resolve: (text: string) => void; reject: (err: Error) => void; timer: ReturnType<typeof setTimeout> }
setPendingReply(pending: PendingReply): void   // D-04: replaces existing slot (clear prior timer, reject prior promise)
clearPendingReply(): void                       // clears timer, nulls slot (req.on('close') / deadline)
dispatchInbound(text: string, conversationId: string): void  // routes through handler; channel='home-assistant'; rawPayload={conversationId}
async send(text: string, _attachments?: Attachment[]): Promise<void>  // resolve open slot else Phase-42 log stub

<!-- Existing contract this plan reads (the slot analogue to copy): -->
From src/channels/voice/adapter.ts:
  private activeSocket: WebSocket | null = null
  async send(text) { const ws = this.activeSocket; if (!ws || ws.readyState !== OPEN) return; ... }
  ws.on('close', () => { if (this.activeSocket === ws) this.activeSocket = null; ... })

From src/types/channel.ts:
  interface InboundMessage { channel: string; text: string; senderName?: string; attachments?: Attachment[]; rawPayload?: unknown }
  interface ChannelAdapter { id; start; stop; send; onMessage; ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade adapter.ts — pendingReply slot lifecycle, send() decision branch, dispatchInbound</name>
  <files>src/channels/home-assistant/adapter.ts</files>
  <read_first>
    - src/channels/home-assistant/adapter.ts (the full Phase-40 stub: constructor sig `(id='home-assistant', headId='default')`, `handler` field, `send()` D-05 warn, test-only `injectMessage`)
    - src/channels/voice/adapter.ts (lines 23-30 slot declaration; 71-90 `send()` decision branch; 92-113 `handleConnection` concurrent-rejection + `ws.on('close')` cleanup — the exact slot-lifecycle analogue)
    - src/types/channel.ts (lines 1-28 — `InboundMessage` + `ChannelAdapter` contracts; note `rawPayload?: unknown`)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-PATTERNS.md (`adapter.ts` section, lines 280-373 — the `PendingReply` interface, setPendingReply/clearPendingReply/send/dispatchInbound translations)
  </read_first>
  <action>
    Add an internal (non-exported) `interface PendingReply { resolve: (text: string) => void; reject: (err: Error) => void; timer: ReturnType<typeof setTimeout> }` and a `private pendingReply: PendingReply | null = null` field on `HomeAssistantChannelAdapter`. Store the resolve/reject + timer — NEVER the Express `Response` object — for testability (mirrors `VoiceChannelAdapter.activeSocket`).

    Add `setPendingReply(pending: PendingReply): void`: implement D-04 replace-on-concurrent — if `this.pendingReply` is already set, `clearTimeout(this.pendingReply.timer)` then `this.pendingReply.reject(new Error('replaced by concurrent turn'))` (so any awaiter unblocks and the stale held request is safely abandoned, no leak), then assign `this.pendingReply = pending`.

    Add `clearPendingReply(): void`: if `this.pendingReply` is set, `clearTimeout(this.pendingReply.timer)` and set `this.pendingReply = null`. Do NOT call reject here — this is the req.on('close')/deadline cleanup path where the router already handled the promise (HACV-04 / SC4: no dangling timer or reference).

    Upgrade `send(text, _attachments?)`: if `this.pendingReply !== null`, `clearTimeout(this.pendingReply.timer)`, `this.pendingReply.resolve(text)`, set `this.pendingReply = null`, and `return` (exactly-once in-turn delivery — D-01). Otherwise (`pendingReply === null`) keep the existing `log.warn` log-and-return as the Phase-42 announce seam — the warn MUST still mention `Phase 42` and MUST NOT include any token value (the existing Block A tests `warn message mentions Phase 42` and `warn message does not log any token value` must keep passing; the warn text may be reworded to reflect "no pendingReply slot open" but must retain the `Phase 42` substring). Do NOT call any HA REST in `send()` — that is strictly Phase 42.

    Add a production method `dispatchInbound(text: string, conversationId: string): void` that calls `this.handler?.({ channel: this.id, text, senderName: 'HA Voice', rawPayload: { conversationId } })`. This is the production inbound path the router uses (Plan 03); keep the existing `injectMessage` as the test-only helper (Phase-40 Block A/B tests depend on it).

    Use `.js` extensions on all intra-project imports. Honor `exactOptionalPropertyTypes` — do not set optional InboundMessage keys to `undefined` (dispatchInbound always provides `senderName` and `rawPayload`, so no conditional spread needed).
  </action>
  <verify>
    <automated>npx vitest run src/channels/home-assistant/adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setPendingReply\|clearPendingReply\|dispatchInbound" src/channels/home-assistant/adapter.ts` returns at least 3 (one definition each).
    - `grep -c "private pendingReply" src/channels/home-assistant/adapter.ts` returns 1.
    - `grep -v '^[[:space:]]*//' src/channels/home-assistant/adapter.ts | grep -c "Response" ` returns 0 (the Express Response object is never stored).
    - `grep -c "Phase 42" src/channels/home-assistant/adapter.ts` returns at least 1 (the null-slot warn retains the Phase-42 seam marker).
    - `grep -v '^[[:space:]]*//' src/channels/home-assistant/adapter.ts | grep -c "fetch(\|axios\|http\.request"` returns 0 (no HA REST call in send() — that is Phase 42).
    - Existing Block A test `warn message mentions Phase 42` and `warn message does not log any token value` still pass under `npx vitest run src/channels/home-assistant/adapter.test.ts`.
    - `npx tsc --noEmit` reports no new errors in `src/channels/home-assistant/adapter.ts`.
  </acceptance_criteria>
  <done>pendingReply slot lifecycle, upgraded send() decision branch, and dispatchInbound exist with no Express Response storage and no HA REST call; existing Block A/B tests preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-02 boot fail-fast for missing HA_INBOUND_API_KEY + Block C tests for slot lifecycle and send upgrade</name>
  <files>src/channels/home-assistant/adapter.ts, src/channels/home-assistant/adapter.test.ts</files>
  <read_first>
    - src/channels/home-assistant/adapter.ts (the version produced by Task 1 — constructor + new slot methods)
    - src/channels/home-assistant/adapter.test.ts (Block A `send() — loud-but-safe (D-05)` at lines 30-68 and Block B `SC3 lastActiveChannel routing` at 108-151 — both MUST stay green; the `vi.spyOn(console, 'warn')` pattern at lines 33-39 to reuse)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-RESEARCH.md (Pattern 7, lines 367-389 — the adapter-constructor fail-fast approach and the exact error-message intent; Pitfall 4, lines 450-454 — read the key at construction time, not module-eval time)
    - .planning/phases/41-inbound-synchronous-reply-endpoint/41-CONTEXT.md (D-02 fail-fast clause; D-04 replace-on-concurrent)
  </read_first>
  <behavior>
    - Constructor throws with a message containing `HA_INBOUND_API_KEY` when `process.env['HA_INBOUND_API_KEY']` is absent/empty (D-02).
    - Constructor succeeds when `process.env['HA_INBOUND_API_KEY']` is a non-empty string.
    - setPendingReply on an empty adapter stores the slot; a subsequent send('reply') resolves the awaiting promise with `'reply'` and leaves the slot null.
    - setPendingReply twice (concurrent turn) rejects the FIRST promise with an Error and clears the first timer; the SECOND slot remains; send('reply') resolves the second promise only (D-04 latest-wins, no leak).
    - clearPendingReply after setPendingReply clears the timer and nulls the slot; a subsequent send('x') hits the null-slot branch (warn, mentions Phase 42) and does NOT throw.
    - send() with an open slot calls clearTimeout (timer does not later fire) — assert the timer's callback never runs after a resolve (e.g. via fake timers advancing past the deadline → reject NOT called).
    - dispatchInbound('hi', 'conv-9') delivers an InboundMessage to the registered handler with `channel:'home-assistant'`, `text:'hi'`, `senderName:'HA Voice'`, `rawPayload:{conversationId:'conv-9'}`.
  </behavior>
  <action>
    In the `HomeAssistantChannelAdapter` constructor, add a boot-time fail-fast (D-02): read `const inboundKey = process.env['HA_INBOUND_API_KEY']` and `if (!inboundKey) throw new Error('[home-assistant] HA_INBOUND_API_KEY is required but missing from .env — set a bearer key that Home Assistant will present on /v1/chat/completions')`. Read the key at CONSTRUCTION time (not module-eval time) so test `.env` population and the index.ts channel-build loop both see the live value (Pitfall 4). This mirrors the Phase-40 D-01 fail-fast posture for missing tokens. Place the check at the TOP of the constructor body before assigning `this.id`/`this.headId`.

    Add a new `// ─── Block C: Phase 41 — pendingReply slot lifecycle + upgraded send + fail-fast ───` section to `src/channels/home-assistant/adapter.test.ts` covering every `<behavior>` row. Because the constructor now requires `HA_INBOUND_API_KEY`, Block C (and any test that constructs the adapter) must set `process.env['HA_INBOUND_API_KEY']` in `beforeEach` and restore in `afterEach` — also add this env setup to the EXISTING Block A and Block B describe blocks so they continue to pass (they construct the adapter without the key today). Reuse the `vi.spyOn(console, 'warn')` pattern for the null-slot send test. For the timer-cancellation behavior, use vitest fake timers (`vi.useFakeTimers()`) and assert the deadline reject does NOT fire after a resolve. Use the `noUncheckedIndexedAccess` null-check idiom for any array index in assertions.
  </action>
  <verify>
    <automated>npx vitest run src/channels/home-assistant/adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/channels/home-assistant/adapter.test.ts` exits 0 with Block A, Block B, and the new Block C all green.
    - `grep -c "HA_INBOUND_API_KEY" src/channels/home-assistant/adapter.ts` returns at least 1 (constructor fail-fast).
    - A test in Block C asserts the constructor throws when the env var is unset and the thrown message matches `/HA_INBOUND_API_KEY/`.
    - A test in Block C asserts the D-04 concurrent-replace rejects the first promise and resolves only the second.
    - `npx vitest run src/channels/home-assistant/` exits 0 (Plan 01 types tests + this plan's adapter tests together — no cross-file regressions).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Constructor fail-fast on missing HA_INBOUND_API_KEY is in place and tested; Block C pins slot lifecycle + send upgrade + D-04 replace; all prior blocks green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot env → adapter construction | The adapter must refuse to exist without the inbound bearer key configured |
| held HTTP slot → head reply | `send()` is the single point that resolves the held connection; a leaked or double-resolved slot is the risk surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Denial of Service | many held connections / leaked timers | mitigate | D-04 single-slot replacement bounds live state to ONE PendingReply per adapter; setPendingReply clears the prior timer + rejects the prior promise; clearPendingReply clears the timer and nulls the slot (tested in Block C) |
| T-41-05 | Information Disclosure | `send()` null-slot warn | mitigate | The warn logs only `text.length`, never a token or bearer value; pinned by the existing `warn message does not log any token value` test (Block A) |
| T-41-06 | Spoofing / Misconfiguration | missing inbound key at boot | mitigate | Constructor fail-fast throws if `HA_INBOUND_API_KEY` is absent (D-02) — the endpoint can never come up unauthenticated; tested in Block C |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (all deps existing); no slopcheck checkpoint required |
</threat_model>

<verification>
- `npx vitest run src/channels/home-assistant/adapter.test.ts` exits 0 (Blocks A, B, C)
- `npx vitest run src/channels/home-assistant/` exits 0 (with Plan 01 in place)
- `npx tsc --noEmit` produces no new errors attributable to this plan
</verification>

<success_criteria>
- pendingReply slot ({resolve, reject, timer}) with setPendingReply (D-04 replace) + clearPendingReply lifecycle, no Express Response stored (HACV-04 / SC4)
- send() resolves the open slot exactly-once, else logs the Phase-42 seam warn with no token leak (HACV-04 / D-01)
- dispatchInbound routes the extracted turn with channel='home-assistant' + conversation id in rawPayload (HACV-03)
- Constructor fail-fast on missing HA_INBOUND_API_KEY (HACV-02 / D-02)
- Existing Block A/B tests preserved; Block C added; tsc-clean
</success_criteria>

<output>
Create `.planning/phases/41-inbound-synchronous-reply-endpoint/41-02-SUMMARY.md` when done
</output>
