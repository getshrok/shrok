---
phase: 51-sensor-dual-sink
plan: 04
type: execute
wave: 3
depends_on: ["51-02", "51-03"]
files_modified:
  - skills/sensors/SKILL.md
  - src/sub-agents/registry.ts
  - src/dashboard/routes/sensors.ts
  - src/dashboard/routes/sensors.test.ts
autonomous: true
requirements: [SENSOR-13, SENSOR-14, SENSOR-15, SENSOR-16]
nyquist_compliant: true

must_haves:
  truths:
    - "SKILL.md teaches the JSON-object stdout contract, the two head-scoped sinks, mandatory head, and self-watermarking"
    - "The create_schedule tool description states a sensor emits a JSON payload {ambient?,event?} fired to the schedule's head"
    - "The dashboard DELETE removes ambient/<head>/<slug>.md across all head dirs (not the flat path)"
    - "The live weather sensor emits the new JSON payload and its ambient lands at ambient/ashley/weather.md (not a failure marker)"
    - "The stale flat ambient/weather.md is removed"
  artifacts:
    - path: "skills/sensors/SKILL.md"
      provides: "rewritten skill teaching the dual-sink JSON contract"
      contains: "JSON"
    - path: "src/dashboard/routes/sensors.ts"
      provides: "per-head ambient DELETE (glob across head dirs)"
      contains: "ambient"
  key_links:
    - from: "src/dashboard/routes/sensors.ts DELETE"
      to: "ambient/*/<slug>.md"
      via: "iterate head subdirs and rm <slug>.md in each"
      pattern: "readdirSync"
---

<objective>
Update the operator/agent-facing surfaces to the new contract and perform the one-time live migration:
1. Full rewrite of `skills/sensors/SKILL.md` to the JSON-object stdout contract, the two head-scoped sinks (ambient pull + sensor_event push), the mandatory target head, and the self-watermarking pattern with a worked example.
2. Update the `create_schedule` tool description (`src/sub-agents/registry.ts` kind:'script' branch) to the new payload contract + mandatory-head framing (description text only — no logic change; headId is already threaded).
3. Fix the dashboard DELETE route (`src/dashboard/routes/sensors.ts`) — it currently removes the flat `ambient/<slug>.md`; with per-head layout it must remove `ambient/<head>/<slug>.md` across all head dirs.
4. One-time migration of the live `weather` sensor (head `ashley`): rewrite `sensor.mjs` to emit the JSON payload, regenerate output under the head-scoped path, and remove the stale flat file. (Human-verify checkpoint — touches the real workspace.)

Purpose: Closes the documentation + dashboard + migration gaps so the reworked primitive is coherent end-to-end. Depends on Plans 02 (runner write path / enqueue) + 03 (injection) being complete so the migrated sensor actually works through the new pipeline.
Output: Coherent docs, a per-head-aware DELETE, and a working live weather sensor on the new contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-sensor-dual-sink/51-CONTEXT.md
@.planning/phases/51-sensor-dual-sink/51-RESEARCH.md
@.planning/phases/51-sensor-dual-sink/51-02-SUMMARY.md
@.planning/phases/51-sensor-dual-sink/51-03-SUMMARY.md

<interfaces>
<!-- New contract (from Plans 01-03), to document in SKILL.md + registry -->
stdout = exactly one JSON object: { "ambient"?: string, "event"?: { "text": string } }
  - both optional; neither = quiet no-op tick; malformed/non-object → sensor error (failure marker)
  - ambient → overwrites ambient/<headId>/<slug>.md (head-scoped); omitted leaves the file stale (D-05)
  - event → enqueues a sensor_event for the schedule's headId → wakes the bound head (push)
  - one sensor → exactly one head (the schedule's headId); no per-event head field

<!-- Dashboard DELETE current code (src/dashboard/routes/sensors.ts ~91):
     fs.rmSync(path.join(ambientDir, `${slug}.md`), { force: true })   ← flat-layout bug
     ambientDir = path.join(workspacePath, 'ambient'); SLUG_RE validates slug first. -->

<!-- Live state (RESEARCH Runtime State Inventory):
     ~/.shrok/workspace/sensors/weather/sensor.mjs  — emits TWO console.log lines (plain body text)
     ~/.shrok/workspace/ambient/weather.md          — 130-byte flat file (old contract)
     schedule sched_1781788477329_vlr4cax.json       — kind:'script', headId:"ashley", taskName:"weather"
     weather is the ONLY live sensor; only head ashley has a sensor. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite skills/sensors/SKILL.md + update create_schedule description</name>
  <files>skills/sensors/SKILL.md, src/sub-agents/registry.ts</files>
  <read_first>
    - skills/sensors/SKILL.md (full 113-line current skill — it teaches the OLD "stdout is the output … ambient/<slug>.md … injected into every head" contract; the disk example uses bare console.log; "remove it entirely" mentions the flat ambient/<slug>.md)
    - src/sub-agents/registry.ts (create_schedule kind:'script' branch ~798-815 — description strings to update; do NOT touch the create logic, headId is already threaded)
    - .planning/phases/51-sensor-dual-sink/51-CONTEXT.md Specific Ideas (the canonical weather worked example + the self-watermark requirement)
  </read_first>
  <action>
    Rewrite skills/sensors/SKILL.md to the new contract. Required content (keep the YAML frontmatter `name: sensors` + description, update the description to mention the JSON payload + two sinks):
    - "What sensors are" — a scheduled model-free script emitting ONE JSON payload routed to two head-scoped sinks; it is bound to exactly one head (its schedule's head).
    - "The contract" — stdout MUST be exactly one JSON object `{ "ambient"?: string, "event"?: { "text": string } }`; both optional; emitting neither is a valid quiet/no-op tick; ANY parse failure or non-object shape is a sensor ERROR that overwrites the head-scoped ambient file with `⚠ Sensor failed on last run: …` (no plain-text fallback — this REPLACES the old "stdout is the output" rule). Keep the slug rule, the 30s timeout, the 2000-byte cap on the ambient body, and the env-inheritance note.
    - "The two sinks":
      (a) **ambient (pull, passive)** — the `ambient` string overwrites `ambient/<headId>/<slug>.md`, injected ONLY into the owning head's turns (head-scoped, not every head — this supersedes the old "every head" language); a quiet tick that omits `ambient` LEAVES the last value stale (does not clear it, D-05); heading derived from slug as before.
      (b) **event (push, active)** — a well-formed `event:{text}` wakes the bound head through the activation loop, framed as an honest sensor observation the head decides whether to surface; omit it on quiet ticks so the head isn't woken every tick.
    - "Target head" — mandatory; it is the sensor's `create_schedule` `headId` (one sensor → one head); the event fires to that head and the ambient lands under it.
    - "Self-watermarking (no built-in dedup)" — the runner does NO dedup/cooldown/edge-detection; a sensor that should fire an `event` only on a TRANSITION keeps its own `state.json`/timestamp inside its `sensors/<slug>/` dir and compares before emitting `event` (mirror the email-check pattern). Include a short worked code sketch reading/writing the watermark file (described in prose + a small JS snippet inside a fenced block — fenced blocks ARE allowed in SKILL.md, only forbidden in plan `<action>` fields).
    - Worked example — the weather sensor: a tick emitting `{ "ambient": "72°F, clear", "event": { "text": "Storm warning issued through 9pm." } }`; a later quiet tick `{ "ambient": "68°F, light rain" }` (ambient updates, no event); explain the script self-watermarks the last storm-alert id so the event does not re-fire every 5 minutes.
    - Update the on-disk `disk` example from bare `console.log(...)` to `console.log(JSON.stringify({ ambient: ... }))`.
    - In "remove it entirely", change the flat `ambient/<slug>.md` reference to the per-head `ambient/<headId>/<slug>.md` layout.

    Then update the `create_schedule` tool description in src/sub-agents/registry.ts (kind:'script' branch): the description should explain a sensor now emits a JSON payload `{ambient?, event?}` where `ambient` is passive per-head context and `event` actively wakes the schedule's head, and that the schedule's `headId` is the sensor's mandatory target head. Update only the human-readable `description` strings (the top-level description and the `kind`/`taskName` field descriptions as appropriate) — do NOT change the inputSchema enum or any create logic.
  </action>
  <verify>
    <automated>grep -ci "JSON" skills/sensors/SKILL.md && grep -ci "self-watermark\|state.json\|watermark" skills/sensors/SKILL.md && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md no longer contains the literal phrase "stdout is the output" (old contract removed): `grep -c "stdout is the output" skills/sensors/SKILL.md` == 0
    - SKILL.md no longer says ambient is injected into "every" head: `grep -ci "every.*head" skills/sensors/SKILL.md` reflects head-scoped language (the "injected into every head/agent prompt" sentence is gone or rephrased to head-scoped)
    - SKILL.md documents the JSON object contract: `grep -c "ambient" skills/sensors/SKILL.md` >= 3 AND mentions `event` and the failure marker
    - SKILL.md teaches self-watermarking with a state-file example: `grep -ci "watermark\|state.json" skills/sensors/SKILL.md` >= 1
    - SKILL.md references the per-head path `ambient/<headId>/<slug>.md` (not the flat `ambient/<slug>.md`) where it describes the ambient file location and removal
    - registry.ts create_schedule description mentions the JSON payload `{ambient?, event?}` and the schedule's head as the target: `grep -ci "ambient.*event\|event.*ambient\|payload" src/sub-agents/registry.ts` >= 1 within the create_schedule description
    - `npx tsc --noEmit` clean (description-only change to registry.ts)
  </acceptance_criteria>
  <done>SKILL.md fully teaches the dual-sink JSON contract + self-watermarking; create_schedule description reflects the new payload + mandatory head.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the dashboard DELETE route for per-head ambient layout</name>
  <files>src/dashboard/routes/sensors.ts, src/dashboard/routes/sensors.test.ts</files>
  <read_first>
    - src/dashboard/routes/sensors.ts (the DELETE /:slug route ~80-94 — `fs.rmSync(path.join(ambientDir, `${slug}.md`), {force:true})`, the flat-layout bug; SLUG_RE guard runs first; `ambientDir = path.join(workspacePath,'ambient')`)
    - src/dashboard/routes/sensors.test.ts (existing route tests — add the per-head delete coverage)
  </read_first>
  <action>
    In src/dashboard/routes/sensors.ts DELETE handler, replace the single flat `fs.rmSync(path.join(ambientDir, `${slug}.md`), {force:true})` with a per-head sweep: the route has no headId in scope and a sensor slug is globally unique on disk (one `sensors/<slug>/` dir), so glob-remove `ambient/<head>/<slug>.md` across every head subdirectory. Implement by `fs.readdirSync(ambientDir, { withFileTypes: true })` (wrapped so an absent `ambient/` dir is swallowed), filtering to directory entries, and for each `fs.rmSync(path.join(ambientDir, entry.name, `${slug}.md`), { force: true })` (force swallows ENOENT). Keep the SLUG_RE guard BEFORE any path.join unchanged (the slug is still validated, so no traversal). The script-dir removal (`fs.rmSync(path.join(sensorsDir, slug), {recursive:true,force:true})`) is unchanged. Use `for...of` over dir entries (noUncheckedIndexedAccess). Then add a test to src/dashboard/routes/sensors.test.ts: create `ambient/headA/<slug>.md` and `ambient/headB/<slug>.md` plus an unrelated `ambient/headA/other.md`, DELETE the slug, assert both head-scoped `<slug>.md` files are gone and `other.md` survives.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/routes/sensors.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readdirSync(ambientDir" src/dashboard/routes/sensors.ts` >= 1 (sweeps head subdirs)
    - `grep -c "path.join(ambientDir, \`\${slug}.md\`)" src/dashboard/routes/sensors.ts` == 0 (old flat removal gone)
    - SLUG_RE guard still runs before any path.join (unchanged)
    - A test asserts DELETE removes `<slug>.md` from BOTH head dirs and leaves an unrelated file in a head dir intact
    - `npx vitest run src/dashboard/routes/sensors.test.ts` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>DELETE removes ambient/<head>/<slug>.md across all head dirs; no flat-path removal remains; per-head delete tested.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the live weather sensor (head ashley) to the JSON contract + remove the stale flat ambient file</name>
  <files>~/.shrok/workspace/sensors/weather/sensor.mjs, ~/.shrok/workspace/ambient/weather.md</files>
  <read_first>
    - ~/.shrok/workspace/sensors/weather/sensor.mjs (the live sensor — currently ends with two plain-text `console.log(line1)` / `console.log(line2)` lines plus a failure-path `console.error` + `process.exit(1)`)
    - .planning/phases/51-sensor-dual-sink/51-RESEARCH.md Runtime State Inventory (the one-time migration spec: weather is the only live sensor, bound to head `ashley`)
  </read_first>
  <action>
    Perform the one-time live migration of the real `weather` sensor (Plans 02+03 must be merged first so the new pipeline works):
    1. Edit `~/.shrok/workspace/sensors/weather/sensor.mjs` so the success output is a SINGLE `console.log(JSON.stringify({ ambient: line1 + (line2 ? '
' + line2 : '') }))` replacing the two separate `console.log(line1)` / `console.log(line2)` plain-text lines. Leave the failure path (`console.error(...)` + `process.exit(1)` in the catch) unchanged — the runner still treats a nonzero exit as a sensor error. This is a normal workspace-script edit (write-file-atomic is for shrok identity/skill writes, not relevant here); just do not corrupt the file.
    2. Remove the stale flat file `~/.shrok/workspace/ambient/weather.md` (the old flat-layout output — under the new per-head layout it would be orphaned and never scanned).
    3. Regenerate output under the new head-scoped path: run the script to confirm its stdout is now exactly one valid JSON object, then trigger the sensor through the new runner for head `ashley` (manually fire its kind:'script' schedule, or directly invoke runSensor for slug `weather`, head `ashley`) so `~/.shrok/workspace/ambient/ashley/weather.md` is written with the JSON-derived body — NOT a `⚠ Sensor failed` marker.
    Do NOT add an `event` field to the production weather script in this migration (it would wake head ashley every tick with no watermark) — ambient-only is the correct steady state; the event sink is opt-in per the SKILL.md self-watermark guidance.
  </action>
  <verify>
    <automated>node ~/.shrok/workspace/sensors/weather/sensor.mjs | head -1 | node -e 'let d="";process.stdin.on("data",c=>d+=c).on("end",()=>{JSON.parse(d);console.log("stdout is valid JSON")})'</automated>
    <human-check>Confirm `cat ~/.shrok/workspace/ambient/ashley/weather.md` shows real conditions (e.g. "72°F, clear …") with `grep -c "Sensor failed" ~/.shrok/workspace/ambient/ashley/weather.md` == 0, and `ls ~/.shrok/workspace/ambient/weather.md` reports the flat file is gone (No such file).</human-check>
  </verify>
  <acceptance_criteria>
    - `node ~/.shrok/workspace/sensors/weather/sensor.mjs | head -1` emits exactly one line that parses as a JSON object (the automated verify above exits 0)
    - `grep -c "JSON.stringify" ~/.shrok/workspace/sensors/weather/sensor.mjs` >= 1 and the two former plain-text `console.log(line1)`/`console.log(line2)` success lines are gone
    - `grep -c "Sensor failed" ~/.shrok/workspace/ambient/ashley/weather.md` == 0 (head-scoped output holds real conditions, not a failure marker)
    - `ls ~/.shrok/workspace/ambient/weather.md` reports "No such file" (stale flat file removed)
    - No `event` field added to the production weather script (ambient-only steady state)
  </acceptance_criteria>
  <done>weather/sensor.mjs emits one JSON object; ambient/ashley/weather.md holds JSON-derived conditions (no failure marker); the stale flat ambient/weather.md is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard DELETE slug → filesystem | slug from request path becomes part of `ambient/<head>/<slug>.md` removal |
| live workspace edit | the migration edits a real operator sensor script + ambient files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04-PT | Tampering | dashboard DELETE slug | mitigate | SLUG_RE (`^[a-z0-9][a-z0-9-]*$`) validates the slug BEFORE any path.join (unchanged); head subdir names come from `readdirSync(ambientDir)` (existing on-disk dirs the runner created), not from the request — no attacker-controlled path segment. |
| T-51-04-DELALL | (correctness) | DELETE glob across head dirs | accept | Removing `<slug>.md` from every head dir is intended (slug is globally unique on disk); an unrelated file in a head dir is left intact (tested). force:true swallows ENOENT so an absent ambient/ or per-head dir is not an error. |
| T-51-04-MIGRATE | (data) | live weather sensor migration | mitigate | The migration is gated behind a blocking human-verify checkpoint; the failure path is self-healing+loud (a non-JSON emit would write a visible failure marker, not corrupt anything), and the flat file removal is reversible (the next run regenerates the head-scoped file). |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/dashboard/routes/sensors.test.ts` green; `npx tsc --noEmit` clean
- `grep -c "stdout is the output" skills/sensors/SKILL.md` == 0
- `grep -ci "watermark\|state.json" skills/sensors/SKILL.md` >= 1
- Human-verify: `ambient/ashley/weather.md` holds JSON-derived conditions (no failure marker); flat `ambient/weather.md` removed
- Full suite + tsc before /gsd:verify-work: `npx vitest run && npx tsc --noEmit`
</verification>

<success_criteria>
SKILL.md + create_schedule teach the new dual-sink JSON contract and mandatory head; the dashboard DELETE is per-head correct and tested; the live weather sensor runs on the new contract with output at ambient/ashley/weather.md and the stale flat file gone.
</success_criteria>

<output>
Create `.planning/phases/51-sensor-dual-sink/51-04-SUMMARY.md` when done. Note the migration outcome (the head-scoped weather output + flat-file removal) explicitly.
</output>
