---
phase: 51-sensor-dual-sink
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - src/head/injector.ts
  - src/head/injector.test.ts
  - src/head/activation.ts
  - src/head/assembler.ts
  - src/sub-agents/tool-surface.ts
  - src/sub-agents/tool-surface.test.ts
autonomous: true
requirements: [SENSOR-14, SENSOR-15]
nyquist_compliant: true

must_haves:
  truths:
    - "A claimed sensor_event injects a <system-event type=\"sensor\"> message plus a respond trigger that wakes the head"
    - "The injectEvent switch has a sensor_event case (it does NOT silently no-op)"
    - "All four scanAmbient call sites pass the head being assembled for"
    - "The head assembler injects only the assembling head's ambient block (uncached region, after Current time:)"
    - "A sub-agent's system prompt scans its spawning head's ambient dir"
    - "deriveQueryText returns the observation text for a sensor_event (memory-retrieval query)"
  artifacts:
    - path: "src/head/injector.ts"
      provides: "injectSensorEvent on Injector interface + InjectorImpl"
      contains: "injectSensorEvent"
    - path: "src/head/activation.ts"
      provides: "injectEvent + formatInjectedEvent sensor_event cases; 2 head-scoped scanAmbient calls"
      contains: "injectSensorEvent"
    - path: "src/head/assembler.ts"
      provides: "head-scoped scanAmbient call + deriveQueryText sensor_event case"
      contains: "this.headId"
  key_links:
    - from: "src/head/activation.ts injectEvent"
      to: "injector.injectSensorEvent"
      via: "case 'sensor_event'"
      pattern: "case 'sensor_event':"
    - from: "src/head/injector.ts injectSensorEvent"
      to: "systemEvent + systemTrigger"
      via: "append assistant systemEvent('sensor') + user systemTrigger('respond')"
      pattern: "systemEvent\\('sensor'"
    - from: "src/head/assembler.ts"
      to: "scanAmbient"
      via: "scanAmbient(resolvedWorkspace, this.headId)"
      pattern: "scanAmbient\\([^,]+, this\\.headId\\)"
---

<objective>
Wire the push-path injection and migrate all per-head ambient read sites:
- Add `injectSensorEvent` to the `Injector` interface + `InjectorImpl`, mirroring `injectWebhookEvent` (assistant-role `systemEvent('sensor', {slug}, text)` + user-role `systemTrigger('respond')`).
- Register `case 'sensor_event'` in `activation.ts`'s `injectEvent` switch (MANDATORY — an unregistered type is claimed, acked, and black-holed: Pitfall 1) and add the `formatInjectedEvent` debug case.
- Add the `deriveQueryText` `sensor_event` case in `assembler.ts` (memory-retrieval query text).
- Update all FOUR `scanAmbient` call sites to pass the head in scope (assembler `this.headId`; activation reminder + task proactive `this.opts.headId`; tool-surface `deps.headId`).

Purpose: SENSOR-15 push injection + SENSOR-14 read side. Depends on Plan 01's `sensor_event` type + head-scoped `scanAmbient` signature. Runs in parallel with Plan 02 (no shared files).
Output: A sensor_event that flows through the activation loop into a head turn, and ambient injection scoped to the owning head at every consumer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-sensor-dual-sink/51-CONTEXT.md
@.planning/phases/51-sensor-dual-sink/51-RESEARCH.md
@.planning/phases/51-sensor-dual-sink/51-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (Wave 1) -->
sensor_event QueueEvent member: { type: 'sensor_event'; id: string; slug: string; text: string; createdAt: string }
scanAmbient now: scanAmbient(workspaceDir: string, headId: string): string

<!-- injectWebhookEvent — the EXACT template to copy (src/head/injector.ts:265-289) -->
injectWebhookEvent appends two TextMessages:
  1. role:'assistant', content: systemEvent('webhook', {...}, body), injected:true
  2. role:'user',      content: systemTrigger('respond'),        injected:true
  via this.messages.append(msg, this.headId) — InjectorImpl has `private readonly headId`.
generateId('msg') + now() are the id/timestamp helpers used by all injector methods.

<!-- Injector interface (src/head/injector.ts:136-140) currently declares:
     injectAgentEvent, injectWebhookEvent, injectHeadMessage -->

<!-- activation.ts injectEvent switch (src/head/activation.ts:1319-1336) — cases for
     agent_completed/question/failed/response, webhook, head_message. NO default — so an
     unregistered type silently no-ops (Pitfall 1). -->
<!-- formatInjectedEvent (src/head/activation.ts:1448-1467) — debug formatter, HAS a default. -->
<!-- The two scanAmbient calls in activation.ts: line ~1140 (reminder proactive, this.opts.headId),
     line ~1217 (task proactive, this.opts.headId). Both currently single-arg. -->

<!-- assembler.ts: scanAmbient call ~line 146 (single-arg, in uncached region after the
     "Current time:" marker at line 130; this.headId is the ctor arg). deriveQueryText switch
     at lines 37-82 (cases for user_message, agent_*, schedule_trigger, webhook, agent_response,
     reminder_trigger, head_message — NO sensor_event yet). -->

<!-- tool-surface.ts: scanAmbient call ~line 82 (single-arg, after its own "Current time:" line
     at 77; deps.headId is a required field at line 33). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add injectSensorEvent + register the activation injectEvent/formatInjectedEvent + assembler deriveQueryText cases</name>
  <files>src/head/injector.ts, src/head/injector.test.ts, src/head/activation.ts, src/head/assembler.ts</files>
  <read_first>
    - src/head/injector.ts (the Injector interface lines 136-140 + InjectorImpl injectWebhookEvent lines 265-289 — copy this method's two-message shape exactly)
    - src/head/activation.ts (injectEvent switch 1319-1336 + formatInjectedEvent 1448-1467 — note injectEvent has NO default, Pitfall 1)
    - src/head/assembler.ts (deriveQueryText switch 37-82 — add a case matching the reminder_trigger/head_message style)
    - src/markers.ts (systemEvent / systemTrigger signatures + escapeXmlBody — body is escaped, head treats <system-event> as structural delimiter)
    - src/head/injector.test.ts (existing injector tests — add the new-method coverage; this is the Wave-0 genuinely-new test per 51-VALIDATION.md)
  </read_first>
  <behavior>
    - injectSensorEvent on a `{type:'sensor_event', slug:'weather', text:'Storm warning'}` appends exactly two messages to the head's MessageStore: an assistant message whose content contains `type="sensor"`, `slug="weather"`, and the (escaped) text; then a user message whose content is the respond trigger
    - Both appended messages carry `injected: true` and are appended for this.headId
    - The injectEvent switch routes a sensor_event to injector.injectSensorEvent (NOT a silent no-op)
    - formatInjectedEvent for a sensor_event returns a systemEvent('sensor', {slug}, text-sliced) string (clean debug log, no JSON.stringify fallback)
    - deriveQueryText for a sensor_event returns the event's text
  </behavior>
  <action>
    In src/head/injector.ts, add `injectSensorEvent(event: QueueEvent & { type: 'sensor_event' }): void` to the `Injector` interface (alongside injectWebhookEvent/injectHeadMessage) and implement it on `InjectorImpl` by copying `injectWebhookEvent` structurally: build an assistant-role TextMessage with `content: systemEvent('sensor', { slug: event.slug }, event.text)` and a user-role TextMessage with `content: systemTrigger('respond')`, both `injected: true`, both via `this.messages.append(msg, this.headId)`. Use `generateId('msg')` + `now()` exactly as the sibling methods do. Do NOT add any extra body instruction — the webhook path carries none and the SOUL/SYSTEM identity already teaches the head to treat `<system-event>` as a structural delimiter it decides whether to surface (D-06 honest framing).

    In src/head/activation.ts, add `case 'sensor_event': this.opts.injector.injectSensorEvent(event); break` to the `injectEvent` switch (this is mandatory per Pitfall 1 — without it the event is claimed/acked/black-holed). Add a `case 'sensor_event': return systemEvent('sensor', { slug: event.slug }, event.text.slice(0, 300))` to `formatInjectedEvent` for clean debug logs.

    In src/head/assembler.ts, add `case 'sensor_event': return trigger.text` to the `deriveQueryText` switch (matches the reminder_trigger/head_message one-liner style — gives memory retrieval a sensible query string).

    In src/head/injector.test.ts, add tests for injectSensorEvent: assert two messages are appended, the first is assistant-role with `type="sensor"` + `slug="weather"` + the text in its content, the second is user-role with the respond trigger, both `injected:true`. (This is the Wave-0 new-method coverage from 51-VALIDATION.md.)
  </action>
  <verify>
    <automated>npx vitest run src/head/injector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "injectSensorEvent" src/head/injector.ts` == 2 (interface declaration + impl method)
    - `grep -c "systemEvent('sensor'" src/head/injector.ts` == 1
    - `grep -c "case 'sensor_event':" src/head/activation.ts` == 2 (injectEvent switch + formatInjectedEvent)
    - `grep -c "injectSensorEvent" src/head/activation.ts` == 1 (the dispatch)
    - `grep -c "case 'sensor_event':" src/head/assembler.ts` == 1 (deriveQueryText)
    - injector.test.ts asserts injectSensorEvent appends exactly 2 messages (assistant systemEvent type="sensor" + user respond trigger), both injected:true
    - `npx vitest run src/head/injector.test.ts` green
  </acceptance_criteria>
  <done>injectSensorEvent exists and is dispatched; the switch is no longer a black hole; deriveQueryText + debug formatter handle sensor_event; new injector test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make all four scanAmbient call sites head-scoped</name>
  <files>src/head/activation.ts, src/head/assembler.ts, src/sub-agents/tool-surface.ts, src/sub-agents/tool-surface.test.ts</files>
  <read_first>
    - src/head/activation.ts (the two scanAmbient calls at ~1140 reminder-proactive and ~1217 task-proactive — both have `this.opts.headId` in scope)
    - src/head/assembler.ts (the scanAmbient call ~146, in the uncached region after the "Current time:" marker at 130; `this.headId` is the ctor arg)
    - src/sub-agents/tool-surface.ts (the scanAmbient call ~82, after the agent "Current time:" line at 77; `deps.headId` required field at line 33)
    - src/sensors/scan.ts (the new two-arg signature from Plan 01)
    - src/sub-agents/tool-surface.test.ts + src/head/assembler.test.ts (existing ambient-placement tests — add the headId dimension)
  </read_first>
  <behavior>
    - The head assembler for head 'ashley' injects ambient content from ambient/ashley/*.md and NOT from ambient/zoey/*.md, placed AFTER the "Current time:" marker (uncached region)
    - A sub-agent spawned by head 'ashley' scans ambient/ashley/*.md (its spawning head's dir, per D-04)
    - Both proactive activation paths scan the loop's own head dir (this.opts.headId)
    - An absent per-head ambient dir produces no ambient block (scanAmbient returns '')
  </behavior>
  <action>
    Update all four single-arg `scanAmbient(...)` calls to pass the head already in scope, with NO change to placement (uncached region, after the "Current time:" marker — Phase 48 D-06/D-07 unchanged):
    - src/head/assembler.ts: `scanAmbient(resolvedWorkspace, this.headId)` (keep the existing `resolvedWorkspace` first arg).
    - src/head/activation.ts (reminder proactive, ~1140): add `, this.opts.headId` as the second arg (keep the existing `this.opts.config.workspacePath.replace(/^~/, os.homedir())` first arg).
    - src/head/activation.ts (task proactive, ~1217): same — add `, this.opts.headId`.
    - src/sub-agents/tool-surface.ts (~82): `scanAmbient(deps.workspacePath, deps.headId)`.
    Then extend src/sub-agents/tool-surface.test.ts (and src/head/assembler.test.ts if its fixtures created flat `ambient/<slug>.md`): write ambient content under a specific head's dir, assert the assembled prompt includes that head's ambient block AND does NOT include another head's block. Update any test fixture that created the old flat `ambient/<slug>.md` to the per-head `ambient/<headId>/<slug>.md` layout.
  </action>
  <verify>
    <automated>npx vitest run src/head/assembler.test.ts src/sub-agents/tool-surface.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scanAmbient([^,)]*, this.headId)" src/head/assembler.ts` == 1
    - `grep -c "scanAmbient([^,)]*, this.opts.headId)" src/head/activation.ts` == 2
    - `grep -c "scanAmbient(deps.workspacePath, deps.headId)" src/sub-agents/tool-surface.ts` == 1
    - `grep -c "scanAmbient([^,)]*)" ` returns zero remaining single-arg calls across src/ (every call now passes a head): verify with `grep -rn "scanAmbient(" src/ | grep -v "scanAmbient([^,)]*,"` returns no call-expression lines (import line allowed)
    - assembler/tool-surface tests assert per-head ambient scoping (head A sees A's block, not head B's)
    - `npx tsc --noEmit` is CLEAN across the whole src/head/* + src/sub-agents/* path (closes the Plan-01 RED for the read sites)
    - `npx vitest run src/head/assembler.test.ts src/sub-agents/tool-surface.test.ts` green
  </acceptance_criteria>
  <done>All four scanAmbient calls pass the head in scope; per-head isolation tested at assembler + tool-surface; tsc clean for head/sub-agents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor_event.text → model context | observation text rendered into the head's prompt as a system-event |
| ambient file body → model context | per-head ambient block injected into the uncached prompt region |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-INJ | Tampering/Info | injectSensorEvent body framing | mitigate | Route event text through `systemEvent('sensor', {slug}, text)` exactly as the `webhook` injector does — `systemEvent` escapes the body via `escapeXmlBody`, and the head treats `<system-event>` as a structural delimiter, not a user instruction (same trust model as webhook, RESEARCH Security Domain). No bespoke string template. |
| T-51-03-BLACKHOLE | (correctness/availability) | injectEvent switch | mitigate | The `case 'sensor_event'` registration is mandatory — without it the event is claimed/acked silently with no head turn (Pitfall 1). An injector test asserting the injected `<system-event type="sensor">` message appears guards against regression. |
| T-51-03-PT | Tampering | scanAmbient call sites | accept | headId at every call site is an operator-defined head id (this.headId / this.opts.headId / deps.headId), not attacker input; the runner (Plan 02) is the only writer and validates the charset, so no traversal-shaped dir exists to read. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/head/injector.test.ts src/head/assembler.test.ts src/sub-agents/tool-surface.test.ts` green
- `grep -c "case 'sensor_event':" src/head/activation.ts` == 2
- `grep -c "injectSensorEvent" src/head/injector.ts` == 2
- `npx tsc --noEmit` clean across src/head + src/sub-agents (combined with Plan 02 this makes the full src tree tsc-clean)
</verification>

<success_criteria>
A claimed sensor_event injects a system-event + respond trigger that wakes the bound head (not a black hole); ambient injection is scoped to the owning head at all four read sites with isolation proven by test.
</success_criteria>

<output>
Create `.planning/phases/51-sensor-dual-sink/51-03-SUMMARY.md` when done.
</output>
