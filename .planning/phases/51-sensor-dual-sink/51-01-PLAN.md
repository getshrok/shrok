---
phase: 51-sensor-dual-sink
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/core.ts
  - src/sensors/scan.ts
  - src/sensors/scan.test.ts
autonomous: true
requirements: [SENSOR-14, SENSOR-15]
nyquist_compliant: true

must_haves:
  truths:
    - "A `sensor_event` is a valid QueueEvent type the compiler knows about"
    - "sensor_event has a priority of 15 (between WEBHOOK=20 and SCHEDULE_TRIGGER=10)"
    - "scanAmbient reads ONLY one head's ambient directory, never the whole tree"
    - "scanAmbient for head A never returns head B's ambient content"
    - "An absent ambient/<headId>/ directory returns '' (not an error)"
  artifacts:
    - path: "src/types/core.ts"
      provides: "sensor_event union member + PRIORITY.SENSOR_EVENT"
      contains: "type: 'sensor_event'"
    - path: "src/sensors/scan.ts"
      provides: "head-scoped scanAmbient(workspaceDir, headId)"
      contains: "ambient', headId"
    - path: "src/sensors/scan.test.ts"
      provides: "per-head dir read + head A/B isolation tests"
      contains: "headId"
  key_links:
    - from: "src/sensors/scan.ts"
      to: "ambient/<headId>/*.md"
      via: "path.join(workspaceDir, 'ambient', headId)"
      pattern: "ambient',\\s*headId"
---

<objective>
Establish the two contract-layer pieces that every downstream plan depends on, with NO consumers wired yet:
1. The `sensor_event` discriminated-union member on `QueueEvent` plus `PRIORITY.SENSOR_EVENT: 15` (the push-path type, consumed by Plan 02's runner enqueue and Plan 03's injector).
2. The head-scoped `scanAmbient(workspaceDir, headId)` signature reading `ambient/<headId>/*.md` (consumed by Plan 02's runner write path and Plan 03's read sites).

Purpose: Defining these contracts first lets Plans 02 and 03 run in parallel against fixed signatures with no scavenger-hunt. This plan owns `src/types/core.ts` and `src/sensors/scan.ts` exclusively.
Output: A typed `sensor_event` event + a head-scoped ambient scanner, both unit-tested in isolation. tsc may show RED at call sites that Plans 02/03 fix — that is expected (see acceptance criteria).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-sensor-dual-sink/51-CONTEXT.md
@.planning/phases/51-sensor-dual-sink/51-RESEARCH.md

<interfaces>
<!-- The existing QueueEvent union members + PRIORITY map this plan extends.
     Extracted from src/types/core.ts — executor uses these directly. -->

Existing PRIORITY map (src/types/core.ts:162-174):
  USER_MESSAGE: 100, HEAD_MESSAGE: 70, AGENT_QUESTION: 50,
  AGENT_COMPLETED/FAILED/RESPONSE: 30, WEBHOOK: 20,
  SCHEDULE_TRIGGER: 10, REMINDER_TRIGGER: 10

The `webhook` union member shape (the template for the new member — note `payload: unknown`, `createdAt: string`):
  { type: 'webhook'; id: string; source: string; event: string; payload: unknown; createdAt: string }

Current scanAmbient (src/sensors/scan.ts): `scanAmbient(workspaceDir: string): string` reads `{workspaceDir}/ambient/*.md`, derives heading via slugToTitle, joins non-empty blocks with `\n\n`, returns '' if dir absent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the sensor_event QueueEvent type + PRIORITY.SENSOR_EVENT</name>
  <files>src/types/core.ts</files>
  <read_first>
    - src/types/core.ts (the QueueEvent union at lines 83-157, QueueEventType at 159, PRIORITY at 162-174 — study the `webhook` and `head_message` members as the shape templates)
  </read_first>
  <behavior>
    - A value `{ type: 'sensor_event', id, slug, text, createdAt }` is assignable to QueueEvent
    - QueueEventType includes the literal 'sensor_event'
    - PRIORITY.SENSOR_EVENT === 15
  </behavior>
  <action>
    Add a new member to the `QueueEvent` discriminated union in src/types/core.ts with exactly these fields: `type: 'sensor_event'`, `id: string`, `slug: string` (which sensor reported — the schedule's taskName/slug), `text: string` (the observation text — D-02 minimum event sub-schema; do NOT add severity/title/priority fields, keep it minimal per CONTEXT Deferred Ideas), `createdAt: string`. Place it adjacent to the `webhook`/`head_message` members for readability. Add a doc comment noting it is the push sink of a sensor's JSON payload (`{ ambient?, event? }`), enqueued for the sensor schedule's headId (SENSOR-15/16). Then add `SENSOR_EVENT: 15` to the `PRIORITY` const object, placed between `WEBHOOK: 20` and `SCHEDULE_TRIGGER: 10`, with a comment justifying 15: a sensor observation is an environmental push like a webhook but the operator chose its cadence and it is the active sibling of the passive ambient sink, so it ranks below a genuine third-party `webhook` (20) yet above scheduler-internal `schedule_trigger`/`reminder_trigger` (10) which themselves spawn/re-enqueue work. `QueueEventType = QueueEvent['type']` derives automatically — no edit needed there.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/types/core.ts 2>&1 | grep -v "^$" | grep -i "core.ts" ; test $? -ne 0 && echo "core.ts type-clean OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type: 'sensor_event'" src/types/core.ts` returns 1
    - `grep -c "SENSOR_EVENT: 15" src/types/core.ts` returns 1
    - The sensor_event member declares exactly the fields: type, id, slug, text, createdAt (no severity/title/priority/headId field — per D-02 minimal + D-07 no per-event head)
    - `npx tsc --noEmit` shows no NEW errors originating in src/types/core.ts itself (errors at unrelated call sites that now must handle the new union member are expected and fixed by Plans 02/03)
  </acceptance_criteria>
  <done>QueueEvent has a sensor_event member with {type,id,slug,text,createdAt}; PRIORITY.SENSOR_EVENT === 15 with documented justification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make scanAmbient head-scoped — scanAmbient(workspaceDir, headId)</name>
  <files>src/sensors/scan.ts, src/sensors/scan.test.ts</files>
  <read_first>
    - src/sensors/scan.ts (full file — the current flat `scanAmbient(workspaceDir)` reading `{workspaceDir}/ambient/*.md`)
    - src/sensors/scan.test.ts (existing flat-layout tests — they create `ambient/<slug>.md` and assert headings; these must be updated to the per-head layout)
  </read_first>
  <behavior>
    - scanAmbient(dir, 'ashley') reads only `{dir}/ambient/ashley/*.md`
    - Writing `ambient/ashley/weather.md` (body "72F") and `ambient/zoey/traffic.md`, then scanAmbient(dir,'ashley') returns a block containing "## Weather" and "72F" and NOT "Traffic" (head A never sees head B — isolation)
    - scanAmbient(dir, 'newhead') where `ambient/newhead/` does not exist returns '' (not an error)
    - Heading derivation (slugToTitle) and empty-body-skip behavior are unchanged
  </behavior>
  <action>
    Change the signature of `scanAmbient` in src/sensors/scan.ts to `scanAmbient(workspaceDir: string, headId: string): string`. The only body change: the directory becomes `path.join(workspaceDir, 'ambient', headId)` instead of `path.join(workspaceDir, 'ambient')`. Everything else — the `readdirSync().filter('.md').sort()`, the `for...of` loop (keep `for...of`, not index access, per noUncheckedIndexedAccess), `slugToTitle`, the empty-body skip, the `try/catch returns ''` for an absent dir — stays byte-identical. Update the JSDoc to say it reads `{workspaceDir}/ambient/{headId}/*.md` and that an absent per-head dir returns ''. Do NOT add a headId charset guard here — the guard lives at the runner write boundary (Plan 02); scan only ever reads dirs the runner created. Then rewrite src/sensors/scan.test.ts: every fixture that wrote `ambient/<slug>.md` now writes `ambient/<headId>/<slug>.md` and calls `scanAmbient(dir, headId)`; ADD a dedicated isolation test that writes one file under head A and one under head B and asserts scanAmbient(dir, A) contains A's content and does NOT contain B's; ADD a test that an absent per-head dir returns ''.
  </action>
  <verify>
    <automated>npx vitest run src/sensors/scan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ambient', headId" src/sensors/scan.ts` returns 1 (the path.join now includes headId)
    - `grep -c "scanAmbient(workspaceDir: string)" src/sensors/scan.ts` returns 0 (old single-arg signature gone)
    - `npx vitest run src/sensors/scan.test.ts` passes, including a test named for head A/B isolation and a test for absent-dir-returns-empty
    - The isolation test asserts head A's scan output does NOT include head B's slug heading or body
    - `for...of` loop retained (no `files[i]` index access introduced)
  </acceptance_criteria>
  <done>scanAmbient takes (workspaceDir, headId), reads only that head's ambient dir, isolation + absent-dir tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem path segment ← headId | headId becomes part of `ambient/<headId>/` read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-PT | Tampering | scanAmbient read path | accept | scanAmbient only READS dirs the runner created; the runner (Plan 02) validates headId charset before any write/mkdir, so no traversal-shaped dir can exist to be read. Read-side hardening is unnecessary and would be defense-in-depth-twice; documented here as a deliberate non-action. No `path.join` with attacker-controlled headId on the read path because headId at every scan call site is an operator-defined head id from config (assembler `this.headId`, activation `this.opts.headId`, tool-surface `deps.headId`). |
| T-51-01-TYPE | Tampering | sensor_event union member | mitigate | The new member uses primitive `string` fields only; no `unknown`/eval. Downstream consumers (Plan 02 enqueue, Plan 03 inject) type-guard before use. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A). No checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run src/sensors/scan.test.ts` green
- `grep -c "SENSOR_EVENT: 15" src/types/core.ts` == 1
- `grep -c "type: 'sensor_event'" src/types/core.ts` == 1
- Note: a full `npx tsc --noEmit` will show RED at scanAmbient call sites (assembler/activation/tool-surface, single-arg calls) and possibly at exhaustiveness switches — this is the expected Wave-1 contract→Wave-2 implementation cadence; Plans 02/03 resolve it.
</verification>

<success_criteria>
sensor_event is a first-class QueueEvent with priority 15; scanAmbient is head-scoped with isolation proven by test. No consumer wiring in this plan.
</success_criteria>

<output>
Create `.planning/phases/51-sensor-dual-sink/51-01-SUMMARY.md` when done. Note the expected tsc RED at unmigrated call sites for the Wave-2 plans.
</output>
