---
phase: 45-ring-delivery-layer-timer-ring-alarm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/types/agent.ts
  - src/sub-agents/local.ts
  - src/channels/home-assistant/adapter.ts
autonomous: true
requirements: [RING-03, RING-07, RING-08, RING-10]
must_haves:
  truths:
    - "config.json may set publicBaseUrl, ringVolume (default 0.5), ringCapHours (default 24) as top-level fields"
    - "An HA channel config may set haMediaPlayerEntityId / haLedEntityId optional overrides"
    - "Every agent tool executor receives ctx.headId so ring_device can resolve its head's HA channel"
    - "The HA adapter can cache a device-reachable base URL learned from an inbound turn and expose its config to the ring runner"
  artifacts:
    - path: "src/config.ts"
      provides: "publicBaseUrl/ringVolume/ringCapHours top-level fields + haMediaPlayerEntityId/haLedEntityId HA-union overrides"
      contains: "ringCapHours"
    - path: "src/types/agent.ts"
      provides: "AgentContext.headId required field"
      contains: "headId: string"
    - path: "src/channels/home-assistant/adapter.ts"
      provides: "cacheBaseUrl/getDeviceReachableBaseUrl/getConfig methods"
      contains: "getDeviceReachableBaseUrl"
  key_links:
    - from: "src/sub-agents/local.ts"
      to: "AgentContext"
      via: "ctx assembly sets headId: this.headId"
      pattern: "headId: this.headId"
---

<objective>
Establish the shared contracts every other Phase-45 plan depends on: the three top-level ring config fields plus the two optional HA-channel entity overrides, the `AgentContext.headId` field (so agent-side `ring_device` can resolve its head's HA channel), and the HA adapter getters/setters the ring runner and router need (config exposure + device-reachable base-URL cache).

Purpose: These are the cross-cutting type/config seams. Landing them first in Wave 1 lets the runner (Plan 02), media route + URL capture (Plan 03), and tool wiring (Plan 04) build against fixed contracts with no scavenger hunt.
Output: Extended `ConfigSchema` + HA union, `AgentContext.headId`, `local.ts` ctx wiring, and four new adapter methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-CONTEXT.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-RESEARCH.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-PATTERNS.md

<interfaces>
<!-- Current contracts the executor edits. Extracted from codebase — no exploration needed. -->

ConfigSchema (src/config.ts:79+, dashboard block ~259-263) — extend with top-level fields:
```
dashboardPort: z.coerce.number().default(8888),
dashboardHost: z.string().default('127.0.0.1'),
```

ChannelConfigSchema home-assistant union member (src/config.ts:51-59):
```
z.object({
  id: z.string().min(1),
  vendor: z.literal('home-assistant'),
  haBaseUrl: z.string().url(),
  haVoiceSatelliteEntityId: z.string().regex(/^assist_satellite\.[a-z0-9_]+$/, '...'),
}),
```

ENV_KEY_ALLOWLIST (src/config.ts:502+) — secrets only; publicBaseUrl/ringVolume/ringCapHours are NOT added here.

AgentContext (src/types/agent.ts:7-19) — currently: agentId, suspend(), complete(), fail(), abortSignal?.

local.ts ctx assembly (src/sub-agents/local.ts:1047-1066) — `this.headId` already a class field (line 145, set from opts.headId).

HomeAssistantChannelAdapter (src/channels/home-assistant/adapter.ts:26-44) — has `private readonly config: HAConfig` (haBaseUrl, haVoiceSatelliteEntityId); pendingReply getter/setter pair (setPendingReply/clearPendingReply at lines 60-75) is the public-method-pair model to mirror.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ConfigSchema + HA union with ring config fields</name>
  <files>src/config.ts</files>
  <read_first>
    - src/config.ts — the file being modified (ConfigSchema at line 79+, dashboard fields 259-263, HA union 51-59, ENV_KEY_ALLOWLIST 502+)
    - 45-PATTERNS.md § `src/config.ts (MODIFIED)` (lines 241-261 of the pattern map) — exact Zod field shapes to add
    - 45-CONTEXT.md § canonical_refs "Config" — publicBaseUrl is behavioral (config.json), not a secret
  </read_first>
  <action>
    In `src/config.ts`, add three OPTIONAL/defaulted top-level fields to the `ConfigSchema` z.object (alongside the dashboard block at ~259-263):
    `publicBaseUrl: z.string().url().optional()`, `ringVolume: z.coerce.number().min(0).max(1).default(0.5)`, `ringCapHours: z.coerce.number().min(1).max(72).default(24)`.
    In the `home-assistant` member of `ChannelConfigSchema` (the z.object at lines 51-59), add two OPTIONAL overrides: `haMediaPlayerEntityId: z.string().optional()` and `haLedEntityId: z.string().optional()`.
    Do NOT add publicBaseUrl/ringVolume/ringCapHours to `ENV_KEY_ALLOWLIST` (they are behavioral config.json settings, not secrets per D-04/CONTEXT.md). Use `exactOptionalPropertyTypes`-safe Zod (`.optional()` is fine; never assign `undefined`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -v '^#' src/config.ts | grep -c 'ringCapHours'` returns >= 1
    - `grep -v '^#' src/config.ts | grep -c 'haMediaPlayerEntityId'` returns >= 1
    - `grep -c "publicBaseUrl" src/config.ts` shows publicBaseUrl present in ConfigSchema but `grep -A40 'ENV_KEY_ALLOWLIST' src/config.ts | grep -c publicBaseUrl` returns 0 (NOT in allowlist)
    - A loadConfig of a config.json omitting all three top-level fields yields ringVolume === 0.5 and ringCapHours === 24 (defaults applied)
  </acceptance_criteria>
  <done>ConfigSchema accepts publicBaseUrl/ringVolume/ringCapHours; HA union accepts haMediaPlayerEntityId/haLedEntityId; defaults applied; allowlist unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AgentContext.headId (required) and wire it in local.ts ctx assembly</name>
  <files>src/types/agent.ts, src/sub-agents/local.ts</files>
  <read_first>
    - src/types/agent.ts — the file being modified (AgentContext at lines 7-19)
    - src/sub-agents/local.ts — the file being modified (ctx assembly at lines 1047-1066; `this.headId` class field at line 145)
    - 45-PATTERNS.md § `src/types/agent.ts (MODIFIED)` + § `src/sub-agents/local.ts (MODIFIED)` (pattern map lines 264-303, 368-383) — exact shape; D-SPAWN-REQUIRED precedent (Phase 34) for required-no-default
    - 45-RESEARCH.md Pitfall 3 — tsc RED after adding required field is expected; fix test fixtures that construct plain ctx objects
  </read_first>
  <behavior>
    - AgentContext now requires a `headId: string` field; objects missing it are tsc errors (type-required, no default — mirrors Phase 34 SpawnOptions.headId).
    - The ctx constructed in LocalAgentRunner sets `headId: this.headId` so every agent tool executor sees its head.
    - Existing tests that build inline AgentContext objects compile after adding `headId: 'test-head'` (or similar) to each.
  </behavior>
  <action>
    In `src/types/agent.ts`, add `headId: string` as a required field immediately after `agentId: string` in the `AgentContext` interface (line ~8), with a comment `// Phase 45 — required; used by ring_device to resolve HA channel`.
    In `src/sub-agents/local.ts` ctx assembly (line 1047), add `headId: this.headId,` immediately after `agentId,`. `this.headId` is already the class field set from `opts.headId` (line 145) — no other local.ts change.
    Run `npx tsc --noEmit`; for every remaining error where a test/fixture constructs a plain `AgentContext` literal, add `headId: 'test-head'` (use the fixture's existing head id where one exists). Do NOT use `?? 'default'` fallbacks — the type requirement is the safety net.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/sub-agents/ src/head/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "headId: string" src/types/agent.ts` returns >= 1 (within AgentContext)
    - `grep -c "headId: this.headId" src/sub-agents/local.ts` returns >= 1
    - `npx tsc --noEmit` exits 0 (all fixtures updated)
    - `npx vitest run src/sub-agents/ src/head/` passes
  </acceptance_criteria>
  <done>AgentContext.headId is required; local.ts ctx sets it from this.headId; all fixtures updated; tsc + sub-agents/head suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add HA adapter base-URL cache + config getter methods</name>
  <files>src/channels/home-assistant/adapter.ts</files>
  <read_first>
    - src/channels/home-assistant/adapter.ts — the file being modified (class fields 27-31; setPendingReply/clearPendingReply public-method-pair at 60-75; HAConfig interface 12-15; D-05 token-safety at 101-108)
    - src/channels/home-assistant/adapter.test.ts — the test file to extend (mock-fetch + env setup pattern at the top)
    - 45-PATTERNS.md § `src/channels/home-assistant/adapter.ts (MODIFIED)` (pattern map lines 205-218) — the four additions and the getter-pair model
  </read_first>
  <behavior>
    - `cacheBaseUrl(url)` stores a device-reachable base URL; `getDeviceReachableBaseUrl()` returns it or null when never set.
    - `getConfig()` returns the adapter's HAConfig (haBaseUrl, haVoiceSatelliteEntityId) for the ring runner to read.
    - `headId` is readable by the startup wiring (already a private field — expose via a `get headId()` accessor or `getHeadId()` so index.ts `find(a => a.headId === ...)` works without widening visibility unsafely).
  </behavior>
  <action>
    In `src/channels/home-assistant/adapter.ts`:
    Add `private deviceReachableBaseUrl: string | null = null` instance field.
    Add `cacheBaseUrl(url: string): void { this.deviceReachableBaseUrl = url }`.
    Add `getDeviceReachableBaseUrl(): string | null { return this.deviceReachableBaseUrl }`.
    Add `getConfig(): HAConfig { return this.config }`.
    Expose the existing `private readonly headId` for startup lookup: add a public getter `get headId(): string { return this.headIdValue }` ONLY if the field is currently `private` and inaccessible — note `index.ts:343` already reads `a.headId` (line 458 wiring), so confirm current access; if `headId` is already accessible to index.ts leave it as-is and do NOT duplicate. Keep D-05: no method logs or embeds `HA_ACCESS_TOKEN`.
    Extend `src/channels/home-assistant/adapter.test.ts` with a describe block asserting: getDeviceReachableBaseUrl() returns null before cacheBaseUrl; returns the cached value after; getConfig() returns the haBaseUrl/haVoiceSatelliteEntityId passed at construction.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/channels/home-assistant/adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getDeviceReachableBaseUrl" src/channels/home-assistant/adapter.ts` returns >= 2 (declaration + return)
    - `grep -c "cacheBaseUrl" src/channels/home-assistant/adapter.ts` returns >= 1
    - `grep -c "getConfig" src/channels/home-assistant/adapter.ts` returns >= 1
    - New adapter.test.ts block: cache round-trip + getConfig assertions pass
    - `grep -c "HA_ACCESS_TOKEN" src/channels/home-assistant/adapter.ts` count is unchanged from baseline (no new token references in the added methods)
  </acceptance_criteria>
  <done>Adapter exposes cacheBaseUrl/getDeviceReachableBaseUrl/getConfig (and headId readable by index.ts); round-trip + config getter tested; token-safety preserved; tsc + adapter suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → ConfigSchema | User-authored config file parsed by Zod at load time |
| inbound HA turn → adapter.cacheBaseUrl | Host-derived URL (set in Plan 03) is stored here — only ever used to build a URL handed to the user's own HA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-CFG | Information Disclosure | ENV_KEY_ALLOWLIST | mitigate | publicBaseUrl/ringVolume/ringCapHours are NOT added to ENV_KEY_ALLOWLIST — they stay in config.json, never written to .env; HA_ACCESS_TOKEN remains the only credential there (D-04) |
| T-45-01-URL | Tampering | adapter.deviceReachableBaseUrl | mitigate | The cached value is only stored here; Plan 03 gates caching to non-loopback Host from a Bearer-authenticated inbound turn. This plan adds the slot only; it is never used for an auth decision |
| T-45-01-TOK | Information Disclosure | new adapter methods | mitigate | No new method reads, logs, or embeds HA_ACCESS_TOKEN — getConfig returns only haBaseUrl/haVoiceSatelliteEntityId (mirrors adapter.ts D-05) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the whole repo (AgentContext.headId fixtures all updated).
- `npx vitest run src/config.test.ts src/sub-agents/ src/head/ src/channels/home-assistant/adapter.test.ts` green.
</verification>

<success_criteria>
- ConfigSchema accepts and defaults the three ring fields; HA union accepts the two entity overrides; allowlist unchanged.
- AgentContext requires headId; LocalAgentRunner supplies it.
- HA adapter exposes config + base-URL cache; no token leakage in new code.
</success_criteria>

<output>
Create `.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-01-SUMMARY.md` when done
</output>
