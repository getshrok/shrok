---
phase: 45-ring-delivery-layer-timer-ring-alarm
plan: 06
type: execute
wave: 4
depends_on: ["45-04"]
files_modified:
  - skills/timer/SKILL.md
  - skills/set-alarm/SKILL.md
  - src/skills/ring-skills.test.ts
autonomous: true
requirements: [TIMER-01, TIMER-02, ALARM-01, ALARM-02, ALARM-03]
must_haves:
  truths:
    - "When a voice-set timer elapses the timer skill calls ring_device(start) before the completion message"
    - "The timer skill is otherwise unchanged — only the ring call is added; no second/competing timer path"
    - "A new set-alarm skill creates a persisted non-ack reminder whose fire-time message instructs the head to call ring_device(start)"
    - "The set-alarm skill never sets requiresAck / nagMinutes / nagHours / nagDays"
  artifacts:
    - path: "skills/timer/SKILL.md"
      provides: "step 3 ring_device(start) hook (additive)"
      contains: "ring_device"
    - path: "skills/set-alarm/SKILL.md"
      provides: "new non-ack alarm skill"
      contains: "create_reminder"
    - path: "src/skills/ring-skills.test.ts"
      provides: "content tests for both skills"
      min_lines: 30
  key_links:
    - from: "skills/set-alarm/SKILL.md"
      to: "create_reminder"
      via: "fire-time message instructing ring_device(start) source alarm"
      pattern: "ring_device"
---

<objective>
Wire the two scheduling layers onto the (already-built) delivery layer: append a single `ring_device(start)` call to step 3 of the existing `timer` skill (the ONLY timer change), and author a new `set-alarm` skill that creates a persisted NON-ACK reminder whose fire-time message instructs the head to call `ring_device(start)`.

Purpose: Timers (TIMER-01/02) and alarms (ALARM-01/02/03) both reuse the shared ring runner — neither owns the noise. The alarm survives restart via the existing reminder system; non-ack means the continuous ring + 24h cap IS the entire alert.
Output: edited `skills/timer/SKILL.md`, new `skills/set-alarm/SKILL.md`, and `src/skills/ring-skills.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-CONTEXT.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-RESEARCH.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-PATTERNS.md
@.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-04-SUMMARY.md

<interfaces>
<!-- Contracts extracted from codebase. -->

skills/timer/SKILL.md — current step 3 (line 17): "When the sleep returns (exit code 0), the time is up. Finish with a short, friendly completion message ... e.g. `⏰ Your 10-minute timer is up.` Include what it was for if they said one (e.g. \"tea's ready\")."

create_reminder tool (src/sub-agents/registry.ts:922-973) — params the set-alarm skill instructs the agent to use:
  message (required) — delivered to the head at fire time (stored as schedule.agentContext).
  triggerAt — ISO 8601 first/only fire (one-time alarm).
  cron — recurring (allowed cadences listed in the tool description; e.g. weekdays "M H * * 1-5").
  requiresAck (boolean) — MUST be omitted/false for alarms.
  nagMinutes/nagHours/nagDays — MUST be omitted for alarms.

Reminder fire path (src/head/activation.ts:1075-1155): non-ack reminder → `systemTrigger('reminder', undefined, message)` enqueued as user_message → head processes → calls ring_device(start). The message text is the whole lever (Pitfall 6 — phrase it as an explicit tool-call instruction).

ring_device (Plan 04): action 'start'|'stop', optional source ('timer'|'alarm'); no-op on non-HA channels (so the skills are safe everywhere).

Skill structure (AGENTS.md / 45-CONTEXT.md canonical_refs Skills): SKILL.md = YAML frontmatter (name kebab-case, description, optional skill-deps/mcp-capabilities/max-per-month-usd) + markdown instructions. Bundled skills ship in repo `skills/` (timer, scheduling are siblings).

parseSkillFile (src/skills/parser.js) — used by src/skills/skills.test.ts to parse SKILL.md frontmatter+instructions in tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ring_device(start) to timer skill step 3 (additive only)</name>
  <files>skills/timer/SKILL.md</files>
  <read_first>
    - skills/timer/SKILL.md — the file being modified (step 3 at line 17; the rest must remain byte-unchanged)
    - 45-PATTERNS.md § `skills/timer/SKILL.md (MODIFIED)` (pattern map lines 458-470) — exact new step-3 wording
    - 45-CONTEXT.md decisions "Timer" — step 3 appends ring_device(start) ONLY; no second/competing timer path (TIMER-02)
  </read_first>
  <action>
    In `skills/timer/SKILL.md`, modify ONLY step 3: keep the existing sleep/completion flow but insert a `ring_device` call before the completion message — "When the sleep returns (exit code 0), the time is up. Call `ring_device` with `action: \"start\"` and `source: \"timer\"` to start the audible alert on the voice device. Then finish with a short, friendly completion message ... e.g. `⏰ Your 10-minute timer is up.` Include what it was for if they said one (e.g. \"tea's ready\"). If you are not running on a Home Assistant voice channel, ring_device is a safe no-op." Do NOT change the frontmatter, steps 1-2, "Guidance & limits", or introduce any alternate timer mechanism (TIMER-02 — additive only).
  </action>
  <verify>
    <automated>grep -c "ring_device" skills/timer/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ring_device" skills/timer/SKILL.md` returns >= 1 and the match is in step 3 with `action: "start"` and `source: "timer"`
    - `grep -c "sleep" skills/timer/SKILL.md` is unchanged from baseline (steps 1-2 intact) — `git diff skills/timer/SKILL.md` shows changes confined to step 3 only
    - No new `bash`/`sleep`/scheduling mechanism added (TIMER-02): the diff touches only the step-3 line
    - The frontmatter (name/description) is byte-unchanged
  </acceptance_criteria>
  <done>timer skill step 3 calls ring_device(start, source timer) before the completion message; nothing else in the skill changed.</done>
</task>

<task type="auto">
  <name>Task 2: New set-alarm skill (non-ack persisted reminder)</name>
  <files>skills/set-alarm/SKILL.md</files>
  <read_first>
    - skills/timer/SKILL.md (structure) + skills/scheduling/SKILL.md (reminder/create-tool usage style) — the authoring analogs
    - src/sub-agents/registry.ts:922-973 — create_reminder param names/semantics (message, triggerAt, cron, requiresAck, nag*)
    - 45-PATTERNS.md § `skills/set-alarm/SKILL.md (NEW)` (pattern map lines 472-495) AND 45-RESEARCH.md § "set-alarm SKILL.md create_reminder call pattern" (lines 617-642) — frontmatter + steps + the critical fire-time message phrasing
    - 45-RESEARCH.md Pitfall 6 — the message MUST be an explicit tool-call instruction ("You MUST call ring_device …"), not "your alarm is going off"
    - 45-CONTEXT.md decisions "Alarm" — NON-ACK; no nag/escalation; persisted reminder; survives restart
  </read_first>
  <action>
    Create `skills/set-alarm/SKILL.md` with frontmatter `name: set-alarm` and a description like "Set a one-time or recurring alarm that rings the Home Assistant voice device at a specific time. Survives restarts. Use instead of a timer for far-future or recurring alerts." (kebab-case name, no slashes). Author numbered instructions modeled on timer/SKILL.md: (1) parse the requested time → ISO 8601 triggerAt using the workspace timezone; (2) for recurring alarms derive an allowed cron cadence (cite the create_reminder allowed-cadence list); (3) call `create_reminder` with `message` set to an EXPLICIT tool-call instruction — e.g. "Your alarm is going off. You MUST call ring_device with action 'start' and source 'alarm'. After calling it, briefly tell the user their alarm fired." — plus `triggerAt` (one-time) and/or `cron` (recurring); (4) confirm to the user. Add an "Important constraints" section: NEVER set requiresAck:true; NEVER set nagMinutes/nagHours/nagDays; the continuous ring + 24h cap is the alert; the alarm persists across restart. Use `write-file-atomic` semantics is N/A here (this is a repo-committed file, written via the Write tool).
  </action>
  <verify>
    <automated>test -f skills/set-alarm/SKILL.md && grep -c "create_reminder\|ring_device" skills/set-alarm/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/set-alarm/SKILL.md` exists with valid YAML frontmatter parseable by parseSkillFile (name === 'set-alarm', non-empty description)
    - `grep -c "create_reminder" skills/set-alarm/SKILL.md` returns >= 1
    - `grep -c "ring_device" skills/set-alarm/SKILL.md` returns >= 1 with action 'start' and source 'alarm' in the fire-time message
    - `grep -ci "requiresAck\|nagMinutes\|nagHours\|nagDays" skills/set-alarm/SKILL.md` — any occurrences are ONLY in the negative-constraint section instructing NOT to set them (ALARM-03); the create_reminder call example does NOT set them
  </acceptance_criteria>
  <done>set-alarm skill creates a non-ack persisted reminder whose fire-time message explicitly instructs ring_device(start, source alarm); never sets ack/nag fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Skill content tests (src/skills/ring-skills.test.ts)</name>
  <files>src/skills/ring-skills.test.ts</files>
  <read_first>
    - src/skills/skills.test.ts — parseSkillFile usage + assertion style (the test analog)
    - skills/timer/SKILL.md + skills/set-alarm/SKILL.md — the files under test (after Tasks 1-2)
    - 45-VALIDATION.md § Per-Task Verification Map rows TIMER-01/02, ALARM-01/02/03 — the behaviors to assert
  </read_first>
  <behavior>
    - TIMER-01: timer SKILL.md content contains a ring_device(start) call in step 3.
    - TIMER-02: timer SKILL.md still describes the single bash/sleep mechanism (no competing path) — assert it mentions sleep and contains exactly one timer mechanism.
    - ALARM-01: set-alarm SKILL.md parses with parseSkillFile; frontmatter.name === 'set-alarm'; non-empty description.
    - ALARM-02: set-alarm SKILL.md message instruction contains ring_device with action 'start' (string assertion).
    - ALARM-03: set-alarm SKILL.md's create_reminder usage does NOT instruct requiresAck/nag — the skill explicitly forbids them.
  </behavior>
  <action>
    Create `src/skills/ring-skills.test.ts` importing `parseSkillFile` from `./parser.js` and reading both SKILL.md files via `fs.readFileSync` (resolve repo paths from `import.meta.url` or process.cwd()). Assert: (TIMER-01) the timer instructions include 'ring_device' and 'start'; (TIMER-02) the timer instructions include 'sleep' and there is no second timer/scheduling mechanism introduced (e.g. assert the body does not add create_reminder/create_schedule as the timer path); (ALARM-01) parseSkillFile(set-alarm content).frontmatter.name === 'set-alarm' and description is a non-empty string; (ALARM-02) the set-alarm instructions contain 'ring_device' and "action 'start'" (or action: "start"); (ALARM-03) the set-alarm body forbids requiresAck/nag (assert it contains a NEVER/do-not directive for requiresAck and that the create_reminder example block does not set requiresAck:true). Use `.js` import extensions.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/skills/ring-skills.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/skills/ring-skills.test.ts` passes
    - Tests cover all five requirements: TIMER-01 (ring_device in step 3), TIMER-02 (single mechanism), ALARM-01 (frontmatter parse), ALARM-02 (fire message calls ring_device start), ALARM-03 (no ack/nag)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Content tests pin both skills' behavior across TIMER-01/02 and ALARM-01/02/03; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill instructions → LLM behavior | SKILL.md text steers the agent/head toward the correct tool calls |
| persisted alarm reminder (disk) → fire-time head activation | A non-ack reminder survives restart and triggers ring_device(start) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06-COMPLY | (functional, ALARM-02) | fire-time reminder message | mitigate | Pitfall 6: the message is phrased as an explicit "You MUST call ring_device with action 'start'…" tool-call instruction (not "your alarm is going off") so the head invokes the tool rather than just speaking. Pinned by ALARM-02 string test |
| T-45-06-ACK | DoS (unwanted nag loop) | set-alarm reminder | mitigate | ALARM-03: skill NEVER sets requiresAck/nag — no escalation machinery; the continuous ring + 24h cap is the entire alert. Pinned by ALARM-03 test |
| T-45-06-DUP | Tampering (competing path) | timer skill | mitigate | TIMER-02: change confined to step 3 (additive ring call) — no second timer mechanism introduced; git-diff + test assert single mechanism |
| T-45-06-OFFLINE | (accepted trade-off) | alarm fire when device offline | accept | ALARM-03 / CONTEXT: if the device is offline at fire time the ring silently fails — no fallback channel in v1.7 (deferred ALARM-F-01) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/skills/ring-skills.test.ts` green.
- `git diff skills/timer/SKILL.md` confined to step 3.
</verification>

<success_criteria>
- timer skill rings on elapse via a single additive ring_device(start) call; no competing timer path.
- set-alarm creates a persisted non-ack reminder that instructs ring_device(start) at fire time.
- Both behaviors are content-tested.
</success_criteria>

<output>
Create `.planning/phases/45-ring-delivery-layer-timer-ring-alarm/45-06-SUMMARY.md` when done
</output>
