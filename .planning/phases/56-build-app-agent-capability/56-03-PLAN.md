---
phase: 56-build-app-agent-capability
plan: 03
type: execute
wave: 2
depends_on: ["56-02"]
files_modified:
  - skills/build-app/SKILL.md
autonomous: true
requirements: [BUILDAPP-01, BUILDAPP-02, BUILDAPP-03, BUILDAPP-04]

must_haves:
  truths:
    - "SKILL.md frontmatter has name: build-app and a non-empty description and parses cleanly via the skill parser"
    - "SKILL.md instructs the agent (delegated sub-agent POV) to copy the seeded example, pick a SLUG_RE-conforming non-reserved slug, and edit only app.ts + meta.json + app.test.ts"
    - "SKILL.md encodes the verify gate: run `tsx app.test.ts` AND an in-process loadApp serving probe — BOTH must pass before declaring the app live"
    - "SKILL.md encodes update (re-verify after, no confirm) and remove (confirm-before-remove via asking the user, then delete + commit) and lists apps by scanning apps/*/meta.json"
    - "SKILL.md points at the host-served /apps/_skill.md for the full ViewNode catalog (no embedded/duplicated catalog)"
  artifacts:
    - path: "skills/build-app/SKILL.md"
      provides: "The build-app skill guidance (author → smoke-test → done; update; remove)"
      min_lines: 60
      contains: "name: build-app"
  key_links:
    - from: "skills/build-app/SKILL.md"
      to: "$SHROK_WORKSPACE_PATH/apps and skills/build-app/example"
      via: "path-discovery prose (resolve env anchors via bash)"
      pattern: "SHROK_WORKSPACE_PATH"
    - from: "skills/build-app/SKILL.md"
      to: "/apps/_skill.md"
      via: "ViewNode-catalog pointer (D-04)"
      pattern: "_skill.md"
    - from: "skills/build-app/SKILL.md"
      to: "loadApp serving probe + tsx app.test.ts"
      via: "verify-gate prose (D-06/D-07)"
      pattern: "app.test.ts"
---

<objective>
Write `skills/build-app/SKILL.md` — the guidance that turns the seeded golden example into a repeatable author→smoke-test→done→update/remove workflow for a DELEGATED SUB-AGENT (D-02). The example (Plan 02) carries the load-bearing code patterns; this skill carries the WORKFLOW: where to write, how to pick a slug, how to verify with NO auth, how to update/remove safely, and where to find the full ViewNode catalog.

Purpose: A skill (not a new tool) is how every shrok capability ships (D-01); this is the single artifact that makes the host-side machinery usable by a model.
Output: `skills/build-app/SKILL.md` (the `example/` subtree already exists from Plan 02; the whole `skills/build-app/` dir seeds to the workspace via the existing only-if-absent skill seeding — no host code change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-build-app-agent-capability/56-CONTEXT.md
@.planning/phases/56-build-app-agent-capability/56-RESEARCH.md
@.planning/phases/56-build-app-agent-capability/56-PATTERNS.md

<interfaces>
<!-- The facts the prose must state correctly. -->
Skill frontmatter contract (src/skills/parser.ts): `name` (string, required, min 1) + `description` (string, required, min 1); `skill-deps`/`mcp-capabilities`/`npm-deps`/`max-per-month-usd` optional. Invalid frontmatter → the loader silently SKIPS the skill (src/skills/loader.ts:56-59), so it must validate.

Path anchors available in agent bash (all in SAFE_PATH_VARS — resolve via bash before use in file tools):
- `$SHROK_WORKSPACE_PATH` — apps dir is `$SHROK_WORKSPACE_PATH/apps/`; the seeded example is `$SHROK_WORKSPACE_PATH/skills/build-app/example/`.
- `$SHROK_ROOT` — repo root, for running the `loadApp` serving probe via tsx (`$SHROK_ROOT/src/apps/discovery.ts`).
- `$SHROK_SKILLS_DIR` — the skills dir.

Slug rule (src/apps/discovery.ts): `SLUG_RE = /^[a-z0-9][a-z0-9-]*$/`, must NOT start with `_` (reserved for `_pkg`/`_skill.md`).

Host endpoints/loaders the workflow references:
- Full ViewNode catalog: host-served at `/apps/_skill.md` (D-04 — do NOT embed a copy).
- Serving probe (D-07, auth-free): import `loadApp` from `$SHROK_ROOT/src/apps/discovery.ts` via tsx, call `loadApp(appsDir, slug)`, assert `loaded.mod` present + `loaded.mod.get()` returns `{vm,state}`. ⚠️ A bare `curl GET /apps/<slug>/api` is `requireAuth`-gated and 401s — do NOT use it as the gate.
- List apps: scan `apps/*/meta.json` (or the existing `listApps` in discovery.ts).
- Remove confirmation: the sub-agent ends its turn asking the user → suspend-as-question → head surfaces it (no new mechanism; the skill just instructs the agent to ASK before deleting).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write skills/build-app/SKILL.md (author → verify → update/remove)</name>
  <files>skills/build-app/SKILL.md</files>
  <read_first>
    - skills/sensors/SKILL.md (structure + the dense description style that ends with a "Read this when…" trigger; the path-discovery prose that resolves `$SHROK_*` via bash before file tools; the SLUG_RE wording)
    - skills/tasks/SKILL.md (frontmatter + section shape)
    - skills/build-app/example/app.ts AND skills/build-app/example/app.test.ts (the files the prose tells the agent to copy/run — reference their real names, the `APP_DB_PATH` override, and the `tsx app.test.ts` command exactly as they exist)
    - .planning/phases/56-build-app-agent-capability/56-PATTERNS.md (section "skills/build-app/SKILL.md" — frontmatter, path-discovery, slug-rule, and the full workflow prose to encode)
    - .planning/phases/56-build-app-agent-capability/56-RESEARCH.md (Pitfalls 2 + 4: the auth-gated curl trap and slug-collision handling; the serving-probe Pattern 3; the D-12 remove-confirmation flow)
    - .planning/phases/56-build-app-agent-capability/56-CONTEXT.md (D-01..D-12 — the locked workflow this prose encodes)
  </read_first>
  <action>
    Write `skills/build-app/SKILL.md` for the delegated sub-agent's POV (the head delegates "build me an app"; the sub-agent authors + verifies + reports "live at /apps/<slug>/"). It must read sensibly if a head with agent-tools runs it directly, but default framing is delegation. Include, as markdown sections:
    (1) FRONTMATTER — YAML with `name: build-app` and a single dense `description` sentence that lists what's covered (author, smoke-test, update, remove a VMS app served at /apps/<slug>/) and ends with an explicit "Read this when the user asks for a small custom app/tool/dashboard." trigger clause. Both fields non-empty.
    (2) WHAT AN APP IS / WHAT YOU AUTHOR — the agent writes ONLY `app.ts` + `meta.json` (+ the adapted `app.test.ts`); the host owns the HTML shell, `/_pkg`, routing, the createAction↔Express adapter, discovery, and per-app error isolation. Apps live at `$SHROK_WORKSPACE_PATH/apps/<slug>/`, each backed by its own co-located `data.sqlite`.
    (3) PATH DISCOVERY — resolve `$SHROK_WORKSPACE_PATH`, `$SHROK_ROOT`, `$SHROK_SKILLS_DIR` via bash before using in file tools; the seeded example is at `$SHROK_WORKSPACE_PATH/skills/build-app/example/`.
    (4) SLUG RULES + COLLISION — the slug must pass `^[a-z0-9][a-z0-9-]*$` and not start with `_`. BEFORE authoring, list existing apps (scan `apps/*/meta.json` / `ls $SHROK_WORKSPACE_PATH/apps/`); if the slug already exists, ASK the user whether to UPDATE that app or pick a new slug — never silently suffix or overwrite (Pitfall 4 / D-09).
    (5) AUTHORING WORKFLOW — copy the seeded `example/{app.ts, meta.json, app.test.ts}` into `apps/<slug>/`, rename/edit the slug + meta, change the store schema + `get()`/`action`/views to fit the ask. Keep `journal_mode=DELETE` and the `APP_DB_PATH` override (do NOT switch to WAL — it breaks git consistency). For anything beyond the example's shape (tables, modals, forms, tabs, charts), READ the host-served `/apps/_skill.md` for the full ViewNode catalog — do NOT guess and do NOT expect an embedded catalog here.
    (6) VERIFY GATE (both required before "done", D-06/D-07) — run the in-process test `tsx app.test.ts` from the app dir (logic gate), AND run the in-process `loadApp` serving probe via tsx against `$SHROK_ROOT/src/apps/discovery.ts` (serving gate — proves discovery + symlink + dynamic import; reproduces the `{ok:true,...get()}` payload). Explicitly warn that a bare `curl GET /apps/<slug>/api` 401s (auth-gated) and must NOT be the gate. On failure, iterate (fix → re-verify) before reporting.
    (7) UPDATE — edit app.ts/meta/test, then RE-RUN both gates (D-08); no user confirmation needed (revertable via the workspace git repo).
    (8) REMOVE — CONFIRM with the user first (end the turn asking, e.g. "Delete app <slug>? it's git-recoverable — yes/no"); only after a yes, delete the app folder and let the run commit the removal as a clean revert point (D-12). Note the workspace git repo makes any app code+data.sqlite recoverable.
    (9) REPORT — tell the head/user the app is live at `/apps/<slug>/`.
    Keep the prose tight and directive; do NOT inline a full ViewNode reference (point at `/apps/_skill.md`).
  </action>
  <acceptance_criteria>
    - `skills/build-app/SKILL.md` frontmatter contains `name: build-app` and a non-empty `description`.
    - The body contains the literal anchors: `SHROK_WORKSPACE_PATH`, `/apps/_skill.md`, `app.test.ts`, `loadApp`, and the slug regex `^[a-z0-9][a-z0-9-]*$`.
    - The body states that a bare `curl`/HTTP GET of `/apps/<slug>/api` is auth-gated (401) and is NOT the serving gate.
    - The body has distinct AUTHOR, VERIFY, UPDATE, and REMOVE sections, and REMOVE instructs asking the user before deleting.
    - The body does NOT embed a full ViewNode catalog (it points at `/apps/_skill.md` instead).
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/build-app/SKILL.md && grep -q 'name: build-app' skills/build-app/SKILL.md && grep -q '/apps/_skill.md' skills/build-app/SKILL.md && grep -q 'loadApp' skills/build-app/SKILL.md && grep -q 'app.test.ts' skills/build-app/SKILL.md</automated>
  </verify>
  <done>SKILL.md exists with valid frontmatter and AUTHOR/VERIFY/UPDATE/REMOVE workflow prose, the auth-gate warning, the loadApp serving probe, and the `/apps/_skill.md` catalog pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the skill parses + content audit</name>
  <files>skills/build-app/SKILL.md</files>
  <read_first>
    - src/skills/parser.ts (parseSkillFile throws on invalid frontmatter; FrontmatterSchema requires name+description)
    - skills/build-app/SKILL.md (the file under validation — do not modify it in this task unless the parse fails)
  </read_first>
  <action>
    Confirm the skill is loadable and complete. Run a tsx one-liner that reads `skills/build-app/SKILL.md`, calls `parseSkillFile(content)` from `src/skills/parser.ts`, and asserts the returned `frontmatter.name === 'build-app'` and a non-empty `frontmatter.description` (parseSkillFile THROWS if the frontmatter is invalid, so a non-throwing run that prints the name is the pass signal). Then run a grep-based content audit asserting the required workflow anchors are present (the same anchors as the Task-1 acceptance set). If either fails, fix SKILL.md and re-run. This task is the automated gate for an otherwise prose-only deliverable.
  </action>
  <acceptance_criteria>
    - A `tsx` invocation of `parseSkillFile` on `skills/build-app/SKILL.md` exits 0 and prints `build-app` (does not throw).
    - `grep -q` succeeds for each anchor: `SHROK_WORKSPACE_PATH`, `/apps/_skill.md`, `loadApp`, `app.test.ts`.
    - `npx tsc --noEmit` exits 0 (remove any temp validation script before finishing so no stray file remains).
  </acceptance_criteria>
  <verify>
    <automated>npx tsx -e "import {parseSkillFile} from './src/skills/parser.ts'; import {readFileSync} from 'node:fs'; const p=parseSkillFile(readFileSync('skills/build-app/SKILL.md','utf8')); if(p.frontmatter.name!=='build-app'||!p.frontmatter.description) process.exit(1); console.log(p.frontmatter.name)"</automated>
  </verify>
  <done>parseSkillFile loads the skill without throwing (valid frontmatter, name build-app), and all workflow anchors are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md guidance → agent actions | The prose steers the agent through host machinery (slug guard, auth-free probe) rather than around it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-SLUG | Tampering | slug selection in SKILL.md | mitigate | The prose instructs slugs that pass `SLUG_RE` and never start with `_`; the host `loadApp`/`listApps` enforce it regardless, so the skill cannot bypass path-traversal protection (it directs the agent to conform, not circumvent). |
| T-56-02-COLLISION | Tampering | slug-collision overwrite | mitigate | The prose mandates listing existing apps first and ASKING the user on a collision (never silent suffix/overwrite), preventing clobber of another app's `app.ts`/`data.sqlite`. |
| T-56-03-PROBE | Elevation of privilege | verification guidance | mitigate | The skill mandates the in-process `loadApp` probe and explicitly forbids relying on a new/auth-bypassing route; it warns the auth-gated curl 401s — no auth carve-out is introduced. |
| T-56-06-REMOVE | Denial of service / data loss | remove workflow | mitigate | Confirm-before-remove (ask the user) + commit-the-removal makes deletion a recoverable, user-approved action (D-12); the workspace git repo is the safety net. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs (skill prose only); supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsx -e "...parseSkillFile..."` prints `build-app` / exits 0 (frontmatter valid → loader will not skip the skill).
- All content anchors present (`grep -q` for SHROK_WORKSPACE_PATH, /apps/_skill.md, loadApp, app.test.ts).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- `skills/build-app/SKILL.md` is a valid, loadable skill (name build-app + description) written for the delegated sub-agent.
- It encodes the full author → verify (test + loadApp probe) → update/remove workflow with the auth-gate warning, slug-collision handling, confirm-before-remove, and the `/apps/_skill.md` ViewNode-catalog pointer.
</success_criteria>

<output>
Create `.planning/phases/56-build-app-agent-capability/56-03-SUMMARY.md` when done.
</output>
