---
phase: 56-build-app-agent-capability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workspace/git.ts
  - src/workspace/git.test.ts
autonomous: true
requirements: [BUILDAPP-04]

must_haves:
  truths:
    - "Creating an app folder under workspace apps/<slug>/ results in app.ts + meta.json + data.sqlite being git-tracked by the workspace recovery repo"
    - "apps/*/data.sqlite-wal, -shm and -journal are never tracked"
    - "An existing workspace whose .gitignore equals the prior pre-apps WORKSPACE_GITIGNORE is auto-upgraded to the new constant (gains !/apps/)"
    - "A user-customized .gitignore is left untouched by the migration"
  artifacts:
    - path: "src/workspace/git.ts"
      provides: "WORKSPACE_GITIGNORE with !/apps/ allowlist + data.sqlite-{wal,shm,journal} exclusions; PRIOR_KNOWN_GITIGNORES-driven migration"
      contains: "!/apps/"
    - path: "src/workspace/git.test.ts"
      provides: "Unit coverage that the prior constant upgrades and a custom ignore is untouched"
      contains: "PRIOR_KNOWN_GITIGNORES"
  key_links:
    - from: "src/workspace/git.ts WORKSPACE_GITIGNORE"
      to: "apps/ tracked, -wal/-shm/-journal excluded"
      via: "allowlist rule + exclusion block"
      pattern: "!/apps/"
    - from: "src/workspace/git.ts ensureWorkspaceRepo migration guard"
      to: "prior pre-apps constant auto-upgrades"
      via: "PRIOR_KNOWN_GITIGNORES membership check"
      pattern: "PRIOR_KNOWN_GITIGNORES"
---

<objective>
Add `apps/` to the workspace git recovery allowlist so agent-authored apps (code + `data.sqlite`) ride the existing auto-commit, while keeping transient sqlite sidecars out — and extend the `.gitignore` migration so EXISTING installs actually upgrade (the live workspace `.gitignore` is byte-identical to the current pre-apps constant, which the existing migration guard skips). Implements D-10 + D-11.

Purpose: Make app create/update/remove recoverable/revertable via the workspace repo (the BUILDAPP-04 safety story) — without it, `!/apps/` added to the constant alone SILENTLY no-ops on every existing workspace.
Output: Modified `src/workspace/git.ts` + extended `src/workspace/git.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-build-app-agent-capability/56-CONTEXT.md
@.planning/phases/56-build-app-agent-capability/56-RESEARCH.md
@.planning/phases/56-build-app-agent-capability/56-PATTERNS.md

<interfaces>
<!-- The exact symbols this plan modifies (read the files for full bodies). -->
From src/workspace/git.ts:
- `export const WORKSPACE_GITIGNORE: string` — the current pre-apps allowlist (allowlist block `!/.env … !/topics/`; exclusion block `skills/*/node_modules/ … skills/*/*.pyc`; OS-junk block).
- `const LEGACY_SEEDED_GITIGNORE = '*.tmp\n.DS_Store\n'` — the only prior version the migration currently recognizes.
- `export function ensureWorkspaceRepo(workspacePath: string): void` — fresh-init at line ~82; migration guard at line ~95 (`if (existing === WORKSPACE_GITIGNORE) return`) and line ~100 (`if (existing !== '' && existing !== LEGACY_SEEDED_GITIGNORE) return`); re-stage block at ~106-114.
- `export function commitWorkspace(...)` — UNCHANGED by this plan (D-11 consistency is handled in the example store's journal mode, Plan 02 — NOT here).
From src/workspace/git.test.ts:
- helper `isTracked(ws, rel): boolean` (~line 21), `git(ws, args)` (~line 13); existing migration tests `'migrates legacy 2-line .gitignore…'` (~line 100) and `'does NOT touch a user-customized .gitignore'` (~line 127).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add apps/ allowlist + generalize the .gitignore migration</name>
  <files>src/workspace/git.ts</files>
  <read_first>
    - src/workspace/git.ts (FULL file — the constant, LEGACY_SEEDED_GITIGNORE, and ensureWorkspaceRepo migration guard you are editing)
    - .planning/phases/56-build-app-agent-capability/56-PATTERNS.md (section "src/workspace/git.ts (MODIFY)" — the exact 3-part change: allowlist line, exclusion lines, PRIOR_KNOWN_GITIGNORES guard generalization)
    - .planning/phases/56-build-app-agent-capability/56-RESEARCH.md (Pitfall 1 + Runtime State Inventory — WHY the live file skips the current guard)
  </read_first>
  <action>
    Make three edits to src/workspace/git.ts, mirroring the existing LEGACY_SEEDED_GITIGNORE migration pattern in the same file:
    (1) In the WORKSPACE_GITIGNORE constant: add an `!/apps/` allowlist line in the allowlist block (after `!/topics/`), and in the "Within allowlisted dirs, still exclude … runtime artifacts" block add three exclusions — `apps/*/data.sqlite-wal`, `apps/*/data.sqlite-shm`, `apps/*/data.sqlite-journal` — mirroring the existing `skills/*/…` exclusion lines. App CODE and `data.sqlite` ARE tracked (D-11); only the transient sqlite sidecars are excluded.
    (2) BEFORE editing the constant's text, capture the CURRENT (pre-apps) constant string VERBATIM into a new `const PRIOR_KNOWN_GITIGNORES: string[]` that also includes `LEGACY_SEEDED_GITIGNORE`. This snapshot is what makes existing installs migrate — it must be byte-identical to today's shipped constant (copy it from the file as it is now, then add `!/apps/` to the LIVE constant only).
    (3) Replace the migration guard `if (existing !== '' && existing !== LEGACY_SEEDED_GITIGNORE) return` with a check against the set: return (leave alone) only when `existing` is non-empty AND not a member of PRIOR_KNOWN_GITIGNORES. Keep the existing `if (existing === WORKSPACE_GITIGNORE) return` already-current short-circuit and the `git rm --cached … / git add .` re-stage block unchanged — that block already re-adds `apps/`.
    Do NOT touch commitWorkspace (D-11 consistency lives in Plan 02's example store, not the commit chokepoint).
  </action>
  <acceptance_criteria>
    - `src/workspace/git.ts` contains the literal line `!/apps/` inside the WORKSPACE_GITIGNORE allowlist block.
    - `src/workspace/git.ts` contains `apps/*/data.sqlite-wal`, `apps/*/data.sqlite-shm`, and `apps/*/data.sqlite-journal` in the exclusion block.
    - `src/workspace/git.ts` declares `const PRIOR_KNOWN_GITIGNORES` whose array includes `LEGACY_SEEDED_GITIGNORE` and a verbatim snapshot of the pre-apps constant (the snapshot does NOT contain `!/apps/`).
    - The migration guard references `PRIOR_KNOWN_GITIGNORES` (the bare `existing !== LEGACY_SEEDED_GITIGNORE` guard is gone).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>git.ts has the `!/apps/` allowlist, the three sidecar exclusions, a PRIOR_KNOWN_GITIGNORES set containing the pre-apps snapshot, and a migration guard keyed on that set; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend git.test.ts — prior-constant upgrade + custom-ignore-untouched</name>
  <files>src/workspace/git.test.ts</files>
  <behavior>
    - Test A (prior-constant upgrades): a workspace whose on-disk `.gitignore` equals the PRIOR pre-apps constant snapshot, after `ensureWorkspaceRepo`, has `.gitignore === WORKSPACE_GITIGNORE`; and after writing `apps/demo/{app.ts,data.sqlite,data.sqlite-wal}` + `commitWorkspace`, `isTracked(ws,'apps/demo/app.ts')` and `isTracked(ws,'apps/demo/data.sqlite')` are true while `isTracked(ws,'apps/demo/data.sqlite-wal')` is false.
    - Test B (custom untouched): the existing `'does NOT touch a user-customized .gitignore'` assertion still holds (a custom ignore string is preserved verbatim) — PRIOR_KNOWN_GITIGNORES must not over-match.
  </behavior>
  <read_first>
    - src/workspace/git.test.ts (FULL — reuse the `isTracked` helper ~line 21, the `git init`→write-ignore→`ensureWorkspaceRepo` idiom of the existing migration tests ~line 100-137, and the `apps/`-write idiom from the fresh-init tracked-set test ~line 50-77)
    - src/workspace/git.ts (to obtain the exact pre-apps snapshot string to write to disk in Test A — it is the entry you added to PRIOR_KNOWN_GITIGNORES in Task 1)
  </read_first>
  <action>
    Add two `it(...)` cases to the `ensureWorkspaceRepo` describe block, copying the structure of the existing `'migrates legacy 2-line .gitignore'` and `'does NOT touch a user-customized .gitignore'` tests. Test A writes the pre-apps snapshot as the on-disk `.gitignore` of a `git init`'d temp workspace, runs `ensureWorkspaceRepo`, asserts the upgrade to `WORKSPACE_GITIGNORE`, then creates `apps/demo/app.ts`, `apps/demo/data.sqlite`, and `apps/demo/data.sqlite-wal`, calls `commitWorkspace(ws,'add app')`, and asserts the tracked/untracked set via `isTracked`. Test B may reuse/keep the existing custom-ignore test verbatim (confirm it still passes after the guard generalization). Import the snapshot by reusing the same literal you placed in PRIOR_KNOWN_GITIGNORES (you may export it from git.ts if cleaner, or inline the same string).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/workspace/git.test.ts` exits 0 with the two new/affected cases green.
    - A failing-first check is possible: reverting Task 1's guard change (back to `existing !== LEGACY_SEEDED_GITIGNORE`) makes Test A FAIL (proves the test actually exercises the new migration path).
    - `src/workspace/git.test.ts` asserts `isTracked(ws,'apps/demo/data.sqlite')` is `true` and `isTracked(ws,'apps/demo/data.sqlite-wal')` is `false`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/workspace/git.test.ts</automated>
  </verify>
  <done>Two cases prove the prior pre-apps constant upgrades (apps code+data.sqlite tracked, -wal excluded) and a custom ignore is untouched; the git.test.ts suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent bash → workspace filesystem | The agent writes app folders into the workspace; the git repo is a local-only recovery layer (never pushed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-IGNORE | Tampering | `WORKSPACE_GITIGNORE` migration | mitigate | The migration only auto-rewrites a `.gitignore` that is empty or matches a known prior snapshot (`PRIOR_KNOWN_GITIGNORES`); a user-customized ignore is left alone (asserted by Test B), so the change cannot silently clobber user intent. |
| T-56-04-SECRET | Information disclosure | `apps/*/data.sqlite` now tracked | accept | The workspace repo is local-only and never leaves the machine (`src/workspace/git.ts` header) — same posture as existing tracked `identity/`/`skills/`. No high-severity exposure; tracking user data is the intended D-11 recovery value. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new dependency); supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/workspace/git.test.ts` green (incl. the two prior-constant/custom-ignore cases).
- Sanity: `grep -n '!/apps/' src/workspace/git.ts` returns the allowlist line.
</verification>

<success_criteria>
- `apps/` is allowlisted in `WORKSPACE_GITIGNORE`; `data.sqlite-{wal,shm,journal}` excluded.
- The migration upgrades a workspace on the prior pre-apps constant (existing installs migrate) and leaves a customized ignore untouched.
- All host typecheck + git unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/56-build-app-agent-capability/56-01-SUMMARY.md` when done.
</output>
