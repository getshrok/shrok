---
phase: 56-build-app-agent-capability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/build-app/example/app.ts
  - skills/build-app/example/meta.json
  - skills/build-app/example/app.test.ts
  - src/apps/build-app-example.test.ts
autonomous: true
requirements: [BUILDAPP-01, BUILDAPP-02, BUILDAPP-03]

must_haves:
  truths:
    - "The golden example app loads via the host loadApp and get() returns a {vm, state} payload"
    - "Each example action returns ok (not ok:false) when dispatched in-process against a temp DB"
    - "An example DB write lands in data.sqlite itself (not stranded in a -wal sidecar) because the store runs journal_mode=DELETE"
    - "The example is shipped code-only — no committed data.sqlite"
  artifacts:
    - path: "skills/build-app/example/app.ts"
      provides: "Single-file node:sqlite VMS app: meta + get() + createAction-wrapped action; overridable DB path; journal_mode=DELETE"
      min_lines: 40
      contains: "journal_mode=DELETE"
    - path: "skills/build-app/example/meta.json"
      provides: "title/icon/desc record read by readMeta"
      contains: "title"
    - path: "skills/build-app/example/app.test.ts"
      provides: "Standalone tsx in-process test: temp APP_DB_PATH, get() + each action asserted"
      contains: "APP_DB_PATH"
    - path: "src/apps/build-app-example.test.ts"
      provides: "Vitest guard: shipped example loads via loadApp, actions pass, write lands in main file (DELETE consistency)"
      contains: "loadApp"
  key_links:
    - from: "skills/build-app/example/app.ts"
      to: "@ashley-shrok/viewmodel-shell/server"
      via: "import createAction/UnknownActionError"
      pattern: "createAction"
    - from: "skills/build-app/example/app.ts store"
      to: "temp sqlite in tests"
      via: "process.env.APP_DB_PATH override"
      pattern: "APP_DB_PATH"
    - from: "skills/build-app/example/app.test.ts"
      to: "./app.ts"
      via: "set APP_DB_PATH BEFORE dynamic import"
      pattern: "process.env.APP_DB_PATH"
    - from: "src/apps/build-app-example.test.ts"
      to: "loadApp(appsDir, slug)"
      via: "production discovery loader"
      pattern: "loadApp"
---

<objective>
Ship the golden, runnable example VMS app the agent copies-and-adapts (D-03), plus the two test artifacts that keep it honest: the example's own standalone in-process `app.test.ts` (the template's verification harness, D-06) and a vitest CI guard that the SHIPPED example loads via the real host `loadApp` and is DELETE-mode consistent (Wave-0 gaps).

Purpose: A Sonnet-class model authors a working app far more reliably by copying a complete reference than by assembling fragments; the CI guard ensures a future edit can't silently ship a broken template.
Output: `skills/build-app/example/{app.ts, meta.json, app.test.ts}` + `src/apps/build-app-example.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-build-app-agent-capability/56-CONTEXT.md
@.planning/phases/56-build-app-agent-capability/56-RESEARCH.md
@.planning/phases/56-build-app-agent-capability/56-PATTERNS.md
@.planning/phases/55-app-serving-subsystem/55-CONTEXT.md

<interfaces>
<!-- Contracts the example MUST satisfy and the test fixtures import. Read source for full bodies. -->
From @ashley-shrok/viewmodel-shell/server (the server export — import shape is load-bearing, resolves via workspace symlink):
- `createAction<State>(handler: (payload: { name: string; state?: State }) => Promise<ShellResponseBody<State>>): (req: Request) => Promise<Response>`
- `UnknownActionError` (throw for unrecognized action names; re-thrown by the host)
- types `ShellResponseBody<State> = { vm: ViewNode; state: State }`, `ViewNode`

From src/apps/discovery.ts (the host loader the CI guard + serving probe use):
- `export type AppMod = { meta?: {title?;icon?;desc?}; get: () => {vm:unknown;state:unknown}; action: (req:Request)=>Promise<Response> }`
- `export async function loadApp(appsDir: string, slug: string): Promise<Loaded | undefined>` — returns `{slug, meta, mod}` on success or `{slug, meta, error}` on failure; NEVER throws.
- `export const SLUG_RE = /^[a-z0-9][a-z0-9-]*$/`

From src/apps/db.ts (canonical init body — DO NOT IMPORT; apps inline their own store):
- `new DatabaseSync(path)` + `PRAGMA` lines (host helper uses WAL; the example uses DELETE per D-11).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the golden example app.ts + meta.json</name>
  <files>skills/build-app/example/app.ts, skills/build-app/example/meta.json</files>
  <behavior>
    - `get()` returns `{ vm, state }` with a non-null `vm` and an initial state.
    - Each fixed action dispatched with `{name, state}` returns `ok !== false` and the expected state delta (e.g. an add action inserts a row; a per-row `del-<id>` deletes it).
    - An unrecognized action name causes the handler to throw `UnknownActionError`.
    - A validation failure (e.g. empty required field) is caught and surfaced as `state.error`, not a throw.
    - With `process.env.APP_DB_PATH` set, the store opens that path instead of the co-located `data.sqlite`.
  </behavior>
  <read_first>
    - src/apps/integration.test.ts (lines ~54-118 — the counter + notes2 `.mjs` fixtures: the canonical minimal single-file `node:sqlite` app, the `new URL('./data.sqlite', import.meta.url).pathname` co-location, the `get()`/`createAction` shape)
    - /home/thenasty/vms-apps/apps/shopping/controller.ts (the most complete in-repo `createAction`/`render()`/`get()` envelope: per-row identity parsed from `payload.name`, `UnknownActionError` re-thrown, other errors → `state.error`)
    - src/apps/db.ts (the canonical DatabaseSync + PRAGMA init body to inline — NOTE its header marks it host/test-only; do NOT import it)
    - .planning/phases/56-build-app-agent-capability/56-PATTERNS.md (section "skills/build-app/example/app.ts" — the required deltas vs the counter fixture)
    - .planning/phases/56-build-app-agent-capability/56-RESEARCH.md (Pattern 1 + Anti-Patterns)
  </read_first>
  <action>
    Write `skills/build-app/example/app.ts` as ONE self-contained module exporting `meta`, `get()`, and a `createAction`-wrapped `action`. Pick a small illustrative CRUD domain (e.g. notes: id/title/body/timestamps). Import shape MUST be exactly `import { createAction, UnknownActionError, type ShellResponseBody, type ViewNode } from "@ashley-shrok/viewmodel-shell/server"` and `import { DatabaseSync } from "node:sqlite"`. Inline the store (DO NOT import `appDb`/`src/apps/db.ts`): resolve the DB path as `process.env.APP_DB_PATH ?? new URL("./data.sqlite", import.meta.url).pathname` (D-05 override), open with `new DatabaseSync(dbPath)`, then run `PRAGMA journal_mode=DELETE` (D-11 — NOT WAL), `PRAGMA foreign_keys=ON`, and a `CREATE TABLE IF NOT EXISTS`. Build a small `buildVm(state) → ViewNode` (a page with a list + an add form/modal is enough; keep it minimal — the ViewNode catalog lives at `/apps/_skill.md`), a `render(state) → { vm, state }`, `export function get()`, and `export const action = createAction<State>(...)` following the shopping-controller envelope: read `payload.state ?? init()`, clear `error`, handle per-row actions by parsing identity out of `payload.name` (e.g. `del-<id>`), handle fixed actions in a `switch`, `throw new UnknownActionError(name)` in the default, and in the `catch` re-throw `UnknownActionError` but convert any other error to `state.error`. Write `meta.json` as a tiny `{ "title", "icon", "desc" }` record. SHIP CODE ONLY — do not create or commit a `data.sqlite` (the app creates it on first run); ensure no `index.html`/frontend file is authored (host owns the shell).
  </action>
  <acceptance_criteria>
    - `skills/build-app/example/app.ts` contains the exact import `from "@ashley-shrok/viewmodel-shell/server"` and `from "node:sqlite"`.
    - `skills/build-app/example/app.ts` contains `process.env.APP_DB_PATH` and the literal `journal_mode=DELETE`; it does NOT contain `journal_mode=WAL` and does NOT import `appDb` or `src/apps/db`.
    - `skills/build-app/example/app.ts` exports `meta`, `get`, and `action` (the three `AppMod` contract members; `get`/`action` required).
    - `skills/build-app/example/meta.json` parses as JSON and has a `title` key.
    - No `skills/build-app/example/data.sqlite` and no `index.html` file exist in the example dir.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'journal_mode=DELETE' skills/build-app/example/app.ts && ! test -e skills/build-app/example/data.sqlite</automated>
  </verify>
  <done>A single-file node:sqlite VMS app exists with the exact server-export import, an APP_DB_PATH override, journal_mode=DELETE, the get()+createAction contract, and a tiny meta.json — code only, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author the example's standalone in-process app.test.ts (the template harness)</name>
  <files>skills/build-app/example/app.test.ts</files>
  <read_first>
    - skills/build-app/example/app.ts (the module under test — its action names + state shape determine the dispatch calls)
    - src/apps/integration.test.ts (lines ~233-246 — the `{name, state}` POST round-trip shape and the asserted `ok`/`vm`/`state` keys this harness reproduces in-process)
    - .planning/phases/56-build-app-agent-capability/56-PATTERNS.md (section "skills/build-app/example/app.test.ts" — temp-DB-before-import + dispatch harness)
    - .planning/phases/56-build-app-agent-capability/56-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
    Write `skills/build-app/example/app.test.ts` as a STANDALONE tsx script (not a vitest file — it must run as `tsx app.test.ts`, exit 0 on pass, `process.exit(1)` on fail). First set `process.env.APP_DB_PATH` to a fresh `fs.mkdtempSync(...)`-based temp sqlite path BEFORE `const app = await import("./app.ts")` (so the module's top-level DatabaseSync init hits the temp path — D-05). Define a small `assert(cond, msg)` helper and a `dispatch(name, state)` that calls `app.action(new Request("http://x/api/action", { method:"POST", headers:{"content-type":"application/json"}, body: JSON.stringify({ name, state }) }))` then `res.json()`. Assert `app.get()` returns truthy `vm` + `state`; then dispatch EACH action the app supports and assert `ok !== false` plus the expected state delta (e.g. add → row present; del → row gone; unknown-action handling per the app's design). End with `console.log("PASS")`. This file is the permanent per-app verification artifact the SKILL tells the agent to run and keep.
  </action>
  <acceptance_criteria>
    - `skills/build-app/example/app.test.ts` sets `process.env.APP_DB_PATH` BEFORE the `await import("./app.ts")` line.
    - `cd skills/build-app/example && npx tsx app.test.ts` exits 0 and prints `PASS`.
    - The test dispatches at least the example's add + delete (or equivalent) actions and asserts a state delta, not just `get()`.
    - The test uses `process.exit(1)` on failure (standalone, not vitest).
  </acceptance_criteria>
  <verify>
    <automated>cd skills/build-app/example && npx tsx app.test.ts</automated>
  </verify>
  <done>A standalone tsx test drives get() + each action in-process against a temp DB and passes; it is the harness the build-app workflow re-runs after every change.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest CI guard — shipped example loads via loadApp + DELETE consistency</name>
  <files>src/apps/build-app-example.test.ts</files>
  <read_first>
    - src/apps/integration.test.ts (whole-file pattern: temp workspace under os.tmpdir, the `ensurePackageSymlink`/router setup that makes `@ashley-shrok/viewmodel-shell/server` resolve from a temp `apps/` dir, the `loadApp` import + the POST/dispatch harness, beforeEach/afterEach temp-dir isolation)
    - src/apps/discovery.ts (lines ~83-147 — `loadApp` returns `{mod}` on success or `{error}`; SLUG_RE; per-app cache keyed by absolute folder path so a fresh temp dir is isolated)
    - src/apps/workspace.ts (`ensurePackageSymlink` — the symlink that lets a copied .ts example resolve the VMS package)
    - skills/build-app/example/app.ts (the example being copied into the fixture + its action names)
    - .planning/phases/56-build-app-agent-capability/56-PATTERNS.md (section "src/apps/build-app-example.test.ts")
    - .planning/phases/56-build-app-agent-capability/56-VALIDATION.md (Wave 0 items — the golden-example-loads test AND the DELETE consistency test)
  </read_first>
  <action>
    Write `src/apps/build-app-example.test.ts` (vitest). In a per-test temp workspace dir: ensure the workspace VMS package symlink exists (call `ensurePackageSymlink` as the integration test does, OR construct the apps router once, so `@ashley-shrok/viewmodel-shell/server` resolves from the copied example); copy the repo's `skills/build-app/example/{app.ts, meta.json}` into `<tmp>/apps/<slug>/` (use a SLUG_RE-conforming slug such as `example-app`); set `process.env.APP_DB_PATH` to a temp sqlite under that dir BEFORE any load so the module init uses it. Then:
    (a) GOLDEN-LOADS: `const loaded = await loadApp(appsDir, slug)`; assert `loaded` is defined, `loaded.error` is undefined, `loaded.mod` is defined, and `loaded.mod.get()` returns truthy `vm` + `state` (this reproduces exactly what `GET /apps/<slug>/api` returns).
    (b) ACTIONS: drive each example action via `loaded.mod.action(new Request(..., {name,state}))` (same POST shape as the integration test) and assert `ok !== false` + a state delta.
    (c) DELETE-CONSISTENCY (D-11): after an action that writes a row, open a SECOND fresh `DatabaseSync` against the SAME `APP_DB_PATH` and assert the row is readable — proving the write landed in the main file under journal_mode=DELETE (not stranded in a `-wal`). Optionally assert no `data.sqlite-wal` sidecar exists for that path.
    Use beforeEach/afterEach to create/clean the temp dir and unset/reset `process.env.APP_DB_PATH`, mirroring the integration test's isolation.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/apps/build-app-example.test.ts` exits 0.
    - The test calls `loadApp(appsDir, slug)` and asserts the SUCCESS branch (`loaded.mod` present, `loaded.error` absent) and `loaded.mod.get()` returns `{vm, state}`.
    - The test contains a second `DatabaseSync` re-open asserting a just-written row is present in the main DB file (DELETE consistency).
    - The test copies from the repo's actual `skills/build-app/example/` (so a future broken edit to the example fails this test).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/apps/build-app-example.test.ts</automated>
  </verify>
  <done>A vitest guard copies the shipped example into a temp workspace, proves it loads via the production `loadApp`, drives its actions, and confirms a write is consistent in `data.sqlite` under DELETE mode — so a broken example edit can never ship silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app module → its own sqlite | Each app opens its OWN co-located DB; no shared host DB import. |
| copied example → VMS package | The example resolves `@ashley-shrok/viewmodel-shell/server` via the workspace symlink, not a relative path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-PROBE | Elevation of privilege | verification path | mitigate | Verification uses the in-process `loadApp` probe + a standalone tsx test — NO new HTTP route and NO auth carve-out, preserving the Phase-55 `requireAuth` boundary (D-08). The example's test never hits an authenticated endpoint. |
| T-56-05-DBHELPER | Tampering | app store isolation | mitigate | The example inlines its own `DatabaseSync` store and must NOT import the host `src/apps/db.ts` (host/test-only); enforced by the Task-1 acceptance grep, keeping per-app DB isolation (APPSRV-05). |
| T-56-04-SECRET | Information disclosure | example ships code-only | mitigate | The example carries no committed `data.sqlite`; DBs are born in the app folder on first run, so no fixture data is shipped. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs (no new dependency); supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `cd skills/build-app/example && npx tsx app.test.ts` prints PASS / exits 0.
- `npx vitest run src/apps/build-app-example.test.ts` green.
- Sanity: `grep -q journal_mode=DELETE skills/build-app/example/app.ts`; `! test -e skills/build-app/example/data.sqlite`.
</verification>

<success_criteria>
- A single-file `node:sqlite` golden example exists with the exact server-export import, an `APP_DB_PATH` override, and `journal_mode=DELETE`.
- The example's standalone `app.test.ts` passes via tsx (the per-app verification harness).
- The vitest guard proves the SHIPPED example loads through the production `loadApp` and is DELETE-consistent.
</success_criteria>

<output>
Create `.planning/phases/56-build-app-agent-capability/56-02-SUMMARY.md` when done.
</output>
