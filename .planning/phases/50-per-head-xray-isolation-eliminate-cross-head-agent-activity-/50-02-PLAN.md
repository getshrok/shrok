---
phase: 50
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/db/agents.ts
  - src/head/activation.ts
  - src/sub-agents/local.ts
  - src/index.ts
  - src/dashboard/events.ts
  - dashboard/src/types/api.ts
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "Every agent_message_added SSE event carries the headId of the agent that produced it (top-level). [D-01: backend/live-stream half of the leak closure]"
    - "Every agent_status_changed SSE event carries the headId of the affected agent (top-level)."
    - "Every memory_retrieval SSE event carries the headId of the activation loop that retrieved it (top-level)."
    - "Every steward_run_added SSE event carries headId via the StewardRun payload (payload.headId, head-attributed at append by Plan 01); the union member and emit are NOT given a top-level headId."
    - "The server-side and client-side DashboardEvent unions declare a top-level headId on exactly the three formerly-leaky members agent_status_changed/agent_message_added/memory_retrieval, in lockstep; steward_run_added stays payload-only in both."
  artifacts:
    - path: "src/dashboard/events.ts"
      provides: "DashboardEvent union with top-level headId on the 3 emit-updated members; steward_run_added unchanged (payload-carried)"
      contains: "agent_message_added"
    - path: "dashboard/src/types/api.ts"
      provides: "client DashboardEvent union mirror (3 top-level headId members) + StewardRun.headId"
      contains: "agent_message_added"
    - path: "src/db/agents.ts"
      provides: "headId threaded onto all 6 emit methods"
      contains: "headId"
  key_links:
    - from: "src/sub-agents/local.ts emitMessageAdded call"
      to: "agent_message_added payload headId"
      via: "this.headId 4th arg"
      pattern: "emitMessageAdded"
    - from: "src/head/activation.ts memory_retrieval emit"
      to: "memory_retrieval event headId"
      via: "this.opts.headId"
      pattern: "memory_retrieval"
    - from: "src/head/activation.ts stewardRunStore.append"
      to: "StewardRun.headId"
      via: "this.opts.headId"
      pattern: "headId: this.opts.headId"
---

<objective>
Tag the three formerly-leaky SSE events that have headId in scope at emit (`agent_message_added`, `agent_status_changed`, `memory_retrieval`) with a top-level `headId`, and carry the fourth (`steward_run_added`) head context INSIDE its `StewardRun` payload. Add the top-level `headId` to those three members in both `DashboardEvent` unions (server `src/dashboard/events.ts` + client `dashboard/src/types/api.ts`) in lockstep; leave `steward_run_added` payload-only. This is the backend half of D-01: events that previously left their head unidentifiable now self-describe (top-level for three, payload-carried for steward), so the client filter (Plan 04) can drop cross-head events.

Purpose: D-01 — close the live-stream half of the leak for `agent_message_added`, `agent_status_changed`, `memory_retrieval`, and `steward_run_added`. Threads `this.opts.headId` into the steward `append` call (consuming Plan 01's `StewardRun.headId`), so the unchanged `steward_run_added` emit payload carries head context.
Output: modified emit sites in `src/db/agents.ts`, `src/head/activation.ts`, `src/sub-agents/local.ts`, `src/index.ts`, and the dual `DashboardEvent` union updates (3 top-level headId members each).

NOTE: This plan runs in Wave 2, AFTER Plan 01 (Wave 1) has merged. Plan 01's `StewardRun.headId` type change is therefore present in this worktree, so the `headId: this.opts.headId` addition to the `stewardRunStore?.append(...)` call at `activation.ts:1057` compiles, and the repo-wide `npx tsc --noEmit` gate in Task 2 can actually pass. `src/db/steward_runs.ts` is Plan 01's file and is NOT in this plan's files_modified — the steward emit there stays untouched; head context rides the payload via `StewardRun.headId`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-CONTEXT.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-PATTERNS.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-RESEARCH.md

<interfaces>
<!-- Current emit signatures (PATTERNS.md, src/db/agents.ts:228-283). headId is added as OPTIONAL so single-head callers compile. -->
updateStatus(id, status): emits { type:'agent_status_changed', payload:{id,status} }            // ADD headId? + spread sibling to payload
suspend(id, question) / resume(id) / complete(id, output) / fail(id, error): same emit shape     // ADD headId?
emitMessageAdded(agentId, message, trigger?): emits { type:'agent_message_added', payload:{...} } // ADD headId? as 4th param
cancelAllActive(): loops getActive(); each AgentState already carries .headId from the DB row

<!-- headId availability at the four emit/call sites (RESEARCH § Leak 1-4, all VERIFIED). -->
src/sub-agents/local.ts:885   this.agentStore.emitMessageAdded(agentId, msg, options.trigger)   // this.headId in scope (field :101)
src/head/activation.ts:778    events.emit('dashboard', { type:'memory_retrieval', payload:{...} }) // this.opts.headId in scope
src/head/activation.ts:1057   stewardRunStore?.append({ id, stewards, createdAt })                // this.opts.headId in scope; StewardRun.headId added by Plan 01 (merged before this wave); head context rides the payload, NOT a top-level event field
src/index.ts (~:312)          updateStatus call site                                              // head id in scope

<!-- The two already-scoped union members showing the top-level-headId pattern to mirror (src/dashboard/events.ts). -->
| { type: 'message_added'; payload: Message; headId: string }
| { type: 'typing'; headId: string }

<!-- steward_run_added stays payload-only — head context lives on StewardRun.headId (Plan 01). Do NOT add a top-level headId to this member. -->
| { type: 'steward_run_added'; payload: StewardRun }   // StewardRun gained headId in Plan 01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread headId onto all six AgentStore emit methods</name>
  <read_first>
    - src/db/agents.ts lines 228-283 (the six emit methods: updateStatus :230, suspend :243, resume :248, emitMessageAdded :253, complete :258, fail :263, cancelAllActive :276-283)
    - .planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-PATTERNS.md section "src/db/agents.ts" (the exact spread pattern to apply)
  </read_first>
  <action>
    In `src/db/agents.ts`, add an optional `headId?: string` parameter to each of `updateStatus`, `suspend`, `resume`, `complete`, `fail`, and `emitMessageAdded`. On each emitted event, include headId via a conditional spread sibling to `payload`: append `, ...(headId !== undefined ? { headId } : {})` to the emitted event object (keeps the field absent rather than present-as-undefined, matching exactOptionalPropertyTypes). For `emitMessageAdded`, add headId as the FOURTH parameter (after `trigger?`). For `cancelAllActive` (lines 276-283), do NOT add a parameter — read each agent's own head from the already-fetched `getActive()` result and emit `{ type:'agent_status_changed', payload:{ id: a.id, status:'retracted' }, headId: a.headId }` per active agent `a` (AgentState already carries `.headId` from the head_id column). Make headId OPTIONAL on the five status methods and emitMessageAdded so call sites that do not yet pass it still compile (RESEARCH anti-pattern: avoid a big-bang non-optional change).
  </action>
  <verify>
    <automated>npx vitest run src/db/agents.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "headId" src/db/agents.ts` is greater than its pre-edit count (all six methods reference headId).
    - `grep -c "headId: a.headId" src/db/agents.ts` returns at least 1 (cancelAllActive emits per-agent head).
    - `npx vitest run src/db/agents.test.ts` passes (existing agent store tests still green).
  </acceptance_criteria>
  <done>All six emit methods can carry headId on their SSE event; cancelAllActive emits per-agent head from the row.</done>
</task>

<task type="auto">
  <name>Task 2: Wire headId at the call sites and update both DashboardEvent unions in lockstep</name>
  <read_first>
    - src/sub-agents/local.ts line 885 (emitMessageAdded call; this.headId field at :101)
    - src/head/activation.ts line 778 (memory_retrieval emit) and lines 1057-1061 (stewardRunStore.append call); this.opts.headId used throughout the file
    - src/index.ts around line 312 (updateStatus call site)
    - src/dashboard/events.ts lines 6-16 (server DashboardEvent union; message_added :7 + typing :13 are the already-scoped members showing the top-level headId pattern)
    - dashboard/src/types/api.ts lines 435-445 (client DashboardEvent union — lockstep mirror) and lines 60-64 (client StewardRun interface)
  </read_first>
  <action>
    Call-site wiring: (1) `src/sub-agents/local.ts:885` — pass `this.headId` as the fourth arg so the call reads `emitMessageAdded(agentId, msg, options.trigger, this.headId)`. (2) `src/head/activation.ts:778` — add `headId: this.opts.headId` as a sibling to `payload` on the `memory_retrieval` emit object. (3) `src/head/activation.ts:1057` — add `headId: this.opts.headId` to the `stewardRunStore?.append({ id, stewards, createdAt })` object (this is on the StewardRun PAYLOAD passed to append; it consumes Plan 01's `StewardRun.headId`, already merged because this plan runs in Wave 2). Leave the `steward_run_added` emit itself (in Plan 01's `src/db/steward_runs.ts`) UNCHANGED — do NOT touch that file; head context rides the payload. (4) `src/index.ts` updateStatus call site (~:312) — pass the in-scope head id as the third arg. Also fix any OTHER updateStatus/suspend/resume/complete/fail call sites that tsc flags AND that have headId in scope; where a call site genuinely lacks head context, leave it (the parameter is optional, so it compiles).
    Union lockstep: in BOTH `src/dashboard/events.ts` (lines 6-16) AND `dashboard/src/types/api.ts` (lines 435-445), add a top-level `; headId: string` to EXACTLY THREE union members — `agent_status_changed`, `agent_message_added`, and `memory_retrieval` (mirror the existing top-level `message_added`/`typing` pattern). Do NOT add a top-level `headId` to `steward_run_added` — leave that member exactly as `{ type: 'steward_run_added'; payload: StewardRun }`; its head context is carried by `StewardRun.headId` on the payload (Plan 01 server-side; mirrored client-side below). In `dashboard/src/types/api.ts` lines 60-64, add `headId: string` to the client `StewardRun` interface (mirror Plan 01's server-side StewardRun change) — this is what makes the payload-carried steward headId type-visible on the client for Plan 04's filter. Leave `usage_updated`, `assistant_name_changed`, `typing`, `theme_changed`, `thresholds_changed`, `message_added`, and `steward_run_added` union members exactly as-is.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "this.headId" src/sub-agents/local.ts` is greater than its pre-edit count (emitMessageAdded call passes this.headId).
    - `grep -c "headId: this.opts.headId" src/head/activation.ts` returns at least 2 (memory_retrieval emit + steward append payload).
    - `grep -c "headId: string" src/dashboard/events.ts` returns exactly 5 (the pre-existing top-level members message_added + typing, PLUS the 3 newly-added top-level members agent_status_changed + agent_message_added + memory_retrieval). `steward_run_added` MUST NOT gain a top-level `headId`.
    - `grep -c "; headId: string" dashboard/src/types/api.ts` increases by exactly 3 over its pre-edit count (the 3 union members), and the client `StewardRun` interface separately gains a `headId: string` line (a 4th total addition, on the interface rather than a union member).
    - `grep -c "type: 'steward_run_added'; payload: StewardRun }" src/dashboard/events.ts` returns 1 with NO trailing `; headId` (the steward member stays payload-only).
    - The `steward_run_added` member's `StewardRun` payload type carries `headId` (server via Plan 01's `src/db/steward_runs.ts`; client via the `StewardRun` interface edit in `dashboard/src/types/api.ts`) — `grep -c "headId: string" dashboard/src/types/api.ts` in the StewardRun interface block returns 1.
    - `npx tsc --noEmit` passes with zero errors across the whole repo. This is the phase's repo-wide clean-compile gate: because Plan 01 (StewardRun.headId, Wave 1) is merged before this wave, the steward append call site compiles here and the whole-repo check passes.
  </acceptance_criteria>
  <done>The three emit-updated leaky SSE events carry a top-level headId; steward_run_added carries head context via its StewardRun payload (member + emit unchanged); both DashboardEvent unions and the client StewardRun interface gained headId in lockstep; the full repo is tsc-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| head activation loop → DashboardEventBus | headId is stamped on the event at emit (top-level for three members) or on the StewardRun payload (steward_run_added); this is the per-head attribution the client filter (Plan 04) trusts. |
| DashboardEventBus → SSE (/api/stream) | One server broadcasts every event to ALL connected clients; isolation is enforced client-side, so every per-head event MUST carry a correct headId (top-level or payload-carried) or it cannot be filtered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03 | Information Disclosure | per-head SSE event with missing headId (fail-open) | mitigate | All four emit sites have headId in scope (RESEARCH § Leak 1-4 VERIFIED): three stamp a top-level headId, steward_run_added carries it on StewardRun.headId. Plan 04's filter reads headId per-type and treats absent/mismatched headId as a DROP (fail-closed), so a forgotten headId hides the event rather than leaking it. cancelAllActive reads per-agent head from the row so retract events are never mis-attributed. |
| T-50-04 | Spoofing | headId sourced from the wrong head context | mitigate | headId is read from the owning runtime object (this.headId / this.opts.headId / a.headId), never from request input — no client-controllable head attribution. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx tsc --noEmit` (whole repo clean — proves both unions in lockstep, steward_run_added stays payload-only, and all required call sites wired; passes because Plan 01 merged in Wave 1)
- `npx vitest run src/db/agents.test.ts`
- `grep` assertions in acceptance_criteria
</verification>

<success_criteria>
- The three emit-updated leaky SSE events carry a top-level headId at emit; steward_run_added carries it via its StewardRun payload.
- Server and client DashboardEvent unions are identical: top-level headId on exactly agent_status_changed/agent_message_added/memory_retrieval; steward_run_added payload-only in both.
- Client StewardRun interface carries headId.
- Full repo tsc-clean.
</success_criteria>

<output>
Create `.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-02-SUMMARY.md` when done.
</output>
