---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/010_steward_runs_head_id.sql
  - src/db/steward_runs.ts
  - src/db/steward_runs.test.ts
autonomous: true
requirements: [D-04, D-01, D-02]
must_haves:
  truths:
    - "A single-head deployment that runs migration 010 sees every existing steward_runs row attributed to head_id='default' (zero behavior change). [D-04]"
    - "StewardRunStore.getRecent(limit, headId) returns only that head's runs; getRecent(limit) (no headId) returns runs across all heads. [D-01 storage half; D-02 per-head backfill window]"
    - "Every steward run written carries its originating head_id in the steward_runs table."
  artifacts:
    - path: "sql/010_steward_runs_head_id.sql"
      provides: "head_id column + per-head index on steward_runs"
      contains: "ADD COLUMN head_id TEXT NOT NULL DEFAULT 'default'"
    - path: "src/db/steward_runs.ts"
      provides: "headId on StewardRun type, head_id insert, getRecent(limit, headId?) overload"
      contains: "headId"
    - path: "src/db/steward_runs.test.ts"
      provides: "head-scoping coverage for getRecent"
      contains: "getRecent"
  key_links:
    - from: "src/db/steward_runs.ts append()"
      to: "steward_runs.head_id column"
      via: "stmtInsert @head_id binding"
      pattern: "head_id"
    - from: "src/db/steward_runs.ts getRecent(limit, headId)"
      to: "steward_runs WHERE head_id = ?"
      via: "head-filtered SELECT"
      pattern: "WHERE head_id = \\?"
---

<objective>
Add the one structural prerequisite for per-head steward isolation: a `head_id` column on the `steward_runs` table (migration 010), and thread `headId` through the `StewardRun` type, `StewardRunStore.append`, and a new head-filtered `getRecent(limit, headId?)` overload. This is the only database work in the phase — the other three leaky surfaces already have `headId` available at emit.

Purpose: Implements D-04 (single-head zero-change via `DEFAULT 'default'`) and the storage half of D-01/D-02 (per-head steward backfill window of 50/60). Unblocks the steward backfill route (Plan 03) and the steward emit threading (Plan 02 wires `activation.ts`'s `append` call).
Output: `sql/010_steward_runs_head_id.sql`, modified `src/db/steward_runs.ts`, new `src/db/steward_runs.test.ts`.

NOTE: This is Wave 1 and runs FIRST. Making `StewardRun.headId` required deliberately breaks the existing `activation.ts:1057` append call site under a full-repo `npx tsc --noEmit` — that call site is Plan 02's domain and is fixed there. This plan therefore does NOT run a repo-wide `tsc --noEmit` gate (it would fail on a not-yet-wired call site outside this plan's scope); the whole-repo clean-compile gate lives in Plan 02 Task 2, which runs in Wave 2 after this plan merges and after it wires the append call. Do not "fix" `activation.ts` here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-CONTEXT.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-PATTERNS.md

<interfaces>
<!-- Current StewardRun + store contract the executor extends (from PATTERNS.md, src/db/steward_runs.ts). -->
StewardRun: { id: string; stewards: Array<{ name: string; ran: boolean; fired: boolean }>; createdAt: string }  // ADD headId: string
StewardRunRow: { id: string; stewards: string; created_at: string }  // ADD head_id: string
append(run: StewardRun): void  // binds @head_id, emits steward_run_added (payload unchanged — run.headId flows through)
getRecent(limit: number): StewardRun[]  // becomes the head-less branch of getRecent(limit, headId?)

<!-- The exact analog to copy from src/db/agents.ts:305-313 (already in the codebase). -->
AgentStore.getRecent(limit: number, headId?: string): headId !== undefined
  ? '... WHERE head_id = ? ORDER BY <ts> DESC LIMIT ?' with .all(headId, limit)
  : '... ORDER BY <ts> DESC LIMIT ?' with .all(limit)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration 010 — steward_runs.head_id</name>
  <read_first>
    - sql/007_agents_head_id.sql (the exact analog — copy verbatim, swap table/index)
    - sql/005_multi_head.sql (the original ADD COLUMN head_id pattern)
    - sql/001_init.sql lines 105-109 (current steward_runs schema: id, stewards, created_at)
  </read_first>
  <action>
    Create `sql/010_steward_runs_head_id.sql` by copying `sql/007_agents_head_id.sql` verbatim and substituting: table `agents` → `steward_runs`; the single DDL becomes `ALTER TABLE steward_runs ADD COLUMN head_id TEXT NOT NULL DEFAULT 'default';` (use the literal constant `'default'` — this is the D-04 backward-compat backfill, populating all existing rows in one statement with no UPDATE loop). Add the index `CREATE INDEX IF NOT EXISTS idx_steward_runs_head_created ON steward_runs (head_id, created_at DESC);` (column order `(head_id, created_at DESC)` matches the per-head newest-first backfill read; index name mirrors `idx_messages_head_created`). Update the comment header to say "Phase 50: Add head_id isolation column to steward_runs" and reference that it mirrors sql/007. Do NOT add an UPDATE backfill statement.
  </action>
  <verify>
    <automated>npx vitest run src/db/db.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN head_id TEXT NOT NULL DEFAULT 'default'" sql/010_steward_runs_head_id.sql` returns 1.
    - `grep -c "idx_steward_runs_head_created" sql/010_steward_runs_head_id.sql` returns at least 1.
    - `grep -v '^--' sql/010_steward_runs_head_id.sql | grep -ci "UPDATE"` returns 0 (no backfill UPDATE).
    - `npx vitest run src/db/db.test.ts` passes (db.test.ts runs all migrations including 010 in order).
  </acceptance_criteria>
  <done>Migration 010 exists, applies cleanly through the migration runner, and adds head_id (DEFAULT 'default') + the head/created compound index to steward_runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread headId through StewardRun type, append, and getRecent + write head-scoping test</name>
  <read_first>
    - src/db/steward_runs.ts (the file being modified — full read; StewardRun interface ~line 4, StewardRunRow ~line 10, rowToRun ~line 16, stmtInsert ~line 29, append ~line 39, getRecent ~line 49)
    - src/db/agents.ts lines 302-314 (the getRecent(limit, headId?) overload to mirror exactly)
    - src/db/agents.test.ts (the full-file test shape to mirror for steward_runs.test.ts — :memory: db + runMigrations + per-head assertions)
  </read_first>
  <behavior>
    - Test 1: append two runs under headId 'A' and one under 'B'; getRecent(10, 'A') returns exactly the 2 'A' runs (newest first), each with headId === 'A'.
    - Test 2: getRecent(10) (no headId) returns all 3 runs across both heads.
    - Test 3: getRecent(10, 'nonexistent') returns an empty array.
  </behavior>
  <action>
    In `src/db/steward_runs.ts`: (1) add `headId: string` to the `StewardRun` interface; (2) add `head_id: string` to `StewardRunRow`; (3) add `headId: row.head_id` to the `rowToRun` mapper; (4) extend `stmtInsert` to `INSERT INTO steward_runs (id, stewards, created_at, head_id) VALUES (@id, @stewards, @created_at, @head_id)`; (5) in `append`, add `head_id: run.headId` to the `stmtInsert.run({...})` object — leave the `eventBus?.emit('dashboard', { type: 'steward_run_added', payload: run })` line UNCHANGED (run.headId now rides on the payload automatically); (6) change `getRecent(limit: number)` to `getRecent(limit: number, headId?: string)` mirroring `AgentStore.getRecent` exactly — when `headId !== undefined` use `SELECT * FROM steward_runs WHERE head_id = ? ORDER BY created_at DESC LIMIT ?` with `.all(headId, limit)`, else the existing head-less `... ORDER BY created_at DESC LIMIT ?` with `.all(limit)`; map rows through `rowToRun`. Then create `src/db/steward_runs.test.ts` mirroring `src/db/agents.test.ts`'s structure (`:memory:` db, `runMigrations(db, MIGRATIONS_DIR)`, `new StewardRunStore(db)`) with the three behavior cases above. Do NOT add a `?? 'default'` fallback in append — the type-required `headId` on StewardRun is the safety net, the SQL DEFAULT is defense-in-depth (mirrors Phase 34 D-ROW-WRITE-FROM-OPTIONS). Do NOT touch `src/head/activation.ts` — making headId required will surface a tsc error at its append call site (activation.ts:1057), which is intentional and is Plan 02's job to wire.
  </action>
  <verify>
    <automated>npx vitest run src/db/steward_runs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/steward_runs.test.ts` passes all three head-scoping cases.
    - `grep -c "headId: string" src/db/steward_runs.ts` returns at least 1 (the StewardRun interface gained `headId: string`).
    - `grep -c "WHERE head_id = ?" src/db/steward_runs.ts` returns at least 1 (head-filtered getRecent branch).
    - `grep -c "@head_id" src/db/steward_runs.ts` returns at least 1 (insert binds head_id).
    - `grep -c "ADD COLUMN head_id TEXT NOT NULL DEFAULT 'default'" sql/010_steward_runs_head_id.sql` returns 1 (the interface/migration pairing that proves the column exists for the new type).
    - NOTE: do NOT run `npx tsc --noEmit` as a gate in this plan. A full-repo type-check will report an error at `src/head/activation.ts:1057` (the append call site doesn't yet pass headId) — that is the expected, intentional consequence of making headId required, and it is resolved by Plan 02 (Wave 2), which also carries the repo-wide `tsc --noEmit` clean-compile gate. The `steward_runs.ts` file itself is internally consistent; the only outstanding type error is at the out-of-scope call site Plan 02 wires.
  </acceptance_criteria>
  <done>StewardRun carries headId end-to-end through the store; getRecent supports per-head filtering identical in shape to AgentStore.getRecent; the new test file pins all three head-scoping behaviors green. The deliberate tsc error at activation.ts:1057 is left for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| head A activation loop → steward_runs table | Each head's steward run is written with its own head_id; the column is the per-head attribution primitive the dashboard filter relies on. |
| steward_runs row → dashboard backfill (Plan 03) | A run mis-attributed at write time would surface in the wrong head's timeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Information Disclosure | steward_runs.head_id default on legacy rows | mitigate | Migration uses `DEFAULT 'default'` (D-04) — legacy rows attribute to the default head exactly as single-head deployments expect; no row becomes attributable to a wrong non-default head. |
| T-50-02 | Tampering | append() head_id binding | mitigate | append writes `run.headId` with no `?? 'default'` coercion; type-required headId on StewardRun forces every caller to supply it (compile-time gate), SQL DEFAULT is defense-in-depth only. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx vitest run src/db/db.test.ts` (migration applies)
- `npx vitest run src/db/steward_runs.test.ts` (head-scoping)
- `grep` assertions in acceptance_criteria
- (Repo-wide `tsc --noEmit` is intentionally NOT a gate here — it runs in Plan 02 after the append call site is wired.)
</verification>

<success_criteria>
- Migration 010 applies cleanly and backfills existing rows to head_id='default'.
- StewardRunStore round-trips headId and filters getRecent by head.
- steward_runs.test.ts is green; steward_runs.ts is internally consistent (the only remaining tsc error is at the activation.ts append call site, which Plan 02 wires).
</success_criteria>

<output>
Create `.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
