---
phase: 50
plan: 03
type: execute
wave: 3
depends_on: ["50-01", "50-02"]
files_modified:
  - src/dashboard/routes/agents.ts
  - src/dashboard/routes/steward_runs.ts
  - src/dashboard/routes/activity.ts
  - dashboard/src/lib/api.ts
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "GET /api/agents/xray-history?head=A returns only head A's recent agent messages (window 50, per head). [D-01: backfill half of leak closure; D-02: window stays 50 per head, no new limit constant]"
    - "GET /api/steward-runs?head=A returns only head A's recent steward runs."
    - "Calling these routes with no ?head= returns the cross-head result unchanged (backward-compatible)."
    - "The client api wrappers append ?head= only when a headId is supplied."
  artifacts:
    - path: "src/dashboard/routes/agents.ts"
      provides: "head-scoped xray-history backfill"
      contains: "getRecent(50, head)"
    - path: "src/dashboard/routes/steward_runs.ts"
      provides: "head-scoped steward backfill (getRecent, not getAll)"
      contains: "getRecent(60, head)"
    - path: "dashboard/src/lib/api.ts"
      provides: "xrayHistory(headId?) + stewardRuns.list(headId?)"
      contains: "encodeURIComponent"
  key_links:
    - from: "src/dashboard/routes/agents.ts /xray-history"
      to: "agents.getRecent(50, head)"
      via: "head guard"
      pattern: "getRecent.50, head."
    - from: "src/dashboard/routes/steward_runs.ts GET /"
      to: "stewardRuns.getRecent(60, head)"
      via: "head guard"
      pattern: "getRecent.60, head."
    - from: "dashboard/src/lib/api.ts xrayHistory"
      to: "/api/agents/xray-history?head="
      via: "headId ternary URL"
      pattern: "xray-history"
---

<objective>
Scope the REST backfill routes by head — `/api/agents/xray-history` and `/api/steward-runs` gain a `?head=` param and call the head-filtered `getRecent`; the dead `/api/activity` route's unscoped reads are scoped as future-proofing — and update the client `api.ts` wrappers to pass `selectedHead`. This is the backfill half of D-01/D-02: with two heads, the initial timeline fetch returns only the selected head's history.

Purpose: D-01 (backfill no longer leaks) + D-02 (xray window stays 50 per head, no new limit constant; steward backfill reuses the existing 60 limit already used in activity.ts). Consumes Plan 01 (`StewardRunStore.getRecent(limit, headId?)`) and Plan 02 (`StewardRun.headId` for the client api types).
Output: head-scoped `agents.ts` / `steward_runs.ts` / `activity.ts` routes + `api.ts` `xrayHistory(headId?)` and `stewardRuns.list(headId?)`.

NOTE: This is Wave 3. Plan 01 (StewardRun.getRecent + head_id column) and Plan 02 (StewardRun.headId on the client type, union narrowing) are both merged before this wave runs, so the head-filtered store read and the client StewardRun.headId type are present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-CONTEXT.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-PATTERNS.md
@.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-RESEARCH.md

<interfaces>
<!-- The canonical head guard already in the same file (src/dashboard/routes/agents.ts GET / route, lines 11-13). Copy verbatim. -->
const head = typeof req.query['head'] === 'string' ? req.query['head'] : undefined
const recent = agents.getRecent(20, head)

<!-- Store reads available after Plans 01/02. -->
AgentStore.getRecent(limit, headId?)         // existing, agents.ts:305
StewardRunStore.getRecent(limit, headId?)    // added by Plan 01

<!-- Client api ternary-URL pattern from commit 352b9dd (dashboard/src/lib/api.ts:133-134). -->
list: (headId?: string) => request(headId ? '/api/agents?head=' + encodeURIComponent(headId) : '/api/agents')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Head-scope the three backend backfill routes</name>
  <read_first>
    - src/dashboard/routes/agents.ts lines 11-14 (GET / route — canonical head guard) and lines 65-90 (the /xray-history route to change; currently uses _req and agents.getRecent(50))
    - src/dashboard/routes/steward_runs.ts lines 9-11 (GET / route; currently stewardRuns.getAll())
    - src/dashboard/routes/activity.ts lines 28-66 (the three unscoped reads: getRecentText('default',60), agents.getRecent(40), stewardRuns.getRecent(60))
    - src/dashboard/routes/messages.ts line 18 (same head guard, for input-validation consistency per RESEARCH Security Domain)
  </read_first>
  <action>
    xray-history (src/dashboard/routes/agents.ts:65): change the handler first arg from _req to req, add the guard reading req.query['head'] (typeof === 'string' else undefined), and change agents.getRecent(50) to agents.getRecent(50, head). Keep window 50 (D-02 — no new limit constant). Rest of the route unchanged.
    steward-runs (src/dashboard/routes/steward_runs.ts:9): change _req to req, add the same head guard, switch stewardRuns.getAll() to stewardRuns.getRecent(60, head). Limit 60 resolves RESEARCH Open Question 1 — 60 mirrors the existing stewardRuns.getRecent(60) already used in activity.ts; no new constant. Client renders descending; timeline assembly handles ordering.
    activity (src/dashboard/routes/activity.ts): dead infrastructure (no live subscriber per RESEARCH Leak 5) but scope it as future-proofing. Add the head guard at the top of the handler (~line 28). Change agents.getRecent(40) to agents.getRecent(40, head) and stewardRuns.getRecent(60) to stewardRuns.getRecent(60, head). For the messages read messages.getRecentText('default', 60), change the hardcoded 'default' to (head ?? 'default') — keeps single-head behavior identical, scopes when head is supplied.
    Use the identical typeof req.query['head'] === 'string' guard at all three sites (matches messages.ts:18 and agents.ts:11 — V5 input-validation consistency).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getRecent(50, head)" src/dashboard/routes/agents.ts` returns at least 1.
    - `grep -c "getRecent(60, head)" src/dashboard/routes/steward_runs.ts` returns at least 1.
    - `grep -c "getAll()" src/dashboard/routes/steward_runs.ts` returns 0 (getAll replaced).
    - `grep -c "head" src/dashboard/routes/activity.ts` is greater than its pre-edit count.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>All three backfill routes accept ?head= and pass it to head-filtered store reads; window limits unchanged (50 xray, 60 steward); single-head behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add headId params to the client api wrappers</name>
  <read_first>
    - dashboard/src/lib/api.ts line 133-140 (agents.list(headId?) ternary URL — the exact analog; xrayHistory at :139-140) and the stewardRuns api object
    - dashboard/src/lib/api.ts (the request helper signature, to match return-type generics)
  </read_first>
  <action>
    In dashboard/src/lib/api.ts: (1) change agents.xrayHistory from a no-arg function to xrayHistory(headId?: string), building the URL with the same ternary as agents.list — when headId is set, append `?head=` + encodeURIComponent(headId) to `/api/agents/xray-history`, else the bare path. Preserve the existing return generic (the messages array shape). (2) Add a headId?: string parameter to stewardRuns.list using the identical ternary against `/api/steward-runs`, preserving its `{ stewardRuns: StewardRun[] }` return generic (StewardRun now carries headId from Plan 02's client-type change). Do not change any other api function.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "xrayHistory: (headId" dashboard/src/lib/api.ts` returns at least 1.
    - `grep -c "encodeURIComponent" dashboard/src/lib/api.ts` is greater than its pre-edit count (xrayHistory + stewardRuns.list both use it).
    - `cd dashboard && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>api.agents.xrayHistory and api.stewardRuns.list both accept an optional headId and append ?head= only when supplied; dashboard type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to REST backfill route (?head=) | The head query param scopes which head's history is returned; an ignored/forgotten param would return cross-head data (a backfill leak). |
| backfill route to store read | The route must pass head into the head-filtered getRecent overload; a missed pass returns the global set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Information Disclosure | backfill route ignoring/forgetting the head param and returning global data | mitigate | Each route reads head via the verified guard and passes it directly into getRecent(limit, head); acceptance grep pins getRecent(50, head) / getRecent(60, head) so a forgotten pass fails the gate. activity.ts hardcoded 'default' becomes (head ?? 'default'). |
| T-50-06 | Tampering | untrusted ?head= query value | mitigate | The typeof req.query['head'] === 'string' guard (matches messages.ts:18 / agents.ts:11) rejects array/object query injection; the value is used only as a SQL bind parameter in the prepared statement (no string interpolation). |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx tsc --noEmit` (server routes) and `cd dashboard && npx tsc --noEmit` (client api)
- `npx vitest run src/dashboard` (route tests still green)
- grep assertions in acceptance_criteria
</verification>

<success_criteria>
- All three backfill routes accept ?head= and scope their store reads.
- xray window stays 50/head; steward backfill uses 60 (existing constant); no new limit constant introduced.
- Client api.xrayHistory and api.stewardRuns.list pass selectedHead through.
- Single-head deployments see identical results (?head= absent or 'default' is a no-op filter).
</success_criteria>

<output>
Create `.planning/phases/50-per-head-xray-isolation-eliminate-cross-head-agent-activity-/50-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
