---
phase: 39-dashboard-reminder-ui
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - dashboard/src/pages/SchedulesPage.tsx
autonomous: false
requirements: [SCHED-01, SCHED-03]
must_haves:
  truths:
    - "The reminder create form (AddReminderForm) has a Requires-acknowledgment toggle with reveal-when-on minutes/hours/days nag inputs, validated for coupling/floor/ceiling, blocking submit with inline messages"
    - "AddReminderForm in repeating mode shows an optional Start date/time field; setting a future datetime sends startAt; a past datetime is rejected inline and blocks submit"
    - "AddScheduleForm (tasks) in repeating mode shows the same optional Start date/time field with the same future-date validation and startAt mapping"
    - "Empty start date preserves today's cron-first-fire behavior (backward compatible); ack off produces an ordinary reminder"
  artifacts:
    - path: "dashboard/src/pages/SchedulesPage.tsx"
      provides: "AddReminderForm ack/nag/start-date controls + AddScheduleForm start-date control"
      contains: "startAt"
  key_links:
    - from: "dashboard/src/pages/SchedulesPage.tsx AddReminderForm"
      to: "api.schedules.create"
      via: "createMutation sends requiresAck + nagIntervalMinutes + startAt"
      pattern: "api\\.schedules\\.create"
    - from: "dashboard/src/pages/SchedulesPage.tsx AddScheduleForm"
      to: "api.schedules.create"
      via: "createMutation sends startAt in repeating mode"
      pattern: "startAt"
---

<objective>
Deliver the create half of the dashboard reminder UI: the Requires-acknowledgment toggle + reveal-when-on nag-slot inputs on the reminder create form (SCHED-01), and the optional Start date/time control on BOTH create forms in repeating mode (SCHED-03 / D-07/D-08). This plan extends the same `SchedulesPage.tsx` file as Plan 02, so it runs after it (Wave 3) to avoid file-ownership conflicts.

Purpose: SCHED-01 requires the create form to set requiresAck and a nag interval; D-01/D-02 specify three multi-slot integer inputs (minutes/hours/days, summed to nagIntervalMinutes) revealed only when the ack toggle is on, with client-side validation (D-04) blocking submit. SCHED-03 requires an optional start date for recurring items mapping the first fire to nextRun while keeping cron (D-10), on both the reminder form (AddReminderForm) and the scheduled-task form (AddScheduleForm) per D-08; a past start date is rejected inline (D-09).
Output: Updated `dashboard/src/pages/SchedulesPage.tsx` (AddReminderForm + AddScheduleForm).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-dashboard-reminder-ui/39-CONTEXT.md
@.planning/phases/39-dashboard-reminder-ui/39-RESEARCH.md
@.planning/phases/39-dashboard-reminder-ui/39-PATTERNS.md
@.planning/phases/39-dashboard-reminder-ui/39-02-SUMMARY.md

<interfaces>
<!-- After Plans 01+02: -->
<!--   api.schedules.create body type carries requiresAck?, nagIntervalMinutes?, startAt? -->
<!--   formatNagInterval(minutes) helper exists in SchedulesPage.tsx -->
<!--   dashboard Schedule type carries requiresAck/nagIntervalMinutes/ackPending -->

<!-- SchedulesPage.tsx reusable analogs: -->
<!--   AddReminderForm ~629-771: type toggle (once/repeating) ~709-722; datetime-local once field ~725-735; -->
<!--     CronPicker for repeating; createMutation spread ~658-669; disabled-submit ~751-768; error display ~751 -->
<!--   AddScheduleForm ~264-443: type toggle + CronPicker/datetime-local ~386-399; createMutation spread ~315-323 -->
<!--   UTC conversion: new Date(value).toISOString() (existing runAt fields) -->
<!--   reveal-when-on: type-toggle gating is the existing conditional-render pattern -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AddReminderForm ack toggle + reveal-when-on nag slots + client validation</name>
  <files>dashboard/src/pages/SchedulesPage.tsx</files>
  <read_first>
    - dashboard/src/pages/SchedulesPage.tsx lines 629-771 (AddReminderForm: state, type toggle ~709-722, createMutation ~658-669, disabled-submit ~751-768, error display)
    - dashboard/src/pages/SchedulesPage.tsx ReminderRow edit modal from Plan 02 (the reveal-when-on nag-slot UI + editNagSum validation pattern to mirror on the create form)
    - .planning/phases/39-dashboard-reminder-ui/39-PATTERNS.md (Reveal-When-On nag-slot block, inline validation messages, disabled condition extension, createMutation spread extension)
    - .planning/phases/39-dashboard-reminder-ui/39-RESEARCH.md (Pitfall 3: omit/null nagIntervalMinutes when ack off; D-04 coupling/floor/ceiling rules)
  </read_first>
  <action>
    Add local state to AddReminderForm: `requiresAck` (boolean, default false), `nagMinutes`/`nagHours`/`nagDays` (numbers, default 0). Compute `const nagSum = nagMinutes + nagHours*60 + nagDays*1440`. Add a "Requires acknowledgment" toggle reusing the enable/disable button shape (relative w-9 h-5 rounded-full) flipping `requiresAck` local state. When `requiresAck` is true, render three labeled number inputs (minutes/hours/days, min={0}, the w-16 text-center input styling) — hidden when off (D-02). Add inline red validation messages above the submit button: when `requiresAck && nagSum===0` → "Set a nag interval when 'Requires acknowledgment' is on."; when `nagSum > 43200` → "Nag interval must be at most 30 days." (floor=1 is satisfied by any positive integer, so no sub-1 message is needed for integer inputs). Extend the submit button `disabled` condition with `|| (requiresAck && nagSum === 0) || nagSum > 43200`. In createMutation's create() call, add the spread `...(requiresAck ? { requiresAck, nagIntervalMinutes: nagSum } : {})` (Pitfall 3 — only send when on; backend nulls otherwise). Reset these fields in the form's success/reset path consistently with the existing fields.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - AddReminderForm has `requiresAck` + `nagMinutes`/`nagHours`/`nagDays` state (grep all four names present in the create-form region)
    - nag slot inputs are gated behind `{requiresAck && (...)}` (reveal-when-on)
    - submit disabled condition includes `requiresAck && nagSum === 0` and `nagSum > 43200`
    - createMutation create() body includes the `...(requiresAck ? { requiresAck, nagIntervalMinutes: nagSum } : {})` spread
    - `cd dashboard && npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The reminder create form lets the user enable acknowledgment, fill multi-slot nag inputs revealed only when on, with client validation blocking invalid submits, and sends requiresAck + nagIntervalMinutes on create.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Optional Start date/time on both create forms (repeating mode)</name>
  <files>dashboard/src/pages/SchedulesPage.tsx</files>
  <read_first>
    - dashboard/src/pages/SchedulesPage.tsx lines 629-771 (AddReminderForm: repeating-mode CronPicker ~725-735, datetime-local pattern, createMutation spread ~658-669)
    - dashboard/src/pages/SchedulesPage.tsx lines 264-443 (AddScheduleForm: repeating CronPicker/datetime-local ~386-399, createMutation spread ~315-323)
    - .planning/phases/39-dashboard-reminder-ui/39-PATTERNS.md (start-date field block, UTC conversion line 319 pattern, createMutation startAt spread)
    - .planning/phases/39-dashboard-reminder-ui/39-RESEARCH.md (Pitfall 6: convert datetime-local to UTC via new Date(v).toISOString(); Pitfall 4: keep cron alongside startAt; Pitfall 7: do not reset CronPicker state after mount)
  </read_first>
  <action>
    In BOTH AddReminderForm and AddScheduleForm: add a `startAt` local state (string, default ''). When `type === 'repeating'`, render an optional "Start date (optional)" `<input type="datetime-local">` BELOW the CronPicker, using the existing form-input styling and a hint "First fire at this time, then repeating. Browser local time (workspace timezone: {tz})". Add inline validation: when `startAt && new Date(startAt) <= new Date()` show a red "Start date must be in the future." message (D-09). Extend the submit `disabled` condition with `|| (startAt !== '' && new Date(startAt) <= new Date())`. In each createMutation create() call, add the spread `...(type === 'repeating' && startAt ? { startAt: new Date(startAt).toISOString() } : {})` (Pitfall 6 UTC conversion; cron is still sent in the existing repeating spread so both cron and nextRun land per Pitfall 4). Empty startAt sends nothing → backend keeps today's cron-first-fire behavior (backward compatible, D-07). Reset startAt on success consistently with existing fields. Do NOT modify CronPicker or its grammar; do not reset the cron state after CronPicker mounts (Pitfall 7).
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit 2>&1 | tail -12 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Start date" dashboard/src/pages/SchedulesPage.tsx` >= 2 (field appears in both forms)
    - Both forms gate the start-date field behind `type === 'repeating'`
    - Both createMutation bodies include `...(type === 'repeating' && startAt ? { startAt: new Date(startAt).toISOString() } : {})`
    - submit disabled condition in both forms includes the past-startAt check
    - `dashboard/src/components/CronPicker.tsx` is unchanged (`git diff --stat dashboard/src/components/CronPicker.tsx` shows no changes)
    - `cd dashboard && npx tsc --noEmit` clean and `npm run build` (dashboard) succeeds
  </acceptance_criteria>
  <done>Both create forms expose an optional start-date field in repeating mode that maps a future datetime to startAt (→ nextRun server-side), rejects past dates inline, and leaves existing empty-start behavior unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify create-form ack toggle, nag slots, and start-date on both forms</name>
  <files>dashboard/src/pages/SchedulesPage.tsx</files>
  <what-built>
    The reminder create form now has a Requires-acknowledgment toggle that reveals minutes/hours/days nag inputs with submit-blocking validation, and both create forms (reminder + scheduled task) have an optional Start date/time field in repeating mode that maps the first fire to the chosen datetime. These React behaviors are not covered by the backend-only vitest config — manual verification per 39-VALIDATION.md.
  </what-built>
  <action>
    Pause for human verification. Tasks 1-2 already automated the implementation; this checkpoint confirms the rendered React create-form behavior that the backend-only vitest config cannot cover. Present the how-to-verify steps to the user and wait for the resume signal.
  </action>
  <how-to-verify>
    1. Open the Schedules page → Add reminder. Confirm nag inputs are hidden until "Requires acknowledgment" is toggled on; toggle on → minutes/hours/days inputs appear.
    2. With ack on and all nag inputs 0 → confirm "Add reminder" is disabled with an inline message. Set nag sum > 30 days → confirm disabled + 30-day message. Set 1h, repeating cadence, Submit → confirm the new reminder shows the NAGS badge + "· nags every 1h".
    3. In repeating mode, set a FUTURE Start date/time and submit → confirm the reminder's next-run shows that chosen time (first fire), not the cron-default. Set a PAST start date → confirm "Start date must be in the future." inline + blocked submit.
    4. Repeat step 3 on the Add scheduled-task form (AddScheduleForm) in repeating mode → confirm the start-date field is present and behaves identically.
    5. Confirm a repeating reminder/task created with EMPTY start date still fires per cron as before (no regression).
  </how-to-verify>
  <verify>
    <human-check>create-form ack toggle reveals nag slots, client validation blocks invalid submit, optional start-date appears in repeating mode on both forms with future-only enforcement, and empty-start preserves cron-first-fire</human-check>
  </verify>
  <resume-signal>Type "approved" or describe what rendered/behaved incorrectly</resume-signal>
  <done>User confirms (resume signal "approved") that the create-form ack/nag controls and the start-date field on both forms render and behave correctly with no regression to empty-start cron firing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard browser → POST /api/schedules (via api.schedules.create) | Create forms send requiresAck/nagIntervalMinutes/startAt; client validation is convenience — the Plan-01 backend route is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | Tampering | create-form nag value sent to POST | mitigate | Client disables submit on coupling/ceiling violation; the Plan-01 POST route re-validates 1 ≤ nag ≤ 43200 + coupling as source of truth (D-04) — a forged client request still hits the server gate |
| T-39-10 | Tampering | create-form startAt | mitigate | Client blocks past dates (D-09) but the Plan-01 POST route independently rejects `startAt <= now` and NaN — prevents surprise immediate-fire/backfill if the client check is bypassed |
| T-39-11 | Tampering | startAt without cron (one-time leak) | mitigate | startAt is sent only when `type === 'repeating'` (and cron is sent in the same spread) so a start-date never silently turns a recurring item one-time (Pitfall 4) |
| T-39-SC | Tampering | npm installs | accept | No package installs in this phase (RESEARCH audit: none new) |
</threat_model>

<verification>
- `cd dashboard && npx tsc --noEmit` clean; `npm run build` (dashboard) succeeds
- CronPicker.tsx unchanged (grammar invariant preserved)
- Manual checkpoint (Task 3) approved: ack toggle + reveal-when-on nag slots + validation on the create form; optional start-date on both forms with future-only enforcement and backward-compatible empty-start behavior
</verification>

<success_criteria>
- SCHED-01 (create half): requiresAck toggle + multi-slot nag inputs (reveal-when-on) with client validation on the reminder create form (D-01/D-02/D-04)
- SCHED-03: optional start-date/time on both create forms in repeating mode → first fire at the chosen datetime, then cron (D-07/D-08/D-09/D-10)
- Backward compatible: empty start date preserves cron-first-fire; ack off yields an ordinary reminder
- CronPicker untouched; dashboard builds and type-checks clean
</success_criteria>

<output>
Create `.planning/phases/39-dashboard-reminder-ui/39-03-SUMMARY.md` when done
</output>
