---
phase: 55-app-serving-subsystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [APPSRV-03]
user_setup: []

must_haves:
  truths:
    - "@ashley-shrok/viewmodel-shell is a normal pinned shrok dependency (D-10)"
    - "The package's /server export loads under Node 22 and exposes createAction + createAgentSkillHandler (D-10)"
    - "The shipped browser bundle (dist/index.js, dist/browser.js) and styles (styles/default.css, styles/themes/dark-purple.css) exist in node_modules for the host to serve at /apps/_pkg/* (APPSRV-03)"
  artifacts:
    - path: "package.json"
      provides: "@ashley-shrok/viewmodel-shell dependency entry"
      contains: "@ashley-shrok/viewmodel-shell"
    - path: "package-lock.json"
      provides: "locked resolution for the new dependency"
      contains: "@ashley-shrok/viewmodel-shell"
  key_links:
    - from: "src/apps/router.ts (Plan 03)"
      to: "@ashley-shrok/viewmodel-shell/server"
      via: "import createAgentSkillHandler"
      pattern: "@ashley-shrok/viewmodel-shell/server"
---

<objective>
Add `@ashley-shrok/viewmodel-shell` (D-10) as a normal, version-pinned shrok dependency so the
host can import its server exports (`createAction`, `createAgentSkillHandler`, ViewNode/error types
from the `/server` subpath) and serve its shipped browser bundle + styles at `/apps/_pkg/*`.

Purpose: this package is the foundation of the entire app-serving subsystem — every app's HTML shell
loads its browser adapter and every app module is authored against its `/server` exports.
Output: an updated `package.json` + `package-lock.json` with the package installed under `node_modules`.

This plan is gated by a blocking package-legitimacy checkpoint (security: supply-chain / T-55-SC)
because no RESEARCH.md Package Legitimacy Audit exists for this phase — fallback policy treats the
package as [ASSUMED] until a human confirms it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-app-serving-subsystem/55-CONTEXT.md
@.planning/phases/55-app-serving-subsystem/55-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Reference install confirmed at /home/thenasty/vms-apps/node_modules/@ashley-shrok/viewmodel-shell -->
Package: @ashley-shrok/viewmodel-shell  version 1.8.0  license MIT  author ashley-shrok (first-party — the user's own framework; repo github.com/ashley-shrok/ViewModelShell, gh authed as ashley-shrok per AGENTS.md)
exports map: "." -> dist/index.js, "./browser" -> dist/browser.js, "./server" -> dist/server.js, "./styles.css" -> styles/default.css, "./themes/dark-purple.css" -> styles/themes/dark-purple.css
files array: ["dist","styles","agent-skill.md","README.md","LICENSE"]
/server exports (dist/server.d.ts): createAction<TState>(handler) => (Request)=>Promise<Response>; createAgentSkillHandler(opts?) => (Request)=>Response; UnknownActionError; ERR_CODES (with UNCAUGHT: "uncaught_exception"); types ShellResponseBody, ViewNode, ErrCode
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (T-55-SC) — confirm @ashley-shrok/viewmodel-shell before install</name>
  <read_first>
    - .planning/phases/55-app-serving-subsystem/55-CONTEXT.md (D-10)
    - /home/thenasty/vms-apps/node_modules/@ashley-shrok/viewmodel-shell/package.json (the reference install: name, version 1.8.0, author ashley-shrok, MIT, repo URL)
  </read_first>
  <what-built>
    Nothing yet — this checkpoint precedes the install. The package legitimacy gate is mandatory
    because this phase has no RESEARCH.md Package Legitimacy Audit table, so the fallback policy
    classifies @ashley-shrok/viewmodel-shell as [ASSUMED] and requires a human confirmation before
    any package-manager install (security_enforcement ON; threat T-55-SC Tampering / supply chain).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@ashley-shrok/viewmodel-shell and confirm: it is published
       under the `@ashley-shrok` scope (the user's own npm scope / the ViewModelShell project the
       user maintains — repo github.com/ashley-shrok/ViewModelShell), MIT-licensed, and version 1.8.0
       (or a newer 1.x the executor intends to pin) exists.
    2. Confirm this is the SAME first-party framework already vendored and in production use at
       /home/thenasty/vms-apps (not a typosquat — the scope and repo must match exactly).
    3. Confirm the version to pin (default: the 1.8.0 already proven in the PoC).
  </how-to-verify>
  <acceptance_criteria>
    - A human has confirmed the npm package identity (scope `@ashley-shrok`, name `viewmodel-shell`, MIT, repo github.com/ashley-shrok/ViewModelShell) and approved the version to install.
    - This checkpoint is NOT auto-approvable regardless of workflow.auto_advance (package legitimacy checkpoints are never auto-approved).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the version to pin, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and pin @ashley-shrok/viewmodel-shell, verify the /server export loads under Node 22</name>
  <read_first>
    - /home/thenasty/shrok/package.json (the existing `dependencies` block, starting ~line 86; current version 0.7.0)
    - /home/thenasty/vms-apps/node_modules/@ashley-shrok/viewmodel-shell/package.json (exports map + files array to confirm what gets served)
    - .planning/phases/55-app-serving-subsystem/55-PATTERNS.md (the "package.json + lockfile — MODIFY (D-10)" section)
  </read_first>
  <action>
    Add `@ashley-shrok/viewmodel-shell` at the approved version (default `1.8.0`) to the `dependencies`
    block of /home/thenasty/shrok/package.json and run `npm install` so package-lock.json records the
    locked resolution and node_modules/@ashley-shrok/viewmodel-shell is populated. Pin a concrete
    version per D-10 (the executor may use `^1.8.0` or an exact `1.8.0` — match shrok's existing
    dependency-pinning style in package.json). Do NOT add it to devDependencies (the host imports it at
    runtime). Do NOT touch dashboard/dist/. After install, confirm the package's `./server` subpath
    export resolves under Node 22 and surfaces `createAction` and `createAgentSkillHandler` as functions
    (web-Fetch-native; PoC-confirmed under Node 22 — this is the D-10 "confirm /server runs under Node"
    requirement). Run `npx tsc --noEmit` to confirm the new dependency does not break shrok's typecheck.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && grep -q '@ashley-shrok/viewmodel-shell' package.json && grep -q '@ashley-shrok/viewmodel-shell' package-lock.json && node -e "import('@ashley-shrok/viewmodel-shell/server').then(m=>{if(typeof m.createAction!=='function'||typeof m.createAgentSkillHandler!=='function'){console.error('missing exports');process.exit(1)};console.log('ok')}).catch(e=>{console.error(e);process.exit(1)})" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies includes `@ashley-shrok/viewmodel-shell` at a pinned version.
    - `package-lock.json` contains a locked entry for `@ashley-shrok/viewmodel-shell`.
    - `node -e "import('@ashley-shrok/viewmodel-shell/server')..."` exits 0 and prints `ok` (both createAction and createAgentSkillHandler are functions under Node 22).
    - `npx tsc --noEmit` exits 0.
    - `dashboard/dist/` is NOT modified or staged.
  </acceptance_criteria>
  <done>The VMS package is a pinned runtime dependency whose /server export loads cleanly under Node 22, and shrok still typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → shrok build | A third-party package is introduced into shrok's runtime (in-process, full trust — same posture as skills/sensors per REQUIREMENTS.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SC | Tampering | npm install of @ashley-shrok/viewmodel-shell | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; pin an exact/locked version recorded in package-lock.json; package is first-party (`@ashley-shrok` scope, MIT, already in production at /home/thenasty/vms-apps). Not auto-approvable. |
| T-55-01 | Elevation | package runs in shrok's process | accept | Apps and their framework run in-process by design (REQUIREMENTS.md "same trust posture as skills/sensors/tasks; no new sandbox"). No new privilege beyond what the host already grants its own code. |
</threat_model>

<verification>
- `grep @ashley-shrok/viewmodel-shell package.json package-lock.json` shows the pinned dependency.
- The Node import smoke-check confirms the `/server` export loads under Node 22.
- `npx tsc --noEmit` is clean.
</verification>

<success_criteria>
- @ashley-shrok/viewmodel-shell is a pinned shrok dependency with a locked lockfile entry.
- The package's server export loads under Node 22 and exposes createAction + createAgentSkillHandler.
- A human has confirmed the package's legitimacy (T-55-SC).
</success_criteria>

<output>
Create `.planning/phases/55-app-serving-subsystem/55-01-SUMMARY.md` when done.
</output>
