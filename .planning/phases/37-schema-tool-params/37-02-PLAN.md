---
phase: 37-schema-tool-params
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/sub-agents/registry.ts
  - src/sub-agents/agents.test.ts
autonomous: true
requirements: [ACK-01, ACK-02, SCHED-04]
must_haves:
  truths:
    - "create_reminder exposes requiresAck + nagMinutes/nagHours/nagDays as input params and the model can set them"
    - "create_reminder with requiresAck:true + nagHours:1 + nagMinutes:30 stores nagIntervalMinutes:90 on the reminder row (slots summed, D-01/D-02)"
    - "create_reminder with requiresAck:true and no/insufficient nag slots returns { error:true } (D-04)"
    - "create_reminder with nag slots present but requiresAck false/omitted returns { error:true } (D-05)"
    - "create_reminder rejects a nag sum below 5 min or above 43200 min when requiresAck:true (D-03 floor/ceiling)"
    - "create_reminder rejects a non-integer / negative nag slot (V5 input validation)"
    - "create_reminder description no longer says 'for one-time reminders only' and documents triggerAt+cron = start-then-repeat (SCHED-04)"
    - "every create_reminder validation reject returns the existing { error:true, message } JSON shape and never throws (D-06)"
    - "create_reminder description documents both the SCHED-04 start-then-repeat fix and the new ack/nag params (D-10)"
    - "no update_reminder/PATCH path is added — create_reminder stays the only writer (D-09 creation-only)"
  artifacts:
    - path: "src/sub-agents/registry.ts"
      provides: "create_reminder requiresAck/nagMinutes/nagHours/nagDays params + boundary validation + slot-sum + createOpts assignment + reworded description"
      contains: "requiresAck"
    - path: "src/sub-agents/agents.test.ts"
      provides: "ack/nag validation rejects, slot-sum round-trip, description assertion, updated property-order test"
      contains: "nagIntervalMinutes"
  key_links:
    - from: "create_reminder.execute()"
      to: "scheduleStore.create(createOpts)"
      via: "summed nagSum assigned to createOpts.nagIntervalMinutes when ack-required"
      pattern: "createOpts.nagIntervalMinutes = nagSum"
    - from: "create_reminder.inputSchema.properties"
      to: "the LLM"
      via: "declared requiresAck/nagMinutes/nagHours/nagDays params"
      pattern: "nagMinutes"
---

<objective>
Wire the acknowledgment params into the `create_reminder` tool: declare `requiresAck` + `nagMinutes`/`nagHours`/`nagDays` in the inputSchema, validate them at the tool boundary (sum the slots, enforce the strict ack↔nag coupling and the 5-min floor / 43200-min ceiling, reject malformed slots), assign the summed total to `createOpts.nagIntervalMinutes`, and reword the description so `triggerAt`+`cron` reads as start-then-repeat (SCHED-04) while documenting the new ack params. Update the now-stale property-order test.

Purpose: Exposes ACK-01/ACK-02 to the LLM with airtight boundary validation (D-03/D-04/D-05/D-06), and closes SCHED-04 (reword only — the backend start-then-repeat already works, verified at registry.ts:992-1003 + scheduler/index.ts:88-91; do NOT change execution).
Output: Modified `src/sub-agents/registry.ts` (params + validation + createOpts + description) and extended `src/sub-agents/agents.test.ts` (rejects, slot-sum round-trip, description assertion, updated property-order test).

Scope fences (locked):
- Depends on Plan 01: `Schedule.requiresAck`, `Schedule.nagIntervalMinutes`, and the optional `CreateScheduleOptions` counterparts must already exist (they do after 37-01) — this plan only writes into them.
- SCHED-04 is reword-only (D-10): do NOT touch the combine logic at registry.ts:992-1003, do NOT touch the scheduler. Only the description + triggerAt param text change.
- `buildReminderTools(store, tz, headId)` signature stays unchanged (Phase 35 D-09 — headId rides the closure).
- `exactOptionalPropertyTypes` ON — assign new fields to createOpts conditionally (mirror the `if (conditionsArg !== undefined)` idiom), never always-assign a maybe-undefined.
- All validation returns `JSON.stringify({ error: true, message })` — never throw (D-06).
- ROADMAP SC1's `nagInterval: '1h'` example is stale (predates the multi-slot decision) — any reference to it in this plan reflects the multi-slot input + stored-minutes shape (D-01/D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-schema-tool-params/37-CONTEXT.md
@.planning/phases/37-schema-tool-params/37-PATTERNS.md
@.planning/phases/37-schema-tool-params/37-VALIDATION.md
@.planning/phases/37-schema-tool-params/37-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from src/sub-agents/registry.ts + src/db/schedules.ts this session. Plan 01 (Wave 1) adds the CreateScheduleOptions fields below. -->

After Plan 01, CreateScheduleOptions (src/db/schedules.ts) includes:
```typescript
requiresAck?: boolean
nagIntervalMinutes?: number | null
```

Current create_reminder description (src/sub-agents/registry.ts:925-927) — to reword:
```typescript
description: 'Create a reminder that fires a notification to the user at a specified time. ' +
  'Use triggerAt (ISO 8601 datetime) for a one-time reminder, or cron for a recurring one. ' +
  'The reminder message should be written as if it is being delivered to the user at fire time.',
```

Current triggerAt param doc (registry.ts:939-942) — remove "for one-time reminders only" framing:
```typescript
triggerAt: {
  type: 'string',
  description: 'ISO 8601 datetime for one-time reminders (e.g. "2026-04-01T09:00:00Z"). Required if cron is not provided.',
},
```

Current createOpts assembly (registry.ts:1021-1035) — the exactOptionalPropertyTypes-safe conditional-assign idiom to mirror:
```typescript
const createOpts: import('../db/schedules.js').CreateScheduleOptions = {
  id, headId, kind: 'reminder', agentContext: message,
  runAt: triggerAt ?? undefined, nextRun: triggerAt ?? undefined,
}
if (cronExpression !== null) { createOpts.cron = cronExpression }
if (conditionsArg !== undefined) { createOpts.conditions = conditionsArg }
scheduleStore.create(createOpts)
```

Current property-order test (src/sub-agents/agents.test.ts:1453-1460) — WILL break, MUST update:
```typescript
const keys = Object.keys(create.definition.inputSchema.properties as object)
expect(keys).toEqual(['message', 'cronTimezone', 'triggerAt', 'cron', 'conditions'])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ack/nag params, boundary validation, slot-sum, createOpts assignment, and reword the description</name>
  <files>src/sub-agents/registry.ts</files>
  <read_first>
    - src/sub-agents/registry.ts (the file being modified — read lines 903-1063: buildReminderTools through create_reminder.execute(). You need: list_reminders projection @915-919, description @925-927, inputSchema.properties @930-952, triggerAt param @939-942, cron param @943-946, the input-bag reads @955-959, the validation blocks @962-988, the combine logic @992-1003 [DO NOT TOUCH], the createOpts assembly @1021-1035)
    - .planning/phases/37-schema-tool-params/37-PATTERNS.md § "3." (input-bag read + validation-block idioms with verbatim excerpts + the 5 new validations in order), § "4." (createOpts conditional-assign idiom), § "5." (description reword + new param docs + the inputSchema property block + the "backend already correct — do not change" note), § "Shared Patterns A" (exactOptionalPropertyTypes), § "B" ({ error:true, message } never throw)
    - .planning/phases/37-schema-tool-params/37-RESEARCH.md § "Architecture Patterns" Pattern 2 (validation order, integer guard), § "Common Pitfalls" Pitfall 1 (exactOptionalPropertyTypes), Pitfall 6 (SCHED-04 scope creep — description only), § "Security Domain" (the nag-bounds DoS + malformed-slot threats), § "Open Questions" (field name nagIntervalMinutes; list_reminders projection — lean include; keep D-03/D-04 distinct error messages)
    - src/db/schedules.ts (read the create() defaults + CreateScheduleOptions after Plan 01 — confirm requiresAck/nagIntervalMinutes options exist before assigning them)
  </read_first>
  <behavior>
    - inputSchema declares requiresAck (boolean), nagMinutes, nagHours, nagDays (each integer/number, optional) with descriptions
    - execute({ message, triggerAt, requiresAck:true, nagHours:1, nagMinutes:30 }) → ok:true and stored nagIntervalMinutes === 90, requiresAck === true
    - execute({ message, triggerAt, requiresAck:true }) (no nag slots) → { error:true, message } mentioning nag interval (D-04)
    - execute({ message, triggerAt, nagMinutes:30 }) (nag without ack) → { error:true } mentioning requiresAck (D-05)
    - execute({ message, triggerAt, requiresAck:true, nagMinutes:3 }) (sum < 5) → { error:true } mentioning the 5-minute floor (D-03 floor)
    - execute({ message, triggerAt, requiresAck:true, nagDays:31 }) (sum > 43200) → { error:true } mentioning the 30-day ceiling (D-03 ceiling)
    - execute({ message, triggerAt, requiresAck:true, nagMinutes:-5 } or :1.5) → { error:true } (non-integer/negative reject, V5)
    - execute({ message, triggerAt }) (no ack at all) → ok:true, stored requiresAck:false, nagIntervalMinutes:null (default path unaffected)
    - description does not contain "for one-time reminders only" and documents triggerAt+cron = start-then-repeat
  </behavior>
  <action>
    Edit `src/sub-agents/registry.ts` inside `create_reminder` (definition + execute), mirroring the verbatim idioms in 37-PATTERNS.md § 3/4/5:
    1. **inputSchema.properties (@930-952):** add `requiresAck` (type 'boolean'), `nagMinutes`, `nagHours`, `nagDays` (type 'integer' or 'number' — match the JSON-schema style used elsewhere in this file for numeric params; if none exists use 'integer'). Pick a DELIBERATE insertion order and remember it for the property-order test in Task 2. Recommended order to minimize churn: append the 4 new keys AFTER `conditions` → `['message','cronTimezone','triggerAt','cron','conditions','requiresAck','nagMinutes','nagHours','nagDays']`. Describe requiresAck as "marks the reminder acknowledgment-required — it keeps nagging every nag interval until the user explicitly acknowledges it" (D-10b) and the three slots as the nag cadence (minutes/hours/days, summed; only valid when requiresAck is true; floor 5 min, ceiling 30 days).
    2. **input-bag reads (after @959):** add `const requiresAckArg = input['requiresAck'] as boolean | undefined`, `const nagMinutes = input['nagMinutes'] as number | undefined` (and nagHours, nagDays). Cast-and-validate — never trust the cast (Shared Pattern, RESEARCH § Security).
    3. **Validation block (place alongside the existing validations, after the message/triggerAt-or-cron presence checks):** mirror the `{ error:true, message }` early-return idiom (§ B). In order:
       (a) per-slot integer guard: for each present slot, reject unless `Number.isInteger(x) && x >= 0` (message names which slot / "must be a non-negative integer").
       (b) compute `nagSum = (nagMinutes ?? 0) + (nagHours ?? 0) * 60 + (nagDays ?? 0) * 1440`.
       (c) D-05: `nagSum > 0 && requiresAckArg !== true` → reject ("nag slots (nagMinutes/nagHours/nagDays) only apply when requiresAck is true").
       (d) D-04: `requiresAckArg === true && nagSum === 0` → reject ("requiresAck requires a nag interval: set nagMinutes/nagHours/nagDays").
       (e) D-03 floor: `requiresAckArg === true && nagSum > 0 && nagSum < 5` → reject ("nag interval must be at least 5 minutes"). Keep this a DISTINCT message from D-04 (RESEARCH Open Question 3 — actionable feedback).
       (f) D-03 ceiling: `nagSum > 43200` → reject ("nag interval must be at most 30 days (43200 minutes)").
       Exact wording is Claude's discretion; keep substrings stable enough that Task 2's `.toMatch(/nag interval/i)` / `/requiresAck/i` style assertions hit.
    4. **createOpts assembly (@1021-1035):** mirror the `if (conditionsArg !== undefined) createOpts.conditions = ...` conditional-assign (Shared Pattern A — exactOptionalPropertyTypes-safe, never always-assign a maybe-undefined). Add `if (requiresAckArg !== undefined) createOpts.requiresAck = requiresAckArg` and `if (nagSum > 0) createOpts.nagIntervalMinutes = nagSum`. Leave the rest to create()'s `?? false` / `?? null` defaults from Plan 01.
    5. **Description reword (SCHED-04, @925-927):** rewrite so it documents: triggerAt alone = one-time; cron alone = recurring (first fire computed from cron); triggerAt + cron together = start-then-repeat (first fire at triggerAt, then repeat on cron). Add a sentence on the new ack params (D-10b). Remove the "Use triggerAt ... for a one-time reminder, or cron for a recurring one" phrasing that frames them as mutually exclusive.
    6. **triggerAt param doc (@939-942):** remove the "for one-time reminders only" framing — describe it as "the first/start fire time (ISO 8601); combine with cron for start-then-repeat".
    7. **list_reminders projection (@915-919, Claude's discretion — lean INCLUDE per RESEARCH Open Question 2):** add `requiresAck: s.requiresAck` and `nagIntervalMinutes: s.nagIntervalMinutes` to the read-only `.map()` projection. Read-only, no validation; helps the Phase 38 ack flow.
    DO NOT touch the combine logic @992-1003 or the scheduler — SCHED-04 is text-only (Pitfall 6). DO NOT change the buildReminderTools signature.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/sub-agents/agents.test.ts -t buildReminderTools</automated>
  </verify>
  <acceptance_criteria>
    - `src/sub-agents/registry.ts` inputSchema declares all four new params: `grep -c "nagMinutes\|nagHours\|nagDays\|requiresAck" src/sub-agents/registry.ts` returns ≥ 8 (4 param decls + reads + validation + createOpts)
    - The slot-sum uses 60 and 1440 multipliers: `grep -c '1440' src/sub-agents/registry.ts` returns ≥ 1
    - createOpts assigns conditionally (exactOptionalPropertyTypes-safe): `grep -c 'createOpts.nagIntervalMinutes = nagSum' src/sub-agents/registry.ts` returns 1; `grep -c 'createOpts.requiresAck = requiresAckArg' src/sub-agents/registry.ts` returns 1
    - description no longer contains the substring "for one-time reminders only": `grep -c 'for one-time reminders only' src/sub-agents/registry.ts` returns 0
    - The combine logic is untouched: `grep -c 'cronExpression = cronArg' src/sub-agents/registry.ts` returns 1 (unchanged)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/sub-agents/agents.test.ts -t buildReminderTools` exits 0 (after Task 2 updates the property-order test; this task alone may leave that one assertion red — see Task 2)
  </acceptance_criteria>
  <done>create_reminder declares + validates + sums + stores the ack params with the strict coupling and bounds; description reworded for start-then-repeat with "one-time reminders only" removed; combine logic + scheduler untouched; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend agents.test.ts — ack/nag rejects, slot-sum round-trip, description + param assertions, and UPDATE the property-order test</name>
  <files>src/sub-agents/agents.test.ts</files>
  <read_first>
    - src/sub-agents/agents.test.ts (the file being modified — read the buildReminderTools describe block @1252-1359: the `ctx` @1253, the `getReminderTools()` helper @1255-1268, the empty-message reject @1282-1288, the conditions-stored round-trip @1327-1349, the inputSchema-declares-property assertion @1351-1358, the list_reminders test @1299; AND the property-order test @1453-1460 inside `describe('phase 23: cronTimezone field')` — this WILL break and MUST be updated)
    - .planning/phases/37-schema-tool-params/37-PATTERNS.md § "6. Test analogs" — tool-boundary reject analog, tool→store round-trip / slot-sum analog, inputSchema-declares-property analog, and the ⚠️ property-order-test-MUST-be-UPDATED block (verbatim excerpt + exact churn note)
    - .planning/phases/37-schema-tool-params/37-VALIDATION.md (Req→Test map: D-03 floor/ceiling, D-04, D-05, D-01/D-02 slot-sum, SC3 description, property-order UPDATE rows — all use `npx vitest run src/sub-agents/agents.test.ts -t ...`)
    - .planning/phases/37-schema-tool-params/37-RESEARCH.md § "Common Pitfalls" Pitfall 3 (property-order test is expected churn, not a regression) and Pitfall 2 (non-null-assert array reads `rows[0]!`)
  </read_first>
  <behavior>
    - reject tests: D-04 (ack no nag), D-05 (nag no ack), D-03 floor (sum<5), D-03 ceiling (sum>43200), non-integer/negative slot — each asserts JSON.parse(result).error === true with a matching message
    - slot-sum round-trip: execute with requiresAck:true + nagHours:1 + nagMinutes:30 → ok:true, and the stored reminder row has nagIntervalMinutes === 90, requiresAck === true
    - default path: execute with no ack params → stored requiresAck:false, nagIntervalMinutes:null
    - inputSchema assertions: requiresAck/nagMinutes/nagHours/nagDays declared; description drops "for one-time reminders only" and documents start-then-repeat (SC3)
    - property-order test updated to the new deliberate key array and passing
    - if list_reminders projection was added in Task 1: one assertion that a listed ack-required reminder reports requiresAck/nagIntervalMinutes
  </behavior>
  <action>
    Add/modify tests in `src/sub-agents/agents.test.ts`, mirroring the verbatim analogs in 37-PATTERNS.md § 6:
    1. **Reject tests (D-03/04/05 + malformed):** in the buildReminderTools describe block, add `it` blocks mirroring the empty-message reject @1282-1288 — `const { createReminder } = await getReminderTools()`, `await createReminder.execute({ message, triggerAt:'2099-01-01T09:00:00Z', ... }, ctx)`, `JSON.parse(result as string)`, `expect(parsed.error).toBe(true)` + a `.toMatch(...)` on the message. Cases: (a) D-04 requiresAck:true + no nag → `/nag interval/i`; (b) D-05 nagMinutes:30 + no requiresAck → `/requiresAck/i`; (c) D-03 floor requiresAck:true + nagMinutes:3 → `/5 minutes|at least 5/i`; (d) D-03 ceiling requiresAck:true + nagDays:31 → `/30 days|43200/i`; (e) non-integer requiresAck:true + nagMinutes:1.5 (or negative) → `/integer|non-negative/i`.
    2. **Slot-sum round-trip (D-01/D-02):** mirror the conditions-stored round-trip @1327-1349 — `execute({ message, triggerAt:'2099-01-01T09:00:00Z', requiresAck:true, nagHours:1, nagMinutes:30 })`, `expect(parsed.ok).toBe(true)`, then `scheduleStore.list().filter(s => s.kind === 'reminder')`, `expect(rows[0]!.nagIntervalMinutes).toBe(90)` and `expect(rows[0]!.requiresAck).toBe(true)` (non-null-assert per Pitfall 2).
    3. **Default path:** mirror the conditions-null test — `execute({ message, triggerAt })` with no ack params → `expect(rows[0]!.requiresAck).toBe(false)` and `expect(rows[0]!.nagIntervalMinutes).toBeNull()`.
    4. **inputSchema declares + description (SC3):** mirror the inputSchema-declares-property assertion @1351-1358 — assert `schema.properties.requiresAck`, `.nagMinutes`, `.nagHours`, `.nagDays` are all defined. Add a description assertion (test name containing "description"): `expect(create.definition.description).not.toMatch(/for one-time reminders only/i)` and `expect(create.definition.description).toMatch(/start.?then.?repeat|repeat on (the )?cron/i)`.
    5. **UPDATE the property-order test (@1453-1460) — REQUIRED, not optional:** change the expected key array to exactly match the deliberate insertion order chosen in Task 1 (recommended: `['message','cronTimezone','triggerAt','cron','conditions','requiresAck','nagMinutes','nagHours','nagDays']`). This is expected churn flagged in RESEARCH Pitfall 3 / PATTERNS § 6 — NOT a regression. Keep the test name containing "property order" so the `-t "property order"` command selects it.
    6. **list_reminders projection (if added in Task 1):** add a one-line assertion to the list_reminders test (@1299) that a created ack-required reminder reports `requiresAck` and `nagIntervalMinutes` in the projection.
    Keep new test descriptions containing the matcher substrings the VALIDATION map filters on ("create_reminder", "description", "property order").
  </action>
  <verify>
    <automated>npx vitest run src/sub-agents/agents.test.ts -t "create_reminder" && npx vitest run src/sub-agents/agents.test.ts -t "property order" && npx vitest run src/sub-agents/agents.test.ts -t "description"</automated>
  </verify>
  <acceptance_criteria>
    - The property-order test is updated and passing: `npx vitest run src/sub-agents/agents.test.ts -t "property order"` exits 0, and `grep -c 'requiresAck' src/sub-agents/agents.test.ts` shows the new key appears in the expected array
    - `npx vitest run src/sub-agents/agents.test.ts -t "create_reminder"` exits 0 with new reject tests (D-03 floor, D-03 ceiling, D-04, D-05, malformed slot) and the slot-sum round-trip (nagIntervalMinutes === 90) all green
    - `npx vitest run src/sub-agents/agents.test.ts -t "description"` exits 0 — asserts the description dropped "for one-time reminders only" and documents start-then-repeat (SC3)
    - `grep -c 'nagIntervalMinutes' src/sub-agents/agents.test.ts` returns ≥ 2 (slot-sum + default-path assertions)
    - `npx tsc --noEmit` exits 0
    - Phase gate: `npx vitest run` (full suite) exits 0 — no regressions across the 1400+ existing tests
  </acceptance_criteria>
  <done>Ack/nag reject tests, slot-sum round-trip, default-path, inputSchema + description assertions all pass; the property-order test is updated to the new key order and green; full suite + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → create_reminder.execute() (tool args) | the LLM is an UNTRUSTED source of tool arguments (V5 Input Validation) — the primary boundary for this phase |
| create_reminder.execute() → ScheduleStore.create() | validated args cross into the typed storage domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Denial of Service | LLM supplies an absurd nag (1-min spam loop or 1000-day) that the Phase 38 nag loop would act on | mitigate | D-03 floor (5 min) + ceiling (43200 min) reject at the tool boundary in create_reminder.execute() — exactly why the bounds exist; pinned by the floor/ceiling reject tests |
| T-37-05 | Tampering | LLM supplies a non-integer / negative / NaN nag slot → malformed nagIntervalMinutes state | mitigate | per-slot `Number.isInteger(x) && x >= 0` guard before summing; reject with `{ error:true, message }`; pinned by the malformed-slot reject test |
| T-37-06 | Tampering | type confusion in the `input` bag (cast does not enforce runtime type) | mitigate | cast-and-validate — `as boolean/number | undefined` then runtime checks (integer guard, `=== true` for requiresAck); never trust the cast alone, consistent with existing message/triggerAt handling |
| T-37-07 | Spoofing/EoP | LLM sets requiresAck without a nag, or a nag without requiresAck, to produce an inconsistent reminder | mitigate | strict coupling (D-04/D-05): both mismatches fail loud with `{ error:true, message }`; no silent default or inference; pinned by the D-04 and D-05 reject tests |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this phase (RESEARCH.md § Package Legitimacy Audit: zero external packages) — no slopcheck checkpoint required |

No SQL/shell/template injection vector (JSON file store). No secrets touched. No auth/session surface (V2/V3 N/A).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (SC4 type gate)
- `npx vitest run src/sub-agents/agents.test.ts` exits 0 (buildReminderTools block + updated property-order test green)
- `npx vitest run` (full suite) exits 0 — phase gate, no regressions
- `grep -c 'for one-time reminders only' src/sub-agents/registry.ts` returns 0 (SCHED-04 reword complete)
- combine logic + scheduler untouched (D-10 reword-only)
</verification>

<success_criteria>
- create_reminder exposes + validates requiresAck + nagMinutes/nagHours/nagDays with strict ack↔nag coupling and 5-min floor / 43200-min ceiling (D-01..D-06)
- slots sum into stored nagIntervalMinutes; default path stores false/null (D-02, D-07)
- description documents triggerAt+cron = start-then-repeat and drops "for one-time reminders only" (SCHED-04 / SC3)
- property-order test updated to the new key order (Pitfall 3 churn, flagged not surprise)
- tsc clean; full suite green (SC4); combine logic + scheduler unchanged (D-10)
</success_criteria>

<output>
Create `.planning/phases/37-schema-tool-params/37-02-SUMMARY.md` when done
</output>
