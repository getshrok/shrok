---
phase: quick-260525-4zu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/timezone-env.ts
  - src/index.ts
  - src/sub-agents/env.ts
  - src/sub-agents/env.test.ts
  - skills/scheduling/SKILL.md
  - skills/tasks/SKILL.md
autonomous: true
requirements: [TZ-13]
must_haves:
  truths:
    - "process.env.TZ equals config.timezone after startup (set once, before spawning/Date-heavy setup)"
    - "Scoped bash env (BASELINE_ENV_KEYS) carries TZ, so bash/bash_no_net subprocesses inherit config.timezone"
    - "Full-env-spread spawn sites (npm tests, evals, git, MCP stdio, skill-dep installer, unshare probe) inherit TZ via process.env.TZ with no per-site change"
    - "applyTimezoneEnv is exported and unit-testable, sets process.env.TZ to the supplied timezone"
    - "Skill docs (scheduling, tasks) state task timestamps follow config.timezone via TZ; existing offset-not-Z guidance preserved"
  artifacts:
    - path: "src/timezone-env.ts"
      provides: "applyTimezoneEnv helper that sets process.env.TZ"
      exports: ["applyTimezoneEnv"]
      min_lines: 5
    - path: "src/sub-agents/env.test.ts"
      provides: "Unit tests for BASELINE_ENV_KEYS/buildScopedEnv TZ handling + applyTimezoneEnv"
      contains: "applyTimezoneEnv"
  key_links:
    - from: "src/index.ts"
      to: "src/timezone-env.ts"
      via: "applyTimezoneEnv(config) call right after loadConfig() at ~line 96"
      pattern: "applyTimezoneEnv"
    - from: "src/sub-agents/registry.ts (executeBash)"
      to: "process.env.TZ"
      via: "BASELINE_ENV_KEYS includes 'TZ'; executeBash copies BASELINE_ENV_KEYS from process.env"
      pattern: "'TZ'"
---

<objective>
Propagate `config.timezone` (the dashboard-settable IANA zone) to every process shrok spawns, so subprocesses (bash tool, sub-agents, MCP servers, npm/eval/git subprocesses) stop falling back to the host OS zone.

Two central changes only — NO per-spawn-site env injection:
  (a) Set `process.env.TZ = config.timezone` ONCE at startup via an exported helper `applyTimezoneEnv`, called in `src/index.ts` immediately after `const config = loadConfig()` (line 95) and before any spawning / heavy Date work.
  (b) Add `'TZ'` to `BASELINE_ENV_KEYS` in `src/sub-agents/env.ts:6` so the SCOPED bash env carries TZ once (a) has set it.

Together these cover ALL spawn sites: scoped bash via the allowlist; full-env-spread/inheriting sites via process.env.TZ; in-process LocalAgentRunner via the main process. Plus skill-doc updates so task authors stop reasoning about the host zone.

Purpose: Eliminate timezone divergence between config.timezone and spawned-process timestamps. Startup-only (takes effect on restart), consistent with ScheduleEvaluatorImpl which already captures config.timezone by value at startup (src/index.ts:418).

Output: New `src/timezone-env.ts`, edits to `src/index.ts` + `src/sub-agents/env.ts`, new `src/sub-agents/env.test.ts`, doc updates in two SKILL.md files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./AGENTS.md

<interfaces>
<!-- Verified from the codebase. Use these directly — no exploration needed. -->

From src/config.ts:154 (timezone schema/default) and :275 (type export):
```typescript
// inside ConfigSchema:
//   timezone: z.string().default(Intl.DateTimeFormat().resolvedOptions().timeZone || 'UTC'),
export type Config = z.infer<typeof ConfigSchema>   // config.timezone: string
```

From src/sub-agents/env.ts:6 (the allowlist to extend) and :21-30 (the filter to test):
```typescript
export const BASELINE_ENV_KEYS = ['PATH', 'HOME', 'USER', 'LOGNAME', 'SHELL', 'TMPDIR', 'TMP', 'TEMP', 'NODE_PATH', 'SHROK_WORKSPACE_PATH', 'WORKSPACE_PATH', 'SHROK_ENV_FILE', 'SHROK_ROOT', 'SHROK_SKILLS_DIR']

export function buildScopedEnv(
  declaredVars: string[],
  envDict: Record<string, string | undefined> = process.env as Record<string, string | undefined>,
): Record<string, string> {
  const env: Record<string, string> = {}
  for (const key of [...BASELINE_ENV_KEYS, ...declaredVars]) {
    if (envDict[key] !== undefined) env[key] = envDict[key]!   // filter-undefined — MUST preserve
  }
  return env
}
```

From src/sub-agents/registry.ts:339-341 (executeBash — copies BASELINE_ENV_KEYS from process.env; no edit needed, it inherits 'TZ' automatically once the allowlist includes it):
```typescript
const base = envOverride ?? Object.fromEntries(
  BASELINE_ENV_KEYS.flatMap(k => process.env[k] !== undefined ? [[k, process.env[k]!]] : [])
)
```

From src/index.ts:89-96 (call site — insert applyTimezoneEnv right after loadConfig):
```typescript
async function main() {
  // ... SHROK_ROOT setup ...
  const config = loadConfig()          // line 95
  registerSecrets(extractSecretValues(config))   // line 96
  setLogLevel(config.logLevel)
```

Test style (from src/sub-agents/output-cap.test.ts:1-3):
```typescript
import { describe, it, expect } from 'vitest'
import { ... } from './output-cap.js'   // note .js extension on relative imports
```
Default vitest config (vitest.config.ts:31) globs `src/**/*.test.ts`, so a new test under src/ is picked up by `npx vitest run`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create applyTimezoneEnv helper and add TZ to the bash allowlist</name>
  <files>src/timezone-env.ts, src/sub-agents/env.ts</files>
  <behavior>
    - applyTimezoneEnv(config) sets process.env.TZ to config.timezone (a non-empty IANA string).
    - applyTimezoneEnv accepts the resolved Config object (typed import of `Config` from './config.js'). It reads config.timezone and assigns process.env.TZ. No reload machinery, no return value needed (void).
    - BASELINE_ENV_KEYS includes 'TZ' so buildScopedEnv and executeBash carry TZ when present in process.env.
    - buildScopedEnv still OMITS TZ when absent from the supplied env dict (the existing filter-undefined behavior is preserved — adding 'TZ' to the allowlist does not force the key to appear).
  </behavior>
  <action>
    Create `src/timezone-env.ts` exporting `applyTimezoneEnv`. Rationale for a new module (not co-locating in env.ts): this is a cross-cutting startup concern that mutates global process.env for ALL spawn paths, not just the scoped-bash domain that env.ts owns; a dedicated single-purpose module keeps it trivially unit-testable and avoids coupling. Import the `Config` type from `'./config.js'` (type-only import). Signature: `export function applyTimezoneEnv(config: Pick<Config, 'timezone'>): void` — accept `Pick<Config, 'timezone'>` so tests can pass a minimal `{ timezone }` literal without constructing a full Config. Body: `process.env.TZ = config.timezone`. Add a one-line comment noting this must be called once at startup before any spawning or heavy Date work, and that it is startup-only (per locked DECISION 2 — no live update). Honor TS strictness (use the `.js` import extension; no `any`).

    Then edit `src/sub-agents/env.ts:6` to add `'TZ'` to the `BASELINE_ENV_KEYS` array (append it — placement within the array does not matter). Do NOT change buildScopedEnv's filter logic — the existing `if (envDict[key] !== undefined)` guard must stay so TZ is only emitted when actually set.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx tsc --noEmit 2>&1 | tail -5 && grep -n "'TZ'" src/sub-agents/env.ts && grep -n "applyTimezoneEnv" src/timezone-env.ts</automated>
  </verify>
  <done>src/timezone-env.ts exports applyTimezoneEnv(config: Pick<Config,'timezone'>): void that sets process.env.TZ = config.timezone; BASELINE_ENV_KEYS contains 'TZ'; buildScopedEnv filter unchanged; tsc --noEmit clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire applyTimezoneEnv into startup and add unit tests</name>
  <files>src/index.ts, src/sub-agents/env.test.ts</files>
  <action>
    Wire-up: In `src/index.ts`, add a top-level import of `applyTimezoneEnv` from `'./timezone-env.js'` (group it with the other local imports near the existing import block). In `main()`, insert `applyTimezoneEnv(config)` on the line immediately AFTER `const config = loadConfig()` (currently line 95) and BEFORE `registerSecrets(...)` / `setLogLevel(...)`. This guarantees process.env.TZ is set before the scheduler (line 418), webhook, agent-runner, and any subprocess spawn touches Date or env. Do NOT add a live-update anywhere; do NOT modify src/dashboard/routes/settings.ts (locked DECISION 2).

    Tests: Create `src/sub-agents/env.test.ts` (none exists today). Use vitest (`import { describe, it, expect, beforeEach, afterEach } from 'vitest'`). Import `{ BASELINE_ENV_KEYS, buildScopedEnv } from './env.js'` and `{ applyTimezoneEnv } from '../timezone-env.js'`. Cover:
      1. `BASELINE_ENV_KEYS` contains 'TZ'.
      2. buildScopedEnv INCLUDES TZ when it is present in the supplied env dict — call `buildScopedEnv([], { TZ: 'America/New_York', PATH: '/usr/bin' })` and assert `result.TZ === 'America/New_York'`.
      3. buildScopedEnv OMITS TZ when absent — call `buildScopedEnv([], { PATH: '/usr/bin' })` and assert `'TZ' in result === false` (preserves filter-undefined behavior).
      4. applyTimezoneEnv sets process.env.TZ to config.timezone — call `applyTimezoneEnv({ timezone: 'Europe/Berlin' })` and assert `process.env.TZ === 'Europe/Berlin'`.
    CRITICAL — leakage guard: SAVE the original `process.env.TZ` in `beforeEach` (capture `const orig = process.env.TZ`, but since beforeEach runs per-test store it in a module-scope `let`), and in `afterEach` RESTORE it: if orig was undefined, `delete process.env.TZ`, else `process.env.TZ = orig`. This prevents the applyTimezoneEnv test from leaking TZ into other test files/shards that may assume TZ is unset. Use the `.js` extension on both relative imports. Honor noUncheckedIndexedAccess (the buildScopedEnv result is `Record<string,string>` so direct property access on a known key is fine; if you index BASELINE_ENV_KEYS, null-check). Do not set process.env.TZ to `undefined` via assignment (exactOptionalPropertyTypes / the env typing) — use `delete process.env.TZ` for the unset case.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/sub-agents/env.test.ts 2>&1 | tail -20 && grep -n "applyTimezoneEnv(config)" src/index.ts</automated>
  </verify>
  <done>src/index.ts calls applyTimezoneEnv(config) immediately after loadConfig(); env.test.ts has 4 passing assertions covering allowlist/include/omit/apply with TZ save-restore in afterEach; tsc clean; the new test file passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update skill docs (scheduling, tasks) for the TZ source</name>
  <files>skills/scheduling/SKILL.md, skills/tasks/SKILL.md</files>
  <action>
    Per locked DECISION 3, document that task timestamps follow config.timezone (the dashboard setting) via the TZ env var, so authors stop reasoning about the host zone — while KEEPING the existing "store local time WITH offset, not UTC/Z" guidance.

    In `skills/scheduling/SKILL.md`, in the `## Watermarks` section (the code block with `last_checked: 2026-04-21T07:00:00-04:00` ends at line 53): add a short paragraph immediately after the closing code fence stating that the offset reflects **config.timezone** (the dashboard timezone setting), propagated to spawned processes via the `TZ` environment variable — so `date -Iseconds`, `new Date()`, and similar calls inside a running task automatically honor the dashboard zone with no per-task code. Keep the existing "not UTC/Z" instruction intact (do not delete or weaken it); the new text explains WHERE the offset comes from.

    In `skills/tasks/SKILL.md`, in the `## MEMORY.md patterns` section, augment the **Watermarks** bullet (lines 76) — which already points to the scheduling skill for format — by adding one sentence: timestamps a task writes/reads follow config.timezone via the TZ env var (the dashboard setting), so authors should not hardcode or assume the host OS zone. Keep the existing reference to the scheduling skill for the canonical format.

    Do not restructure the files; these are additive doc edits only.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && grep -li "config.timezone" skills/scheduling/SKILL.md skills/tasks/SKILL.md && grep -l "TZ" skills/scheduling/SKILL.md && grep -i "not UTC" skills/scheduling/SKILL.md</automated>
  </verify>
  <done>Both SKILL.md files mention config.timezone + TZ as the zone source; scheduling SKILL.md retains the "not UTC/Z" offset guidance; edits are additive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main process → spawned subprocess (bash tool, MCP stdio, npm/git/eval) | Environment vars cross here; TZ is now propagated. Value comes from config (operator-controlled via dashboard), not from agent input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4zu-01 | Tampering | TZ value injected into subprocess env | accept | config.timezone is validated as a string by zod (ConfigSchema) and set by the operator via the dashboard, not by agent/user message input. Node treats an unrecognized TZ as UTC — no shell-injection vector (TZ is an env value, not interpolated into a command). |
| T-4zu-02 | Information Disclosure | TZ leaking into unrelated test shards | mitigate | env.test.ts saves and restores the original process.env.TZ in afterEach (delete when originally unset) to prevent cross-test/shard leakage. |
| T-4zu-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added; no package-manager install tasks in this plan. Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
1. `cd /home/thenasty/shrok && npx tsc --noEmit` — clean (no errors).
2. `cd /home/thenasty/shrok && npx vitest run` — FULL suite passes (confirms no cross-test TZ leakage and the new env.test.ts passes).
3. `grep -n "'TZ'" src/sub-agents/env.ts` — TZ present in BASELINE_ENV_KEYS.
4. `grep -n "applyTimezoneEnv(config)" src/index.ts` — wired immediately after loadConfig().
5. No edits to src/icw/*, dashboard/dist/, or src/dashboard/routes/settings.ts.
6. No .map files staged: `git status --porcelain | grep '\.map$'` returns nothing.
</verification>

<success_criteria>
- process.env.TZ is set to config.timezone once at startup (src/index.ts, right after loadConfig).
- BASELINE_ENV_KEYS includes 'TZ'; scoped bash subprocesses inherit it; full-env-spread sites inherit it via process.env.TZ; no per-site injection added.
- applyTimezoneEnv is exported, typed, and unit-tested; buildScopedEnv filter-undefined behavior preserved and tested.
- Skill docs (scheduling, tasks) state config.timezone-via-TZ as the zone source while retaining the offset-not-Z guidance.
- FINAL GATE: `npx tsc --noEmit` and `npx vitest run` (full suite) both pass.
</success_criteria>

<output>
Create `.planning/quick/260525-4zu-export-config-timezone-as-tz-to-spawned-/260525-4zu-SUMMARY.md` when done.
</output>
