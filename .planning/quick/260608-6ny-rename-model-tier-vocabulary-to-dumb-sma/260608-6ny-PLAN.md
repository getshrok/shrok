---
phase: quick-260608-6ny
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/llm.ts
  - src/config.ts
  - src/llm/router.ts
  - src/llm/index.ts
  - src/types/agent.ts
  - src/head/index.ts
  - src/sub-agents/registry.ts
  - src/sub-agents/local.ts
  - src/system.ts
  - src/head/assembler.ts
  - config.json
  - src/llm/llm.test.ts
  - src/config.test.ts
  - CHANGELOG.md
autonomous: true
requirements: [GH-31]
must_haves:
  truths:
    - "The Tier type is dumb | smart | genius; router resolves all three new tier names to model IDs"
    - "An existing config.json using old keys (anthropicModelStandard/Capable/Expert, gemini*, openai*) still resolves to the correct model IDs"
    - "An existing config.json setting headModel/agentModel/stewardModel to 'standard'|'capable'|'expert' still resolves correctly"
    - "The head's spawn_agent tool accepts an optional model enum ['dumb','smart','genius'], defaulting to smart, and passes it into SpawnOptions"
    - "The sub-agent registry spawn_agent tool description names dumb/smart/genius"
    - "npx tsc --noEmit passes and the targeted vitest suites pass"
  artifacts:
    - path: "src/types/llm.ts"
      provides: "Tier = 'dumb'|'smart'|'genius' + TierModels { dumb; smart; genius }"
      contains: "dumb"
    - path: "src/config.ts"
      provides: "Renamed config keys + role defaults + backward-compat normalizer in loadConfig"
      contains: "smart"
    - path: "src/config.test.ts"
      provides: "Tests proving old keys + old role-default values normalize to new vocabulary"
      contains: "anthropicModelStandard"
  key_links:
    - from: "src/config.ts loadConfig"
      to: "ConfigSchema"
      via: "normalizeLegacyModelConfig(merged) before schema parse"
      pattern: "normalizeLegacy"
    - from: "src/head/index.ts spawn_agent handler"
      to: "SpawnOptions.model"
      via: "conditional spread of input['model']"
      pattern: "input\\['model'\\]"
---

<objective>
Rename the internal model-tier vocabulary `standard`/`capable`/`expert` → `dumb`/`smart`/`genius` end-to-end (type, config keys, role defaults, router resolution, repo config.json, construction site, tests), with a non-destructive backward-compat shim so existing external configs keep working. Then expose a `model` arg on the HEAD's `spawn_agent` tool and align the sub-agent registry tool vocabulary.

Mapping: standard→dumb, capable→smart, expert→genius. `smart` is the default tier.

Purpose: GitHub issue #31 — capability-framed tier names that describe the worker's smarts, plus head-level control over which tier a spawned agent runs at.

Output: renamed vocabulary throughout src/, a read-time normalizer in the config loader, a new `model` enum on the head spawn tool, updated tests, and a CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Confirmed anchors. Use directly — no further codebase exploration needed. -->

src/types/llm.ts:44-50 (CURRENT — to be renamed):
```typescript
export type Tier = 'standard' | 'capable' | 'expert'
export interface TierModels {
  standard: string
  capable: string
  expert: string
}
```

src/config.ts:104-125 (CURRENT keys + role defaults to rename):
```typescript
anthropicModelStandard / anthropicModelCapable / anthropicModelExpert
geminiModelStandard / geminiModelCapable / geminiModelExpert
openaiModelStandard / openaiModelCapable / openaiModelExpert
headModel: default 'capable'    agentModel: default 'capable'
composerModel: default 'standard'   stewardModel: default 'standard'
memoryModel: default 'standard'   memoryChunkingModel: default 'capable'
memoryArchivalModel: default 'standard'   memoryRetrievalModel: default 'standard'
```

src/config.ts:346-377 (loadConfig — `merged` object is assembled here, BEFORE schema parse; the normalizer must run on `merged`/`jsonConfig`).

src/llm/index.ts:14-40 (config-key → TierModels construction, three providers):
```typescript
tierModels: { standard: config.anthropicModelStandard, capable: ..., expert: ... }
```

src/llm/router.ts:23-24 (SingleProviderRouter) + :30 (TIERS set) + :51 (MultiProviderRouter) — `model in this.tierModels` / `TIERS.has(model)` resolution.

src/head/index.ts:34-46 (HEAD spawn_agent schema — currently NO model param) + :261-280 (handler — currently does NOT pass model into SpawnOptions).

src/sub-agents/registry.ts:35 (sub-agent spawn_agent model param description — currently names 'standard'|'capable'|'expert').

src/sub-agents/local.ts:1189-1190 (handleSpawnAgent already reads input['model']; default chain `modelArg ?? parentOptions.model ?? this.agentModel`). Also :168-169 (`this.agentModel ?? 'capable'`, `this.stewardModel ?? 'standard'`).

src/types/agent.ts:73 (SpawnOptions.model comment) + :46 (AgentState.model — free-form string, no rename needed).

src/system.ts:339 + src/head/assembler.ts:407 (`getCapableModel`) — comments/helper names referencing the old vocabulary.

config.json:3-11 (repo base config — old keys).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename Tier vocabulary + add backward-compat normalizer in the config loader</name>
  <files>src/types/llm.ts, src/llm/router.ts, src/llm/index.ts, src/config.ts, src/system.ts, src/head/assembler.ts, config.json, src/config.test.ts</files>
  <behavior>
    - New Tier type resolves: router.complete('dumb'|'smart'|'genius', …) maps to the configured model ID for that tier.
    - Old config keys honored: a merged config object containing only `anthropicModelStandard`/`Capable`/`Expert` (and gemini*/openai* equivalents) normalizes so the built TierModels has correct dumb/smart/genius model IDs.
    - Old role-default VALUES honored: a config setting `headModel:'capable'`, `agentModel:'standard'`, `stewardModel:'expert'` normalizes to `smart`/`dumb`/`genius` respectively (apply to every role-model field: head/agent/composer/steward/memory/memoryChunking/memoryArchival/memoryRetrieval).
    - New keys take precedence when both old and new are present (new wins, old ignored).
  </behavior>
  <action>
Per GH-31 locked decisions 1, 2, 5.

(a) src/types/llm.ts: change `Tier` to `'dumb' | 'smart' | 'genius'` and `TierModels` fields to `dumb`/`smart`/`genius`.

(b) src/llm/router.ts: update the `TIERS` set literal to `['dumb','smart','genius']`. The `model in this.tierModels` / `TIERS.has(model)` resolution logic is value-agnostic and needs no other change once the set is updated. OPTIONAL ROBUSTNESS (recommended per decision 5): also accept the legacy names as aliases in both routers — if `model` is one of standard/capable/expert, map it to dumb/smart/genius before resolution, so a direct legacy tier name passed at call time still resolves. Implement via a small `LEGACY_TIER_ALIAS` record `{ standard:'dumb', capable:'smart', expert:'genius' }` exported from a shared spot (config.ts or a new tiny module) and reused by the normalizer in (e).

(c) src/llm/index.ts: rename the three `tierModels` object literals' keys to `dumb`/`smart`/`genius`, reading from the renamed config keys (`config.anthropicModelDumb` etc.).

(d) src/config.ts: rename the nine provider model keys to `anthropicModelDumb/Smart/Genius`, `geminiModelDumb/Smart/Genius`, `openaiModelDumb/Smart/Genius` (keep the same `.default(...)` model-ID values). Rename the role-default VALUES from 'capable'/'standard' to 'smart'/'dumb' (headModel→'smart', agentModel→'smart', composerModel→'dumb', stewardModel→'dumb', memoryModel→'dumb', memoryChunkingModel→'smart', memoryArchivalModel→'dumb', memoryRetrievalModel→'dumb'). Update the explanatory comment at :116-117 to name dumb/smart/genius with the capability heuristic. Also update the two fallback literals in src/sub-agents/local.ts:168-169 (`?? 'smart'`, `?? 'dumb'`) and any `?? 'capable'`/`?? 'standard'` literals you find in the renamed files. Also update comments in src/system.ts:339 and the `getCapableModel`→rename to `getSmartModel` in src/head/assembler.ts:407-420 (and its call site).

(e) src/config.ts loadConfig: add `normalizeLegacyModelConfig(merged)` applied to the `merged` object (after line 371, before the null-strip / schema parse). It must: (1) for each provider, if a new key is absent but the legacy key is present, copy the legacy value into the new key (Standard→Dumb, Capable→Smart, Expert→Genius); new key always wins if present; (2) for each role-model field, if its VALUE is one of standard/capable/expert, rewrite it to dumb/smart/genius via `LEGACY_TIER_ALIAS`. Do NOT delete the legacy keys from disk and do NOT write anything back — normalization is in-memory only (decision 5: no destructive rewrite). Keep the function pure and exported so the test can call it directly.

(f) config.json (repo base): rename the nine keys to the new `*Dumb/*Smart/*Genius` names, same model-ID values.

(g) src/config.test.ts: add a focused describe block (create the file if it does not exist; otherwise append) proving the shim:
  - old provider keys only → normalizer/loadConfig yields correct dumb/smart/genius model IDs;
  - new keys present alongside old → new wins;
  - role-default value 'capable'/'standard'/'expert' → normalized to smart/dumb/genius;
  - direct new role value 'genius' passes through unchanged.
Test the normalizer directly where possible (pure function) to avoid filesystem coupling; if loadConfig is needed, drive it via the `USER_CONFIG_PATH` env override (config.ts:355) pointing at a tmp JSON file written in the test.

Do NOT inline any code blocks here — implement directly in the files per the interfaces above.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx tsc --noEmit && npx vitest run src/config.test.ts src/llm/llm.test.ts</automated>
  </verify>
  <done>Tier is dumb/smart/genius everywhere; config.test.ts proves old keys AND old role-default values normalize to the new vocabulary; tsc clean; llm.test.ts updated and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose model arg on head spawn_agent + align registry vocabulary + wire handler</name>
  <files>src/head/index.ts, src/sub-agents/registry.ts, src/sub-agents/local.ts</files>
  <behavior>
    - Head spawn_agent schema exposes an optional `model` enum ['dumb','smart','genius'].
    - When the head calls spawn_agent with model:'genius', the resulting SpawnOptions carries model:'genius'.
    - When omitted, no model key is forced into SpawnOptions (runner default 'smart' applies downstream).
    - Sub-agent registry spawn_agent tool description names dumb/smart/genius (not standard/capable/expert).
  </behavior>
  <action>
Per GH-31 locked decisions 3 and 4.

(a) src/head/index.ts:37-45 — add a `model` property to the spawn_agent `inputSchema.properties`: `{ type: 'string', enum: ['dumb','smart','genius'], description: '...' }`. Keep `model` OUT of `required`. Description heuristic (decision 3): "Worker capability tier. dumb = trivial single-fact lookups / web searches only; smart = everyday work (default); genius = hard multi-step / reasoning-heavy work. Omit to use smart." Also fold a one-line heuristic into the tool's top-level `description` so the model sees it without reading the param.

(b) src/head/index.ts:261-280 (spawn_agent handler) — read `input['model']` and conditionally spread it into `spawnOpts`, mirroring the sub-agent pattern (local.ts:1189). Use `...(input['model'] ? { model: input['model'] as string } : {})`. Do NOT set a default here — the runner's `agentModel` default (now 'smart') handles omission, keeping out-of-box behavior identical.

(c) src/sub-agents/registry.ts:35 — change the `model` param description from `('standard'|'capable'|'expert')` to `('dumb'|'smart'|'genius')` with the same capability heuristic wording as (a). The registry handler path already reads input['model'] via local.ts handleSpawnAgent — no handler change needed beyond Task 1's local.ts default-literal fix.

Do NOT inline code blocks in this section — edit the files directly.
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx tsc --noEmit && npx vitest run src/sub-agents/agents.test.ts</automated>
  </verify>
  <done>Head spawn_agent schema has the model enum defaulting to smart-via-omission; handler passes model into SpawnOptions only when supplied; registry description names dumb/smart/genius; tsc clean; agents.test.ts passing.</done>
</task>

<task type="auto">
  <name>Task 3: Full type-check, targeted test sweep, and CHANGELOG entry</name>
  <files>CHANGELOG.md, src/llm/llm.test.ts, src/sub-agents/agents.test.ts, src/dashboard/routes/settings.ts, src/dashboard/routes/settings.test.ts</files>
  <action>
(a) Grep the whole src/ tree one more time for any remaining literal `'standard'`/`'capable'`/`'expert'` used as a TIER VALUE (NOT unrelated words — e.g. first-boot.ts `StandardOutPath` is a launchd plist key, leave it). Files known to still reference the keys: src/dashboard/routes/settings.ts:60-62 and :183-191 (the `ENV_KEY_ALLOWLIST`/settings echo of the nine provider model keys) and src/dashboard/routes/settings.test.ts — update those key NAMES to the new `*Dumb/*Smart/*Genius` keys, and update any role-default fixture VALUES ('capable'/'standard') in the .test.ts to 'smart'/'dumb'. Update remaining test fixture values across the files that asserted old tier strings (llm.test.ts, agents.test.ts, and any others surfaced by grep) to the new vocabulary so assertions match.

(b) CHANGELOG.md (per AGENTS.md rules — user language, `closes #31`, NO internal planning refs): under the in-flight `## [0.3.0]` section, add to `### Changed` a bullet describing the capability-framed tier rename and the new head-side model control. Example wording: "Model tiers renamed to **dumb / smart / genius** (capability-framed: dumb for trivial lookups, smart for everyday work — the default, genius for heavy reasoning). The head's spawn-agent tool gained a `model` control so it can pick the tier per task. Existing config.json files using the old key names or tier values keep working unchanged. (closes #31)". Keep it to one or two bullets, user language only.

(c) Run the full type-check and the targeted suites that touch tiers/config/spawn.

Do NOT commit dashboard/dist (AGENTS.md). Work stays on main (no feature branch).
  </action>
  <verify>
    <automated>cd /home/thenasty/shrok && npx tsc --noEmit && npx vitest run src/config.test.ts src/llm/llm.test.ts src/sub-agents/agents.test.ts src/dashboard/routes/settings.test.ts</automated>
  </verify>
  <done>No stray old tier-value literals remain (grep clean except unrelated launchd keys); settings allowlist + tests updated; CHANGELOG has the #31 entry; tsc clean; all targeted suites green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (whole project).
- `npx vitest run src/config.test.ts src/llm/llm.test.ts src/sub-agents/agents.test.ts src/dashboard/routes/settings.test.ts` passes.
- `grep -rn "'standard'\|'capable'\|'expert'" src/ --include="*.ts"` returns only unrelated, non-tier matches (e.g. launchd `StandardOutPath`).
- Manual reasoning check: a hypothetical user config.json with `{"anthropicModelCapable":"X","headModel":"capable"}` resolves headModel→smart and the smart-tier anthropic model→"X".
</verification>

<success_criteria>
- Tier vocabulary is dumb/smart/genius across types, config keys, role defaults, router, construction, repo config.json, and tests.
- Backward-compat shim (read-time, in-memory, non-destructive) honors old provider keys AND old role-default values, with new keys/values winning when both present — proven by config.test.ts.
- Head spawn_agent exposes an optional dumb/smart/genius model enum (default smart-via-omission) and passes it into SpawnOptions; registry tool speaks the same vocabulary.
- CHANGELOG.md updated with the #31 entry in user language.
- tsc clean; targeted vitest suites green; no dashboard/dist committed; work on main.
</success_criteria>

<output>
Create `.planning/quick/260608-6ny-rename-model-tier-vocabulary-to-dumb-sma/260608-6ny-SUMMARY.md` when done.
</output>
