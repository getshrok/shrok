---
phase: quick-260525-6d5
plan: 01
subsystem: skills/rename
tags: [rename, cascade, skills, tasks, schedules, usage, dashboard]
dependency_graph:
  requires: []
  provides:
    - UnifiedLoader.rename (full cascade orchestration)
    - FileSystemKindLoader.updateDepReferences (extracted, public)
    - FileSystemKindLoader.rewriteOwnFrontmatterName (private helper)
    - ScheduleStore.renameTask
    - UsageStore.renameTarget
    - kind route cascade path + enriched response shape
    - dashboard warning banner for body-ref renames
  affects:
    - src/skills/loader.ts
    - src/skills/unified.ts
    - src/db/schedules.ts
    - src/db/usage.ts
    - src/dashboard/routes/kind.ts
    - src/dashboard/server.ts
    - dashboard/src/lib/api.ts
    - dashboard/src/components/kind/KindEditorPage.tsx
tech_stack:
  added: []
  patterns:
    - cross-kind cascade via UnifiedLoader.rename
    - word-boundary regex scan for body-ref warnings
    - conditional spread for exactOptionalPropertyTypes compliance
    - prepared statement for parameterized SQL UPDATE
key_files:
  created: []
  modified:
    - src/skills/loader.ts
    - src/skills/unified.ts
    - src/db/schedules.ts
    - src/db/usage.ts
    - src/dashboard/routes/kind.ts
    - src/dashboard/routes/kind.test.ts
    - src/dashboard/server.ts
    - src/skills/skills.test.ts
    - src/skills/unified.test.ts
    - src/db/schedules.test.ts
    - src/db/usage.test.ts
    - dashboard/src/lib/api.ts
    - dashboard/src/components/kind/KindEditorPage.tsx
decisions:
  - "Cascade orchestration lives on UnifiedLoader.rename (spans both loaders + stores); kind route delegates to it when wired, falls back to bare renameSkill for back-compat"
  - "ScheduleStore.renameTask mutates rows directly (same pattern as markFired/advanceNextRun) rather than routing through SchedulePatch — taskName is intentionally absent from the Pick per Phase 35 D-13"
  - "Own-body warning scan captures the entry's raw body via listAll().find().path + parseSkillFile BEFORE the move; dep body scan happens after the move"
  - "server.ts switched skills mount from createSkillsRouter to createKindRouter directly; createSkillsRouter/skills.ts kept on disk as deprecated shim"
  - "Dashboard warning banner is a simple amber-bordered div with dismiss button, no toast library"
metrics:
  duration: resumed (OOM crash interrupted previous run)
  completed: "2026-05-25"
  tasks: 3
  files_modified: 13
---

# Phase quick-260525-6d5 Plan 01: Cascade Task/Skill Rename Across All Refs Summary

**One-liner:** Full cross-kind rename cascade via UnifiedLoader.rename — own frontmatter rewrite, cross-kind skill-deps, schedule taskName, usage.target_name, body-ref warnings, and dashboard warning banner.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Loader own-frontmatter rewrite + extracted updateDepReferences + ScheduleStore.renameTask + UsageStore.renameTarget | `be71adf` | loader.ts, schedules.ts, usage.ts, skills.test.ts, schedules.test.ts, usage.test.ts |
| 2 | UnifiedLoader.rename cascade orchestration + body-ref warnings | `88f7a67` | unified.ts, unified.test.ts |
| 3 | Wire deps into kind route + server, enrich response, surface warnings | `9059548` | kind.ts, kind.test.ts, server.ts, api.ts, KindEditorPage.tsx |

## Test Results (Final Gate)

**`npx tsc --noEmit`:** clean (0 errors)

**Targeted test suite (`npx vitest run src/skills/skills.test.ts src/skills/unified.test.ts src/db/schedules.test.ts src/db/usage.test.ts src/dashboard/routes/kind.test.ts`):**

- `src/db/schedules.test.ts`: 30 passed
- `src/skills/skills.test.ts`: 55 passed
- `src/dashboard/routes/kind.test.ts`: 13 passed (7 existing + 1 back-compat + 5 cascade)
- `src/skills/unified.test.ts`: 23 passed
- `src/db/usage.test.ts`: 11 passed
- **Total: 132 passed, 0 failed**

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed dead variables causing TS error in unified.ts**
- **Found during:** Task 2 (resuming from crash)
- **Issue:** The interrupted run left `ownDir` (line 142) and `markerFilename` (line 141) as dead code — `ownDir` referenced `(owningLoader as FileSystemKindLoader).skillsLoader?.root` which doesn't exist on `FileSystemKindLoader` (only on `UnifiedLoader`). This caused `TS2339: Property 'skillsLoader' does not exist on type 'FileSystemKindLoader'`.
- **Fix:** Removed both unused variables; kept the working `owningLoader.listAll().find()` approach for path resolution. Also removed unused `path` import from unified.ts.
- **Files modified:** `src/skills/unified.ts`
- **Commit:** `88f7a67`

**2. [Rule 1 - Bug] Fixed agents INSERT NOT NULL constraint in test**
- **Found during:** Task 2 test run
- **Issue:** `INSERT INTO agents (id, skill_name, status, task, trigger) VALUES (?, ?, 'completed', '', NULL)` — the `trigger` column has `NOT NULL DEFAULT 'manual'` per `sql/001_init.sql:28`. Passing `NULL` explicitly violated the constraint.
- **Fix:** Removed `trigger` from the column list (and its `NULL` value) so the database applies the `DEFAULT 'manual'`.
- **Files modified:** `src/skills/unified.test.ts`
- **Commit:** `88f7a67`

## What Was Found in Partial Task 2 State

When the previous executor was interrupted, both `src/skills/unified.ts` and `src/skills/unified.test.ts` were substantially complete but had not been committed. The implementation was logically correct (all cascade steps present) but contained the two bugs above. The test file was fully written. After fixing the two bugs, all 23 new unified.test.ts tests passed on first run.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes beyond those in the plan's threat model. All threat mitigations applied as specified:
- T-6d5-01: `safeSkillName` gates UnifiedLoader.rename before any filesystem mutation
- T-6d5-02: `UPDATE usage SET target_name = ? WHERE target_name = ?` uses bound params
- T-6d5-05: SQL scoped to `usage` table only; agents.skill_name untouched (verified by test)

## Known Stubs

None — all data paths are wired end-to-end.

## Self-Check: PASSED

Commits verified:
- `be71adf` — exists in git log
- `88f7a67` — exists in git log
- `9059548` — exists in git log

Key files verified:
- `src/skills/unified.ts` — UnifiedLoader.rename present
- `src/skills/unified.test.ts` — describe('UnifiedLoader.rename') block present (23 tests)
- `src/dashboard/routes/kind.ts` — unifiedLoader optional field present in CreateKindRouterOptions
- `src/dashboard/server.ts` — createKindRouter wired with unifiedLoader/scheduleStore/usageStore for both skills and tasks
- `dashboard/src/lib/api.ts` — widened return type with optional fields
- `dashboard/src/components/kind/KindEditorPage.tsx` — renameNotice state + warning banner present
