---
phase: quick-260621-r73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/head/index.ts
  - src/sub-agents/local.ts
  - src/sub-agents/compose-context.ts
  - src/sub-agents/compose-context.test.ts
  - src/head/classifier.ts
  - src/head/classifier.test.ts
  - src/config.ts
  - src/system.ts
  - src/index.ts
  - src/dashboard/routes/settings.ts
  - src/dashboard/routes/settings.test.ts
  - dashboard/src/pages/settings/ExperimentalTab.tsx
  - dashboard/src/pages/settings/draft.tsx
  - dashboard/src/types/api.ts
  - src/head/head.test.ts
  - src/sub-agents/agents.test.ts
  - src/head/steward.test.ts
  - src/tool-description.test.ts
  - src/sub-agents/output-cap.test.ts
  - src/config.test.ts
  - CHANGELOG.md
  - package.json
  - dashboard/package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "spawn_agent has a single rich `task` param (no `context`); the head's task text is delivered to the sub-agent directly"
    - "message_agent has a single all-in-one `message` param (no `context`); the head's message is delivered to the sub-agent directly"
    - "composeVerbatimContext / classifyAndCompose / classifyMessages machinery is gone — no source references the deleted modules"
    - "config knobs agentContextComposer and snapshotTokenBudget are removed from backend config, system wiring, and dashboard Settings UI"
    - "runMessageAgentSteward and runSpawnAgentSteward gates still run (content-orthogonal, untouched)"
    - "npx tsc --noEmit is clean and the full vitest suite is green"
    - "version bumped to 0.7.0 in both package.json files; CHANGELOG has a user-facing entry"
  artifacts:
    - path: "src/head/index.ts"
      provides: "collapsed spawn_agent + message_agent defs and direct-delivery dispatch"
    - path: "src/sub-agents/local.ts"
      provides: "spawn path with composer removed, agent receives head's task directly"
  key_links:
    - from: "src/head/index.ts message_agent handler"
      to: "agentRunner.update"
      via: "direct delivery of head message"
      pattern: "agentRunner\\.update"
    - from: "src/sub-agents/local.ts"
      to: "agentFirstMessage"
      via: "task/context delivered directly, no composeVerbatimContext"
      pattern: "agentFirstMessage"
---

<objective>
Reverse issue #45's "verbatim context pass-through" machinery. Collapse `spawn_agent`'s `task`+`context` and `message_agent`'s `message`+`context` into single all-in-one params delivered directly to the sub-agent. Delete the now-dead composer/classifier modules and the `agentContextComposer`/`snapshotTokenBudget` config knobs (backend + dashboard). Keep behavioral stewards intact.

Purpose: Stop fighting the model. Trust the head to write a rich task/message containing both WHAT is wanted and as much CONTEXT as needed; the receiving sub-agent is itself capable and self-corrects head over-prescription. The hard verbatim guarantee becomes a SOFT encouragement in the param description.

Output: Simpler head tool surface, dead code removed, config cleaned, suite green, version bumped.

SETTLED DESIGN — do NOT re-litigate. This is a deliberate reversal of commits b75b47e / 47dc1cb / d11f068 (#45 strict message_agent split) and the spawn-side composer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./AGENTS.md
@./CLAUDE.md

# Governing project rules baked in (from AGENTS.md):
# - Work directly on `main`. NO feature branch, NO PR.
# - NEVER stage dashboard/dist (CI is the sole writer). Edit dashboard SOURCE only.
# - Commits must be ATOMIC and SEPARATE per the grouping in Tasks 1/2/3 below.
# - Validation gate: `npx tsc --noEmit` clean AND `npx vitest run` green before "done".
# - Versioning: minor bump for a notable user-facing change; current 0.6.0 → 0.7.0 in BOTH package.json files in lockstep.

# Current relevant call sites (line numbers approximate — re-grep before editing):
# spawn_agent def: buildHeadSpawnAgentDef, src/head/index.ts ~46
# message_agent def: HEAD_TOOLS array, src/head/index.ts ~106
# spawn_agent dispatch: src/head/index.ts ~360
# message_agent dispatch: src/head/index.ts ~385-485
# spawn path composer: src/sub-agents/local.ts ~486-549
# config knobs: src/config.ts:196 (snapshotTokenBudget), :228 (agentContextComposer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Behavioral flip — collapse params, direct-delivery dispatch, delete composer/classifier</name>
  <files>src/head/index.ts, src/sub-agents/local.ts, src/sub-agents/compose-context.ts, src/sub-agents/compose-context.test.ts, src/head/classifier.ts, src/head/classifier.test.ts</files>
  <action>
**spawn_agent def** (`buildHeadSpawnAgentDef` ~line 46): Collapse `task`+`context` into a SINGLE rich `task` param. Drop `context` from `properties` and from `required` (required becomes `['description','task','name']`, plus `'model'` when `agentModelDynamic`). Rewrite the `task` description to SOFTLY encourage the head to give as much context as possible — state what the task is PLUS all relevant detail/constraints/preferences, and note it can relay things verbatim (exact user wording, IDs, links, names, exact values) when that helps the sub-agent. Keep the "say what is wanted, not how" guidance but SOFT (the agent decides the approach). Rewrite `baseDesc` to drop the "paste into `context`" RELAY framing — it should now say: write a good all-in-one `task`. PRESERVE the `agentModelDynamic` branch (#37 dynamic model arg) and `ackDesc` unchanged.

**message_agent def** (HEAD_TOOLS array ~line 106): Drop `context` from `properties` and `required` (required becomes `['agentId','message']`). Make `message` the all-in-one delivered turn — rewrite its description to softly encourage including the new user turns / reply / added detail with verbatim relay where useful. Rewrite the tool `description` to drop the "paste into `context`, message is a label" framing.

**spawn_agent dispatch** (~line 360): In the `SpawnOptions` literal, remove the `...(input['context'] ? { context: ... } : {})` spread — `task` alone carries the head's context now. Keep `headHistory` plumbing removal deferred to local.ts (the option can stay passed but is now unused by the composer-free path; if `headHistory` becomes fully unreferenced after local.ts edits, remove the `...(this.opts.getHistory ? { headHistory: ... } : {})` spread too and drop `getHistory` only if nothing else uses it — verify with grep before removing).

**message_agent dispatch** (~lines 385-485): Read the delivered turn from `input['message']` instead of `input['context']`. Rename the local `const context = input['context']` to `const message = input['message'] as string` and update the three steward calls (`runMessageAgentSteward`, `runResumeSteward`, and the running-agent check-in steward) to pass `message` where they currently pass `context` — the stewards judge the head's stated follow-up, which is now `message`. Remove the entire `composeVerbatimContext` block (~lines 458-482): delete the comment, the `getRecentTurns`/`getRecent`/`signal`/`composed` logic, and set `deliveredBody` directly to `message`. The final `agentRunner.update(agentId, delivered, ...)` stays; `delivered` wraps `message` in the existing `Here's the latest from the conversation — continue your work based on it:` envelope.

**Remove the import** `import { composeVerbatimContext } from '../sub-agents/compose-context.js'` from src/head/index.ts. Remove the now-unused `getRecentTurns?` option field (~line 246) and `snapshotTokenBudget?` field (~line 248) from the executor options interface ONLY if grep confirms no remaining reference inside head/index.ts (snapshotTokenBudget removal is finalized in Task 2's wiring — leave the interface field if Task 2 needs it, but the read at ~470 is deleted here).

**spawn path** (src/sub-agents/local.ts ~486-549): Remove the entire `composeVerbatimContext` block (~486-507): the comment, the `if (options.headHistory?.length && ...)` branch, `composedEmpty`, and `history.push(...composed.messages)`. Remove the `import { composeVerbatimContext }`. Simplify `agentFirstMessage` (~525-533): all branches collapse — the agent receives the head's `task` (which now carries pasted context) directly. Use: `options.context ? \`${options.task}\n\nRelevant messages from the conversation:\n"""\n${options.context}\n"""\` : options.task` for ALL triggers (the `options.context` arm is a defensive fallback for any eval/nested caller that still sets `context`; `manual` no longer special-cases the composer). Drop the `trigger === 'manual'` composer special-case entirely. Leave `updateWorkStart(agentId, history.length)` and the attachment/skill-injection logic below untouched.

**Delete dead files**: `git rm src/sub-agents/compose-context.ts src/sub-agents/compose-context.test.ts src/head/classifier.ts src/head/classifier.test.ts`. VERIFIED: `classifyAndCompose`/`classifyMessages` have no other consumers. After deletion, grep `composeVerbatimContext|classifyAndCompose|classifyMessages` across src/ — must return zero non-test hits and zero references in any remaining test.

**Fix behavioral tests**: Update src/head/head.test.ts, src/sub-agents/agents.test.ts, src/head/steward.test.ts, src/tool-description.test.ts, src/sub-agents/output-cap.test.ts. The #45 parity tests asserting "message dropped / context delivered / composer invoked" MUST FLIP to assert direct delivery: spawn delivers `task`, message_agent delivers `message`. Any test importing the deleted modules or referencing the removed `context` param / composer must be removed or rewritten to the new contract. Tests asserting the required-params arrays must expect the collapsed sets.

Commit as ONE atomic commit (behavioral flip): `feat(head): collapse spawn/message_agent to all-in-one task/message, drop #45 verbatim composer` — source + deletions + tests together. Work on `main`, no branch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; echo "---grep dead refs (expect 0)---"; grep -rn "composeVerbatimContext\|classifyAndCompose\|classifyMessages" src/ --include="*.ts" | wc -l; echo "---targeted tests---"; npx vitest run src/head/head.test.ts src/sub-agents/agents.test.ts src/head/steward.test.ts src/tool-description.test.ts src/sub-agents/output-cap.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>spawn_agent/message_agent have single all-in-one params; dispatch delivers head text directly; composer/classifier files deleted; zero dead refs; targeted tests green; tsc clean (config-knob tsc errors deferred to Task 2 are acceptable here only if they live in config/system/dashboard, not in head/index.ts or local.ts).</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup sweep — remove agentContextComposer + snapshotTokenBudget config knobs (backend + dashboard)</name>
  <files>src/config.ts, src/system.ts, src/index.ts, src/sub-agents/local.ts, src/head/index.ts, src/dashboard/routes/settings.ts, src/dashboard/routes/settings.test.ts, src/config.test.ts, dashboard/src/pages/settings/ExperimentalTab.tsx, dashboard/src/pages/settings/draft.tsx, dashboard/src/types/api.ts</files>
  <action>
Remove both dead config knobs everywhere. They are now unreferenced by runtime logic after Task 1.

**Backend:**
- src/config.ts: delete `snapshotTokenBudget: z.coerce.number().default(100_000)` (~line 196) and `agentContextComposer: z.coerce.boolean().default(false)` (~line 228).
- src/system.ts: delete the `agentContextComposer?` option field (~105-106), the `const agentContextComposer = deps.agentContextComposer ?? config.agentContextComposer` line (~339), and the `snapshotTokenBudget`/`agentContextComposer` entries passed into the runner options (~385-386) and the message_agent executor opts (~454). Remove the `// #45 message_agent verbatim composer` comment.
- src/index.ts: delete `config.snapshotTokenBudget = config.contextWindowTokens` (~225).
- src/sub-agents/local.ts: delete the `snapshotTokenBudget?`/`agentContextComposer?` option fields (~88-89), the `private snapshotTokenBudget`/`private agentContextComposer` decls (~126-127), and their ctor assignments (~174-175).
- src/head/index.ts: delete the `snapshotTokenBudget?` executor-options field (~248) if not already removed in Task 1.

**Dashboard SOURCE (never touch dashboard/dist):**
- src/dashboard/routes/settings.ts: remove `'agentContextComposer'` from the CONFIG_JSON_FIELDS list (~66), remove `'snapshotTokenBudget'` from its list (~68), and delete the `agentContextComposer: bool(...)` (~222) and `snapshotTokenBudget: num(...)` (~238) builder lines.
- dashboard/src/types/api.ts: remove `agentContextComposer: boolean` (~338) and `snapshotTokenBudget: number` (~366) from the settings type.
- dashboard/src/pages/settings/draft.tsx: remove both fields from the draft type (~49, ~81), the initializer (~130, ~160), the dirty-check comparisons (~215, ~242), and the PATCH-body builders (~288, ~315).
- dashboard/src/pages/settings/ExperimentalTab.tsx: remove the `agentContextComposer` toggle block (~34-38) and the `snapshotTokenBudget` input block (~48-51, which is `disabled` on the composer). If removing them empties a section/fieldset, remove the now-empty wrapper too.

**Tests:**
- src/config.test.ts: remove/adjust any assertion on the two deleted defaults.
- src/dashboard/routes/settings.test.ts: remove/adjust any assertion that the settings PUT round-trips these two fields.

Run `npx tsc --noEmit` for root AND `cd dashboard && npx tsc --noEmit` for dashboard source — both clean. Do NOT run `npm run build` and do NOT stage dashboard/dist.

Commit as ONE atomic cleanup commit: `chore(config): remove dead agentContextComposer + snapshotTokenBudget knobs`.
  </action>
  <verify>
    <automated>echo "---root tsc---"; npx tsc --noEmit 2>&1 | tail -5; echo "---dashboard tsc---"; cd dashboard && npx tsc --noEmit 2>&1 | tail -5; cd ..; echo "---grep knobs (expect 0)---"; grep -rn "agentContextComposer\|snapshotTokenBudget" src/ dashboard/src/ --include="*.ts" --include="*.tsx" | wc -l; echo "---config/settings tests---"; npx vitest run src/config.test.ts src/dashboard/routes/settings.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>Zero references to either knob across src/ and dashboard/src/; root + dashboard tsc clean; config + settings tests green; dashboard/dist untouched and unstaged.</done>
</task>

<task type="auto">
  <name>Task 3: Full validation gate, CHANGELOG, and version bump to 0.7.0</name>
  <files>CHANGELOG.md, package.json, dashboard/package.json</files>
  <action>
**Full suite gate** (must pass before committing this task): run `npx tsc --noEmit` (root) — clean; `cd dashboard && npx tsc --noEmit` — clean; `npx vitest run` (full sharded-equivalent local run) — green. If any failure surfaces from a file not yet touched (e.g. an integration test importing the old contract), fix it to the new direct-delivery behavior. Do not proceed until both tsc passes and the full vitest run is green.

**CHANGELOG.md**: Under the in-flight top version section, add a user-facing entry (user language, not engineering). The in-flight header should be `## [0.7.0]`. Add under `### Changed`: a bullet describing that spawning and continuing agents now takes a single all-in-one task/message (you give the agent the full request in your own words, including any details/context that help) rather than a separate context field. Do NOT reference issue #45, planning artifacts, or requirement IDs. Per AGENTS.md changelog rules: this is a notable user-facing behavior change, so it belongs.

**Version bump** (per AGENTS.md — minor bump, current 0.6.0 → 0.7.0, both in lockstep):
- package.json `version` → `0.7.0`
- dashboard/package.json `version` → `0.7.0`
- Date the in-flight CHANGELOG header: `## [0.7.0] — 2026-06-21` and open a fresh `## [next]` (or `## [0.7.1]`) section above it per the repo convention.

**Commit** as ONE atomic commit: `chore: bump version to 0.7.0`. SOURCE ONLY — never stage dashboard/dist. Per AGENTS.md you MAY also create the annotated tag `git tag -a v0.7.0 -m "v0.7.0"` — but do NOT push (the task does not push; pushing/tagging-remote is the user's call unless they ask). Leave the push for the user.

Work on `main`, no branch, no PR.
  </action>
  <verify>
    <automated>echo "---full tsc root---"; npx tsc --noEmit 2>&1 | tail -3; echo "---full tsc dashboard---"; cd dashboard && npx tsc --noEmit 2>&1 | tail -3; cd ..; echo "---versions (expect 0.7.0 x2)---"; node -p "require('./package.json').version"; node -p "require('./dashboard/package.json').version"; echo "---changelog has 0.7.0---"; grep -c "0.7.0" CHANGELOG.md; echo "---full suite---"; npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <done>Root + dashboard tsc clean; full vitest suite green; both package.json files at 0.7.0; CHANGELOG has dated 0.7.0 user-facing entry; three atomic commits exist on main (behavioral flip, cleanup sweep, version bump); dashboard/dist never staged.</done>
</task>

</tasks>

<verification>
- `grep -rn "composeVerbatimContext\|classifyAndCompose\|classifyMessages\|agentContextComposer\|snapshotTokenBudget" src/ dashboard/src/ --include="*.ts" --include="*.tsx"` returns zero hits.
- spawn_agent required params = `['description','task','name'(,'model' if dynamic)]`; message_agent required = `['agentId','message']`.
- `git log --oneline -3` shows three separate atomic commits matching the grouping.
- `git status` shows no staged `dashboard/dist/` changes.
- `npx tsc --noEmit` (root) and `cd dashboard && npx tsc --noEmit` both clean; `npx vitest run` green.
</verification>

<success_criteria>
spawn_agent and message_agent each take a single all-in-one param delivered directly to the sub-agent; the #45 verbatim composer + classifier modules are deleted with no dangling references; agentContextComposer + snapshotTokenBudget knobs are gone from backend and dashboard; runMessageAgentSteward / runSpawnAgentSteward (and post-activation spawnSteward / actionComplianceSteward) gates remain intact; both package.json files read 0.7.0 with a dated user-facing CHANGELOG entry; tsc clean and full vitest suite green; three atomic commits on main, no branch, no PR, dashboard/dist never staged.
</success_criteria>

<output>
Create `.planning/quick/260621-r73-simplify-spawn-agent-message-agent-to-al/260621-r73-SUMMARY.md` when done.
</output>
