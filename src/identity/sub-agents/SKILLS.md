# Skills

Skills are listed in your system prompt under `$SHROK_SKILLS_DIR`. Use `read_file` and `bash` to work with them. To learn how skills are structured (for creating or editing), read the `skills` skill's SKILL.md.
